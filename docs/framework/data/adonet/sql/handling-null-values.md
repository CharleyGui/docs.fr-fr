---
title: Gestion des valeurs null
description: En savoir plus sur la façon dont le Fournisseur de données .NET Framework pour SQL Server gère la valeur null, et en savoir plus sur la valeur null et SqlBoolean, la logique à trois valeurs et l’assignation de valeurs NULL.
ms.date: 03/30/2017
dev_langs:
- csharp
- vb
ms.assetid: f18b288f-b265-4bbe-957f-c6833c0645ef
ms.openlocfilehash: a4d086d81f1c2c959780366cfeb59f2d265bc40c
ms.sourcegitcommit: 33deec3e814238fb18a49b2a7e89278e27888291
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 06/02/2020
ms.locfileid: "84286453"
---
# <a name="handling-null-values"></a><span data-ttu-id="d18a6-103">Gestion des valeurs null</span><span class="sxs-lookup"><span data-stu-id="d18a6-103">Handling Null Values</span></span>
<span data-ttu-id="d18a6-104">Une valeur Null dans une base de données relationnelle est utilisée lorsque la valeur d’une colonne est inconnue ou manquante.</span><span class="sxs-lookup"><span data-stu-id="d18a6-104">A null value in a relational database is used when the value in a column is unknown or missing.</span></span> <span data-ttu-id="d18a6-105">Une valeur Null n’est ni une chaîne vide (pour les types de données caractère ou DateHeure), ni une valeur zéro (pour les types de données numériques).</span><span class="sxs-lookup"><span data-stu-id="d18a6-105">A null is neither an empty string (for character or datetime data types) nor a zero value (for numeric data types).</span></span> <span data-ttu-id="d18a6-106">La spécification SQL-92 ANSI indique qu’une valeur Null doit être la même pour tous les types de données, de sorte que toutes les valeurs Null sont gérées de manière cohérente.</span><span class="sxs-lookup"><span data-stu-id="d18a6-106">The ANSI SQL-92 specification states that a null must be the same for all data types, so that all nulls are handled consistently.</span></span> <span data-ttu-id="d18a6-107">L’espace de noms <xref:System.Data.SqlTypes> fournit une sémantique Null en implémentant l’interface <xref:System.Data.SqlTypes.INullable>.</span><span class="sxs-lookup"><span data-stu-id="d18a6-107">The <xref:System.Data.SqlTypes> namespace provides null semantics by implementing the <xref:System.Data.SqlTypes.INullable> interface.</span></span> <span data-ttu-id="d18a6-108">Chacun des types de données de <xref:System.Data.SqlTypes> a sa propre propriété `IsNull` et une valeur `Null` qui peut être assignée à une instance de ce type de données.</span><span class="sxs-lookup"><span data-stu-id="d18a6-108">Each of the data types in <xref:System.Data.SqlTypes> has its own `IsNull` property and a `Null` value that can be assigned to an instance of that data type.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="d18a6-109">La version 2,0 de .NET Framework a introduit la prise en charge des types valeur Nullable, qui permettent aux programmeurs d’étendre un type valeur pour représenter toutes les valeurs du type sous-jacent.</span><span class="sxs-lookup"><span data-stu-id="d18a6-109">The .NET Framework version 2.0 introduced support for nullable value types, which allow programmers to extend a value type to represent all values of the underlying type.</span></span> <span data-ttu-id="d18a6-110">Ces types valeur CLR Nullable représentent une instance de la <xref:System.Nullable> structure.</span><span class="sxs-lookup"><span data-stu-id="d18a6-110">These CLR nullable value types represent an instance of the <xref:System.Nullable> structure.</span></span> <span data-ttu-id="d18a6-111">Cette fonctionnalité est particulièrement utile lorsque les types valeur sont boxed et unboxed, ce qui offre une meilleure compatibilité avec les types d’objets.</span><span class="sxs-lookup"><span data-stu-id="d18a6-111">This capability is especially useful when value types are boxed and unboxed, providing enhanced compatibility with object types.</span></span> <span data-ttu-id="d18a6-112">Les types valeur CLR Nullable ne sont pas destinés au stockage des valeurs null de base de données, car une valeur null SQL ANSI ne se comporte pas de la même façon qu’une `null` référence (ou `Nothing` dans Visual Basic).</span><span class="sxs-lookup"><span data-stu-id="d18a6-112">CLR nullable value types are not intended for storage of database nulls because an ANSI SQL null does not behave the same way as a `null` reference (or `Nothing` in Visual Basic).</span></span> <span data-ttu-id="d18a6-113">Pour travailler avec les valeurs Null SQL ANSI de la base de données, utilisez les valeurs Null <xref:System.Data.SqlTypes> au lieu de <xref:System.Nullable>.</span><span class="sxs-lookup"><span data-stu-id="d18a6-113">For working with database ANSI SQL null values, use <xref:System.Data.SqlTypes> nulls rather than <xref:System.Nullable>.</span></span> <span data-ttu-id="d18a6-114">Pour plus d’informations sur l’utilisation des types Nullable de valeur CLR dans Visual Basic consultez [types valeur Nullable](../../../../visual-basic/programming-guide/language-features/data-types/nullable-value-types.md), et pour C#, consultez [types valeur Nullable](../../../../csharp/language-reference/builtin-types/nullable-value-types.md).</span><span class="sxs-lookup"><span data-stu-id="d18a6-114">For more information on working with CLR value nullable types in Visual Basic see [Nullable Value Types](../../../../visual-basic/programming-guide/language-features/data-types/nullable-value-types.md), and for C# see [Nullable value types](../../../../csharp/language-reference/builtin-types/nullable-value-types.md).</span></span>  
  
## <a name="nulls-and-three-valued-logic"></a><span data-ttu-id="d18a6-115">Valeurs null et logique à trois valeurs</span><span class="sxs-lookup"><span data-stu-id="d18a6-115">Nulls and Three-Valued Logic</span></span>  
 <span data-ttu-id="d18a6-116">L’autorisation de valeurs Null dans les définitions de colonne introduit une logique ternaire dans votre application.</span><span class="sxs-lookup"><span data-stu-id="d18a6-116">Allowing null values in column definitions introduces three-valued logic into your application.</span></span> <span data-ttu-id="d18a6-117">Une comparaison peut évaluer entre une et trois conditions :</span><span class="sxs-lookup"><span data-stu-id="d18a6-117">A comparison can evaluate to one of three conditions:</span></span>  
  
- <span data-ttu-id="d18a6-118">True</span><span class="sxs-lookup"><span data-stu-id="d18a6-118">True</span></span>  
  
- <span data-ttu-id="d18a6-119">False</span><span class="sxs-lookup"><span data-stu-id="d18a6-119">False</span></span>  
  
- <span data-ttu-id="d18a6-120">Unknown</span><span class="sxs-lookup"><span data-stu-id="d18a6-120">Unknown</span></span>  
  
 <span data-ttu-id="d18a6-121">Étant donné que la valeur Null est considérée comme inconnue, deux valeurs Null comparées l’une avec l’autre ne sont pas considérées comme égales.</span><span class="sxs-lookup"><span data-stu-id="d18a6-121">Because null is considered to be unknown, two null values compared to each other are not considered to be equal.</span></span> <span data-ttu-id="d18a6-122">Dans les expressions utilisant des opérateurs arithmétiques, si l’un des opérandes a la valeur Null, le résultat est également Null.</span><span class="sxs-lookup"><span data-stu-id="d18a6-122">In expressions using arithmetic operators, if any of the operands is null, the result is null as well.</span></span>  
  
## <a name="nulls-and-sqlboolean"></a><span data-ttu-id="d18a6-123">Valeurs Null et SqlBoolean</span><span class="sxs-lookup"><span data-stu-id="d18a6-123">Nulls and SqlBoolean</span></span>  
 <span data-ttu-id="d18a6-124">La comparaison entre tout <xref:System.Data.SqlTypes> retourne une <xref:System.Data.SqlTypes.SqlBoolean>.</span><span class="sxs-lookup"><span data-stu-id="d18a6-124">Comparison between any <xref:System.Data.SqlTypes> will return a <xref:System.Data.SqlTypes.SqlBoolean>.</span></span> <span data-ttu-id="d18a6-125">La fonction `IsNull` pour chaque `SqlType` retourne un <xref:System.Data.SqlTypes.SqlBoolean> et peut être utilisée pour vérifier les valeurs Null.</span><span class="sxs-lookup"><span data-stu-id="d18a6-125">The `IsNull` function for each `SqlType` returns a <xref:System.Data.SqlTypes.SqlBoolean> and can be used to check for null values.</span></span> <span data-ttu-id="d18a6-126">Les tables de vérité suivantes montrent comment les opérateurs AND, OR et NOT fonctionnent dans la présence d’une valeur Null.</span><span class="sxs-lookup"><span data-stu-id="d18a6-126">The following truth tables show how the AND, OR, and NOT operators function in the presence of a null value.</span></span> <span data-ttu-id="d18a6-127">(T = true (vrai), F = false (faux) et U = unknown (inconnu) ou Null.)</span><span class="sxs-lookup"><span data-stu-id="d18a6-127">(T=true, F=false, and U=unknown, or null.)</span></span>  
  
 <span data-ttu-id="d18a6-128">![Table de vérité](./media/truthtable-bpuedev11.gif "TruthTable_bpuedev11")</span><span class="sxs-lookup"><span data-stu-id="d18a6-128">![Truth Table](./media/truthtable-bpuedev11.gif "TruthTable_bpuedev11")</span></span>  
  
### <a name="understanding-the-ansi_nulls-option"></a><span data-ttu-id="d18a6-129">Compréhension de l'option ANSI_NULLS</span><span class="sxs-lookup"><span data-stu-id="d18a6-129">Understanding the ANSI_NULLS Option</span></span>  
 <span data-ttu-id="d18a6-130"><xref:System.Data.SqlTypes> fournit la même sémantique que lorsque l’option ANSI_NULLS est activée dans SQL Server.</span><span class="sxs-lookup"><span data-stu-id="d18a6-130"><xref:System.Data.SqlTypes> provides the same semantics as when the ANSI_NULLS option is set on in SQL Server.</span></span> <span data-ttu-id="d18a6-131">Tous les opérateurs arithmétiques (+,-, \* ,/,%), les opérateurs de bits (~, &, \| ) et la plupart des fonctions retournent Null si l’un des opérandes ou arguments a la valeur null, à l’exception de la propriété `IsNull` .</span><span class="sxs-lookup"><span data-stu-id="d18a6-131">All arithmetic operators (+, -, \*, /, %), bitwise operators (~, &, \|), and most functions return null if any of the operands or arguments is null, except for the property `IsNull`.</span></span>  
  
 <span data-ttu-id="d18a6-132">La norme ANSI SQL-92 ne prend pas en charge *columnName* = NULL dans une clause WHERE.</span><span class="sxs-lookup"><span data-stu-id="d18a6-132">The ANSI SQL-92 standard does not support *columnName* = NULL in a WHERE clause.</span></span> <span data-ttu-id="d18a6-133">Dans SQL Server, l’option ANSI_NULLS contrôle à la fois la possibilité de valeur Null par défaut dans la base de données et l’évaluation des comparaisons par rapport aux valeurs Null.</span><span class="sxs-lookup"><span data-stu-id="d18a6-133">In SQL Server, the ANSI_NULLS option controls both default nullability in the database and evaluation of comparisons against null values.</span></span> <span data-ttu-id="d18a6-134">Si ANSI_NULLS est activé (valeur par défaut), l’opérateur IS NULL doit être utilisé dans les expressions lors du test des valeurs Null.</span><span class="sxs-lookup"><span data-stu-id="d18a6-134">If ANSI_NULLS is turned on (the default), the IS NULL operator must be used in expressions when testing for null values.</span></span> <span data-ttu-id="d18a6-135">Par exemple, la comparaison suivante donne toujours un résultat inconnu quand ANSI_NULLS est activé :</span><span class="sxs-lookup"><span data-stu-id="d18a6-135">For example, the following comparison always yields unknown when ANSI_NULLS is on:</span></span>  
  
```sql
colname > NULL  
```  
  
 <span data-ttu-id="d18a6-136">La comparaison avec une variable contenant une valeur Null génère également Inconnu :</span><span class="sxs-lookup"><span data-stu-id="d18a6-136">Comparison to a variable containing a null value also yields unknown:</span></span>  
  
```sql
colname > @MyVariable  
```  
  
 <span data-ttu-id="d18a6-137">Utilisez le prédicat IS NULL ou IS NOT NULL pour tester si une valeur est NULL.</span><span class="sxs-lookup"><span data-stu-id="d18a6-137">Use the IS NULL or IS NOT NULL predicate to test for a null value.</span></span> <span data-ttu-id="d18a6-138">Ceci peut compliquer encore la clause WHERE.</span><span class="sxs-lookup"><span data-stu-id="d18a6-138">This can add complexity to the WHERE clause.</span></span> <span data-ttu-id="d18a6-139">Par exemple, la colonne TerritoryID de la table AdventureWorks Customer autorise les valeurs Null.</span><span class="sxs-lookup"><span data-stu-id="d18a6-139">For example, the TerritoryID column in the AdventureWorks Customer table allows null values.</span></span> <span data-ttu-id="d18a6-140">Si une instruction SELECT doit rechercher des valeurs NULL en plus des autres, elle doit inclure un prédicat IS NULL :</span><span class="sxs-lookup"><span data-stu-id="d18a6-140">If a SELECT statement is to test for null values in addition to others, it must include an IS NULL predicate:</span></span>  
  
```sql
SELECT CustomerID, AccountNumber, TerritoryID  
FROM AdventureWorks.Sales.Customer  
WHERE TerritoryID IN (1, 2, 3)  
   OR TerritoryID IS NULL  
```  
  
 <span data-ttu-id="d18a6-141">Si vous désactivez ANSI_NULLS dans SQL Server, vous pouvez créer des expressions qui utilisent l’opérateur d’égalité pour effectuer une comparaison avec la valeur Null.</span><span class="sxs-lookup"><span data-stu-id="d18a6-141">If you set ANSI_NULLS off in SQL Server, you can create expressions that use the equality operator to compare to null.</span></span> <span data-ttu-id="d18a6-142">Toutefois, vous ne pouvez pas empêcher des connexions différentes de définir des options Null pour cette connexion.</span><span class="sxs-lookup"><span data-stu-id="d18a6-142">However, you can't prevent different connections from setting null options for that connection.</span></span> <span data-ttu-id="d18a6-143">L’utilisation de IS NULL pour tester les valeurs Null fonctionne toujours, quels que soient les paramètres de ANSI_NULLS pour une connexion.</span><span class="sxs-lookup"><span data-stu-id="d18a6-143">Using IS NULL to test for null values always works, regardless of the ANSI_NULLS settings for a connection.</span></span>  
  
 <span data-ttu-id="d18a6-144">La désactivation de ANSI_NULLS n’est pas prise en charge dans un `DataSet`, qui suit toujours la norme ANSI SQL-92 pour la gestion des valeurs Null dans <xref:System.Data.SqlTypes>.</span><span class="sxs-lookup"><span data-stu-id="d18a6-144">Setting ANSI_NULLS off is not supported in a `DataSet`, which always follows the ANSI SQL-92 standard for handling null values in <xref:System.Data.SqlTypes>.</span></span>  
  
## <a name="assigning-null-values"></a><span data-ttu-id="d18a6-145">Assignation de valeurs null</span><span class="sxs-lookup"><span data-stu-id="d18a6-145">Assigning Null Values</span></span>  
 <span data-ttu-id="d18a6-146">Les valeurs Null sont spéciales et leur sémantique de stockage et d’affectation diffère selon les systèmes de stockage et systèmes de type.</span><span class="sxs-lookup"><span data-stu-id="d18a6-146">Null values are special, and their storage and assignment semantics differ across different type systems and storage systems.</span></span> <span data-ttu-id="d18a6-147">Un `Dataset` est conçu pour être utilisé avec différents systèmes de type et de stockage.</span><span class="sxs-lookup"><span data-stu-id="d18a6-147">A `Dataset` is designed to be used with different type and storage systems.</span></span>  
  
 <span data-ttu-id="d18a6-148">Cette section décrit la sémantique Null pour l’affectation de valeurs Null à un <xref:System.Data.DataColumn> dans un <xref:System.Data.DataRow> sur les différents systèmes de type.</span><span class="sxs-lookup"><span data-stu-id="d18a6-148">This section describes the null semantics for assigning null values to a <xref:System.Data.DataColumn> in a <xref:System.Data.DataRow> across the different type systems.</span></span>  
  
 `DBNull.Value`  
 <span data-ttu-id="d18a6-149">Cette affectation est valide pour un `DataColumn` de n’importe quel type.</span><span class="sxs-lookup"><span data-stu-id="d18a6-149">This assignment is valid for a `DataColumn` of any type.</span></span> <span data-ttu-id="d18a6-150">Si le type implémente `INullable`, `DBNull.Value` est forcé dans la valeur Null fortement typée appropriée.</span><span class="sxs-lookup"><span data-stu-id="d18a6-150">If the type implements `INullable`, `DBNull.Value` is coerced into the appropriate strongly typed Null value.</span></span>  
  
 `SqlType.Null`  
 <span data-ttu-id="d18a6-151">Tous les types de données <xref:System.Data.SqlTypes> implémentent `INullable`.</span><span class="sxs-lookup"><span data-stu-id="d18a6-151">All <xref:System.Data.SqlTypes> data types implement `INullable`.</span></span> <span data-ttu-id="d18a6-152">Si la valeur Null fortement typée peut être convertie dans le type de données de la colonne à l’aide d’opérateurs de forçage de type implicite, l’affectation doit y être soumise.</span><span class="sxs-lookup"><span data-stu-id="d18a6-152">If the strongly typed null value can be converted into the column's data type using implicit cast operators, the assignment should go through.</span></span> <span data-ttu-id="d18a6-153">Sinon, une exception de forçage de type non valide est levée.</span><span class="sxs-lookup"><span data-stu-id="d18a6-153">Otherwise an invalid cast exception is thrown.</span></span>  
  
 `null`  
 <span data-ttu-id="d18a6-154">Si « Null » est une valeur autorisée pour le type de données `DataColumn` donné, il est converti en `DbNull.Value` approprié ou `Null` associé au type de `INullable` (`SqlType.Null`)</span><span class="sxs-lookup"><span data-stu-id="d18a6-154">If 'null' is a legal value for the given `DataColumn` data type, it is coerced into the appropriate `DbNull.Value` or `Null` associated with the `INullable` type (`SqlType.Null`)</span></span>  
  
 `derivedUdt.Null`  
 <span data-ttu-id="d18a6-155">Pour les colonnes UDT, les valeurs Null sont toujours stockées en fonction du type associé à `DataColumn`.</span><span class="sxs-lookup"><span data-stu-id="d18a6-155">For UDT columns, nulls are always stored based on the type associated with the `DataColumn`.</span></span> <span data-ttu-id="d18a6-156">Prenons le cas d’un UDT associé à un `DataColumn` qui n’implémente pas `INullable` alors que sa sous-classe le fait.</span><span class="sxs-lookup"><span data-stu-id="d18a6-156">Consider the case of a UDT associated with a `DataColumn` that does not implement `INullable` while its sub-class does.</span></span> <span data-ttu-id="d18a6-157">Dans ce cas, si une valeur Null fortement typée associée à la classe dérivée est attribuée, elle est stockée en tant que `DbNull.Value` non typée, car le stockage Null est toujours cohérent avec le type de données de DataColumn.</span><span class="sxs-lookup"><span data-stu-id="d18a6-157">In this case, if a strongly typed null value associated with the derived class is assigned, it is stored as an untyped `DbNull.Value`, because null storage is always consistent with the DataColumn's data type.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="d18a6-158">La structure `Nullable<T>` ou <xref:System.Nullable> n’est pas prise en charge actuellement dans le `DataSet`.</span><span class="sxs-lookup"><span data-stu-id="d18a6-158">The `Nullable<T>` or <xref:System.Nullable> structure is not currently supported in the `DataSet`.</span></span>  
  
### <a name="multiple-column-row-assignment"></a><span data-ttu-id="d18a6-159">Assignation de plusieurs colonnes (lignes)</span><span class="sxs-lookup"><span data-stu-id="d18a6-159">Multiple Column (Row) Assignment</span></span>  
 <span data-ttu-id="d18a6-160">`DataTable.Add`, `DataTable.LoadDataRow` ou d’autres API acceptant une <xref:System.Data.DataRow.ItemArray%2A> mappée à une ligne, mappez « Null » à la valeur par défaut de DataColumn.</span><span class="sxs-lookup"><span data-stu-id="d18a6-160">`DataTable.Add`, `DataTable.LoadDataRow`, or other APIs that accept an <xref:System.Data.DataRow.ItemArray%2A> that gets mapped to a row, map 'null' to the DataColumn's default value.</span></span> <span data-ttu-id="d18a6-161">Si un objet du tableau contient `DbNull.Value` ou son équivalent fortement typé, les mêmes règles que celles décrites ci-dessus sont appliquées.</span><span class="sxs-lookup"><span data-stu-id="d18a6-161">If an object in the array contains `DbNull.Value` or its strongly typed counterpart, the same rules as described above are applied.</span></span>  
  
 <span data-ttu-id="d18a6-162">En outre, les règles suivantes s’appliquent à une instance des affectations Null `DataRow.["columnName"]` :</span><span class="sxs-lookup"><span data-stu-id="d18a6-162">In addition, the following rules apply for an instance of `DataRow.["columnName"]` null assignments:</span></span>  
  
1. <span data-ttu-id="d18a6-163">La valeur par défaut *default* est `DbNull.Value` pour tout, à l’exception des colonnes null fortement typées contenant la valeur null fortement typée appropriée.</span><span class="sxs-lookup"><span data-stu-id="d18a6-163">The default *default* value is `DbNull.Value` for all except the strongly typed null columns where it is the appropriate strongly typed null value.</span></span>  
  
2. <span data-ttu-id="d18a6-164">Les valeurs Null ne sont jamais écrites lors de la sérialisation vers des fichiers XML (comme dans « xsi:nil »).</span><span class="sxs-lookup"><span data-stu-id="d18a6-164">Null values are never written out during serialization to XML files (as in "xsi:nil").</span></span>  
  
3. <span data-ttu-id="d18a6-165">Toutes les valeurs non Null, y compris les valeurs par défaut, sont toujours écrites lors de la sérialisation en XML.</span><span class="sxs-lookup"><span data-stu-id="d18a6-165">All non-null values, including defaults, are always written out while serializing to XML.</span></span> <span data-ttu-id="d18a6-166">Contrairement à la sémantique XSD/XML, où une valeur Null (xsi:nil) est explicite et que la valeur par défaut est implicite (si elle n’est pas présente dans XML, un analyseur de validation peut l’obtenir à partir d’un schéma XSD associé).</span><span class="sxs-lookup"><span data-stu-id="d18a6-166">This is unlike XSD/XML semantics where a null value (xsi:nil) is explicit and the default value is implicit (if not present in XML, a validating parser can get it from an associated XSD schema).</span></span> <span data-ttu-id="d18a6-167">Le contraire est vrai pour une `DataTable` : une valeur Null est implicite et la valeur par défaut est explicite.</span><span class="sxs-lookup"><span data-stu-id="d18a6-167">The opposite is true for a `DataTable`: a null value is implicit and the default value is explicit.</span></span>  
  
4. <span data-ttu-id="d18a6-168">La Valeur Null est attribuée à toutes les valeurs de colonne manquantes pour les lignes lues à partir de l’entrée XML.</span><span class="sxs-lookup"><span data-stu-id="d18a6-168">All missing column values for rows read from XML input are assigned NULL.</span></span> <span data-ttu-id="d18a6-169">La valeur par défaut de DataColumn est attribuée aux lignes créées à l’aide de <xref:System.Data.DataTable.NewRow%2A> ou à des méthodes similaires.</span><span class="sxs-lookup"><span data-stu-id="d18a6-169">Rows created using <xref:System.Data.DataTable.NewRow%2A> or similar methods are assigned the DataColumn's default value.</span></span>  
  
5. <span data-ttu-id="d18a6-170">La méthode <xref:System.Data.DataRow.IsNull%2A> renvoie `true` pour `DbNull.Value` et `INullable.Null`.</span><span class="sxs-lookup"><span data-stu-id="d18a6-170">The <xref:System.Data.DataRow.IsNull%2A> method returns `true` for both `DbNull.Value` and `INullable.Null`.</span></span>  
  
## <a name="assigning-null-values"></a><span data-ttu-id="d18a6-171">Assignation de valeurs null</span><span class="sxs-lookup"><span data-stu-id="d18a6-171">Assigning Null Values</span></span>  
 <span data-ttu-id="d18a6-172">La valeur par défaut pour toute instance <xref:System.Data.SqlTypes> est Null.</span><span class="sxs-lookup"><span data-stu-id="d18a6-172">The default value for any <xref:System.Data.SqlTypes> instance is null.</span></span>  
  
 <span data-ttu-id="d18a6-173">Les valeurs Null dans <xref:System.Data.SqlTypes> sont spécifiques au type et ne peuvent pas être représentées par une valeur unique, telle que `DbNull`.</span><span class="sxs-lookup"><span data-stu-id="d18a6-173">Nulls in <xref:System.Data.SqlTypes> are type-specific and cannot be represented by a single value, such as `DbNull`.</span></span> <span data-ttu-id="d18a6-174">Utilisez la propriété `IsNull` pour vérifier les valeurs Null.</span><span class="sxs-lookup"><span data-stu-id="d18a6-174">Use the `IsNull` property to check for nulls.</span></span>  
  
 <span data-ttu-id="d18a6-175">Les valeurs Null peuvent être attribuées à un <xref:System.Data.DataColumn> comme indiqué dans l’exemple de code suivant.</span><span class="sxs-lookup"><span data-stu-id="d18a6-175">Null values can be assigned to a <xref:System.Data.DataColumn> as shown in the following code example.</span></span> <span data-ttu-id="d18a6-176">Vous pouvez attribuer directement des valeurs Null à des variables `SqlTypes` sans déclencher d’exception.</span><span class="sxs-lookup"><span data-stu-id="d18a6-176">You can directly assign null values to `SqlTypes` variables without triggering an exception.</span></span>  
  
### <a name="example"></a><span data-ttu-id="d18a6-177">Exemple</span><span class="sxs-lookup"><span data-stu-id="d18a6-177">Example</span></span>  
 <span data-ttu-id="d18a6-178">L’exemple de code suivant crée une <xref:System.Data.DataTable> avec deux colonnes définies comme <xref:System.Data.SqlTypes.SqlInt32> et <xref:System.Data.SqlTypes.SqlString>.</span><span class="sxs-lookup"><span data-stu-id="d18a6-178">The following code example creates a <xref:System.Data.DataTable> with two columns defined as <xref:System.Data.SqlTypes.SqlInt32> and <xref:System.Data.SqlTypes.SqlString>.</span></span> <span data-ttu-id="d18a6-179">Le code ajoute une ligne de valeurs connues, une ligne de valeurs Null, puis itère au sein de la <xref:System.Data.DataTable>, en attribuant les valeurs aux variables et en affichant le résultat dans la fenêtre de console.</span><span class="sxs-lookup"><span data-stu-id="d18a6-179">The code adds one row of known values, one row of null values and then iterates through the <xref:System.Data.DataTable>, assigning the values to variables and displaying the output in the console window.</span></span>  
  
 [!code-csharp[DataWorks SqlTypes.IsNull#1](../../../../../samples/snippets/csharp/VS_Snippets_ADO.NET/DataWorks SqlTypes.IsNull/CS/source.cs#1)]
 [!code-vb[DataWorks SqlTypes.IsNull#1](../../../../../samples/snippets/visualbasic/VS_Snippets_ADO.NET/DataWorks SqlTypes.IsNull/VB/source.vb#1)]  
  
 <span data-ttu-id="d18a6-180">Cet exemple produit les résultats suivants :</span><span class="sxs-lookup"><span data-stu-id="d18a6-180">This example displays the following results:</span></span>  
  
```output
isColumnNull=False, ID=123, Description=Side Mirror  
isColumnNull=True, ID=Null, Description=Null  
```  
  
## <a name="comparing-null-values-with-sqltypes-and-clr-types"></a><span data-ttu-id="d18a6-181">Comparaison de valeurs null à SqlTypes et des types CLR</span><span class="sxs-lookup"><span data-stu-id="d18a6-181">Comparing Null Values with SqlTypes and CLR Types</span></span>  
 <span data-ttu-id="d18a6-182">Lors de la comparaison de valeurs Null, il est important de comprendre la différence entre la façon dont la méthode `Equals` évalue les valeurs Null dans les <xref:System.Data.SqlTypes> par rapport à la façon dont elle fonctionne avec les types CLR.</span><span class="sxs-lookup"><span data-stu-id="d18a6-182">When comparing null values, it is important to understand the difference between the way the `Equals` method evaluates null values in <xref:System.Data.SqlTypes> as compared with the way it works with CLR types.</span></span> <span data-ttu-id="d18a6-183">Toutes les méthodes <xref:System.Data.SqlTypes>`Equals` utilisent la sémantique de base de données pour évaluer les valeurs Null : si une des valeurs ou les deux sont Null, la comparaison génère la valeur Null.</span><span class="sxs-lookup"><span data-stu-id="d18a6-183">All of the <xref:System.Data.SqlTypes>`Equals` methods use database semantics for evaluating null values: if either or both of the values is null, the comparison yields null.</span></span> <span data-ttu-id="d18a6-184">En revanche, l’utilisation de la méthode `Equals` CLR sur deux <xref:System.Data.SqlTypes> produira la valeur true si les deux sont Null.</span><span class="sxs-lookup"><span data-stu-id="d18a6-184">On the other hand, using the CLR `Equals` method on two <xref:System.Data.SqlTypes> will yield true if both are null.</span></span> <span data-ttu-id="d18a6-185">Cela reflète la différence entre l’utilisation d’une méthode d’instance telle que la méthode `String.Equals` CLR et l’utilisation de la méthode statique/partagée, `SqlString.Equals`.</span><span class="sxs-lookup"><span data-stu-id="d18a6-185">This reflects the difference between using an instance method such as the CLR `String.Equals` method, and using the static/shared method, `SqlString.Equals`.</span></span>  
  
 <span data-ttu-id="d18a6-186">L’exemple suivant illustre la différence de résultats entre la méthode `SqlString.Equals` et la méthode `String.Equals` quand chacun reçoit une paire de valeurs Null, puis une paire de chaînes vides.</span><span class="sxs-lookup"><span data-stu-id="d18a6-186">The following example demonstrates the difference in results between the `SqlString.Equals` method and the `String.Equals` method when each is passed a pair of null values and then a pair of empty strings.</span></span>  
  
 [!code-csharp[DataWorks SqlTypes.CompareNulls#1](../../../../../samples/snippets/csharp/VS_Snippets_ADO.NET/DataWorks SqlTypes.CompareNulls/CS/source.cs#1)]
 [!code-vb[DataWorks SqlTypes.CompareNulls#1](../../../../../samples/snippets/visualbasic/VS_Snippets_ADO.NET/DataWorks SqlTypes.CompareNulls/VB/source.vb#1)]  
  
 <span data-ttu-id="d18a6-187">Ce code produit la sortie suivante :</span><span class="sxs-lookup"><span data-stu-id="d18a6-187">The code produces the following output:</span></span>  
  
```output
SqlString.Equals shared/static method:  
  Two nulls=Null  
  
String.Equals instance method:  
  Two nulls=True  
  
SqlString.Equals shared/static method:  
  Two empty strings=True  
  
String.Equals instance method:  
  Two empty strings=True
```  
  
## <a name="see-also"></a><span data-ttu-id="d18a6-188">Voir aussi</span><span class="sxs-lookup"><span data-stu-id="d18a6-188">See also</span></span>

- [<span data-ttu-id="d18a6-189">Types de données SQL Server et ADO.NET</span><span class="sxs-lookup"><span data-stu-id="d18a6-189">SQL Server Data Types and ADO.NET</span></span>](sql-server-data-types.md)
- [<span data-ttu-id="d18a6-190">Vue d'ensemble d’ADO.NET</span><span class="sxs-lookup"><span data-stu-id="d18a6-190">ADO.NET Overview</span></span>](../ado-net-overview.md)
