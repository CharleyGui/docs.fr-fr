---
title: Écrire un hôte de runtime .NET Core personnalisé
description: Découvrez comment héberger le runtime .NET Core à partir du code natif pour prendre en charge des scénarios avancés nécessitant un contrôle du fonctionnement du runtime .NET Core.
author: mjrousos
ms.topic: how-to
ms.date: 12/21/2018
ms.openlocfilehash: 03cf188fc74e8a70798c0bcc4a6940730abfc07c
ms.sourcegitcommit: 5b475c1855b32cf78d2d1bbb4295e4c236f39464
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 09/24/2020
ms.locfileid: "91180462"
---
# <a name="write-a-custom-net-core-host-to-control-the-net-runtime-from-your-native-code"></a><span data-ttu-id="d1e1c-103">Écrire un hôte .NET Core personnalisé pour contrôler le runtime .NET à partir de votre code natif</span><span class="sxs-lookup"><span data-stu-id="d1e1c-103">Write a custom .NET Core host to control the .NET runtime from your native code</span></span>

<span data-ttu-id="d1e1c-104">Comme tout code managé, les applications .NET Core sont exécutées par un hôte.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-104">Like all managed code, .NET Core applications are executed by a host.</span></span> <span data-ttu-id="d1e1c-105">L’hôte est chargé du démarrage du runtime (notamment des composants comme le JIT et le récupérateur de mémoire) et de l’appel des points d’entrée managés.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-105">The host is responsible for starting the runtime (including components like the JIT and garbage collector) and invoking managed entry points.</span></span>

<span data-ttu-id="d1e1c-106">L’hébergement du runtime .NET Core est un scénario avancé et, dans la plupart des cas, les développeurs .NET Core n’ont pas à se soucier de l’hébergement, car les processus de génération .NET Core fournissent un hôte par défaut pour exécuter les applications .NET Core.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-106">Hosting the .NET Core runtime is an advanced scenario and, in most cases, .NET Core developers don't need to worry about hosting because .NET Core build processes provide a default host to run .NET Core applications.</span></span> <span data-ttu-id="d1e1c-107">Toutefois, dans certains cas spécifiques, il peut être utile d’héberger explicitement le runtime .NET Core, pour appeler le code managé dans un processus natif ou pour mieux contrôler le fonctionnement du runtime.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-107">In some specialized circumstances, though, it can be useful to explicitly host the .NET Core runtime, either as a means of invoking managed code in a native process or in order to gain more control over how the runtime works.</span></span>

<span data-ttu-id="d1e1c-108">Cet article donne une vue d’ensemble des étapes nécessaires pour démarrer le runtime .NET Core à partir du code natif et y exécuter du code managé.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-108">This article gives an overview of the steps necessary to start the .NET Core runtime from native code and execute managed code in it.</span></span>

## <a name="prerequisites"></a><span data-ttu-id="d1e1c-109">Prérequis</span><span class="sxs-lookup"><span data-stu-id="d1e1c-109">Prerequisites</span></span>

<span data-ttu-id="d1e1c-110">Étant donné que les hôtes sont des applications natives, ce didacticiel traite de la construction d’une application C++ pour héberger .NET Core.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-110">Because hosts are native applications, this tutorial covers constructing a C++ application to host .NET Core.</span></span> <span data-ttu-id="d1e1c-111">Vous avez besoin d’un environnement de développement C++ (comme celui fourni par [Visual Studio](https://aka.ms/vsdownload?utm_source=mscom&utm_campaign=msdocs)).</span><span class="sxs-lookup"><span data-stu-id="d1e1c-111">You will need a C++ development environment (such as that provided by [Visual Studio](https://aka.ms/vsdownload?utm_source=mscom&utm_campaign=msdocs)).</span></span>

<span data-ttu-id="d1e1c-112">Vous avez également besoin d’une application .NET Core simple pour tester l’hôte, vous devez donc installer le [SDK .NET Core](https://dotnet.microsoft.com/download) et [créer une petite application de test .NET Core](with-visual-studio.md) (par exemple, une application « Hello World »).</span><span class="sxs-lookup"><span data-stu-id="d1e1c-112">You will also want a simple .NET Core application to test the host with, so you should install the [.NET Core SDK](https://dotnet.microsoft.com/download) and [build a small .NET Core test app](with-visual-studio.md) (such as a 'Hello World' app).</span></span> <span data-ttu-id="d1e1c-113">L’application « Hello World » créée par le nouveau modèle de projet de console .NET Core est suffisante.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-113">The 'Hello World' app created by the new .NET Core console project template is sufficient.</span></span>

## <a name="hosting-apis"></a><span data-ttu-id="d1e1c-114">API d’hébergement</span><span class="sxs-lookup"><span data-stu-id="d1e1c-114">Hosting APIs</span></span>

<span data-ttu-id="d1e1c-115">Vous pouvez utiliser trois API différentes pour héberger .NET Core.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-115">There are three different APIs that can be used to host .NET Core.</span></span> <span data-ttu-id="d1e1c-116">Cet article (et ses [exemples](https://github.com/dotnet/samples/tree/master/core/hosting)associés) couvre toutes les options.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-116">This article (and its associated [samples](https://github.com/dotnet/samples/tree/master/core/hosting)) covers all options.</span></span>

* <span data-ttu-id="d1e1c-117">La méthode d’hébergement conseillée pour le runtime .NET Core dans .NET Core 3.0 et versions ultérieures est d’utiliser les API des bibliothèques `nethost` et `hostfxr`.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-117">The preferred method of hosting the .NET Core runtime in .NET Core 3.0 and above is with the `nethost` and `hostfxr` libraries' APIs.</span></span> <span data-ttu-id="d1e1c-118">Ces points d’entrée gèrent la complexité de la recherche et de la configuration du runtime pour l’initialisation, et permettent à la fois de lancer une application managée et d’appeler une méthode managée statique.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-118">These entry points handle the complexity of finding and setting up the runtime for initialization and allow both launching a managed application and calling into a static managed method.</span></span>
* <span data-ttu-id="d1e1c-119">La méthode recommandée pour héberger le Runtime .NET Core avant .NET Core 3,0 est l' [`coreclrhost.h`](https://github.com/dotnet/runtime/blob/master/src/coreclr/src/hosts/inc/coreclrhost.h) API.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-119">The preferred method of hosting the .NET Core runtime prior to .NET Core 3.0 is with the [`coreclrhost.h`](https://github.com/dotnet/runtime/blob/master/src/coreclr/src/hosts/inc/coreclrhost.h) API.</span></span> <span data-ttu-id="d1e1c-120">Cette API expose des fonctions qui permettent de démarrer et d’arrêter facilement le runtime et d’appeler du code managé (soit en lançant un exécutable managé, soit en appelant des méthodes managées statiques).</span><span class="sxs-lookup"><span data-stu-id="d1e1c-120">This API exposes functions for easily starting and stopping the runtime and invoking managed code (either by launching a managed exe or by calling static managed methods).</span></span>

## <a name="sample-hosts"></a><span data-ttu-id="d1e1c-121">Exemples d’hôtes</span><span class="sxs-lookup"><span data-stu-id="d1e1c-121">Sample Hosts</span></span>

<span data-ttu-id="d1e1c-122">Des [exemples d’hôtes](https://github.com/dotnet/samples/tree/master/core/hosting) illustrant les étapes décrites dans les tutoriels ci-dessous sont disponibles dans le dépôt GitHub dotnet/samples.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-122">[Sample hosts](https://github.com/dotnet/samples/tree/master/core/hosting) demonstrating the steps outlined in the tutorials below are available in the dotnet/samples GitHub repository.</span></span> <span data-ttu-id="d1e1c-123">Les commentaires dans les exemples associent clairement les étapes numérotées de ces tutoriels à l’endroit où elles sont exécutées dans l’exemple.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-123">Comments in the samples clearly associate the numbered steps from these tutorials with where they're performed in the sample.</span></span> <span data-ttu-id="d1e1c-124">Pour obtenir des instructions de téléchargement, consultez [Exemples et didacticiels](../../samples-and-tutorials/index.md#view-and-download-samples).</span><span class="sxs-lookup"><span data-stu-id="d1e1c-124">For download instructions, see [Samples and Tutorials](../../samples-and-tutorials/index.md#view-and-download-samples).</span></span>

<span data-ttu-id="d1e1c-125">N’oubliez pas que les exemples d’hôtes sont destinés à être utilisés dans un contexte d’apprentissage. Ils ne s’attardent donc pas sur la vérification des erreurs et privilégient la lisibilité par rapport à l’efficacité.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-125">Keep in mind that the sample hosts are meant to be used for learning purposes, so they are light on error checking and are designed to emphasize readability over efficiency.</span></span>

## <a name="create-a-host-using-nethosth-and-hostfxrh"></a><span data-ttu-id="d1e1c-126">Créer un hôte à l’aide `nethost.h` de et `hostfxr.h`</span><span class="sxs-lookup"><span data-stu-id="d1e1c-126">Create a host using `nethost.h` and `hostfxr.h`</span></span>

<span data-ttu-id="d1e1c-127">Les étapes suivantes décrivent comment utiliser les bibliothèques `nethost` et `hostfxr` pour démarrer le runtime .NET Core dans une application native et appeler une méthode statique managée.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-127">The following steps detail how to use the `nethost` and `hostfxr` libraries to start the .NET Core runtime in a native application and call into a managed static method.</span></span> <span data-ttu-id="d1e1c-128">[L’exemple](https://github.com/dotnet/samples/tree/master/core/hosting/HostWithHostFxr) utilise l’en-tête `nethost` et la bibliothèque installée avec le kit de développement logiciel .NET et des copies des fichiers [`coreclr_delegates.h`](https://github.com/dotnet/core-setup/blob/master/src/corehost/cli/coreclr_delegates.h) et [`hostfxr.h`](https://github.com/dotnet/core-setup/blob/master/src/corehost/cli/hostfxr.h) à partir du référentiel [dotnet/core-setup](https://github.com/dotnet/core-setup).</span><span class="sxs-lookup"><span data-stu-id="d1e1c-128">The [sample](https://github.com/dotnet/samples/tree/master/core/hosting/HostWithHostFxr) uses the `nethost` header and library installed with the .NET SDK and copies of the [`coreclr_delegates.h`](https://github.com/dotnet/core-setup/blob/master/src/corehost/cli/coreclr_delegates.h) and [`hostfxr.h`](https://github.com/dotnet/core-setup/blob/master/src/corehost/cli/hostfxr.h) files from the [dotnet/core-setup](https://github.com/dotnet/core-setup) repository.</span></span>

### <a name="step-1---load-hostfxr-and-get-exported-hosting-functions"></a><span data-ttu-id="d1e1c-129">Étape 1 : charger `hostfxr` et obtenir les fonctions d’hébergement exportées</span><span class="sxs-lookup"><span data-stu-id="d1e1c-129">Step 1 - Load `hostfxr` and get exported hosting functions</span></span>

<span data-ttu-id="d1e1c-130">La bibliothèque `nethost` fournit la fonction `get_hostfxr_path` pour localiser la bibliothèque `hostfxr`.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-130">The `nethost` library provides the `get_hostfxr_path` function for locating the `hostfxr` library.</span></span> <span data-ttu-id="d1e1c-131">La bibliothèque `hostfxr` expose des fonctions pour l’hébergement du runtime .NET Core.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-131">The `hostfxr` library exposes functions for hosting the .NET Core runtime.</span></span> <span data-ttu-id="d1e1c-132">Vous trouverez la liste complète des fonctions dans [`hostfxr.h`](https://github.com/dotnet/core-setup/blob/master/src/corehost/cli/hostfxr.h) et dans le [document de conception de l’hébergement natif](https://github.com/dotnet/core-setup/blob/master/Documentation/design-docs/native-hosting.md).</span><span class="sxs-lookup"><span data-stu-id="d1e1c-132">The full list of functions can be found in [`hostfxr.h`](https://github.com/dotnet/core-setup/blob/master/src/corehost/cli/hostfxr.h) and the [native hosting design document](https://github.com/dotnet/core-setup/blob/master/Documentation/design-docs/native-hosting.md).</span></span> <span data-ttu-id="d1e1c-133">L’exemple et ce didacticiel utilisent les éléments suivants :</span><span class="sxs-lookup"><span data-stu-id="d1e1c-133">The sample and this tutorial use the following:</span></span>

* <span data-ttu-id="d1e1c-134">`hostfxr_initialize_for_runtime_config`: Initialise un contexte hôte et prépare l’initialisation du Runtime .NET Core à l’aide de la configuration du runtime spécifiée.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-134">`hostfxr_initialize_for_runtime_config`: Initializes a host context and prepares for initialization of the .NET Core runtime using the specified runtime configuration.</span></span>
* <span data-ttu-id="d1e1c-135">`hostfxr_get_runtime_delegate`: Obtient un délégué pour les fonctionnalités d’exécution.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-135">`hostfxr_get_runtime_delegate`: Gets a delegate for runtime functionality.</span></span>
* <span data-ttu-id="d1e1c-136">`hostfxr_close`: Ferme un contexte hôte.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-136">`hostfxr_close`: Closes a host context.</span></span>

<span data-ttu-id="d1e1c-137">La bibliothèque `hostfxr` peut être obtenue avec `get_hostfxr_path`.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-137">The `hostfxr` library is found using `get_hostfxr_path`.</span></span> <span data-ttu-id="d1e1c-138">Elle est ensuite chargée, et ses exportations sont récupérées.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-138">It is then loaded and its exports are retrieved.</span></span>

[!code-cpp[HostFxrHost#LoadHostFxr](~/samples/snippets/core/tutorials/netcore-hosting/csharp/HostWithHostFxr/src/NativeHost/nativehost.cpp#LoadHostFxr)]

### <a name="step-2---initialize-and-start-the-net-core-runtime"></a><span data-ttu-id="d1e1c-139">Étape 2 : Initialiser et démarrer le runtime .NET Core</span><span class="sxs-lookup"><span data-stu-id="d1e1c-139">Step 2 - Initialize and start the .NET Core runtime</span></span>

<span data-ttu-id="d1e1c-140">Les fonctions `hostfxr_initialize_for_runtime_config` et `hostfxr_get_runtime_delegate` initialisent et démarrent le runtime .NET Core à l’aide de la configuration du runtime pour le composant managé qui est chargé.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-140">The `hostfxr_initialize_for_runtime_config` and `hostfxr_get_runtime_delegate` functions initialize and start the .NET Core runtime using the runtime configuration for the managed component that will be loaded.</span></span> <span data-ttu-id="d1e1c-141">La fonction `hostfxr_get_runtime_delegate` est utilisée pour obtenir un délégué de runtime qui permet le chargement d’un assembly managé et l’obtention d’un pointeur de fonction dans une méthode statique de cet assembly.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-141">The `hostfxr_get_runtime_delegate` function is used to get a runtime delegate that allows loading a managed assembly and getting a function pointer to a static method in that assembly.</span></span>

[!code-cpp[HostFxrHost#Initialize](~/samples/snippets/core/tutorials/netcore-hosting/csharp/HostWithHostFxr/src/NativeHost/nativehost.cpp#Initialize)]

### <a name="step-3---load-managed-assembly-and-get-function-pointer-to-a-managed-method"></a><span data-ttu-id="d1e1c-142">Étape 3 : Charger l’assembly managé et obtenir le pointeur de fonction sur une méthode managée</span><span class="sxs-lookup"><span data-stu-id="d1e1c-142">Step 3 - Load managed assembly and get function pointer to a managed method</span></span>

<span data-ttu-id="d1e1c-143">Le délégué de runtime est appelé pour charger l’assembly managé et obtenir un pointeur de fonction vers une méthode managée.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-143">The runtime delegate is called to load the managed assembly and get a function pointer to a managed method.</span></span> <span data-ttu-id="d1e1c-144">Le délégué nécessite le chemin d’accès de l’assembly, le nom de type et le nom de méthode en entrée, et retourne un pointeur de fonction qui peut être utilisé pour appeler la méthode managée.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-144">The delegate requires the assembly path, type name, and method name as inputs and returns a function pointer that can be used to invoke the managed method.</span></span>

[!code-cpp[HostFxrHost#LoadAndGet](~/samples/snippets/core/tutorials/netcore-hosting/csharp/HostWithHostFxr/src/NativeHost/nativehost.cpp#LoadAndGet)]

<span data-ttu-id="d1e1c-145">En passant `nullptr` comme nom de type de délégué lors de l’appel du délégué du runtime, l’exemple utilise une signature par défaut pour la méthode managée :</span><span class="sxs-lookup"><span data-stu-id="d1e1c-145">By passing `nullptr` as the delegate type name when calling the runtime delegate, the sample uses a default signature for the managed method:</span></span>

```csharp
public delegate int ComponentEntryPoint(IntPtr args, int sizeBytes);
```

<span data-ttu-id="d1e1c-146">Une signature différente peut être utilisée en spécifiant le nom du type de délégué lors de l’appel du délégué de runtime.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-146">A different signature can be used by specifying the delegate type name when calling the runtime delegate.</span></span>

### <a name="step-4---run-managed-code"></a><span data-ttu-id="d1e1c-147">Étape 4 : Exécuter le code managé !</span><span class="sxs-lookup"><span data-stu-id="d1e1c-147">Step 4 - Run managed code!</span></span>

<span data-ttu-id="d1e1c-148">L’hôte natif peut maintenant appeler la méthode managée et lui passer les paramètres souhaités.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-148">The native host can now call the managed method and pass it the desired parameters.</span></span>

[!code-cpp[HostFxrHost#CallManaged](~/samples/snippets/core/tutorials/netcore-hosting/csharp/HostWithHostFxr/src/NativeHost/nativehost.cpp#CallManaged)]

## <a name="create-a-host-using-coreclrhosth"></a><span data-ttu-id="d1e1c-149">Créer un hôte à l’aide de `coreclrhost.h`</span><span class="sxs-lookup"><span data-stu-id="d1e1c-149">Create a host using `coreclrhost.h`</span></span>

<span data-ttu-id="d1e1c-150">Les étapes suivantes détaillent comment utiliser l' `coreclrhost.h` API pour démarrer le Runtime .net core dans une application native et appeler une méthode statique managée.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-150">The following steps detail how to use the `coreclrhost.h` API to start the .NET Core runtime in a native application and call into a managed static method.</span></span> <span data-ttu-id="d1e1c-151">Les extraits de code de ce document utilisent des API spécifiques à Windows, mais l’[exemple d’hôte complet](https://github.com/dotnet/samples/tree/master/core/hosting/HostWithCoreClrHost) indique les chemins de code pour Windows et Linux.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-151">The code snippets in this document use some Windows-specific APIs, but the [full sample host](https://github.com/dotnet/samples/tree/master/core/hosting/HostWithCoreClrHost) shows both Windows and Linux code paths.</span></span>

<span data-ttu-id="d1e1c-152">L' [hôte de coexécution UNIX](https://github.com/dotnet/runtime/tree/master/src/coreclr/src/hosts/unixcorerun) présente un exemple plus complexe et réaliste d’hébergement à l’aide de `coreclrhost.h` .</span><span class="sxs-lookup"><span data-stu-id="d1e1c-152">The [Unix CoreRun host](https://github.com/dotnet/runtime/tree/master/src/coreclr/src/hosts/unixcorerun) shows a more complex, real-world example of hosting using `coreclrhost.h`.</span></span>

### <a name="step-1---find-and-load-coreclr"></a><span data-ttu-id="d1e1c-153">Étape 1 : Rechercher et charger CoreCLR</span><span class="sxs-lookup"><span data-stu-id="d1e1c-153">Step 1 - Find and load CoreCLR</span></span>

<span data-ttu-id="d1e1c-154">Les API de runtime .NET Core se trouvent dans *coreclr.dll* (sur Windows), dans *libcoreclr.so* (sur Linux) ou dans *libcoreclr.dylib* (sur macOS).</span><span class="sxs-lookup"><span data-stu-id="d1e1c-154">The .NET Core runtime APIs are in *coreclr.dll* (on Windows), in *libcoreclr.so* (on Linux), or in *libcoreclr.dylib* (on macOS).</span></span> <span data-ttu-id="d1e1c-155">La première étape à suivre pour héberger .NET Core consiste à charger la bibliothèque CoreCLR.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-155">The first step to hosting .NET Core is to load the CoreCLR library.</span></span> <span data-ttu-id="d1e1c-156">Certains hôtes sondent des chemins différents ou utilisent des paramètres d’entrée pour trouver la bibliothèque, tandis que d’autres savent la charger à partir d’un chemin donné (à côté de l’hôte, par exemple, ou à partir d’un emplacement dans l’ordinateur).</span><span class="sxs-lookup"><span data-stu-id="d1e1c-156">Some hosts probe different paths or use input parameters to find the library while others know to load it from a certain path (next to the host, for example, or from a machine-wide location).</span></span>

<span data-ttu-id="d1e1c-157">Une fois trouvé, la bibliothèque est chargée avec `LoadLibraryEx` (sur Windows) ou `dlopen` (sur Linux/MacOS).</span><span class="sxs-lookup"><span data-stu-id="d1e1c-157">Once found, the library is loaded with `LoadLibraryEx` (on Windows) or `dlopen` (on Linux/macOS).</span></span>

[!code-cpp[CoreClrHost#1](~/samples/snippets/core/tutorials/netcore-hosting/csharp/HostWithCoreClrHost/src/SampleHost.cpp#1)]

### <a name="step-2---get-net-core-hosting-functions"></a><span data-ttu-id="d1e1c-158">Étape 2 : Obtenir les fonctions d’hébergement .NET Core</span><span class="sxs-lookup"><span data-stu-id="d1e1c-158">Step 2 - Get .NET Core hosting functions</span></span>

<span data-ttu-id="d1e1c-159">CoreClrHost propose plusieurs méthodes importantes et utiles pour héberger .NET Core :</span><span class="sxs-lookup"><span data-stu-id="d1e1c-159">CoreClrHost has several important methods useful for hosting .NET Core:</span></span>

* <span data-ttu-id="d1e1c-160">`coreclr_initialize`: Démarre le Runtime .NET Core et configure l’AppDomain par défaut (et uniquement).</span><span class="sxs-lookup"><span data-stu-id="d1e1c-160">`coreclr_initialize`: Starts the .NET Core runtime and sets up the default (and only) AppDomain.</span></span>
* <span data-ttu-id="d1e1c-161">`coreclr_execute_assembly`: Exécute un assembly managé.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-161">`coreclr_execute_assembly`: Executes a managed assembly.</span></span>
* <span data-ttu-id="d1e1c-162">`coreclr_create_delegate`: Crée un pointeur de fonction vers une méthode managée.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-162">`coreclr_create_delegate`: Creates a function pointer to a managed method.</span></span>
* <span data-ttu-id="d1e1c-163">`coreclr_shutdown`: Arrête le Runtime .NET Core.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-163">`coreclr_shutdown`: Shuts down the .NET Core runtime.</span></span>
* <span data-ttu-id="d1e1c-164">`coreclr_shutdown_2`: Comme `coreclr_shutdown` , mais récupère également le code de sortie du code managé.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-164">`coreclr_shutdown_2`: Like `coreclr_shutdown`, but also retrieves the managed code's exit code.</span></span>

<span data-ttu-id="d1e1c-165">Après le chargement de la bibliothèque CoreCLR, l’étape suivante consiste à obtenir des références à ces fonctions à l’aide de `GetProcAddress` (sur Windows) ou `dlsym` (sur Linux/MacOS).</span><span class="sxs-lookup"><span data-stu-id="d1e1c-165">After loading the CoreCLR library, the next step is to get references to these functions using `GetProcAddress` (on Windows) or `dlsym` (on Linux/macOS).</span></span>

[!code-cpp[CoreClrHost#2](~/samples/snippets/core/tutorials/netcore-hosting/csharp/HostWithCoreClrHost/src/SampleHost.cpp#2)]

### <a name="step-3---prepare-runtime-properties"></a><span data-ttu-id="d1e1c-166">Étape 3 : Préparer les propriétés du runtime</span><span class="sxs-lookup"><span data-stu-id="d1e1c-166">Step 3 - Prepare runtime properties</span></span>

<span data-ttu-id="d1e1c-167">Avant de démarrer le runtime, il est nécessaire de préparer certaines propriétés pour spécifier le comportement (en particulier celui du chargeur d’assembly).</span><span class="sxs-lookup"><span data-stu-id="d1e1c-167">Before starting the runtime, it is necessary to prepare some properties to specify behavior (especially concerning the assembly loader).</span></span>

<span data-ttu-id="d1e1c-168">Parmi les propriétés communes, citons les suivantes :</span><span class="sxs-lookup"><span data-stu-id="d1e1c-168">Common properties include:</span></span>

* <span data-ttu-id="d1e1c-169">`TRUSTED_PLATFORM_ASSEMBLIES` Liste de chemins d’assembly (délimités par « ; » sur Windows et « : » sur Linux) que le runtime pourra résoudre par défaut.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-169">`TRUSTED_PLATFORM_ASSEMBLIES` This is a list of assembly paths (delimited by ';' on Windows and ':' on Linux) which the runtime will be able to resolve by default.</span></span> <span data-ttu-id="d1e1c-170">Certains hôtes ont des manifestes codés en dur listant les assemblys qu’ils peuvent charger.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-170">Some hosts have hard-coded manifests listing assemblies they can load.</span></span> <span data-ttu-id="d1e1c-171">D’autres placent les bibliothèques à certains emplacements (par exemple, à côté de *coreclr.dll*) dans cette liste.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-171">Others will put any library in certain locations (next to *coreclr.dll*, for example) on this list.</span></span>
* <span data-ttu-id="d1e1c-172">`APP_PATHS` Il s’agit d’une liste de chemins où rechercher un assembly s’il est introuvable dans la liste TPA (liste d’assemblys de plateforme sécurisée).</span><span class="sxs-lookup"><span data-stu-id="d1e1c-172">`APP_PATHS` This is a list of paths to probe in for an assembly if it can't be found in the trusted platform assemblies (TPA) list.</span></span> <span data-ttu-id="d1e1c-173">Étant donné que l’hôte a davantage de contrôle sur les assemblys chargés à l’aide de la liste TPA, il est recommandé aux hôtes de déterminer les assemblys qu’ils comptent charger et de les lister explicitement.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-173">Because the host has more control over which assemblies are loaded using the TPA list, it is a best practice for hosts to determine which assemblies they expect to load and list them explicitly.</span></span> <span data-ttu-id="d1e1c-174">Toutefois, si la détection au moment de l’exécution est nécessaire, cette propriété peut activer ce scénario.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-174">If probing at run time is needed, however, this property can enable that scenario.</span></span>
* <span data-ttu-id="d1e1c-175">`APP_NI_PATHS` Cette liste est similaire à APP_PATHS, sauf qu’il s’agit de chemins où sonder des images natives.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-175">`APP_NI_PATHS` This list is similar to APP_PATHS except that it's meant to be paths that will be probed for native images.</span></span>
* <span data-ttu-id="d1e1c-176">`NATIVE_DLL_SEARCH_DIRECTORIES` Cette propriété est une liste de chemins où le chargeur doit sonder les bibliothèques natives appelées avec p/invoke.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-176">`NATIVE_DLL_SEARCH_DIRECTORIES` This property is a list of paths the loader should probe when looking for native libraries called via p/invoke.</span></span>
* <span data-ttu-id="d1e1c-177">`PLATFORM_RESOURCE_ROOTS` Cette liste comprend les chemins d’accès pour détecter les assemblys satellites de ressources (dans les sous-répertoires spécifiques à la culture).</span><span class="sxs-lookup"><span data-stu-id="d1e1c-177">`PLATFORM_RESOURCE_ROOTS` This list includes paths to probe in for resource satellite assemblies (in culture-specific subdirectories).</span></span>

<span data-ttu-id="d1e1c-178">Dans cet exemple d’hôte, la liste TPA est construite en listant simplement toutes les bibliothèques du répertoire actif :</span><span class="sxs-lookup"><span data-stu-id="d1e1c-178">In this sample host, the TPA list is constructed by simply listing all libraries in the current directory:</span></span>

[!code-cpp[CoreClrHost#7](~/samples/snippets/core/tutorials/netcore-hosting/csharp/HostWithCoreClrHost/src/SampleHost.cpp#7)]

<span data-ttu-id="d1e1c-179">Comme l’exemple est simple, il n’a besoin que de la propriété `TRUSTED_PLATFORM_ASSEMBLIES` :</span><span class="sxs-lookup"><span data-stu-id="d1e1c-179">Because the sample is simple, it only needs the `TRUSTED_PLATFORM_ASSEMBLIES` property:</span></span>

[!code-cpp[CoreClrHost#3](~/samples/snippets/core/tutorials/netcore-hosting/csharp/HostWithCoreClrHost/src/SampleHost.cpp#3)]

### <a name="step-4---start-the-runtime"></a><span data-ttu-id="d1e1c-180">Étape 4 : Démarrer le runtime</span><span class="sxs-lookup"><span data-stu-id="d1e1c-180">Step 4 - Start the runtime</span></span>

<span data-ttu-id="d1e1c-181">`coreclrhost.h` Les API démarrent le runtime et créent l’AppDomain par défaut tout avec un seul appel.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-181">`coreclrhost.h` APIs start the runtime and create the default AppDomain all with a single call.</span></span> <span data-ttu-id="d1e1c-182">La fonction `coreclr_initialize` prend un chemin de base, un nom et les propriétés décrites précédemment, puis retourne un descripteur à l’hôte par le biais du paramètre `hostHandle`.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-182">The `coreclr_initialize` function takes a base path, name, and the properties described earlier and returns back a handle to the host via the `hostHandle` parameter.</span></span>

[!code-cpp[CoreClrHost#4](~/samples/snippets/core/tutorials/netcore-hosting/csharp/HostWithCoreClrHost/src/SampleHost.cpp#4)]

### <a name="step-5---run-managed-code"></a><span data-ttu-id="d1e1c-183">Étape 5 : Exécuter le code managé</span><span class="sxs-lookup"><span data-stu-id="d1e1c-183">Step 5 - Run managed code!</span></span>

<span data-ttu-id="d1e1c-184">Le runtime étant démarré, l’hôte peut appeler le code managé.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-184">With the runtime started, the host can call managed code.</span></span> <span data-ttu-id="d1e1c-185">Pour cela, deux méthodes sont disponibles.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-185">This can be done in a couple of different ways.</span></span> <span data-ttu-id="d1e1c-186">L’exemple de code lié à ce tutoriel utilise la fonction `coreclr_create_delegate` permettant de créer un délégué à une méthode managée statique.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-186">The sample code linked to this tutorial uses the `coreclr_create_delegate` function to create a delegate to a static managed method.</span></span> <span data-ttu-id="d1e1c-187">Cette API prend le [nom d’assembly](../../standard/assembly/names.md), le nom de type qualifié par un espace de noms et le nom de méthode comme entrées, puis retourne un délégué qui peut être utilisé pour appeler la méthode.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-187">This API takes the [assembly name](../../standard/assembly/names.md), namespace-qualified type name, and method name as inputs and returns a delegate that can be used to invoke the method.</span></span>

[!code-cpp[CoreClrHost#5](~/samples/snippets/core/tutorials/netcore-hosting/csharp/HostWithCoreClrHost/src/SampleHost.cpp#5)]

<span data-ttu-id="d1e1c-188">Dans cet exemple, l’hôte peut désormais appeler `managedDelegate` pour exécuter la méthode `ManagedWorker.DoWork`.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-188">In this sample, the host can now call `managedDelegate` to run the `ManagedWorker.DoWork` method.</span></span>

<span data-ttu-id="d1e1c-189">Vous pouvez également utiliser la fonction `coreclr_execute_assembly` pour lancer un exécutable managé.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-189">Alternatively, the `coreclr_execute_assembly` function can be used to launch a managed executable.</span></span> <span data-ttu-id="d1e1c-190">Cette API prend un chemin d’assembly et un tableau d’arguments comme paramètres d’entrée.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-190">This API takes an assembly path and array of arguments as input parameters.</span></span> <span data-ttu-id="d1e1c-191">Elle charge l’assembly à ce chemin et appelle sa méthode main.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-191">It loads the assembly at that path and invokes its main method.</span></span>

```c++
int hr = executeAssembly(
        hostHandle,
        domainId,
        argumentCount,
        arguments,
        "HelloWorld.exe",
        (unsigned int*)&exitCode);
```

### <a name="step-6---shutdown-and-clean-up"></a><span data-ttu-id="d1e1c-192">Étape 6 : Arrêter et nettoyer</span><span class="sxs-lookup"><span data-stu-id="d1e1c-192">Step 6 - Shutdown and clean up</span></span>

<span data-ttu-id="d1e1c-193">Enfin, quand l’hôte termine l’exécution du code managé, le runtime .NET Core est arrêté avec `coreclr_shutdown` ou `coreclr_shutdown_2`.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-193">Finally, when the host is done running managed code, the .NET Core runtime is shut down with `coreclr_shutdown` or `coreclr_shutdown_2`.</span></span>

[!code-cpp[CoreClrHost#6](~/samples/snippets/core/tutorials/netcore-hosting/csharp/HostWithCoreClrHost/src/SampleHost.cpp#6)]

<span data-ttu-id="d1e1c-194">CoreCLR ne prend pas en charge la réinitialisation ou le déchargement.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-194">CoreCLR does not support reinitialization or unloading.</span></span> <span data-ttu-id="d1e1c-195">N’appelez pas `coreclr_initialize` à nouveau ou ne déchargez pas la bibliothèque CoreCLR.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-195">Do not call `coreclr_initialize` again or unload the CoreCLR library.</span></span>

## <a name="conclusion"></a><span data-ttu-id="d1e1c-196">Conclusion</span><span class="sxs-lookup"><span data-stu-id="d1e1c-196">Conclusion</span></span>
<span data-ttu-id="d1e1c-197">Une fois que votre hôte est créé, il peut être testé en l’exécutant à partir de la ligne de commande et en passant tous les arguments attendus par l’hôte.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-197">Once your host is built, it can be tested by running it from the command line and passing any arguments the host expects.</span></span> <span data-ttu-id="d1e1c-198">Quand vous spécifiez l’application .NET Core que l’hôte doit exécuter, utilisez le fichier .dll généré par `dotnet build`.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-198">When specifying the .NET Core app for the host to run, be sure to use the .dll that is produced by `dotnet build`.</span></span> <span data-ttu-id="d1e1c-199">Les exécutables (fichiers .exe) générés par `dotnet publish` pour les applications autonomes sont l’hôte .NET Core par défaut (pour que l’application puisse être lancée directement à partir de la ligne de commande dans les scénarios principaux) ; le code utilisateur est compilé dans une dll du même nom.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-199">Executables (.exe files) produced by `dotnet publish` for self-contained applications are actually the default .NET Core host (so that the app can be launched directly from the command line in mainline scenarios); user code is compiled into a dll of the same name.</span></span>

<span data-ttu-id="d1e1c-200">Si ce n’est pas le cas, double-Vérifiez que *coreclr.dll* est disponible à l’emplacement attendu par l’hôte, que toutes les bibliothèques d’infrastructure nécessaires se trouvent dans la liste TPA et que le nombre de bits de CoreCLR (32 bits ou 64 bits) correspond à la façon dont l’hôte a été créé.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-200">If things don't work initially, double-check that *coreclr.dll* is available in the location expected by the host, that all necessary Framework libraries are in the TPA list, and that CoreCLR's bitness (32-bit or 64-bit) matches how the host was built.</span></span>

<span data-ttu-id="d1e1c-201">L’hébergement du runtime .NET Core est un scénario avancé sans utilité pour un grand nombre de développeurs, mais qui peut être très utile pour ceux qui doivent lancer du code managé à partir d’un processus natif ou qui ont besoin de davantage de contrôle sur le comportement du runtime .NET Core.</span><span class="sxs-lookup"><span data-stu-id="d1e1c-201">Hosting the .NET Core runtime is an advanced scenario that many developers won't require, but for those who need to launch managed code from a native process, or who need more control over the .NET Core runtime's behavior, it can be very useful.</span></span>
