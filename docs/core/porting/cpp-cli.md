---
title: Migrer les projets CMD/CLI à .NET Core
description: En savoir plus sur le portage des projets CMD/CLI à .NET Core.
author: mjrousos
ms.date: 01/10/2020
ms.openlocfilehash: eb03f2a5ff42e8279fd3ebd6ee6fb6d955f6798d
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 03/14/2020
ms.locfileid: "75964929"
---
# <a name="how-to-port-a-ccli-project-to-net-core"></a><span data-ttu-id="943eb-103">Comment porter un projet CMD/CLI à .NET Core</span><span class="sxs-lookup"><span data-stu-id="943eb-103">How to port a C++/CLI project to .NET Core</span></span>

<span data-ttu-id="943eb-104">À partir de .NET Core 3.1 et Visual Studio 2019 version 16.4, [les projets C'/CLI](/cpp/dotnet/dotnet-programming-with-cpp-cli-visual-cpp) peuvent cibler .NET Core.</span><span class="sxs-lookup"><span data-stu-id="943eb-104">Beginning with .NET Core 3.1 and Visual Studio 2019 version 16.4, [C++/CLI projects](/cpp/dotnet/dotnet-programming-with-cpp-cli-visual-cpp) can target .NET Core.</span></span> <span data-ttu-id="943eb-105">Ce support permet de porter des applications de bureau Windows avec des couches interop C'/CLI à .NET Core.</span><span class="sxs-lookup"><span data-stu-id="943eb-105">This support makes it possible to port Windows desktop applications with C++/CLI interop layers to .NET Core.</span></span> <span data-ttu-id="943eb-106">Cet article décrit comment portuairer les projets CMD/CLI du cadre .NET au .NET Core 3.1.</span><span class="sxs-lookup"><span data-stu-id="943eb-106">This article describes how to port C++/CLI projects from .NET Framework to .NET Core 3.1.</span></span>

## <a name="ccli-net-core-limitations"></a><span data-ttu-id="943eb-107">Limitations de base CMD/CLI .NET</span><span class="sxs-lookup"><span data-stu-id="943eb-107">C++/CLI .NET Core limitations</span></span>

<span data-ttu-id="943eb-108">Il y a quelques limites importantes au portage des projets de C/CLI à .NET Core par rapport à d’autres langues :</span><span class="sxs-lookup"><span data-stu-id="943eb-108">There are some important limitations to porting C++/CLI projects to .NET Core compared to other languages:</span></span>

* <span data-ttu-id="943eb-109">La prise en charge de C/CLI pour .NET Core est Uniquement Windows.</span><span class="sxs-lookup"><span data-stu-id="943eb-109">C++/CLI support for .NET Core is Windows only.</span></span>
* <span data-ttu-id="943eb-110">Les projets C/CLI ne peuvent pas cibler .NET Standard, seulement .NET Core (ou .NET Framework).</span><span class="sxs-lookup"><span data-stu-id="943eb-110">C++/CLI projects can't target .NET Standard, only .NET Core (or .NET Framework).</span></span>
* <span data-ttu-id="943eb-111">Les projets CMD/CLI ne prennent pas en charge le nouveau format de fichier de projet de type SDK.</span><span class="sxs-lookup"><span data-stu-id="943eb-111">C++/CLI projects don't support the new SDK-style project file format.</span></span> <span data-ttu-id="943eb-112">Au lieu de cela, même lorsque vous ciblez .NET Core, les projets C/CLI utilisent le format de fichier vcxproj existant.</span><span class="sxs-lookup"><span data-stu-id="943eb-112">Instead, even when targeting .NET Core, C++/CLI projects use the existing vcxproj file format.</span></span>
* <span data-ttu-id="943eb-113">Les projets C/CLI ne peuvent pas multitarget plusieurs plates-formes .NET.</span><span class="sxs-lookup"><span data-stu-id="943eb-113">C++/CLI projects can't multitarget multiple .NET platforms.</span></span> <span data-ttu-id="943eb-114">Si vous avez besoin de construire un projet CMD/CLI pour .NET Framework et .NET Core, utilisez des fichiers de projets distincts.</span><span class="sxs-lookup"><span data-stu-id="943eb-114">If you need to build a C++/CLI project for both .NET Framework and .NET Core, use separate project files.</span></span>
* <span data-ttu-id="943eb-115">.NET Core ne `-clr:pure` prend `-clr:safe` pas en `-clr:netcore` charge ou compilation, `-clr` seulement la nouvelle option (qui est équivalente à pour .NET Framework).</span><span class="sxs-lookup"><span data-stu-id="943eb-115">.NET Core doesn't support `-clr:pure` or `-clr:safe` compilation, only the new `-clr:netcore` option (which is equivalent to `-clr` for .NET Framework).</span></span>

## <a name="port-a-ccli-project"></a><span data-ttu-id="943eb-116">Projet de port a CMD/CLI</span><span class="sxs-lookup"><span data-stu-id="943eb-116">Port a C++/CLI project</span></span>

<span data-ttu-id="943eb-117">Pour effectuer un projet CMD/CLI à .NET Core, modifiez les modifications suivantes au fichier vcxproj.</span><span class="sxs-lookup"><span data-stu-id="943eb-117">To port a C++/CLI project to .NET Core, make the following changes to the vcxproj file.</span></span> <span data-ttu-id="943eb-118">Ces étapes de migration diffèrent des étapes nécessaires pour d’autres types de projets, car les projets C/CLI n’utilisent pas de fichiers de projets de type SDK.</span><span class="sxs-lookup"><span data-stu-id="943eb-118">These migration steps differ from the steps needed for other project types because C++/CLI projects don't use SDK-style project files.</span></span>

1. <span data-ttu-id="943eb-119">Remplacer `<CLRSupport>true</CLRSupport>` les `<CLRSupport>NetCore</CLRSupport>`propriétés par .</span><span class="sxs-lookup"><span data-stu-id="943eb-119">Replace `<CLRSupport>true</CLRSupport>` properties with `<CLRSupport>NetCore</CLRSupport>`.</span></span> <span data-ttu-id="943eb-120">Cette propriété est souvent dans les groupes de propriété spécifiques à la configuration, de sorte que vous devrez peut-être le remplacer à plusieurs endroits.</span><span class="sxs-lookup"><span data-stu-id="943eb-120">This property is often in configuration-specific property groups, so you may need to replace it in multiple places.</span></span>
2. <span data-ttu-id="943eb-121">Remplacer `<TargetFrameworkVersion>` les `<TargetFramework>netcoreapp3.1</TargetFramework>`propriétés par .</span><span class="sxs-lookup"><span data-stu-id="943eb-121">Replace `<TargetFrameworkVersion>` properties with `<TargetFramework>netcoreapp3.1</TargetFramework>`.</span></span>
3. <span data-ttu-id="943eb-122">Supprimer toutes les références `<Reference Include="System" />`cadre .NET (comme ).</span><span class="sxs-lookup"><span data-stu-id="943eb-122">Remove any .NET Framework references (like `<Reference Include="System" />`).</span></span> <span data-ttu-id="943eb-123">.NET Core SDK assemblages sont `<CLRSupport>NetCore</CLRSupport>`automatiquement référencés lors de l’utilisation .</span><span class="sxs-lookup"><span data-stu-id="943eb-123">.NET Core SDK assemblies are automatically referenced when using `<CLRSupport>NetCore</CLRSupport>`.</span></span>
4. <span data-ttu-id="943eb-124">Mettre à jour l’utilisation de l’API dans les fichiers cpp, au besoin, pour supprimer les API indisponibles pour .NET Core.</span><span class="sxs-lookup"><span data-stu-id="943eb-124">Update API usage in cpp files, as necessary, to remove APIs unavailable to .NET Core.</span></span> <span data-ttu-id="943eb-125">Étant donné que les projets CMD/CLI ont tendance à être des couches interop assez minces, il n’y a souvent pas beaucoup de changements nécessaires.</span><span class="sxs-lookup"><span data-stu-id="943eb-125">Because C++/CLI projects tend to be fairly thin interop layers, there are often not many changes needed.</span></span> <span data-ttu-id="943eb-126">[L’analyseur de portabilité .NET](../../standard/analyzers/portability-analyzer.md) peut être utilisé pour identifier les API .NET non pris en charge utilisés par les binaires C/CLI tout comme avec les binaires purement gérés.</span><span class="sxs-lookup"><span data-stu-id="943eb-126">The [.NET Portability Analyzer](../../standard/analyzers/portability-analyzer.md) can be used to identify unsupported .NET APIs used by C++/CLI binaries just as with purely managed binaries.</span></span> <span data-ttu-id="943eb-127">Des lignes directrices pour déterminer la portabilité du code et la mise à jour des projets pour travailler avec les API de base .NET sont disponibles dans les directives de [portage de bibliothèque](./libraries.md#determine-portability).</span><span class="sxs-lookup"><span data-stu-id="943eb-127">Guidelines for determining code portability and updating projects to work with .NET Core APIs are available in the [library porting guidance](./libraries.md#determine-portability).</span></span>

### <a name="wpf-and-windows-forms-usage"></a><span data-ttu-id="943eb-128">Utilisation des formulaires WPF et Windows</span><span class="sxs-lookup"><span data-stu-id="943eb-128">WPF and Windows Forms usage</span></span>

<span data-ttu-id="943eb-129">Les projets .NET Core CMD/CLI peuvent utiliser les formulaires Windows et les API WPF.</span><span class="sxs-lookup"><span data-stu-id="943eb-129">.NET Core C++/CLI projects can use Windows Forms and WPF APIs.</span></span> <span data-ttu-id="943eb-130">Pour utiliser ces API de bureau Windows, vous devez ajouter des références-cadres explicites aux bibliothèques d’interface utilisateur.</span><span class="sxs-lookup"><span data-stu-id="943eb-130">To use these Windows desktop APIs, you need to add explicit framework references to the UI libraries.</span></span> <span data-ttu-id="943eb-131">Les projets de type SDK qui utilisent les API `Microsoft.NET.Sdk.WindowsDesktop` de bureau Windows font automatiquement référence aux bibliothèques-cadres nécessaires en utilisant le SDK.</span><span class="sxs-lookup"><span data-stu-id="943eb-131">SDK-style projects that use Windows desktop APIs reference the necessary framework libraries automatically by using the `Microsoft.NET.Sdk.WindowsDesktop` SDK.</span></span> <span data-ttu-id="943eb-132">Étant donné que les projets CMD/CLI n’utilisent pas le format de projet de type SDK, ils doivent ajouter des références-cadres explicites lors du ciblage de .NET Core.</span><span class="sxs-lookup"><span data-stu-id="943eb-132">Because C++/CLI projects don't use the SDK-style project format, they need to add explicit framework references when targeting .NET Core.</span></span>

<span data-ttu-id="943eb-133">Pour utiliser les API Windows Forms, ajoutez cette référence au fichier vcxproj :</span><span class="sxs-lookup"><span data-stu-id="943eb-133">To use Windows Forms APIs, add this reference to the vcxproj file:</span></span>

```xml
<!-- Reference all of Windows Forms -->
<FrameworkReference Include="Microsoft.WindowsDesktop.App.WindowsForms" />
```

<span data-ttu-id="943eb-134">Pour utiliser les API WPF, ajoutez cette référence au fichier vcxproj :</span><span class="sxs-lookup"><span data-stu-id="943eb-134">To use WPF APIs, add this reference to the vcxproj file:</span></span>

```xml
<!-- Reference all of WPF -->
<FrameworkReference Include="Microsoft.WindowsDesktop.App.WPF" />
```

<span data-ttu-id="943eb-135">Pour utiliser les formulaires Windows et les API WPF, ajoutez cette référence au fichier vcxproj :</span><span class="sxs-lookup"><span data-stu-id="943eb-135">To use both Windows Forms and WPF APIs, add this reference to the vcxproj file:</span></span>

```xml
<!-- Reference the entirety of the Windows desktop framework:
     Windows Forms, WPF, and the types that provide integration between them -->
<FrameworkReference Include="Microsoft.WindowsDesktop.App" />
```

<span data-ttu-id="943eb-136">Actuellement, il n’est pas possible d’ajouter ces références à l’aide du gestionnaire de référence de Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="943eb-136">Currently, it's not possible to add these references using Visual Studio's reference manager.</span></span> <span data-ttu-id="943eb-137">Au lieu de cela, mettre à jour le fichier du projet manuellement.</span><span class="sxs-lookup"><span data-stu-id="943eb-137">Instead, update the project file manually.</span></span> <span data-ttu-id="943eb-138">Cette mise à jour peut être faite dans Visual Studio en déchargeant le projet, puis en éditant le fichier du projet.</span><span class="sxs-lookup"><span data-stu-id="943eb-138">This update can be done in Visual Studio by unloading the project and then editing the project file.</span></span> <span data-ttu-id="943eb-139">Vous pouvez également utiliser un autre éditeur comme VS Code.</span><span class="sxs-lookup"><span data-stu-id="943eb-139">You can also use another editor like VS Code.</span></span>

## <a name="build-without-msbuild"></a><span data-ttu-id="943eb-140">Construire sans MSBuild</span><span class="sxs-lookup"><span data-stu-id="943eb-140">Build without MSBuild</span></span>

<span data-ttu-id="943eb-141">Il est également possible de construire des projets CMD/CLI sans utiliser MSBuild.</span><span class="sxs-lookup"><span data-stu-id="943eb-141">It's also possible to build C++/CLI projects without using MSBuild.</span></span> <span data-ttu-id="943eb-142">Suivez ces étapes pour construire un projet CMD/CLI pour .NET Core directement avec *cl.exe* et *link.exe*:</span><span class="sxs-lookup"><span data-stu-id="943eb-142">Follow these steps to build a C++/CLI project for .NET Core directly with *cl.exe* and *link.exe*:</span></span>

1. <span data-ttu-id="943eb-143">Lors de la compilation, passer `-clr:netcore` à *cl.exe*.</span><span class="sxs-lookup"><span data-stu-id="943eb-143">When compiling, pass `-clr:netcore` to *cl.exe*.</span></span>
2. <span data-ttu-id="943eb-144">Référence nécessaire .NET Assemblages de référence de base.</span><span class="sxs-lookup"><span data-stu-id="943eb-144">Reference necessary .NET Core reference assemblies.</span></span>
3. <span data-ttu-id="943eb-145">Lors de la liaison, fournir le répertoire `LibPath` d’hôte de l’application .NET Core comme un (de sorte que *ijwhost.lib* peut être trouvé).</span><span class="sxs-lookup"><span data-stu-id="943eb-145">When linking, provide the .NET Core app host directory as a `LibPath` (so that *ijwhost.lib* can be found).</span></span>
4. <span data-ttu-id="943eb-146">Copiez *ijwhost.dll* (de l’annuaire d’hôte de l’application .NET Core) à l’annuaire de sortie du projet.</span><span class="sxs-lookup"><span data-stu-id="943eb-146">Copy *ijwhost.dll* (from the .NET Core app host directory) to the project's output directory.</span></span>
5. <span data-ttu-id="943eb-147">Assurez-vous qu’un fichier [runtimeconfig.json](https://github.com/dotnet/cli/blob/master/Documentation/specs/runtime-configuration-file.md) existe pour le premier composant de l’application qui exécutera le code géré.</span><span class="sxs-lookup"><span data-stu-id="943eb-147">Make sure a [runtimeconfig.json](https://github.com/dotnet/cli/blob/master/Documentation/specs/runtime-configuration-file.md) file exists for the first component of the application that will run managed code.</span></span> <span data-ttu-id="943eb-148">Si l’application dispose d’un point d’entrée géré, un `runtime.config` fichier sera créé et copié automatiquement.</span><span class="sxs-lookup"><span data-stu-id="943eb-148">If the application has a managed entry point, a `runtime.config` file will be created and copied automatically.</span></span> <span data-ttu-id="943eb-149">Si l’application a un point d’entrée `runtimeconfig.json` natif, cependant, vous devez créer un fichier pour la première bibliothèque C /CLI pour utiliser le temps d’exécution .NET Core.</span><span class="sxs-lookup"><span data-stu-id="943eb-149">If the application has a native entry point, though, you need to create a `runtimeconfig.json` file for the first C++/CLI library to use the .NET Core runtime.</span></span>

## <a name="known-issues"></a><span data-ttu-id="943eb-150">Problèmes connus</span><span class="sxs-lookup"><span data-stu-id="943eb-150">Known issues</span></span>

<span data-ttu-id="943eb-151">Il ya quelques problèmes connus à surveiller lors de la collaboration avec des projets C '/CLI ciblant .NET Core.</span><span class="sxs-lookup"><span data-stu-id="943eb-151">There are a couple known issues to look out for when working with C++/CLI projects targeting .NET Core.</span></span>

* <span data-ttu-id="943eb-152">Une référence-cadre WPF dans les projets .NET Core C/CLI provoque actuellement des avertissements supplémentaires sur l’impossibilité d’importer des symboles.</span><span class="sxs-lookup"><span data-stu-id="943eb-152">A WPF framework reference in .NET Core C++/CLI projects currently causes some extraneous warnings about being unable to import symbols.</span></span> <span data-ttu-id="943eb-153">Ces avertissements peuvent être ignorés en toute sécurité et doivent être corrigés bientôt.</span><span class="sxs-lookup"><span data-stu-id="943eb-153">These warnings can be safely ignored and should be fixed soon.</span></span>
* <span data-ttu-id="943eb-154">Si l’application a un point d’entrée natif, la bibliothèque CMD/CLI qui exécute d’abord le code géré a besoin d’un fichier [runtimeconfig.json.](https://github.com/dotnet/cli/blob/master/Documentation/specs/runtime-configuration-file.md)</span><span class="sxs-lookup"><span data-stu-id="943eb-154">If the application has a native entry point, the C++/CLI library that first executes managed code needs a [runtimeconfig.json](https://github.com/dotnet/cli/blob/master/Documentation/specs/runtime-configuration-file.md) file.</span></span> <span data-ttu-id="943eb-155">Ce fichier config est utilisé lorsque le temps d’exécution .NET Core commence.</span><span class="sxs-lookup"><span data-stu-id="943eb-155">This config file is used when the .NET Core runtime starts.</span></span> <span data-ttu-id="943eb-156">Les projets C/CLI ne `runtimeconfig.json` créent pas encore de fichiers à l’heure de la construction, de sorte que le fichier doit être généré manuellement.</span><span class="sxs-lookup"><span data-stu-id="943eb-156">C++/CLI projects don't create `runtimeconfig.json` files automatically at build time yet, so the file must be generated manually.</span></span> <span data-ttu-id="943eb-157">Si une bibliothèque CMD/CLI est appelée à partir d’un point d’entrée géré, la bibliothèque CMD/CLI n’a pas besoin d’un `runtimeconfig.json` fichier (puisque l’assemblage du point d’entrée en aura un qui est utilisé lors du démarrage de l’exécution).</span><span class="sxs-lookup"><span data-stu-id="943eb-157">If a C++/CLI library is called from a managed entry point, then the C++/CLI library doesn't need a `runtimeconfig.json` file (since the entry point assembly will have one that is used when starting the runtime).</span></span> <span data-ttu-id="943eb-158">Un fichier `runtimeconfig.json` d’échantillon simple est montré ci-dessous.</span><span class="sxs-lookup"><span data-stu-id="943eb-158">A simple sample `runtimeconfig.json` file is shown below.</span></span> <span data-ttu-id="943eb-159">Pour plus d’informations, voir les [spécifications sur GitHub](https://github.com/dotnet/cli/blob/master/Documentation/specs/runtime-configuration-file.md).</span><span class="sxs-lookup"><span data-stu-id="943eb-159">For more information, see the [spec on GitHub](https://github.com/dotnet/cli/blob/master/Documentation/specs/runtime-configuration-file.md).</span></span>

    ```json
    {
          "runtimeOptions": {
             "tfm": "netcoreapp3.1",
             "framework": {
                "name": "Microsoft.NETCore.App",
                "version": "3.1.0"
             }
          }
    }
    ```
