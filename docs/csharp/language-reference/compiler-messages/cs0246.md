---
description: Erreur du compilateur CS0246
title: Erreur du compilateur CS0246
ms.date: 01/23/2018
f1_keywords:
- CS0246
helpviewer_keywords:
- CS0246
ms.assetid: 4948fae2-2cc0-4ce4-b98c-ea69a8120b71
ms.openlocfilehash: 6eeb9cf562b1371b325ab8b17a79d6db2f96f0fa
ms.sourcegitcommit: d579fb5e4b46745fd0f1f8874c94c6469ce58604
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 08/30/2020
ms.locfileid: "89138486"
---
# <a name="compiler-error-cs0246"></a><span data-ttu-id="62095-103">Erreur du compilateur CS0246</span><span class="sxs-lookup"><span data-stu-id="62095-103">Compiler Error CS0246</span></span>

<span data-ttu-id="62095-104">Le type ou le nom d’espace de noms 'type/espace de noms' est introuvable (une directive using ou une référence d’assembly est-elle manquante ?)</span><span class="sxs-lookup"><span data-stu-id="62095-104">The type or namespace name 'type/namespace' could not be found (are you missing a using directive or an assembly reference?)</span></span>  
  
<span data-ttu-id="62095-105">Un type ou un espace de noms utilisé dans le programme est introuvable.</span><span class="sxs-lookup"><span data-stu-id="62095-105">A type or namespace that is used in the program was not found.</span></span> <span data-ttu-id="62095-106">Vous avez peut-être oublié de référencer ([-Reference](../compiler-options/reference-compiler-option.md)) l’assembly qui contient le type, ou vous n’avez peut-être pas ajouté la [directive using](../keywords/using-directive.md)requise.</span><span class="sxs-lookup"><span data-stu-id="62095-106">You might have forgotten to reference ([-reference](../compiler-options/reference-compiler-option.md)) the assembly that contains the type, or you might not have added the required [using directive](../keywords/using-directive.md).</span></span>  <span data-ttu-id="62095-107">Il se peut aussi qu’il y ait un problème avec l’assembly que vous tentez de référencer.</span><span class="sxs-lookup"><span data-stu-id="62095-107">Or, there might be an issue with the assembly you are trying to reference.</span></span>  
  
<span data-ttu-id="62095-108">Les situations suivantes provoquent l’erreur de compilateur CS0246.</span><span class="sxs-lookup"><span data-stu-id="62095-108">The following situations cause compiler error CS0246.</span></span>  
  
- <span data-ttu-id="62095-109">Vous avez peut-être mal orthographié le nom du type ou de l’espace de noms.</span><span class="sxs-lookup"><span data-stu-id="62095-109">Did you misspell the name of the type or namespace?</span></span> <span data-ttu-id="62095-110">Si le nom n’est pas correctement orthographié, le compilateur ne pourra pas trouver la définition du type ou de l’espace de noms.</span><span class="sxs-lookup"><span data-stu-id="62095-110">Without the correct name, the compiler cannot find the definition for the type or namespace.</span></span> <span data-ttu-id="62095-111">Cela se produit souvent quand la casse utilisée n’est pas la bonne.</span><span class="sxs-lookup"><span data-stu-id="62095-111">This often occurs because the casing used in the name of the type is not correct.</span></span> <span data-ttu-id="62095-112">Par exemple, `Dataset ds;` génère l’erreur CS0246 parce que le « s » de « Dataset » doit être en majuscule.</span><span class="sxs-lookup"><span data-stu-id="62095-112">For example, `Dataset ds;` generates CS0246 because the s in Dataset must be capitalized.</span></span>  
  
- <span data-ttu-id="62095-113">Si l’erreur concerne un nom d’espace de noms, avez-vous ajouté une référence ([-Reference](../compiler-options/reference-compiler-option.md)) à l’assembly qui contient l’espace de noms ?</span><span class="sxs-lookup"><span data-stu-id="62095-113">If the error is for a namespace name, did you add a reference ([-reference](../compiler-options/reference-compiler-option.md)) to the assembly that contains the namespace?</span></span> <span data-ttu-id="62095-114">Par exemple, votre code peut contenir la directive `using Accessibility`.</span><span class="sxs-lookup"><span data-stu-id="62095-114">For example, your code might contain the directive `using Accessibility`.</span></span> <span data-ttu-id="62095-115">Toutefois, si votre projet ne référence pas l’assembly Accessibility.dll, l’erreur CS0246 se produit.</span><span class="sxs-lookup"><span data-stu-id="62095-115">However, if your project does not reference the assembly Accessibility.dll, error CS0246 is reported.</span></span> <span data-ttu-id="62095-116">Pour plus d’informations, consultez [gestion des références dans un projet](/visualstudio/ide/managing-references-in-a-project) .</span><span class="sxs-lookup"><span data-stu-id="62095-116">For more information, see [Managing references in a project](/visualstudio/ide/managing-references-in-a-project)</span></span>  
  
- <span data-ttu-id="62095-117">Si l’erreur concerne un nom de type, avez-vous inclus la [directive using](../keywords/using-directive.md)appropriée ou, vous pouvez également qualifier complètement le nom du type ?</span><span class="sxs-lookup"><span data-stu-id="62095-117">If the error is for a type name, did you include the proper [using directive](../keywords/using-directive.md), or, alternatively, fully qualify the name of the type?</span></span> <span data-ttu-id="62095-118">Prenons la déclaration suivante : `DataSet ds`.</span><span class="sxs-lookup"><span data-stu-id="62095-118">Consider the following declaration: `DataSet ds`.</span></span> <span data-ttu-id="62095-119">Pour utiliser le type `DataSet` , vous avez besoin de deux choses.</span><span class="sxs-lookup"><span data-stu-id="62095-119">To use the `DataSet` type, you need two things.</span></span> <span data-ttu-id="62095-120">Tout d’abord, vous avez besoin d’une référence à l’assembly qui contient la définition du type `DataSet` .</span><span class="sxs-lookup"><span data-stu-id="62095-120">First, you need a reference to the assembly that contains the definition for the `DataSet` type.</span></span> <span data-ttu-id="62095-121">Ensuite, vous avez besoin d’une directive `using` pour l’espace de noms où se trouve `DataSet` .</span><span class="sxs-lookup"><span data-stu-id="62095-121">Second, you need a `using` directive for the namespace where `DataSet` is located.</span></span> <span data-ttu-id="62095-122">Par exemple, étant donné que `DataSet` se trouve dans l’espace de noms **System.Data** , la directive suivante doit être placée au début de votre code : `using System.Data`.</span><span class="sxs-lookup"><span data-stu-id="62095-122">For example, because `DataSet` is located in the **System.Data** namespace, you need the following directive at the beginning of your code: `using System.Data`.</span></span>  
  
     <span data-ttu-id="62095-123">La directive `using` n’est pas obligatoire.</span><span class="sxs-lookup"><span data-stu-id="62095-123">The `using` directive is not required.</span></span> <span data-ttu-id="62095-124">Toutefois, si vous omettez la directive, vous devez faire du type `DataSet` un nom qualifié complet quand vous faites référence à celui-ci.</span><span class="sxs-lookup"><span data-stu-id="62095-124">However, if you omit the directive, you must fully qualify the `DataSet` type when referring to it.</span></span> <span data-ttu-id="62095-125">Un nom qualifié complet permet de spécifier à la fois l’espace de noms et le type chaque fois que vous faites référence au type dans votre code.</span><span class="sxs-lookup"><span data-stu-id="62095-125">Full qualification means that you specify both the namespace and the type each time you refer to the type in your code.</span></span> <span data-ttu-id="62095-126">Si vous omettez la directive `using` dans l’exemple précédent, vous devez écrire `System.Data.DataSet ds` pour déclarer `ds` au lieu de `DataSet ds`.</span><span class="sxs-lookup"><span data-stu-id="62095-126">If you omit the `using` directive in the previous example, you must write `System.Data.DataSet ds` to declare `ds` instead of `DataSet ds`.</span></span>  
  
- <span data-ttu-id="62095-127">Vous avez peut-être utilisé une variable ou un autre élément de langage alors qu’un type était attendu.</span><span class="sxs-lookup"><span data-stu-id="62095-127">Did you use a variable or some other language element where a type was expected?</span></span> <span data-ttu-id="62095-128">Par exemple, dans une instruction **is** , si vous utilisez un objet `Type` au lieu d’un type, vous obtenez l’erreur CS0246.</span><span class="sxs-lookup"><span data-stu-id="62095-128">For example, in an **is** statement, if you use a `Type` object instead of an actual type, you get error CS0246.</span></span>  

- <span data-ttu-id="62095-129">Faisiez-vous référence à l’assembly qui a été créé avec une version de framework supérieure au framework cible du programme ?</span><span class="sxs-lookup"><span data-stu-id="62095-129">Did you reference the assembly that was built against a higher framework version than the target framework of the program?</span></span> <span data-ttu-id="62095-130">Faisiez-vous référence au projet qui cible une version de framework supérieure au framework cible du programme ?</span><span class="sxs-lookup"><span data-stu-id="62095-130">Or did you reference the project that is targeting a higher framework version than the target framework of the program?</span></span> <span data-ttu-id="62095-131">Par exemple, vous travaillez sur le projet qui cible .NET Framework 4.6.1 et vous utilisez le type du projet qui cible .NET Framework 4.7.1.</span><span class="sxs-lookup"><span data-stu-id="62095-131">For example, you work on the project that is targeting .NET Framework 4.6.1 and use the type from the project that is targeting .NET Framework 4.7.1.</span></span> <span data-ttu-id="62095-132">Puis, vous obtenez l’erreur CS0246.</span><span class="sxs-lookup"><span data-stu-id="62095-132">Then you get error CS0246.</span></span>
  
- <span data-ttu-id="62095-133">Vous avez peut-être utilisé une *directive using alias* sans faire du nom du type un nom qualifié complet.</span><span class="sxs-lookup"><span data-stu-id="62095-133">Did you use a *using alias directive* without fully qualifying the type name?</span></span> <span data-ttu-id="62095-134">Une directive `using` alias n’utilise pas les directives `using` du fichier de code source pour résoudre les types.</span><span class="sxs-lookup"><span data-stu-id="62095-134">A `using` alias directive does not use the `using` directives in the source code file to resolve types.</span></span> <span data-ttu-id="62095-135">L’exemple suivant génère l’erreur CS0246, car le type `List<int>` ne porte pas un nom qualifié complet.</span><span class="sxs-lookup"><span data-stu-id="62095-135">The following example generates CS0246 because the type `List<int>` is not fully qualified.</span></span> <span data-ttu-id="62095-136">La directive `using` pour `System.Collections.Generic` n’empêche pas l’erreur de se produire.</span><span class="sxs-lookup"><span data-stu-id="62095-136">The `using` directive for `System.Collections.Generic` does not prevent the error.</span></span>  
  
    ```csharp  
    using System.Collections.Generic;  
  
    // The following declaration generates CS0246.  
    using myAliasName = List<int>;
  
    // To avoid the error, fully qualify List.  
    using myAliasName2 = System.Collections.Generic.List<int>;  
    ```  
  
     <span data-ttu-id="62095-137">Si vous obtenez cette erreur avec du code qui fonctionnait précédemment, recherchez tout d’abord les références manquantes ou non résolues dans l’Explorateur de solutions.</span><span class="sxs-lookup"><span data-stu-id="62095-137">If you get this error in code that was previously working, first look for missing or unresolved references in Solution Explorer.</span></span> <span data-ttu-id="62095-138">Avez-vous besoin de réinstaller un package [NuGet](https://www.nuget.org/) ?</span><span class="sxs-lookup"><span data-stu-id="62095-138">Do you need to reinstall a [NuGet](https://www.nuget.org/) package?</span></span> <span data-ttu-id="62095-139">Pour plus d’informations sur la façon dont le système de génération recherche les références, consultez [Resolving file references in team build](https://docs.microsoft.com/archive/blogs/manishagarwal/resolving-file-references-in-team-build-part-2).</span><span class="sxs-lookup"><span data-stu-id="62095-139">For information about how the build system searches for references, see [Resolving file references in team build](https://docs.microsoft.com/archive/blogs/manishagarwal/resolving-file-references-in-team-build-part-2).</span></span> <span data-ttu-id="62095-140">Si toutes les références semblent correctes, consultez l’historique du contrôle du code source pour voir ce qui a été modifié dans votre fichier .csproj et/ou dans votre fichier source local.</span><span class="sxs-lookup"><span data-stu-id="62095-140">If all references seem to be correct, look in your source control history to see what has changed in your .csproj file and/or your local source file.</span></span>  
  
     <span data-ttu-id="62095-141">Si vous n’avez pas encore réussi à accéder à la référence, utilisez l’Explorateur d’objets pour examiner l’assembly qui est supposé contenir cet espace de noms, puis vérifiez que l’espace de noms s’y trouve.</span><span class="sxs-lookup"><span data-stu-id="62095-141">If you haven’t successfully accessed the reference yet, use the Object Browser to inspect the assembly that is supposed to contain this namespace and verify that the namespace is present.</span></span> <span data-ttu-id="62095-142">Si vous utilisez l’Explorateur d’objets pour vérifier si l’assembly contient l’espace de noms, essayez de supprimer la directive `using` de l’espace de noms pour voir les autres problèmes que cela cause.</span><span class="sxs-lookup"><span data-stu-id="62095-142">If you verify with Object Browser that the assembly contains the namespace, try removing the `using` directive for the namespace and see what else breaks.</span></span> <span data-ttu-id="62095-143">L’origine du problème peut provenir d’un autre type d’un autre assembly.</span><span class="sxs-lookup"><span data-stu-id="62095-143">The root problem may be with some other type in another assembly.</span></span>  
  
<span data-ttu-id="62095-144">L’exemple suivant génère l’erreur CS0246, car une directive `using` requise est manquante.</span><span class="sxs-lookup"><span data-stu-id="62095-144">The following example generates CS0246 because a necessary `using` directive is missing.</span></span>  
  
```csharp  
// CS0246.cs  
//using System.Diagnostics;  
  
public class MyClass  
{  
    // The following line causes CS0246. To fix the error, uncomment  
    // the using directive for the namespace for this attribute,  
    // System.Diagnostics.  
    [Conditional("A")]  
    public void Test()  
    {  
    }  
  
    public static void Main()  
    {  
    }  
}  
```  
  
<span data-ttu-id="62095-145">L’exemple suivant provoque l’erreur CS0246, car un objet de type `Type` a été utilisé là où un type était attendu.</span><span class="sxs-lookup"><span data-stu-id="62095-145">The following example causes CS0246 because an object of type `Type` was used where an actual type was expected.</span></span>  
  
```csharp  
// CS0246b.cs  
using System;  
  
class ExampleClass  
{  
    public bool supports(object o, Type t)  
    {  
        // The following line causes CS0246. You must use an  
        // actual type, such as ExampleClass, String, or Type.  
        if (o is t)  
        {  
            return true;  
        }  
        return false;  
    }  
}  
  
class Program  
{  
    public static void Main()  
    {  
        ExampleClass myC = new ExampleClass();  
        myC.supports(myC, myC.GetType());  
    }  
}  
```
