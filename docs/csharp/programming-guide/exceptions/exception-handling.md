---
title: Gestion des exceptions - Guide de programmation C#
description: En savoir plus sur la gestion des exceptions. Consultez des exemples d’instructions Try-Catch, try-finally et try-catch-finally.
ms.date: 07/20/2015
helpviewer_keywords:
- exception handling [C#], about exception handling
- exceptions [C#], handling
ms.assetid: b4e4ecf2-b907-4e58-891f-2563762258e9
ms.openlocfilehash: 8f7dc027396e327f08a591ced6bd6df176a17606
ms.sourcegitcommit: 5b475c1855b32cf78d2d1bbb4295e4c236f39464
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 09/24/2020
ms.locfileid: "91178682"
---
# <a name="exception-handling-c-programming-guide"></a><span data-ttu-id="2c97e-104">Gestion des exceptions (Guide de programmation C#)</span><span class="sxs-lookup"><span data-stu-id="2c97e-104">Exception Handling (C# Programming Guide)</span></span>

<span data-ttu-id="2c97e-105">Un bloc [try](../../language-reference/keywords/try-catch.md) est utilisé par les programmeurs C# pour partitionner du code susceptible d’être affecté par une exception.</span><span class="sxs-lookup"><span data-stu-id="2c97e-105">A [try](../../language-reference/keywords/try-catch.md) block is used by C# programmers to partition code that might be affected by an exception.</span></span> <span data-ttu-id="2c97e-106">Des blocs [catch](../../language-reference/keywords/try-catch.md) associés sont utilisés pour gérer les exceptions générées.</span><span class="sxs-lookup"><span data-stu-id="2c97e-106">Associated [catch](../../language-reference/keywords/try-catch.md) blocks are used to handle any resulting exceptions.</span></span> <span data-ttu-id="2c97e-107">Un bloc [finally](../../language-reference/keywords/try-finally.md) contient du code qui s’exécute dans tous les cas, qu’une exception soit levée ou non dans le bloc `try` (il peut s’agir par exemple de la libération des ressources allouées dans le bloc `try`).</span><span class="sxs-lookup"><span data-stu-id="2c97e-107">A [finally](../../language-reference/keywords/try-finally.md) block contains code that is run regardless of whether or not an exception is thrown in the `try` block, such as releasing resources that are allocated in the `try` block.</span></span> <span data-ttu-id="2c97e-108">Un bloc `try` doit être associé à un ou plusieurs blocs `catch`, à un bloc `finally`, ou aux deux.</span><span class="sxs-lookup"><span data-stu-id="2c97e-108">A `try` block requires one or more associated `catch` blocks, or a `finally` block, or both.</span></span>  
  
 <span data-ttu-id="2c97e-109">Les exemples suivants montrent une instruction `try-catch`, une instruction `try-finally` et une instruction `try-catch-finally`.</span><span class="sxs-lookup"><span data-stu-id="2c97e-109">The following examples show a `try-catch` statement, a `try-finally` statement, and a `try-catch-finally` statement.</span></span>  
  
 [!code-csharp[csProgGuideExceptions#6](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideExceptions/CS/Exceptions.cs#6)]  
  
 [!code-csharp[csProgGuideExceptions#7](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideExceptions/CS/Exceptions.cs#7)]  
  
 [!code-csharp[csProgGuideExceptions#8](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideExceptions/CS/Exceptions.cs#8)]  
  
 <span data-ttu-id="2c97e-110">Un bloc `try` sans bloc `catch` ou `finally` provoque une erreur du compilateur.</span><span class="sxs-lookup"><span data-stu-id="2c97e-110">A `try` block without a `catch` or `finally` block causes a compiler error.</span></span>  
  
## <a name="catch-blocks"></a><span data-ttu-id="2c97e-111">Blocs catch</span><span class="sxs-lookup"><span data-stu-id="2c97e-111">Catch Blocks</span></span>  

 <span data-ttu-id="2c97e-112">Un bloc `catch` peut spécifier le type d’exception à intercepter.</span><span class="sxs-lookup"><span data-stu-id="2c97e-112">A `catch` block can specify the type of exception to catch.</span></span> <span data-ttu-id="2c97e-113">La spécification de type est appelée *filtre d’exception*.</span><span class="sxs-lookup"><span data-stu-id="2c97e-113">The type specification is called an *exception filter*.</span></span> <span data-ttu-id="2c97e-114">Le type d’exception doit être dérivé de <xref:System.Exception>.</span><span class="sxs-lookup"><span data-stu-id="2c97e-114">The exception type should be derived from <xref:System.Exception>.</span></span> <span data-ttu-id="2c97e-115">En général, ne spécifiez pas <xref:System.Exception> comme filtre d’exception, sauf si vous savez comment gérer toutes les exceptions susceptibles d’être levées dans le bloc `try` ou si vous avez inclus une instruction [throw](../../language-reference/keywords/throw.md) à la fin de votre bloc `catch`.</span><span class="sxs-lookup"><span data-stu-id="2c97e-115">In general, do not specify <xref:System.Exception> as the exception filter unless either you know how to handle all exceptions that might be thrown in the `try` block, or you have included a [throw](../../language-reference/keywords/throw.md) statement at the end of your `catch` block.</span></span>  
  
 <span data-ttu-id="2c97e-116">Vous pouvez chaîner plusieurs blocs `catch` avec des filtres d’exception différents.</span><span class="sxs-lookup"><span data-stu-id="2c97e-116">Multiple `catch` blocks with different exception filters can be chained together.</span></span> <span data-ttu-id="2c97e-117">Les blocs `catch` sont évalués de haut en bas dans votre code, mais un seul bloc `catch` est exécuté pour chaque exception levée.</span><span class="sxs-lookup"><span data-stu-id="2c97e-117">The `catch` blocks are evaluated from top to bottom in your code, but only one `catch` block is executed for each exception that is thrown.</span></span> <span data-ttu-id="2c97e-118">Le premier bloc `catch` qui spécifie le type exact ou une classe de base de l’exception levée est exécuté.</span><span class="sxs-lookup"><span data-stu-id="2c97e-118">The first `catch` block that specifies the exact type or a base class of the thrown exception is executed.</span></span> <span data-ttu-id="2c97e-119">Si aucun bloc `catch` ne spécifie un filtre d’exception correspondant, un bloc `catch` qui n’a pas de filtre est sélectionné, s’il y en a un dans l’instruction.</span><span class="sxs-lookup"><span data-stu-id="2c97e-119">If no `catch` block specifies a matching exception filter, a `catch` block that does not have a filter is selected, if one is present in the statement.</span></span> <span data-ttu-id="2c97e-120">Lors du positionnement des blocs `catch`, il est important de placer en premier les types d’exception les plus spécifiques (c’est-à-dire les plus dérivés).</span><span class="sxs-lookup"><span data-stu-id="2c97e-120">It is important to position `catch` blocks with the most specific (that is, the most derived) exception types first.</span></span>  
  
 <span data-ttu-id="2c97e-121">Vous devez intercepter les exceptions quand les conditions suivantes sont remplies :</span><span class="sxs-lookup"><span data-stu-id="2c97e-121">You should catch exceptions when the following conditions are true:</span></span>  
  
- <span data-ttu-id="2c97e-122">Vous savez pourquoi l’exception a été levée et vous pouvez implémenter une récupération spécifique, par exemple inviter l’utilisateur à entrer un nouveau nom de fichier quand vous interceptez un objet <xref:System.IO.FileNotFoundException>.</span><span class="sxs-lookup"><span data-stu-id="2c97e-122">You have a good understanding of why the exception might be thrown, and you can implement a specific recovery, such as prompting the user to enter a new file name when you catch a <xref:System.IO.FileNotFoundException> object.</span></span>  
  
- <span data-ttu-id="2c97e-123">Vous pouvez créer et lever une nouvelle exception plus spécifique.</span><span class="sxs-lookup"><span data-stu-id="2c97e-123">You can create and throw a new, more specific exception.</span></span>  
  
     [!code-csharp[csProgGuideExceptions#9](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideExceptions/CS/Exceptions.cs#9)]  
  
- <span data-ttu-id="2c97e-124">Vous voulez traiter partiellement une exception avant de la transmettre en vue d’un traitement supplémentaire.</span><span class="sxs-lookup"><span data-stu-id="2c97e-124">You want to partially handle an exception before passing it on for additional handling.</span></span> <span data-ttu-id="2c97e-125">Dans l’exemple suivant, un bloc `catch` est utilisé pour ajouter une entrée à un journal d’erreurs avant de relever l’exception.</span><span class="sxs-lookup"><span data-stu-id="2c97e-125">In the following example, a `catch` block is used to add an entry to an error log before re-throwing the exception.</span></span>  
  
     [!code-csharp[csProgGuideExceptions#10](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideExceptions/CS/Exceptions.cs#10)]  
  
## <a name="finally-blocks"></a><span data-ttu-id="2c97e-126">Blocs Finally</span><span class="sxs-lookup"><span data-stu-id="2c97e-126">Finally Blocks</span></span>  

 <span data-ttu-id="2c97e-127">Un bloc `finally` vous permet de nettoyer les actions qui sont exécutées dans un bloc `try`.</span><span class="sxs-lookup"><span data-stu-id="2c97e-127">A `finally` block enables you to clean up actions that are performed in a `try` block.</span></span> <span data-ttu-id="2c97e-128">S’il est présent, le bloc `finally` s’exécute en dernier, après le bloc `try` et tout bloc `catch` mis en correspondance.</span><span class="sxs-lookup"><span data-stu-id="2c97e-128">If present, the `finally` block executes last, after the `try` block and any matched `catch` block.</span></span> <span data-ttu-id="2c97e-129">Un bloc `finally` s’exécute toujours, qu’une exception soit levée ou non, et même si aucun bloc `catch` correspondant au type d’exception n’est trouvé.</span><span class="sxs-lookup"><span data-stu-id="2c97e-129">A `finally` block always runs, regardless of whether an exception is thrown or a `catch` block matching the exception type is found.</span></span>  
  
 <span data-ttu-id="2c97e-130">Le bloc `finally` peut être utilisé pour libérer des ressources telles que des flux de fichiers, des connexions de base de données et des handles graphiques, sans attendre que le récupérateur de mémoire dans le runtime finalise les objets.</span><span class="sxs-lookup"><span data-stu-id="2c97e-130">The `finally` block can be used to release resources such as file streams, database connections, and graphics handles without waiting for the garbage collector in the runtime to finalize the objects.</span></span> <span data-ttu-id="2c97e-131">Pour plus d’informations, consultez [Instruction using](../../language-reference/keywords/using-statement.md).</span><span class="sxs-lookup"><span data-stu-id="2c97e-131">See [using Statement](../../language-reference/keywords/using-statement.md) for more information.</span></span>  
  
 <span data-ttu-id="2c97e-132">Dans l’exemple suivant, le bloc `finally` est utilisé pour fermer un fichier ouvert dans le bloc `try`.</span><span class="sxs-lookup"><span data-stu-id="2c97e-132">In the following example, the `finally` block is used to close a file that is opened in the `try` block.</span></span> <span data-ttu-id="2c97e-133">Notez que l’état du handle de fichier est vérifié avant la fermeture du fichier.</span><span class="sxs-lookup"><span data-stu-id="2c97e-133">Notice that the state of the file handle is checked before the file is closed.</span></span> <span data-ttu-id="2c97e-134">Si le bloc `try` ne peut pas ouvrir le fichier, le handle de fichier a encore la valeur `null` et le bloc `finally` n’essaie pas de le fermer.</span><span class="sxs-lookup"><span data-stu-id="2c97e-134">If the `try` block cannot open the file, the file handle still has the value `null` and the `finally` block does not try to close it.</span></span> <span data-ttu-id="2c97e-135">En guise d’alternative, si le fichier est ouvert avec succès dans le bloc `try`, le bloc `finally` ferme le fichier ouvert.</span><span class="sxs-lookup"><span data-stu-id="2c97e-135">Alternatively, if the file is opened successfully in the `try` block, the `finally` block closes the open file.</span></span>  
  
 [!code-csharp[csProgGuideExceptions#11](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideExceptions/CS/Exceptions.cs#11)]  
  
## <a name="c-language-specification"></a><span data-ttu-id="2c97e-136">Spécification du langage C#</span><span class="sxs-lookup"><span data-stu-id="2c97e-136">C# Language Specification</span></span>  

<span data-ttu-id="2c97e-137">Pour plus d’informations, consultez [Exceptions](~/_csharplang/spec/exceptions.md) et [Instruction try](~/_csharplang/spec/statements.md#the-try-statement) dans la [spécification du langage C#](/dotnet/csharp/language-reference/language-specification/introduction).</span><span class="sxs-lookup"><span data-stu-id="2c97e-137">For more information, see [Exceptions](~/_csharplang/spec/exceptions.md) and [The try statement](~/_csharplang/spec/statements.md#the-try-statement) in the [C# Language Specification](/dotnet/csharp/language-reference/language-specification/introduction).</span></span> <span data-ttu-id="2c97e-138">La spécification du langage est la source de référence pour la syntaxe C# et son utilisation.</span><span class="sxs-lookup"><span data-stu-id="2c97e-138">The language specification is the definitive source for C# syntax and usage.</span></span>
  
## <a name="see-also"></a><span data-ttu-id="2c97e-139">Voir aussi</span><span class="sxs-lookup"><span data-stu-id="2c97e-139">See also</span></span>

- [<span data-ttu-id="2c97e-140">Référence C#</span><span class="sxs-lookup"><span data-stu-id="2c97e-140">C# Reference</span></span>](../../language-reference/index.md)
- [<span data-ttu-id="2c97e-141">Guide de programmation C#</span><span class="sxs-lookup"><span data-stu-id="2c97e-141">C# Programming Guide</span></span>](../index.md)
- [<span data-ttu-id="2c97e-142">Exceptions et gestion des exceptions</span><span class="sxs-lookup"><span data-stu-id="2c97e-142">Exceptions and Exception Handling</span></span>](./index.md)
- [<span data-ttu-id="2c97e-143">try-catch</span><span class="sxs-lookup"><span data-stu-id="2c97e-143">try-catch</span></span>](../../language-reference/keywords/try-catch.md)
- [<span data-ttu-id="2c97e-144">try-finally</span><span class="sxs-lookup"><span data-stu-id="2c97e-144">try-finally</span></span>](../../language-reference/keywords/try-finally.md)
- [<span data-ttu-id="2c97e-145">try-catch-finally</span><span class="sxs-lookup"><span data-stu-id="2c97e-145">try-catch-finally</span></span>](../../language-reference/keywords/try-catch-finally.md)
- [<span data-ttu-id="2c97e-146">using, instruction</span><span class="sxs-lookup"><span data-stu-id="2c97e-146">using Statement</span></span>](../../language-reference/keywords/using-statement.md)
