---
title: Contraintes sur les paramètres de type - Guide de programmation C#
ms.date: 04/12/2018
helpviewer_keywords:
- generics [C#], type constraints
- type constraints [C#]
- type parameters [C#], constraints
- unbound type parameter [C#]
ms.openlocfilehash: 2962de53eab132ad02aaf679fdd6037bd24fa714
ms.sourcegitcommit: 927b7ea6b2ea5a440c8f23e3e66503152eb85591
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 04/16/2020
ms.locfileid: "81463885"
---
# <a name="constraints-on-type-parameters-c-programming-guide"></a><span data-ttu-id="00c7f-102">Contraintes sur les paramètres de type (Guide de programmation C#)</span><span class="sxs-lookup"><span data-stu-id="00c7f-102">Constraints on type parameters (C# Programming Guide)</span></span>

<span data-ttu-id="00c7f-103">Les contraintes informent le compilateur sur les fonctionnalités que doit avoir un argument de type.</span><span class="sxs-lookup"><span data-stu-id="00c7f-103">Constraints inform the compiler about the capabilities a type argument must have.</span></span> <span data-ttu-id="00c7f-104">Sans contrainte, l’argument de type peut être n’importe quel type.</span><span class="sxs-lookup"><span data-stu-id="00c7f-104">Without any constraints, the type argument could be any type.</span></span> <span data-ttu-id="00c7f-105">Le compilateur peut seulement deviner les membres de <xref:System.Object?displayProperty=nameWithType>, qui est la classe de base par excellence de tous les types .NET.</span><span class="sxs-lookup"><span data-stu-id="00c7f-105">The compiler can only assume the members of <xref:System.Object?displayProperty=nameWithType>, which is the ultimate base class for any .NET type.</span></span> <span data-ttu-id="00c7f-106">Pour plus d’informations, consultez [Pourquoi utiliser des contraintes](#why-use-constraints).</span><span class="sxs-lookup"><span data-stu-id="00c7f-106">For more information, see [Why use constraints](#why-use-constraints).</span></span> <span data-ttu-id="00c7f-107">Si le code client utilise un type qui ne satisfait pas à une contrainte, le compilateur émet une erreur.</span><span class="sxs-lookup"><span data-stu-id="00c7f-107">If client code uses a type that doesn't satisfy a constraint, the compiler issues an error.</span></span> <span data-ttu-id="00c7f-108">Les contraintes sont spécifiées à l’aide du mot clé contextuel `where`.</span><span class="sxs-lookup"><span data-stu-id="00c7f-108">Constraints are specified by using the `where` contextual keyword.</span></span> <span data-ttu-id="00c7f-109">Le tableau suivant liste les sept types de contrainte :</span><span class="sxs-lookup"><span data-stu-id="00c7f-109">The following table lists the seven types of constraints:</span></span>

|<span data-ttu-id="00c7f-110">Contrainte</span><span class="sxs-lookup"><span data-stu-id="00c7f-110">Constraint</span></span>|<span data-ttu-id="00c7f-111">Description</span><span class="sxs-lookup"><span data-stu-id="00c7f-111">Description</span></span>|
|----------------|-----------------|
|`where T : struct`|<span data-ttu-id="00c7f-112">L’argument de type doit être un type de valeur non annulable.</span><span class="sxs-lookup"><span data-stu-id="00c7f-112">The type argument must be a non-nullable value type.</span></span> <span data-ttu-id="00c7f-113">Pour plus d’informations sur les types de valeur nulable, voir [les types de valeur nulable](../../language-reference/builtin-types/nullable-value-types.md).</span><span class="sxs-lookup"><span data-stu-id="00c7f-113">For information about nullable value types, see [Nullable value types](../../language-reference/builtin-types/nullable-value-types.md).</span></span> <span data-ttu-id="00c7f-114">Étant donné que tous les types de `struct` valeur ont `new()` un constructeur sans paramètres accessible, `new()` la contrainte implique la contrainte et ne peut pas être combinée avec la contrainte.</span><span class="sxs-lookup"><span data-stu-id="00c7f-114">Because all value types have an accessible parameterless constructor, the `struct` constraint implies the `new()` constraint and can't be combined with the `new()` constraint.</span></span> <span data-ttu-id="00c7f-115">Vous ne pouvez pas `struct` combiner la `unmanaged` contrainte avec la contrainte.</span><span class="sxs-lookup"><span data-stu-id="00c7f-115">You also can't combine the `struct` constraint with the `unmanaged` constraint.</span></span>|
|`where T : class`|<span data-ttu-id="00c7f-116">L’argument de type doit être un type référence.</span><span class="sxs-lookup"><span data-stu-id="00c7f-116">The type argument must be a reference type.</span></span> <span data-ttu-id="00c7f-117">Cette contrainte s’applique également à tous les types de classe, d’interface, de délégué ou de tableau.</span><span class="sxs-lookup"><span data-stu-id="00c7f-117">This constraint applies also to any class, interface, delegate, or array type.</span></span> <span data-ttu-id="00c7f-118">Dans un contexte in annulable dans le C `T` 8.0 ou plus tard, doit être un type de référence non annulable.</span><span class="sxs-lookup"><span data-stu-id="00c7f-118">In a nullable context in C# 8.0 or later, `T` must be a non-nullable reference type.</span></span> |
|`where T : class?`|<span data-ttu-id="00c7f-119">L’argument type doit être un type de référence, soit in nullable, soit non annulable.</span><span class="sxs-lookup"><span data-stu-id="00c7f-119">The type argument must be a reference type, either nullable or non-nullable.</span></span> <span data-ttu-id="00c7f-120">Cette contrainte s’applique également à tous les types de classe, d’interface, de délégué ou de tableau.</span><span class="sxs-lookup"><span data-stu-id="00c7f-120">This constraint applies also to any class, interface, delegate, or array type.</span></span>|
|`where T : notnull`|<span data-ttu-id="00c7f-121">L’argument de type doit être un type non-nullable.</span><span class="sxs-lookup"><span data-stu-id="00c7f-121">The type argument must be a non-nullable type.</span></span> <span data-ttu-id="00c7f-122">L’argument peut être un type de référence non annulable dans C 8.0 ou plus tard, ou un type de valeur non annulable.</span><span class="sxs-lookup"><span data-stu-id="00c7f-122">The argument can be a non-nullable reference type in C# 8.0 or later, or a non-nullable value type.</span></span> |
|`where T : unmanaged`|<span data-ttu-id="00c7f-123">L’argument de type doit être un type non-nullable [nonmané](../../language-reference/builtin-types/unmanaged-types.md).</span><span class="sxs-lookup"><span data-stu-id="00c7f-123">The type argument must be a non-nullable [unmanaged type](../../language-reference/builtin-types/unmanaged-types.md).</span></span> <span data-ttu-id="00c7f-124">La `unmanaged` contrainte implique `struct` la contrainte et ne peut pas `struct` `new()` être combinée avec les ou les contraintes.</span><span class="sxs-lookup"><span data-stu-id="00c7f-124">The `unmanaged` constraint implies the `struct` constraint and can't be combined with either the `struct` or `new()` constraints.</span></span>|
|`where T : new()`|<span data-ttu-id="00c7f-125">L’argument de type doit avoir un constructeur sans paramètre public.</span><span class="sxs-lookup"><span data-stu-id="00c7f-125">The type argument must have a public parameterless constructor.</span></span> <span data-ttu-id="00c7f-126">Quand vous utilisez la contrainte `new()` avec d’autres contraintes, elle doit être spécifiée en dernier.</span><span class="sxs-lookup"><span data-stu-id="00c7f-126">When used together with other constraints, the `new()` constraint must be specified last.</span></span> <span data-ttu-id="00c7f-127">La `new()` contrainte ne peut pas `struct` être `unmanaged` combinée avec les contraintes et les contraintes.</span><span class="sxs-lookup"><span data-stu-id="00c7f-127">The `new()` constraint can't be combined with the `struct` and `unmanaged` constraints.</span></span>|
|<span data-ttu-id="00c7f-128">`where T :` *\<nom_classe_de_base>*</span><span class="sxs-lookup"><span data-stu-id="00c7f-128">`where T :` *\<base class name>*</span></span>|<span data-ttu-id="00c7f-129">L’argument de type doit être la classe de base spécifiée ou en dériver.</span><span class="sxs-lookup"><span data-stu-id="00c7f-129">The type argument must be or derive from the specified base class.</span></span> <span data-ttu-id="00c7f-130">Dans un contexte in annulable dans le C `T` 8.0 et plus tard, doit être un type de référence non annulable dérivé de la classe de base spécifiée.</span><span class="sxs-lookup"><span data-stu-id="00c7f-130">In a nullable context in C# 8.0 and later, `T` must be a non-nullable reference type derived from the specified base class.</span></span> |
|<span data-ttu-id="00c7f-131">`where T :`*nom de classe de base>? \<*</span><span class="sxs-lookup"><span data-stu-id="00c7f-131">`where T :` *\<base class name>?*</span></span>|<span data-ttu-id="00c7f-132">L’argument de type doit être la classe de base spécifiée ou en dériver.</span><span class="sxs-lookup"><span data-stu-id="00c7f-132">The type argument must be or derive from the specified base class.</span></span> <span data-ttu-id="00c7f-133">Dans un contexte infiratable dans le `T` C 8.0 et plus tard, peut être soit un type nul ou non annulable dérivé de la classe de base spécifiée.</span><span class="sxs-lookup"><span data-stu-id="00c7f-133">In a nullable context in C# 8.0 and later, `T` may be either a nullable or non-nullable type derived from the specified base class.</span></span> |
|<span data-ttu-id="00c7f-134">`where T :`nom d’interface>\* \<\*</span><span class="sxs-lookup"><span data-stu-id="00c7f-134">`where T :` *\<interface name>*</span></span>|<span data-ttu-id="00c7f-135">L’argument de type doit être ou implémenter l’interface spécifiée.</span><span class="sxs-lookup"><span data-stu-id="00c7f-135">The type argument must be or implement the specified interface.</span></span> <span data-ttu-id="00c7f-136">Plusieurs contraintes d’interface peuvent être spécifiées.</span><span class="sxs-lookup"><span data-stu-id="00c7f-136">Multiple interface constraints can be specified.</span></span> <span data-ttu-id="00c7f-137">L’interface qui impose les contraintes peut également être générique.</span><span class="sxs-lookup"><span data-stu-id="00c7f-137">The constraining interface can also be generic.</span></span> <span data-ttu-id="00c7f-138">Dans un contexte in annulable dans C 8.0 et plus tard, `T` doit être un type non-nullable qui implémente l’interface spécifiée.</span><span class="sxs-lookup"><span data-stu-id="00c7f-138">In a nullable context in C# 8.0 and later, `T` must be a non-nullable type that implements the specified interface.</span></span>|
|<span data-ttu-id="00c7f-139">`where T :`nom d’interface>? \* \<\*</span><span class="sxs-lookup"><span data-stu-id="00c7f-139">`where T :` *\<interface name>?*</span></span>|<span data-ttu-id="00c7f-140">L’argument de type doit être ou implémenter l’interface spécifiée.</span><span class="sxs-lookup"><span data-stu-id="00c7f-140">The type argument must be or implement the specified interface.</span></span> <span data-ttu-id="00c7f-141">Plusieurs contraintes d’interface peuvent être spécifiées.</span><span class="sxs-lookup"><span data-stu-id="00c7f-141">Multiple interface constraints can be specified.</span></span> <span data-ttu-id="00c7f-142">L’interface qui impose les contraintes peut également être générique.</span><span class="sxs-lookup"><span data-stu-id="00c7f-142">The constraining interface can also be generic.</span></span> <span data-ttu-id="00c7f-143">Dans un contexte in annulable dans le `T` C 8.0, peut être un type de référence nul, un type de référence non annulable ou un type de valeur.</span><span class="sxs-lookup"><span data-stu-id="00c7f-143">In a nullable context in C# 8.0, `T` may be a nullable reference type, a non-nullable reference type, or a value type.</span></span> <span data-ttu-id="00c7f-144">`T`peut ne pas être un type de valeur nulle.</span><span class="sxs-lookup"><span data-stu-id="00c7f-144">`T` may not be a nullable value type.</span></span>|
|`where T : U`|<span data-ttu-id="00c7f-145">L’argument de `T` type fourni pour doit `U`être ou dériver de l’argument fourni pour .</span><span class="sxs-lookup"><span data-stu-id="00c7f-145">The type argument supplied for `T` must be or derive from the argument supplied for `U`.</span></span> <span data-ttu-id="00c7f-146">Dans un contexte in `U` annulable, s’il `T` s’agit d’un type de référence non annulable, doit être de type de référence non annulable.</span><span class="sxs-lookup"><span data-stu-id="00c7f-146">In a nullable context, if `U` is a non-nullable reference type, `T` must be non-nullable reference type.</span></span> <span data-ttu-id="00c7f-147">S’il `U` s’agit `T` d’un type de référence in annulable, peut être soit infiruble ou non annulable.</span><span class="sxs-lookup"><span data-stu-id="00c7f-147">If `U` is a nullable reference type, `T` may be either nullable or non-nullable.</span></span> |

## <a name="why-use-constraints"></a><span data-ttu-id="00c7f-148">Pourquoi utiliser des contraintes</span><span class="sxs-lookup"><span data-stu-id="00c7f-148">Why use constraints</span></span>

<span data-ttu-id="00c7f-149">Les contraintes spécifient les capacités et les attentes d’un paramètre de type.</span><span class="sxs-lookup"><span data-stu-id="00c7f-149">Constraints specify the capabilities and expectations of a type parameter.</span></span> <span data-ttu-id="00c7f-150">Déclarer ces contraintes signifie que vous pouvez utiliser les opérations et les appels de méthode du type de contrainte.</span><span class="sxs-lookup"><span data-stu-id="00c7f-150">Declaring those constraints means you can use the operations and method calls of the constraining type.</span></span> <span data-ttu-id="00c7f-151">Si votre classe ou méthode générique utilise une opération sur les membres <xref:System.Object?displayProperty=nameWithType>génériques au-delà de la simple affectation ou d’appeler des méthodes non prises en charge par, vous devrez appliquer des contraintes au paramètre de type.</span><span class="sxs-lookup"><span data-stu-id="00c7f-151">If your generic class or method uses any operation on the generic members beyond simple assignment or calling any methods not supported by <xref:System.Object?displayProperty=nameWithType>, you'll have to apply constraints to the type parameter.</span></span> <span data-ttu-id="00c7f-152">Par exemple, la contrainte de classe de base indique au compilateur que seuls les objets de ce type ou dérivés de ce type seront utilisés comme arguments de type.</span><span class="sxs-lookup"><span data-stu-id="00c7f-152">For example, the base class constraint tells the compiler that only objects of this type or derived from this type will be used as type arguments.</span></span> <span data-ttu-id="00c7f-153">Une fois que le compilateur a cette garantie, il peut autoriser les méthodes de ce type à être appelées dans la classe générique.</span><span class="sxs-lookup"><span data-stu-id="00c7f-153">Once the compiler has this guarantee, it can allow methods of that type to be called in the generic class.</span></span> <span data-ttu-id="00c7f-154">L’exemple de code suivant illustre la fonctionnalité que vous pouvez ajouter à la classe `GenericList<T>` (dans [Introduction aux génériques](../../../standard/generics/index.md)) en appliquant une contrainte de classe de base.</span><span class="sxs-lookup"><span data-stu-id="00c7f-154">The following code example demonstrates the functionality you can add to the `GenericList<T>` class (in [Introduction to Generics](../../../standard/generics/index.md)) by applying a base class constraint.</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#9)]

<span data-ttu-id="00c7f-155">La contrainte permet à la classe générique d’utiliser la propriété `Employee.Name`.</span><span class="sxs-lookup"><span data-stu-id="00c7f-155">The constraint enables the generic class to use the `Employee.Name` property.</span></span> <span data-ttu-id="00c7f-156">La contrainte spécifie que tous les éléments de type `T` sont soit un objet `Employee`, soit un objet qui hérite de `Employee`, et rien d’autre.</span><span class="sxs-lookup"><span data-stu-id="00c7f-156">The constraint specifies that all items of type `T` are guaranteed to be either an `Employee` object or an object that inherits from `Employee`.</span></span>

<span data-ttu-id="00c7f-157">Plusieurs contraintes peuvent être appliquées au même paramètre de type, et les contraintes elles-mêmes peuvent être des types génériques, comme suit :</span><span class="sxs-lookup"><span data-stu-id="00c7f-157">Multiple constraints can be applied to the same type parameter, and the constraints themselves can be generic types, as follows:</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#10)]

<span data-ttu-id="00c7f-158">En appliquant la contrainte `where T : class`, évitez d’utiliser les opérateurs `==` et `!=` sur le paramètre de type, car ces opérateurs testent uniquement l’identité des références, et non l’égalité des valeurs.</span><span class="sxs-lookup"><span data-stu-id="00c7f-158">When applying the `where T : class` constraint, avoid the `==` and `!=` operators on the type parameter because these operators will test for reference identity only, not for value equality.</span></span> <span data-ttu-id="00c7f-159">Ce comportement se produit même si ces opérateurs sont surchargés dans un type qui est utilisé comme argument.</span><span class="sxs-lookup"><span data-stu-id="00c7f-159">This behavior occurs even if these operators are overloaded in a type that is used as an argument.</span></span> <span data-ttu-id="00c7f-160">Le code suivant illustre ce point ; la sortie a la valeur false même si la classe <xref:System.String> surcharge l’opérateur `==`.</span><span class="sxs-lookup"><span data-stu-id="00c7f-160">The following code illustrates this point; the output is false even though the <xref:System.String> class overloads the `==` operator.</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#11)]

<span data-ttu-id="00c7f-161">Le compilateur sait `T` seulement qu’il s’agit d’un type de référence au moment de la compilation et doit utiliser les opérateurs par défaut qui sont valables pour tous les types de référence.</span><span class="sxs-lookup"><span data-stu-id="00c7f-161">The compiler only knows that `T` is a reference type at compile time and must use the default operators that are valid for all reference types.</span></span> <span data-ttu-id="00c7f-162">Si vous devez tester l’égalité des valeurs, il est recommandé d’appliquer également la contrainte `where T : IEquatable<T>` ou `where T : IComparable<T>` et d’implémenter l’interface dans toute classe qui sera utilisée pour construire la classe générique.</span><span class="sxs-lookup"><span data-stu-id="00c7f-162">If you must test for value equality, the recommended way is to also apply the `where T : IEquatable<T>` or `where T : IComparable<T>` constraint and implement the interface in any class that will be used to construct the generic class.</span></span>

## <a name="constraining-multiple-parameters"></a><span data-ttu-id="00c7f-163">Utilisation de contraintes dans plusieurs paramètres</span><span class="sxs-lookup"><span data-stu-id="00c7f-163">Constraining multiple parameters</span></span>

<span data-ttu-id="00c7f-164">Vous pouvez appliquer des contraintes à plusieurs paramètres et plusieurs contraintes à un seul paramètre, comme indiqué dans l’exemple suivant :</span><span class="sxs-lookup"><span data-stu-id="00c7f-164">You can apply constraints to multiple parameters, and multiple constraints to a single parameter, as shown in the following example:</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#12)]

## <a name="unbounded-type-parameters"></a><span data-ttu-id="00c7f-165">Paramètres de type unbounded</span><span class="sxs-lookup"><span data-stu-id="00c7f-165">Unbounded type parameters</span></span>

 <span data-ttu-id="00c7f-166">Les paramètres de type qui n’ont aucune contrainte, tels que T dans la classe publique `SampleClass<T>{}`, sont appelés paramètres de type unbounded.</span><span class="sxs-lookup"><span data-stu-id="00c7f-166">Type parameters that have no constraints, such as T in public class `SampleClass<T>{}`, are called unbounded type parameters.</span></span> <span data-ttu-id="00c7f-167">Les paramètres de type unbounded obéissent aux règles suivantes :</span><span class="sxs-lookup"><span data-stu-id="00c7f-167">Unbounded type parameters have the following rules:</span></span>

- <span data-ttu-id="00c7f-168">Les `!=` `==` opérateurs et les opérateurs ne peuvent pas être utilisés parce qu’il n’y a aucune garantie que l’argument de type concret soutiendra ces opérateurs.</span><span class="sxs-lookup"><span data-stu-id="00c7f-168">The `!=` and `==` operators can't be used because there's no guarantee that the concrete type argument will support these operators.</span></span>
- <span data-ttu-id="00c7f-169">Ils peuvent être convertis vers et depuis `System.Object` ou être explicitement convertis vers tout type d’interface.</span><span class="sxs-lookup"><span data-stu-id="00c7f-169">They can be converted to and from `System.Object` or explicitly converted to any interface type.</span></span>
- <span data-ttu-id="00c7f-170">Vous pouvez les comparer à [null](../../language-reference/keywords/null.md).</span><span class="sxs-lookup"><span data-stu-id="00c7f-170">You can compare them to [null](../../language-reference/keywords/null.md).</span></span> <span data-ttu-id="00c7f-171">Si un paramètre unbounded est comparé à `null`, la comparaison retourne toujours la valeur false si l’argument de type est un type valeur.</span><span class="sxs-lookup"><span data-stu-id="00c7f-171">If an unbounded parameter is compared to `null`, the comparison will always return false if the type argument is a value type.</span></span>

## <a name="type-parameters-as-constraints"></a><span data-ttu-id="00c7f-172">Paramètres de type en tant que contraintes</span><span class="sxs-lookup"><span data-stu-id="00c7f-172">Type parameters as constraints</span></span>

<span data-ttu-id="00c7f-173">L’utilisation d’un paramètre de type générique comme contrainte est utile quand une fonction membre dotée de son propre paramètre de type doit contraindre ce paramètre au paramètre du type conteneur, comme le montre l’exemple suivant :</span><span class="sxs-lookup"><span data-stu-id="00c7f-173">The use of a generic type parameter as a constraint is useful when a member function with its own type parameter has to constrain that parameter to the type parameter of the containing type, as shown in the following example:</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#13)]

<span data-ttu-id="00c7f-174">Dans l’exemple précédent, `T` est une contrainte de type dans le contexte de la méthode `Add` et un paramètre de type unbounded dans le contexte de la classe `List`.</span><span class="sxs-lookup"><span data-stu-id="00c7f-174">In the previous example, `T` is a type constraint in the context of the `Add` method, and an unbounded type parameter in the context of the `List` class.</span></span>

<span data-ttu-id="00c7f-175">Les paramètres de type peuvent également être utilisés comme contraintes dans les définitions de classes génériques.</span><span class="sxs-lookup"><span data-stu-id="00c7f-175">Type parameters can also be used as constraints in generic class definitions.</span></span> <span data-ttu-id="00c7f-176">Le paramètre de type doit être déclaré entre crochets pointus, ainsi que tous les autres paramètres de type :</span><span class="sxs-lookup"><span data-stu-id="00c7f-176">The type parameter must be declared within the angle brackets together with any other type parameters:</span></span>

[!code-csharp[using the class and struct constraints](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#14)]

<span data-ttu-id="00c7f-177">L’utilité des paramètres de type en tant que contraintes avec les classes génériques est limitée, car le compilateur ne peut rien deviner à propos du paramètre de type en dehors du fait qu’il dérive de `System.Object`.</span><span class="sxs-lookup"><span data-stu-id="00c7f-177">The usefulness of type parameters as constraints with generic classes is limited because the compiler can assume nothing about the type parameter except that it derives from `System.Object`.</span></span> <span data-ttu-id="00c7f-178">Utilisez des paramètres de type en tant que contraintes sur les classes génériques dans les scénarios dans lesquels vous souhaitez mettre en application une relation d’héritage entre deux paramètres de type.</span><span class="sxs-lookup"><span data-stu-id="00c7f-178">Use type parameters as constraints on generic classes in scenarios in which you want to enforce an inheritance relationship between two type parameters.</span></span>

## <a name="notnull-constraint"></a><span data-ttu-id="00c7f-179">Contrainte NotNull</span><span class="sxs-lookup"><span data-stu-id="00c7f-179">NotNull constraint</span></span>

<span data-ttu-id="00c7f-180">En commençant par C 8.0 dans un contexte `notnull` in annulable, vous pouvez utiliser la contrainte pour spécifier que l’argument type doit être un type de valeur non annulable ou un type de référence non annulable.</span><span class="sxs-lookup"><span data-stu-id="00c7f-180">Beginning with C# 8.0 in a nullable context, you can use the `notnull` constraint to specify that the type argument must be a non-nullable value type or non-nullable reference type.</span></span> <span data-ttu-id="00c7f-181">La `notnull` contrainte ne peut être `nullable enable` utilisée que dans un contexte.</span><span class="sxs-lookup"><span data-stu-id="00c7f-181">The `notnull` constraint can only be used in a `nullable enable` context.</span></span> <span data-ttu-id="00c7f-182">Le compilateur génère un avertissement `notnull` si vous ajoutez la contrainte dans un contexte oubliable.</span><span class="sxs-lookup"><span data-stu-id="00c7f-182">The compiler generates a warning if you add the `notnull` constraint in a nullable oblivious context.</span></span>

<span data-ttu-id="00c7f-183">Contrairement à d’autres contraintes, `notnull` lorsqu’un argument type viole la contrainte, le `nullable enable` compilateur génère un avertissement lorsque ce code est compilé dans un contexte.</span><span class="sxs-lookup"><span data-stu-id="00c7f-183">Unlike other constraints, when a type argument violates the `notnull` constraint, the compiler generates a warning when that code is compiled in a `nullable enable` context.</span></span> <span data-ttu-id="00c7f-184">Si le code est compilé dans un contexte inconsible, le compilateur ne génère pas d’avertissements ou d’erreurs.</span><span class="sxs-lookup"><span data-stu-id="00c7f-184">If the code is compiled in a nullable oblivious context, the compiler doesn't generate any warnings or errors.</span></span>

<span data-ttu-id="00c7f-185">Commençant par le C 8.0 dans `class` un contexte où il est in annulable, la contrainte précise que l’argument type doit être un type de référence non annulable.</span><span class="sxs-lookup"><span data-stu-id="00c7f-185">Beginning with C# 8.0 in a nullable context, the `class` constraint specifies that the type argument must be a non-nullable reference type.</span></span> <span data-ttu-id="00c7f-186">Dans un contexte in annulable, lorsqu’un paramètre type est un type de référence nul, le compilateur génère un avertissement.</span><span class="sxs-lookup"><span data-stu-id="00c7f-186">In a nullable context, when a type parameter is a nullable reference type, the compiler generates a warning.</span></span>

## <a name="unmanaged-constraint"></a><span data-ttu-id="00c7f-187">Contrainte non managée</span><span class="sxs-lookup"><span data-stu-id="00c7f-187">Unmanaged constraint</span></span>

<span data-ttu-id="00c7f-188">En commençant par le C 7.3, vous pouvez utiliser la `unmanaged` contrainte pour spécifier que le paramètre de type doit être un type non-nullable non [managéré](../../language-reference/builtin-types/unmanaged-types.md).</span><span class="sxs-lookup"><span data-stu-id="00c7f-188">Beginning with C# 7.3, you can use the `unmanaged` constraint to specify that the type parameter must be a non-nullable [unmanaged type](../../language-reference/builtin-types/unmanaged-types.md).</span></span> <span data-ttu-id="00c7f-189">La contrainte `unmanaged` vous permet d’écrire des routines réutilisables à appliquer aux types qui peuvent être manipulés comme blocs de mémoire, comme illustré dans l’exemple suivant :</span><span class="sxs-lookup"><span data-stu-id="00c7f-189">The `unmanaged` constraint enables you to write reusable routines to work with types that can be manipulated as blocks of memory, as shown in the following example:</span></span>

[!code-csharp[using the unmanaged constraint](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#15)]

<span data-ttu-id="00c7f-190">La méthode précédente doit être compilée dans un contexte `unsafe`, car elle utilise l’opérateur `sizeof` sur un type qui n’est pas connu pour être un type intégré.</span><span class="sxs-lookup"><span data-stu-id="00c7f-190">The preceding method must be compiled in an `unsafe` context because it uses the `sizeof` operator on a type not known to be a built-in type.</span></span> <span data-ttu-id="00c7f-191">Sans la contrainte `unmanaged`, l’opérateur `sizeof` n’est pas disponible.</span><span class="sxs-lookup"><span data-stu-id="00c7f-191">Without the `unmanaged` constraint, the `sizeof` operator is unavailable.</span></span>

<span data-ttu-id="00c7f-192">La `unmanaged` contrainte implique `struct` la contrainte et ne peut pas être combiné avec elle.</span><span class="sxs-lookup"><span data-stu-id="00c7f-192">The `unmanaged` constraint implies the `struct` constraint and can't be combined with it.</span></span> <span data-ttu-id="00c7f-193">Parce `struct` que la contrainte `new()` implique la `unmanaged` contrainte, la contrainte ne `new()` peut pas être combinée avec la contrainte ainsi.</span><span class="sxs-lookup"><span data-stu-id="00c7f-193">Because the `struct` constraint implies the `new()` constraint, the `unmanaged` constraint can't be combined with the `new()` constraint as well.</span></span>

## <a name="delegate-constraints"></a><span data-ttu-id="00c7f-194">Contraintes de délégué</span><span class="sxs-lookup"><span data-stu-id="00c7f-194">Delegate constraints</span></span>

<span data-ttu-id="00c7f-195">À partir de C# 7.3, vous pouvez aussi utiliser <xref:System.Delegate?displayProperty=nameWithType> ou <xref:System.MulticastDelegate?displayProperty=nameWithType> comme contrainte de classe de base.</span><span class="sxs-lookup"><span data-stu-id="00c7f-195">Also beginning with C# 7.3, you can use <xref:System.Delegate?displayProperty=nameWithType> or <xref:System.MulticastDelegate?displayProperty=nameWithType> as a base class constraint.</span></span> <span data-ttu-id="00c7f-196">Le CLR a toujours autorisé cette contrainte, contrairement au langage C#.</span><span class="sxs-lookup"><span data-stu-id="00c7f-196">The CLR always allowed this constraint, but the C# language disallowed it.</span></span> <span data-ttu-id="00c7f-197">La contrainte `System.Delegate` vous permet d’écrire du code qui fonctionne avec les délégués en mode type sécurisé.</span><span class="sxs-lookup"><span data-stu-id="00c7f-197">The `System.Delegate` constraint enables you to write code that works with delegates in a type-safe manner.</span></span> <span data-ttu-id="00c7f-198">Le code suivant définit une méthode d’extension qui combine deux délégués à condition qu’ils soient du même type :</span><span class="sxs-lookup"><span data-stu-id="00c7f-198">The following code defines an extension method that combines two delegates provided they're the same type:</span></span>

[!code-csharp[using the delegate constraint](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#16)]

<span data-ttu-id="00c7f-199">Vous pouvez utiliser la méthode ci-dessus pour combiner des délégués qui sont du même type :</span><span class="sxs-lookup"><span data-stu-id="00c7f-199">You can use the above method to combine delegates that are the same type:</span></span>

[!code-csharp[using the unmanaged constraint](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#17)]

<span data-ttu-id="00c7f-200">Si vous supprimez les commentaires de la dernière ligne, il ne sera pas compilé.</span><span class="sxs-lookup"><span data-stu-id="00c7f-200">If you uncomment the last line, it won't compile.</span></span> <span data-ttu-id="00c7f-201">Les `first` `test` deux et sont des types de délégués, mais ils sont différents types de délégués.</span><span class="sxs-lookup"><span data-stu-id="00c7f-201">Both `first` and `test` are delegate types, but they're different delegate types.</span></span>

## <a name="enum-constraints"></a><span data-ttu-id="00c7f-202">Contraintes d’enum</span><span class="sxs-lookup"><span data-stu-id="00c7f-202">Enum constraints</span></span>

<span data-ttu-id="00c7f-203">À compter de C# 7.3, vous pouvez également spécifier le type <xref:System.Enum?displayProperty=nameWithType> comme contrainte de classe de base.</span><span class="sxs-lookup"><span data-stu-id="00c7f-203">Beginning in C# 7.3, you can also specify the <xref:System.Enum?displayProperty=nameWithType> type as a base class constraint.</span></span> <span data-ttu-id="00c7f-204">Le CLR a toujours autorisé cette contrainte, contrairement au langage C#.</span><span class="sxs-lookup"><span data-stu-id="00c7f-204">The CLR always allowed this constraint, but the C# language disallowed it.</span></span> <span data-ttu-id="00c7f-205">Les génériques utilisant `System.Enum` fournissent une programmation de type sécurisé aux résultats de cache issus de l’utilisation de méthodes statiques dans `System.Enum`.</span><span class="sxs-lookup"><span data-stu-id="00c7f-205">Generics using `System.Enum` provide type-safe programming to cache results from using the static methods in `System.Enum`.</span></span> <span data-ttu-id="00c7f-206">L’exemple suivant recherche toutes les valeurs valides d’un type enum, puis génère un dictionnaire qui mappe ces valeurs à sa représentation sous forme de chaîne.</span><span class="sxs-lookup"><span data-stu-id="00c7f-206">The following sample finds all the valid values for an enum type, and then builds a dictionary that maps those values to its string representation.</span></span>

[!code-csharp[using the enum constraint](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#18)]

<span data-ttu-id="00c7f-207">Les méthodes utilisées pour faire usage de la réflexion, qui a des implications de performance.</span><span class="sxs-lookup"><span data-stu-id="00c7f-207">The methods used to make use of reflection, which has performance implications.</span></span> <span data-ttu-id="00c7f-208">Vous pouvez appeler cette méthode pour générer une collection qui est mise en cache et réutilisée, plutôt que de répéter les appels qui nécessitent la réflexion.</span><span class="sxs-lookup"><span data-stu-id="00c7f-208">You can call this method to build a collection that is cached and reused rather than repeating the calls that require reflection.</span></span>

<span data-ttu-id="00c7f-209">Vous pouvez l’utiliser comme montré dans l’exemple suivant pour créer un enum et générer un dictionnaire de ses valeurs et de ses noms :</span><span class="sxs-lookup"><span data-stu-id="00c7f-209">You could use it as shown in the following sample to create an enum and build a dictionary of its values and names:</span></span>

[!code-csharp[enum definition](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#19)]

[!code-csharp[using the enum constrained method](~/samples/snippets/csharp/keywords/GenericWhereConstraints.cs#20)]

## <a name="see-also"></a><span data-ttu-id="00c7f-210">Voir aussi</span><span class="sxs-lookup"><span data-stu-id="00c7f-210">See also</span></span>

- <xref:System.Collections.Generic>
- [<span data-ttu-id="00c7f-211">Guide de programmation CMD</span><span class="sxs-lookup"><span data-stu-id="00c7f-211">C# Programming Guide</span></span>](../index.md)
- [<span data-ttu-id="00c7f-212">Introduction aux génériques</span><span class="sxs-lookup"><span data-stu-id="00c7f-212">Introduction to Generics</span></span>](./index.md)
- [<span data-ttu-id="00c7f-213">Classes génériques</span><span class="sxs-lookup"><span data-stu-id="00c7f-213">Generic Classes</span></span>](./generic-classes.md)
- [<span data-ttu-id="00c7f-214">nouvelle contrainte</span><span class="sxs-lookup"><span data-stu-id="00c7f-214">new Constraint</span></span>](../../language-reference/keywords/new-constraint.md)
