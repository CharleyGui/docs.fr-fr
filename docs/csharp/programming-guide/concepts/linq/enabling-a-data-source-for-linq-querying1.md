---
title: Activation d'une source de données pour l'interrogation LINQ
ms.date: 07/20/2015
ms.assetid: d2ef04a5-31a6-45cb-af9a-a5ce7732662c
ms.openlocfilehash: 9a143f0da74d4e91ef697f468d7fda225e75245b
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 03/14/2020
ms.locfileid: "75635767"
---
# <a name="enabling-a-data-source-for-linq-querying"></a><span data-ttu-id="aef4d-102">Activation d'une source de données pour l'interrogation LINQ</span><span class="sxs-lookup"><span data-stu-id="aef4d-102">Enabling a Data Source for LINQ Querying</span></span>
<span data-ttu-id="aef4d-103">Il existe différentes façons d’étendre le LINQ pour permettre à toute source de données d’être interrogée dans le modèle LINQ.</span><span class="sxs-lookup"><span data-stu-id="aef4d-103">There are various ways to extend LINQ to enable any data source to be queried in the LINQ pattern.</span></span> <span data-ttu-id="aef4d-104">La source de données peut être une structure de données, un service Web, un système de fichiers ou une base de données, par exemple.</span><span class="sxs-lookup"><span data-stu-id="aef4d-104">The data source might be a data structure, a Web service, a file system, or a database, to name some.</span></span> <span data-ttu-id="aef4d-105">Le modèle LINQ permet aux clients d’interroger facilement une source de données pour laquelle la requête LINQ est activée, car la syntaxe et le modèle de la requête ne changent pas.</span><span class="sxs-lookup"><span data-stu-id="aef4d-105">The LINQ pattern makes it easy for clients to query a data source for which LINQ querying is enabled, because the syntax and pattern of the query does not change.</span></span> <span data-ttu-id="aef4d-106">La façon dont LINQ peut être étendue à ces sources de données est la suivante :</span><span class="sxs-lookup"><span data-stu-id="aef4d-106">The ways in which LINQ can be extended to these data sources include the following:</span></span>  
  
- <span data-ttu-id="aef4d-107">Mise en <xref:System.Collections.Generic.IEnumerable%601> œuvre de l’interface en un type pour permettre à LINQ d’interroger les objets de ce type.</span><span class="sxs-lookup"><span data-stu-id="aef4d-107">Implementing the <xref:System.Collections.Generic.IEnumerable%601> interface in a type to enable LINQ to Objects querying of that type.</span></span>  
  
- <span data-ttu-id="aef4d-108">Création de méthodes standard <xref:System.Linq.Enumerable.Where%2A> d’opérateur de requête telles que et <xref:System.Linq.Enumerable.Select%2A> qui étendent un type, pour permettre la requête personnalisée LINQ de ce type.</span><span class="sxs-lookup"><span data-stu-id="aef4d-108">Creating standard query operator methods such as <xref:System.Linq.Enumerable.Where%2A> and <xref:System.Linq.Enumerable.Select%2A> that extend a type, to enable custom LINQ querying of that type.</span></span>  
  
- <span data-ttu-id="aef4d-109">Création d'un fournisseur pour votre source de données qui implémente l'interface <xref:System.Linq.IQueryable%601>.</span><span class="sxs-lookup"><span data-stu-id="aef4d-109">Creating a provider for your data source that implements the <xref:System.Linq.IQueryable%601> interface.</span></span> <span data-ttu-id="aef4d-110">Un fournisseur qui implémente cette interface reçoit des requêtes LINQ sous forme d’arbres d’expression, qu’il peut exécuter de manière personnalisée, par exemple à distance.</span><span class="sxs-lookup"><span data-stu-id="aef4d-110">A provider that implements this interface receives LINQ queries in the form of expression trees, which it can execute in a custom way, for example remotely.</span></span>  
  
- <span data-ttu-id="aef4d-111">Créer un fournisseur pour votre source de données qui tire parti d’une technologie LINQ existante.</span><span class="sxs-lookup"><span data-stu-id="aef4d-111">Creating a provider for your data source that takes advantage of an existing LINQ technology.</span></span> <span data-ttu-id="aef4d-112">Ce type de fournisseur permettrait non seulement l'interrogation, mais également l'insertion, la mise à jour et la suppression d'opérations et le mappage pour des types définis par l'utilisateur.</span><span class="sxs-lookup"><span data-stu-id="aef4d-112">Such a provider would enable not only querying, but also insert, update, and delete operations and mapping for user-defined types.</span></span>  
  
 <span data-ttu-id="aef4d-113">Cette rubrique décrit ces options.</span><span class="sxs-lookup"><span data-stu-id="aef4d-113">This topic discusses these options.</span></span>  
  
## <a name="how-to-enable-linq-querying-of-your-data-source"></a><span data-ttu-id="aef4d-114">Comment activer l'interrogation LINQ de votre source de données</span><span class="sxs-lookup"><span data-stu-id="aef4d-114">How to Enable LINQ Querying of Your Data Source</span></span>  
  
### <a name="in-memory-data"></a><span data-ttu-id="aef4d-115">Données en mémoire</span><span class="sxs-lookup"><span data-stu-id="aef4d-115">In-Memory Data</span></span>  
 <span data-ttu-id="aef4d-116">Il y a deux façons d’activer la requête LINQ des données en mémoire.</span><span class="sxs-lookup"><span data-stu-id="aef4d-116">There are two ways you can enable LINQ querying of in-memory data.</span></span> <span data-ttu-id="aef4d-117">Si les données sont d’un type qui implémente, <xref:System.Collections.Generic.IEnumerable%601>vous pouvez interroger les données en utilisant LINQ à des objets.</span><span class="sxs-lookup"><span data-stu-id="aef4d-117">If the data is of a type that implements <xref:System.Collections.Generic.IEnumerable%601>, you can query the data by using LINQ to Objects.</span></span> <span data-ttu-id="aef4d-118">S’il n’est pas logique d’activer votre <xref:System.Collections.Generic.IEnumerable%601> type en implémentant l’interface, vous pouvez définir les méthodes d’opérateur de requête standard LINQ dans ce type ou créer des méthodes d’opérateur de requête standard LINQ qui étendent le type.</span><span class="sxs-lookup"><span data-stu-id="aef4d-118">If it does not make sense to enable enumeration of your type by implementing the <xref:System.Collections.Generic.IEnumerable%601> interface, you can define LINQ standard query operator methods in that type or create LINQ standard query operator methods that extend the type.</span></span> <span data-ttu-id="aef4d-119">Les implémentations personnalisées des opérateurs de requête standard doivent utiliser l'exécution différée pour retourner les résultats.</span><span class="sxs-lookup"><span data-stu-id="aef4d-119">Custom implementations of the standard query operators should use deferred execution to return the results.</span></span>  
  
### <a name="remote-data"></a><span data-ttu-id="aef4d-120">Données distantes</span><span class="sxs-lookup"><span data-stu-id="aef4d-120">Remote Data</span></span>  
 <span data-ttu-id="aef4d-121">La meilleure option pour permettre à LINQ de poser <xref:System.Linq.IQueryable%601> des questions sur une source de données à distance est de mettre en œuvre l’interface.</span><span class="sxs-lookup"><span data-stu-id="aef4d-121">The best option for enabling LINQ querying of a remote data source is to implement the <xref:System.Linq.IQueryable%601> interface.</span></span> <span data-ttu-id="aef4d-122">Toutefois, cette méthode diffère de l'extension d'un fournisseur tel que [!INCLUDE[vbtecdlinq](~/includes/vbtecdlinq-md.md)] pour une source de données.</span><span class="sxs-lookup"><span data-stu-id="aef4d-122">However, this differs from extending a provider such as [!INCLUDE[vbtecdlinq](~/includes/vbtecdlinq-md.md)] for a data source.</span></span> <span data-ttu-id="aef4d-123">Aucun modèle de fournisseur pour étendre [!INCLUDE[vbtecdlinq](~/includes/vbtecdlinq-md.md)]les technologies LINQ existantes, comme , à d’autres types de sources de données n’est disponible dans Visual Studio 2008.</span><span class="sxs-lookup"><span data-stu-id="aef4d-123">No provider models for extending existing LINQ technologies, such as [!INCLUDE[vbtecdlinq](~/includes/vbtecdlinq-md.md)], to other types of data source are available in Visual Studio 2008.</span></span>
  
## <a name="iqueryable-linq-providers"></a><span data-ttu-id="aef4d-124">Fournisseurs LINQ IQueryable</span><span class="sxs-lookup"><span data-stu-id="aef4d-124">IQueryable LINQ Providers</span></span>  
 <span data-ttu-id="aef4d-125">Les fournisseurs de <xref:System.Linq.IQueryable%601> LINQ qui mettent en œuvre peuvent varier considérablement dans leur complexité.</span><span class="sxs-lookup"><span data-stu-id="aef4d-125">LINQ providers that implement <xref:System.Linq.IQueryable%601> can vary widely in their complexity.</span></span> <span data-ttu-id="aef4d-126">Cette section décrit les différents niveaux de complexité.</span><span class="sxs-lookup"><span data-stu-id="aef4d-126">This section discusses the different levels of complexity.</span></span>  
  
 <span data-ttu-id="aef4d-127">Un fournisseur `IQueryable` moins complexe peut interagir avec une méthode unique d'un service Web.</span><span class="sxs-lookup"><span data-stu-id="aef4d-127">A less complex `IQueryable` provider might interface with a single method of a Web service.</span></span> <span data-ttu-id="aef4d-128">Ce type de fournisseur est très particulier, car il attend des informations spécifiques dans les requêtes qu'il gère.</span><span class="sxs-lookup"><span data-stu-id="aef4d-128">This type of provider is very specific because it expects specific information in the queries that it handles.</span></span> <span data-ttu-id="aef4d-129">Il se caractérise par un système de type fermé, exposant peut-être un type de résultat unique.</span><span class="sxs-lookup"><span data-stu-id="aef4d-129">It has a closed type system, perhaps exposing a single result type.</span></span> <span data-ttu-id="aef4d-130">La plus grande part de l'exécution de la requête est effectuée localement, par exemple à l'aide des implémentations <xref:System.Linq.Enumerable> des opérateurs de requête standard.</span><span class="sxs-lookup"><span data-stu-id="aef4d-130">Most of the execution of the query occurs locally, for example by using the <xref:System.Linq.Enumerable> implementations of the standard query operators.</span></span> <span data-ttu-id="aef4d-131">Un fournisseur moins complexe peut examiner une seule expression d'appel de méthode dans l'arborescence d'expression qui représente la requête, et la logique restante de la requête doit être gérée ailleurs.</span><span class="sxs-lookup"><span data-stu-id="aef4d-131">A less complex provider might examine only one method call expression in the expression tree that represents the query, and let the remaining logic of the query be handled elsewhere.</span></span>  
  
 <span data-ttu-id="aef4d-132">Un fournisseur `IQueryable` de complexité moyenne peut cibler une source de données qui possède un langage de requête partiellement expressif.</span><span class="sxs-lookup"><span data-stu-id="aef4d-132">An `IQueryable` provider of medium complexity might target a data source that has a partially expressive query language.</span></span> <span data-ttu-id="aef4d-133">S'il cible un service Web, il peut interagir avec plusieurs méthodes du service Web et sélectionner la méthode d'appel en fonction de la question posée par la requête.</span><span class="sxs-lookup"><span data-stu-id="aef4d-133">If it targets a Web service, it might interface with more than one method of the Web service and select the method to call based on the question that the query poses.</span></span> <span data-ttu-id="aef4d-134">Un fournisseur de complexité moyenne aurait un système plus sophistiqué que celui d'un fournisseur simple, qui serait néanmoins un système de type fixe.</span><span class="sxs-lookup"><span data-stu-id="aef4d-134">A provider of medium complexity would have a richer type system than a simple provider, but it would still be a fixed type system.</span></span> <span data-ttu-id="aef4d-135">Par exemple, le fournisseur peut exposer des types qui ont des relations un-à-plusieurs qui peuvent être parcourues, mais il ne fournirait pas de technologie de mappage pour les types définis par l'utilisateur.</span><span class="sxs-lookup"><span data-stu-id="aef4d-135">For example, the provider might expose types that have one-to-many relationships that can be traversed, but it would not provide mapping technology for user-defined types.</span></span>  
  
 <span data-ttu-id="aef4d-136">Un `IQueryable` fournisseur complexe, [!INCLUDE[vbtecdlinq](~/includes/vbtecdlinq-md.md)] comme le fournisseur, peut traduire des requêtes complètes de LINQ dans un langage de requête expressif, comme SQL.</span><span class="sxs-lookup"><span data-stu-id="aef4d-136">A complex `IQueryable` provider, such as the [!INCLUDE[vbtecdlinq](~/includes/vbtecdlinq-md.md)] provider, might translate complete LINQ queries to an expressive query language, such as SQL.</span></span> <span data-ttu-id="aef4d-137">Un fournisseur complexe est plus général qu'un fournisseur moins complexe, car il peut gérer une plus grande gamme de questions dans la requête.</span><span class="sxs-lookup"><span data-stu-id="aef4d-137">A complex provider is more general than a less complex provider, because it can handle a wider variety of questions in the query.</span></span> <span data-ttu-id="aef4d-138">Il possède également un système de type ouvert et doit donc contenir une infrastructure étendue pour mapper les types définis par l'utilisateur.</span><span class="sxs-lookup"><span data-stu-id="aef4d-138">It also has an open type system and therefore must contain extensive infrastructure to map user-defined types.</span></span> <span data-ttu-id="aef4d-139">Développer un fournisseur complexe est une tâche difficile.</span><span class="sxs-lookup"><span data-stu-id="aef4d-139">Developing a complex provider requires a significant amount of effort.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="aef4d-140">Voir aussi</span><span class="sxs-lookup"><span data-stu-id="aef4d-140">See also</span></span>

- <xref:System.Linq.IQueryable%601>
- <xref:System.Collections.Generic.IEnumerable%601>
- <xref:System.Linq.Enumerable>
- [<span data-ttu-id="aef4d-141">Vue d’ensemble des opérateurs de requête standard (C#)</span><span class="sxs-lookup"><span data-stu-id="aef4d-141">Standard Query Operators Overview (C#)</span></span>](./standard-query-operators-overview.md)
- [<span data-ttu-id="aef4d-142">LINQ to Objects (C#)</span><span class="sxs-lookup"><span data-stu-id="aef4d-142">LINQ to Objects (C#)</span></span>](./linq-to-objects.md)
