---
title: Sérialisation (C#)
description: La sérialisation convertit un objet en un flux d’octets pour stocker l’objet ou le transmettre à la mémoire, à une base de données ou à un fichier.
ms.date: 01/02/2020
ms.openlocfilehash: 29625648b19c97556c107997ef9ecd3f0f971cbf
ms.sourcegitcommit: 7499bdb428d63ed0e19e97f54d3d576c41598659
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 07/31/2020
ms.locfileid: "87455748"
---
# <a name="serialization-c"></a><span data-ttu-id="94323-103">Sérialisation (C#)</span><span class="sxs-lookup"><span data-stu-id="94323-103">Serialization (C#)</span></span>

<span data-ttu-id="94323-104">La sérialisation est le processus de conversion d’un objet en flux d’octets pour stocker l’objet ou le transmettre à la mémoire, une base de données ou un fichier.</span><span class="sxs-lookup"><span data-stu-id="94323-104">Serialization is the process of converting an object into a stream of bytes to store the object or transmit it to memory, a database, or a file.</span></span> <span data-ttu-id="94323-105">Son principal objectif est d’enregistrer l’état d’un objet afin de pouvoir le recréer si nécessaire.</span><span class="sxs-lookup"><span data-stu-id="94323-105">Its main purpose is to save the state of an object in order to be able to recreate it when needed.</span></span> <span data-ttu-id="94323-106">Le processus inverse est appelé désérialisation.</span><span class="sxs-lookup"><span data-stu-id="94323-106">The reverse process is called deserialization.</span></span>

## <a name="how-serialization-works"></a><span data-ttu-id="94323-107">Fonctionnement de la sérialisation</span><span class="sxs-lookup"><span data-stu-id="94323-107">How serialization works</span></span>

<span data-ttu-id="94323-108">Cette illustration montre le processus global de sérialisation :</span><span class="sxs-lookup"><span data-stu-id="94323-108">This illustration shows the overall process of serialization:</span></span>

![Graphique sérialisation](./media/index/serialization-process.gif)

<span data-ttu-id="94323-110">L’objet est sérialisé en un flux qui contient les données.</span><span class="sxs-lookup"><span data-stu-id="94323-110">The object is serialized to a stream that carries the data.</span></span> <span data-ttu-id="94323-111">Le flux peut également contenir des informations sur le type de l’objet, telles que sa version, sa culture et son nom d’assembly.</span><span class="sxs-lookup"><span data-stu-id="94323-111">The stream may also have information about the object's type, such as its version, culture, and assembly name.</span></span> <span data-ttu-id="94323-112">À partir de ce flux, l’objet peut être stocké dans une base de données, un fichier ou une mémoire.</span><span class="sxs-lookup"><span data-stu-id="94323-112">From that stream, the object can be stored in a database, a file, or memory.</span></span>

### <a name="uses-for-serialization"></a><span data-ttu-id="94323-113">Usages de la sérialisation</span><span class="sxs-lookup"><span data-stu-id="94323-113">Uses for serialization</span></span>

<span data-ttu-id="94323-114">La sérialisation permet au développeur d’enregistrer l’état d’un objet et de le recréer en fonction des besoins, en fournissant le stockage des objets ainsi que l’échange de données.</span><span class="sxs-lookup"><span data-stu-id="94323-114">Serialization allows the developer to save the state of an object and re-create it as needed, providing storage of objects as well as data exchange.</span></span> <span data-ttu-id="94323-115">Par le biais de la sérialisation, un développeur peut effectuer des actions telles que :</span><span class="sxs-lookup"><span data-stu-id="94323-115">Through serialization, a developer can perform actions such as:</span></span>

* <span data-ttu-id="94323-116">Envoi de l’objet à une application distante à l’aide d’un service Web</span><span class="sxs-lookup"><span data-stu-id="94323-116">Sending the object to a remote application by using a web service</span></span>
* <span data-ttu-id="94323-117">Passage d’un objet d’un domaine à un autre</span><span class="sxs-lookup"><span data-stu-id="94323-117">Passing an object from one domain to another</span></span>
* <span data-ttu-id="94323-118">Passage d’un objet via un pare-feu sous forme de chaîne JSON ou XML</span><span class="sxs-lookup"><span data-stu-id="94323-118">Passing an object through a firewall as a JSON or XML string</span></span>
* <span data-ttu-id="94323-119">Gestion des informations relatives à la sécurité ou aux utilisateurs entre les applications</span><span class="sxs-lookup"><span data-stu-id="94323-119">Maintaining security or user-specific information across applications</span></span>

## <a name="json-serialization"></a><span data-ttu-id="94323-120">Sérialisation JSON</span><span class="sxs-lookup"><span data-stu-id="94323-120">JSON serialization</span></span>

<span data-ttu-id="94323-121">L' <xref:System.Text.Json> espace de noms contient des classes pour la sérialisation et la désérialisation JavaScript Object Notation (JSON).</span><span class="sxs-lookup"><span data-stu-id="94323-121">The <xref:System.Text.Json> namespace contains classes for JavaScript Object Notation (JSON) serialization and deserialization.</span></span> <span data-ttu-id="94323-122">JSON est une norme ouverte qui est couramment utilisée pour partager des données sur le Web.</span><span class="sxs-lookup"><span data-stu-id="94323-122">JSON is an open standard that is commonly used for sharing data across the web.</span></span>

<span data-ttu-id="94323-123">La sérialisation JSON sérialise les propriétés publiques d’un objet dans une chaîne, un tableau d’octets ou un flux conforme à [la spécification JSON RFC 8259](https://tools.ietf.org/html/rfc8259).</span><span class="sxs-lookup"><span data-stu-id="94323-123">JSON serialization serializes the public properties of an object into a string, byte array, or stream that conforms to [the RFC 8259 JSON specification](https://tools.ietf.org/html/rfc8259).</span></span> <span data-ttu-id="94323-124">Pour contrôler la manière dont <xref:System.Text.Json.JsonSerializer> sérialise ou désérialise une instance de la classe :</span><span class="sxs-lookup"><span data-stu-id="94323-124">To control the way <xref:System.Text.Json.JsonSerializer> serializes or deserializes an instance of the class:</span></span>

* <span data-ttu-id="94323-125">Utiliser un <xref:System.Text.Json.JsonSerializerOptions> objet</span><span class="sxs-lookup"><span data-stu-id="94323-125">Use a <xref:System.Text.Json.JsonSerializerOptions> object</span></span>
* <span data-ttu-id="94323-126">Appliquer des attributs de l' <xref:System.Text.Json.Serialization> espace de noms à des classes ou des propriétés</span><span class="sxs-lookup"><span data-stu-id="94323-126">Apply attributes from the <xref:System.Text.Json.Serialization> namespace to classes or properties</span></span>
* [<span data-ttu-id="94323-127">Implémenter des convertisseurs personnalisés</span><span class="sxs-lookup"><span data-stu-id="94323-127">Implement custom converters</span></span>](../../../../standard/serialization/system-text-json-converters-how-to.md)

## <a name="binary-and-xml-serialization"></a><span data-ttu-id="94323-128">Sérialisation binaire et XML</span><span class="sxs-lookup"><span data-stu-id="94323-128">Binary and XML serialization</span></span>

<span data-ttu-id="94323-129">L' <xref:System.Runtime.Serialization> espace de noms contient des classes pour la sérialisation et la désérialisation binaires et XML.</span><span class="sxs-lookup"><span data-stu-id="94323-129">The <xref:System.Runtime.Serialization> namespace contains classes for binary and XML serialization and deserialization.</span></span>

<span data-ttu-id="94323-130">La sérialisation binaire utilise l’encodage binaire pour produire une sérialisation compacte qui peut servir notamment aux flux réseau par socket ou stockage.</span><span class="sxs-lookup"><span data-stu-id="94323-130">Binary serialization uses binary encoding to produce compact serialization for uses such as storage or socket-based network streams.</span></span> <span data-ttu-id="94323-131">Dans la sérialisation binaire, tous les membres, y compris en lecture seule, sont sérialisés et les performances sont améliorées.</span><span class="sxs-lookup"><span data-stu-id="94323-131">In binary serialization, all members, even members that are read-only, are serialized, and performance is enhanced.</span></span>

[!INCLUDE [binary-serialization-warning](~/includes/binary-serialization-warning.md)]

<span data-ttu-id="94323-132">La sérialisation XML sérialise les champs et les propriétés publics d’un objet ou les paramètres et valeurs renvoyés de méthodes, en un flux XML conforme à un document XSD (langage de définition de schéma XML) spécifique.</span><span class="sxs-lookup"><span data-stu-id="94323-132">XML serialization serializes the public fields and properties of an object, or the parameters and return values of methods, into an XML stream that conforms to a specific XML Schema definition language (XSD) document.</span></span> <span data-ttu-id="94323-133">La sérialisation XML permet d’obtenir des classes fortement typées avec des propriétés et des champs publics convertis au format XML.</span><span class="sxs-lookup"><span data-stu-id="94323-133">XML serialization results in strongly typed classes with public properties and fields that are converted to XML.</span></span> <span data-ttu-id="94323-134"><xref:System.Xml.Serialization>contient des classes pour sérialiser et désérialiser du code XML.</span><span class="sxs-lookup"><span data-stu-id="94323-134"><xref:System.Xml.Serialization> contains classes for serializing and deserializing XML.</span></span> <span data-ttu-id="94323-135">Vous pouvez appliquer des attributs à des classes et des membres de classes pour contrôler la manière dont <xref:System.Xml.Serialization.XmlSerializer> sérialise ou désérialise une instance de la classe.</span><span class="sxs-lookup"><span data-stu-id="94323-135">You apply attributes to classes and class members to control the way the <xref:System.Xml.Serialization.XmlSerializer> serializes or deserializes an instance of the class.</span></span>

### <a name="making-an-object-serializable"></a><span data-ttu-id="94323-136">Rendre un objet sérialisable</span><span class="sxs-lookup"><span data-stu-id="94323-136">Making an object serializable</span></span>

<span data-ttu-id="94323-137">Pour la sérialisation binaire ou XML, vous avez besoin des éléments suivants :</span><span class="sxs-lookup"><span data-stu-id="94323-137">For binary or XML serialization, you need:</span></span>

* <span data-ttu-id="94323-138">Objet à sérialiser.</span><span class="sxs-lookup"><span data-stu-id="94323-138">The object to be serialized</span></span>
* <span data-ttu-id="94323-139">Flux destiné à contenir l’objet sérialisé</span><span class="sxs-lookup"><span data-stu-id="94323-139">A stream to contain the serialized object</span></span>
* <span data-ttu-id="94323-140">Une <xref:System.Runtime.Serialization.Formatter?displayProperty=fullName> instance</span><span class="sxs-lookup"><span data-stu-id="94323-140">A <xref:System.Runtime.Serialization.Formatter?displayProperty=fullName> instance</span></span>

<span data-ttu-id="94323-141">Appliquez l' <xref:System.SerializableAttribute> attribut à un type pour indiquer que les instances du type peuvent être sérialisées.</span><span class="sxs-lookup"><span data-stu-id="94323-141">Apply the <xref:System.SerializableAttribute> attribute to a type to indicate that instances of the type can be serialized.</span></span> <span data-ttu-id="94323-142">Une exception est levée si vous tentez une sérialisation alors que le type n’a pas l’attribut <xref:System.SerializableAttribute>.</span><span class="sxs-lookup"><span data-stu-id="94323-142">An  exception is thrown if you attempt to serialize but the type doesn't have the <xref:System.SerializableAttribute> attribute.</span></span>

<span data-ttu-id="94323-143">Pour empêcher la sérialisation d’un champ, appliquez l' <xref:System.NonSerializedAttribute> attribut.</span><span class="sxs-lookup"><span data-stu-id="94323-143">To prevent a field from being serialized, apply the <xref:System.NonSerializedAttribute> attribute.</span></span> <span data-ttu-id="94323-144">Si un champ d’un type sérialisable contient un pointeur, un handle ou une autre structure de données propre à un environnement particulier et qu’il n’est pas possible de le reconstituer de manière significative dans un autre environnement, vous pouvez le rendre non sérialisable.</span><span class="sxs-lookup"><span data-stu-id="94323-144">If a field of a serializable type contains a pointer, a handle, or some other data structure that is specific to a particular environment, and the field cannot be meaningfully reconstituted in a different environment, then you may want to make it nonserializable.</span></span>

<span data-ttu-id="94323-145">Si une classe sérialisée contient des références à des objets d’autres classes marqués par <xref:System.SerializableAttribute>, ces objets seront également sérialisés.</span><span class="sxs-lookup"><span data-stu-id="94323-145">If a serialized class contains references to objects of other classes that are marked <xref:System.SerializableAttribute>, those objects will also be serialized.</span></span>

### <a name="basic-and-custom-serialization"></a><span data-ttu-id="94323-146">Sérialisation de base et sérialisation personnalisée</span><span class="sxs-lookup"><span data-stu-id="94323-146">Basic and custom serialization</span></span>

<span data-ttu-id="94323-147">La sérialisation binaire et XML peut être effectuée de deux manières, de base et personnalisée.</span><span class="sxs-lookup"><span data-stu-id="94323-147">Binary and XML serialization can be performed in two ways, basic and custom.</span></span>

<span data-ttu-id="94323-148">La sérialisation de base utilise .NET pour sérialiser automatiquement l’objet.</span><span class="sxs-lookup"><span data-stu-id="94323-148">Basic serialization uses .NET to automatically serialize the object.</span></span> <span data-ttu-id="94323-149">La seule exigence est que l’attribut est appliqué à la classe <xref:System.SerializableAttribute> .</span><span class="sxs-lookup"><span data-stu-id="94323-149">The only requirement is that the class has the <xref:System.SerializableAttribute> attribute applied.</span></span> <span data-ttu-id="94323-150"><xref:System.NonSerializedAttribute> peut être utilisé pour empêcher que certains champs ne soient sérialisés.</span><span class="sxs-lookup"><span data-stu-id="94323-150">The <xref:System.NonSerializedAttribute> can be used to keep specific fields from being serialized.</span></span>

<span data-ttu-id="94323-151">Quand vous utilisez la sérialisation de base, le contrôle de version des objets peut créer des problèmes.</span><span class="sxs-lookup"><span data-stu-id="94323-151">When you use basic serialization, the versioning of objects may create problems.</span></span> <span data-ttu-id="94323-152">Vous pouvez utiliser la sérialisation personnalisée quand les problèmes de contrôle de version sont importants.</span><span class="sxs-lookup"><span data-stu-id="94323-152">You would use custom serialization when versioning issues are important.</span></span> <span data-ttu-id="94323-153">La sérialisation de base est le moyen le plus simple d’effectuer la sérialisation, mais elle ne fournit pas beaucoup de contrôle sur le processus.</span><span class="sxs-lookup"><span data-stu-id="94323-153">Basic serialization is the easiest way to perform serialization, but it does not provide much control over the process.</span></span>

<span data-ttu-id="94323-154">Dans la sérialisation personnalisée, vous pouvez spécifier exactement quels objets sont sérialisés et de quelle façon.</span><span class="sxs-lookup"><span data-stu-id="94323-154">In custom serialization, you can specify exactly which objects will be serialized and how it will be done.</span></span> <span data-ttu-id="94323-155">La classe doit être marquée <xref:System.SerializableAttribute> et implémenter l’interface <xref:System.Runtime.Serialization.ISerializable>.</span><span class="sxs-lookup"><span data-stu-id="94323-155">The class must be marked <xref:System.SerializableAttribute> and implement the <xref:System.Runtime.Serialization.ISerializable> interface.</span></span> <span data-ttu-id="94323-156">Si vous souhaitez également que votre objet soit désérialisé de manière personnalisée, utilisez un constructeur personnalisé.</span><span class="sxs-lookup"><span data-stu-id="94323-156">If you want your object to be deserialized in a custom manner as well, use a custom constructor.</span></span>

## <a name="designer-serialization"></a><span data-ttu-id="94323-157">Sérialisation du concepteur</span><span class="sxs-lookup"><span data-stu-id="94323-157">Designer serialization</span></span>

<span data-ttu-id="94323-158">La sérialisation du concepteur est une forme particulière de sérialisation qui implique le type de persistance d’objets associé aux outils de développement.</span><span class="sxs-lookup"><span data-stu-id="94323-158">Designer serialization is a special form of serialization that involves the kind of object persistence associated with development tools.</span></span> <span data-ttu-id="94323-159">La sérialisation du concepteur est le processus qui consiste à convertir un graphique d’objet en un fichier source utilisable ultérieurement pour récupérer le graphique d’objet.</span><span class="sxs-lookup"><span data-stu-id="94323-159">Designer serialization is the process of converting an object graph into a source file that can later be used to recover the object graph.</span></span> <span data-ttu-id="94323-160">Un fichier source peut contenir du code, des balises ou même des tables SQL.</span><span class="sxs-lookup"><span data-stu-id="94323-160">A source file can contain code, markup, or even SQL table information.</span></span>

## <a name="related-topics-and-examples"></a><a name="BKMK_RelatedTopics"></a> <span data-ttu-id="94323-161">Rubriques connexes et exemples</span><span class="sxs-lookup"><span data-stu-id="94323-161">Related Topics and Examples</span></span>  

<span data-ttu-id="94323-162">[Présentation deSystem.Text.Js](../../../../standard/serialization/system-text-json-overview.md) Montre comment récupérer la `System.Text.Json` bibliothèque.</span><span class="sxs-lookup"><span data-stu-id="94323-162">[System.Text.Json overview](../../../../standard/serialization/system-text-json-overview.md) Shows how to get the `System.Text.Json` library.</span></span>

<span data-ttu-id="94323-163">[Comment sérialiser et désérialiser JSON dans .net](../../../../standard/serialization/system-text-json-how-to.md).</span><span class="sxs-lookup"><span data-stu-id="94323-163">[How to serialize and deserialize JSON in .NET](../../../../standard/serialization/system-text-json-how-to.md).</span></span>
<span data-ttu-id="94323-164">Montre comment lire et écrire des données d’objet dans et à partir de JSON à l’aide de la <xref:System.Text.Json.JsonSerializer> classe.</span><span class="sxs-lookup"><span data-stu-id="94323-164">Shows how to read and write object data to and from JSON using the <xref:System.Text.Json.JsonSerializer> class.</span></span>

[<span data-ttu-id="94323-165">Procédure pas à pas : persistance d’un objet dans Visual Studio (C#)</span><span class="sxs-lookup"><span data-stu-id="94323-165">Walkthrough: Persisting an Object in Visual Studio (C#)</span></span>](walkthrough-persisting-an-object-in-visual-studio.md)  
<span data-ttu-id="94323-166">Montre comment utiliser la sérialisation pour rendre les données d’un objet persistantes entre les instances, afin de stocker des valeurs et de les récupérer lors de la prochaine instanciation de l’objet.</span><span class="sxs-lookup"><span data-stu-id="94323-166">Demonstrates how serialization can be used to persist an object's data between instances, allowing you to store values and retrieve them the next time the object is instantiated.</span></span>

[<span data-ttu-id="94323-167">Comment lire des données d’objet à partir d’un fichier XML (C#)</span><span class="sxs-lookup"><span data-stu-id="94323-167">How to read object data from an XML file (C#)</span></span>](how-to-read-object-data-from-an-xml-file.md)  
<span data-ttu-id="94323-168">Montre comment lire les données d’objet écrites précédemment dans un fichier XML en utilisant la classe <xref:System.Xml.Serialization.XmlSerializer>.</span><span class="sxs-lookup"><span data-stu-id="94323-168">Shows how to read object data that was previously written to an XML file using the <xref:System.Xml.Serialization.XmlSerializer> class.</span></span>

[<span data-ttu-id="94323-169">Comment écrire des données d’objet dans un fichier XML (C#)</span><span class="sxs-lookup"><span data-stu-id="94323-169">How to write object data to an XML file (C#)</span></span>](how-to-write-object-data-to-an-xml-file.md)  
<span data-ttu-id="94323-170">Montre comment écrire l’objet depuis une classe vers un fichier XML en utilisant la classe <xref:System.Xml.Serialization.XmlSerializer>.</span><span class="sxs-lookup"><span data-stu-id="94323-170">Shows how to write the object from a class to an XML file using the <xref:System.Xml.Serialization.XmlSerializer> class.</span></span>
