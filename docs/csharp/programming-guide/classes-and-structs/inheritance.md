---
title: Héritage - Guide de programmation C#
description: L’héritage en C# vous permet de créer de nouvelles classes qui réutilisent, étendent et modifient le comportement défini dans d’autres classes.
ms.date: 02/07/2020
helpviewer_keywords:
- abstract methods [C#]
- abstract classes [C#]
- inheritance [C#]
- derived classes [C#]
- virtual methods [C#]
- C# language, inheritance
ms.assetid: 81d64ee4-50f9-4d6c-a8dc-257c348d2eea
ms.openlocfilehash: 6b94f58801af188655474f9c7de76b79381e721d
ms.sourcegitcommit: 3d84eac0818099c9949035feb96bbe0346358504
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 07/21/2020
ms.locfileid: "86864253"
---
# <a name="inheritance-c-programming-guide"></a><span data-ttu-id="a8af4-103">Héritage (Guide de programmation C#)</span><span class="sxs-lookup"><span data-stu-id="a8af4-103">Inheritance (C# Programming Guide)</span></span>

<span data-ttu-id="a8af4-104">L’héritage, avec l’encapsulation et le polymorphisme, est l’une des trois principales caractéristiques de la programmation orientée objet.</span><span class="sxs-lookup"><span data-stu-id="a8af4-104">Inheritance, together with encapsulation and polymorphism, is one of the three primary characteristics of object-oriented programming.</span></span> <span data-ttu-id="a8af4-105">L’héritage vous permet de créer de nouvelles classes qui réutilisent, étendent et modifient le comportement défini dans d’autres classes.</span><span class="sxs-lookup"><span data-stu-id="a8af4-105">Inheritance enables you to create new classes that reuse, extend, and modify the behavior defined in other classes.</span></span> <span data-ttu-id="a8af4-106">La classe dont les membres sont hérités porte le nom de *classe de base* et la classe qui hérite de ces membres porte le nom de *classe dérivée*.</span><span class="sxs-lookup"><span data-stu-id="a8af4-106">The class whose members are inherited is called the *base class*, and the class that inherits those members is called the *derived class*.</span></span> <span data-ttu-id="a8af4-107">Une classe dérivée ne peut avoir qu’une seule classe de base directe.</span><span class="sxs-lookup"><span data-stu-id="a8af4-107">A derived class can have only one direct base class.</span></span> <span data-ttu-id="a8af4-108">Toutefois, l’héritage est transitif.</span><span class="sxs-lookup"><span data-stu-id="a8af4-108">However, inheritance is transitive.</span></span> <span data-ttu-id="a8af4-109">Si `ClassC` est dérivée de, `ClassB` et `ClassB` est dérivée de `ClassA` , `ClassC` hérite des membres déclarés dans `ClassB` et `ClassA` .</span><span class="sxs-lookup"><span data-stu-id="a8af4-109">If `ClassC` is derived from `ClassB`, and `ClassB` is derived from `ClassA`, `ClassC` inherits the members declared in `ClassB` and `ClassA`.</span></span>

> [!NOTE]
> <span data-ttu-id="a8af4-110">Les structs ne prennent pas en charge l’héritage, mais ils peuvent implémenter les interfaces.</span><span class="sxs-lookup"><span data-stu-id="a8af4-110">Structs do not support inheritance, but they can implement interfaces.</span></span> <span data-ttu-id="a8af4-111">Pour plus d'informations, consultez [Interfaces](../interfaces/index.md).</span><span class="sxs-lookup"><span data-stu-id="a8af4-111">For more information, see [Interfaces](../interfaces/index.md).</span></span>

<span data-ttu-id="a8af4-112">D’un point de vue conceptuel, une classe dérivée est une spécialisation de la classe de base.</span><span class="sxs-lookup"><span data-stu-id="a8af4-112">Conceptually, a derived class is a specialization of the base class.</span></span> <span data-ttu-id="a8af4-113">Par exemple, si vous avez une classe de base `Animal`, vous pouvez avoir une classe dérivée nommée `Mammal` et une autre classe dérivée nommée `Reptile`.</span><span class="sxs-lookup"><span data-stu-id="a8af4-113">For example, if you have a base class `Animal`, you might have one derived class that is named `Mammal` and another derived class that is named `Reptile`.</span></span> <span data-ttu-id="a8af4-114">Un `Mammal` est un `Animal`, et un `Reptile` est un `Animal`, mais chaque classe dérivée représente des spécialisations différentes de la classe de base.</span><span class="sxs-lookup"><span data-stu-id="a8af4-114">A `Mammal` is an `Animal`, and a `Reptile` is an `Animal`, but each derived class represents different specializations of the base class.</span></span>

<span data-ttu-id="a8af4-115">Les déclarations d’interface peuvent définir une implémentation par défaut pour ses membres.</span><span class="sxs-lookup"><span data-stu-id="a8af4-115">Interface declarations may define a default implementation for its members.</span></span> <span data-ttu-id="a8af4-116">Ces implémentations sont héritées par les interfaces dérivées et par les classes qui implémentent ces interfaces.</span><span class="sxs-lookup"><span data-stu-id="a8af4-116">These implementations are inherited by derived interfaces, and by classes that implement those interfaces.</span></span> <span data-ttu-id="a8af4-117">Pour plus d’informations sur les méthodes d’interface par défaut, consultez l’article sur les [interfaces](../../language-reference/keywords/interface.md) dans la section Référence du langage.</span><span class="sxs-lookup"><span data-stu-id="a8af4-117">For more information on default interface methods, see the article on [interfaces](../../language-reference/keywords/interface.md) in the language reference section.</span></span>

<span data-ttu-id="a8af4-118">Quand vous définissez une classe à dériver d’une autre classe, la classe dérivée obtient implicitement tous les membres de la classe de base, à l’exception de ses constructeurs et de ses finaliseurs.</span><span class="sxs-lookup"><span data-stu-id="a8af4-118">When you define a class to derive from another class, the derived class implicitly gains all the members of the base class, except for its constructors and finalizers.</span></span> <span data-ttu-id="a8af4-119">La classe dérivée réutilise le code de la classe de base sans avoir à la réimplémenter.</span><span class="sxs-lookup"><span data-stu-id="a8af4-119">The derived class reuses the code in the base class without having to reimplement it.</span></span> <span data-ttu-id="a8af4-120">Vous pouvez ajouter d’autres membres dans la classe dérivée.</span><span class="sxs-lookup"><span data-stu-id="a8af4-120">You can add more members in the derived class.</span></span> <span data-ttu-id="a8af4-121">La classe dérivée étend les fonctionnalités de la classe de base.</span><span class="sxs-lookup"><span data-stu-id="a8af4-121">The derived class extends the functionality of the base class.</span></span>

<span data-ttu-id="a8af4-122">L’illustration suivante montre une classe `WorkItem` qui représente un élément de travail dans un processus métier.</span><span class="sxs-lookup"><span data-stu-id="a8af4-122">The following illustration shows a class `WorkItem` that represents an item of work in some business process.</span></span> <span data-ttu-id="a8af4-123">Comme toutes les classes, elle dérive de <xref:System.Object?displayProperty=nameWithType> et hérite de toutes ses méthodes.</span><span class="sxs-lookup"><span data-stu-id="a8af4-123">Like all classes, it derives from <xref:System.Object?displayProperty=nameWithType> and inherits all its methods.</span></span> <span data-ttu-id="a8af4-124">`WorkItem` ajoute cinq de ses propres membres.</span><span class="sxs-lookup"><span data-stu-id="a8af4-124">`WorkItem` adds five members of its own.</span></span> <span data-ttu-id="a8af4-125">Ces membres incluent un constructeur, car les constructeurs ne sont pas hérités.</span><span class="sxs-lookup"><span data-stu-id="a8af4-125">These members include a constructor, because constructors aren't inherited.</span></span> <span data-ttu-id="a8af4-126">La classe `ChangeRequest` hérite de `WorkItem` et représente un type particulier d’élément de travail.</span><span class="sxs-lookup"><span data-stu-id="a8af4-126">Class `ChangeRequest` inherits from `WorkItem` and represents a particular kind of work item.</span></span> <span data-ttu-id="a8af4-127">`ChangeRequest` ajoute deux membres supplémentaires aux membres qu’elle hérite de `WorkItem` et de <xref:System.Object>.</span><span class="sxs-lookup"><span data-stu-id="a8af4-127">`ChangeRequest` adds two more members to the members that it inherits from `WorkItem` and from <xref:System.Object>.</span></span> <span data-ttu-id="a8af4-128">Elle doit ajouter son propre constructeur et ajoute également `originalItemID`.</span><span class="sxs-lookup"><span data-stu-id="a8af4-128">It must add its own constructor, and it also adds `originalItemID`.</span></span> <span data-ttu-id="a8af4-129">La propriété `originalItemID` permet à l’instance `ChangeRequest` d’être associée au `WorkItem` d’origine auquel s’applique la demande de modification.</span><span class="sxs-lookup"><span data-stu-id="a8af4-129">Property `originalItemID` enables the `ChangeRequest` instance to be associated with the original `WorkItem` to which the change request applies.</span></span>

![Diagramme illustrant l’héritage de classe](./media/inheritance/class-inheritance-diagram.png)

<span data-ttu-id="a8af4-131">L’exemple suivant montre comment les relations de classe de l’illustration précédente sont exprimées en langage C#.</span><span class="sxs-lookup"><span data-stu-id="a8af4-131">The following example shows how the class relationships demonstrated in the previous illustration are expressed in C#.</span></span> <span data-ttu-id="a8af4-132">L’exemple montre également comment `WorkItem` substitue la méthode virtuelle <xref:System.Object.ToString%2A?displayProperty=nameWithType>, et comment la classe `ChangeRequest` hérite de l’implémentation `WorkItem` de la méthode.</span><span class="sxs-lookup"><span data-stu-id="a8af4-132">The example also shows how `WorkItem` overrides the virtual method <xref:System.Object.ToString%2A?displayProperty=nameWithType>, and how the `ChangeRequest` class inherits the `WorkItem` implementation of the method.</span></span> <span data-ttu-id="a8af4-133">Le premier bloc définit les classes :</span><span class="sxs-lookup"><span data-stu-id="a8af4-133">The first block defines the classes:</span></span>

[!code-csharp[DefineClasses](~/samples/snippets/csharp/objectoriented/inheritance.cs#Classes)]

<span data-ttu-id="a8af4-134">Le bloc suivant montre comment utiliser les classes de base et dérivées :</span><span class="sxs-lookup"><span data-stu-id="a8af4-134">This next block shows how to use the base and derived classes:</span></span>

[!code-csharp[UseClasses](~/samples/snippets/csharp/objectoriented/inheritance.cs#UseClasses)]

## <a name="abstract-and-virtual-methods"></a><span data-ttu-id="a8af4-135">Méthodes abstraites et virtuelles</span><span class="sxs-lookup"><span data-stu-id="a8af4-135">Abstract and virtual methods</span></span>

<span data-ttu-id="a8af4-136">Lorsqu’une classe de base déclare une méthode en tant que [`virtual`](../../language-reference/keywords/virtual.md) , une classe dérivée peut [`override`](../../language-reference/keywords/override.md) la méthode avec sa propre implémentation.</span><span class="sxs-lookup"><span data-stu-id="a8af4-136">When a base class declares a method as [`virtual`](../../language-reference/keywords/virtual.md), a derived class can [`override`](../../language-reference/keywords/override.md) the method with its own implementation.</span></span> <span data-ttu-id="a8af4-137">Si une classe de base déclare un membre en tant que [`abstract`](../../language-reference/keywords/abstract.md) , cette méthode doit être substituée dans toute classe non abstraite qui hérite directement de cette classe.</span><span class="sxs-lookup"><span data-stu-id="a8af4-137">If a base class declares a member as [`abstract`](../../language-reference/keywords/abstract.md), that method must be overridden in any non-abstract class that directly inherits from that class.</span></span> <span data-ttu-id="a8af4-138">Si une classe dérivée est abstraite, elle hérite des membres abstraits sans les implémenter.</span><span class="sxs-lookup"><span data-stu-id="a8af4-138">If a derived class is itself abstract, it inherits abstract members without implementing them.</span></span> <span data-ttu-id="a8af4-139">Les membres virtuels et abstraits sont la base du polymorphisme, qui est la deuxième caractéristique principale de la programmation orientée objet.</span><span class="sxs-lookup"><span data-stu-id="a8af4-139">Abstract and virtual members are the basis for polymorphism, which is the second primary characteristic of object-oriented programming.</span></span> <span data-ttu-id="a8af4-140">Pour plus d’informations, consultez [Polymorphisme](./polymorphism.md).</span><span class="sxs-lookup"><span data-stu-id="a8af4-140">For more information, see [Polymorphism](./polymorphism.md).</span></span>

## <a name="abstract-base-classes"></a><span data-ttu-id="a8af4-141">Classes de base abstraites</span><span class="sxs-lookup"><span data-stu-id="a8af4-141">Abstract base classes</span></span>

<span data-ttu-id="a8af4-142">Pour empêcher l’instanciation directe, vous pouvez déclarer une classe comme étant [abstract](../../language-reference/keywords/abstract.md) à l’aide de l’opérateur [new](../../language-reference/operators/new-operator.md).</span><span class="sxs-lookup"><span data-stu-id="a8af4-142">You can declare a class as [abstract](../../language-reference/keywords/abstract.md) if you want to prevent direct instantiation by using the [new](../../language-reference/operators/new-operator.md) operator.</span></span> <span data-ttu-id="a8af4-143">Une classe abstraite ne peut être utilisée que si une nouvelle classe en est dérivée.</span><span class="sxs-lookup"><span data-stu-id="a8af4-143">An abstract class can be used only if a new class is derived from it.</span></span> <span data-ttu-id="a8af4-144">Une classe abstraite peut contenir une ou plusieurs signatures de méthode qui sont également déclarées comme abstraites.</span><span class="sxs-lookup"><span data-stu-id="a8af4-144">An abstract class can contain one or more method signatures that themselves are declared as abstract.</span></span> <span data-ttu-id="a8af4-145">Ces signatures spécifient les paramètres et la valeur de retour, mais n’ont aucune implémentation (corps de méthode).</span><span class="sxs-lookup"><span data-stu-id="a8af4-145">These signatures specify the parameters and return value but have no implementation (method body).</span></span> <span data-ttu-id="a8af4-146">Une classe abstraite ne doit pas contenir de membres abstraits ; Toutefois, si une classe contient un membre abstrait, la classe elle-même doit être déclarée comme abstraite.</span><span class="sxs-lookup"><span data-stu-id="a8af4-146">An abstract class doesn't have to contain abstract members; however, if a class does contain an abstract member, the class itself must be declared as abstract.</span></span> <span data-ttu-id="a8af4-147">Les classes dérivées qui ne sont pas abstraites elles-mêmes doivent fournir l’implémentation pour toutes les méthodes abstraites d’une classe de base abstraite.</span><span class="sxs-lookup"><span data-stu-id="a8af4-147">Derived classes that aren't abstract themselves must provide the implementation for any abstract methods from an abstract base class.</span></span> <span data-ttu-id="a8af4-148">Pour plus d’informations, consultez [Classes abstract et sealed, et membres de classe](abstract-and-sealed-classes-and-class-members.md).</span><span class="sxs-lookup"><span data-stu-id="a8af4-148">For more information, see [Abstract and Sealed Classes and Class Members](abstract-and-sealed-classes-and-class-members.md).</span></span>

## <a name="interfaces"></a><span data-ttu-id="a8af4-149">Interfaces</span><span class="sxs-lookup"><span data-stu-id="a8af4-149">Interfaces</span></span>

<span data-ttu-id="a8af4-150">Une *interface* est un type référence qui définit un jeu de membres.</span><span class="sxs-lookup"><span data-stu-id="a8af4-150">An *interface* is a reference type that defines a set of members.</span></span> <span data-ttu-id="a8af4-151">Toutes les classes et les structs qui implémentent cette interface doivent implémenter ce jeu de membres.</span><span class="sxs-lookup"><span data-stu-id="a8af4-151">All classes and structs that implement that interface must implement that set of members.</span></span> <span data-ttu-id="a8af4-152">Une interface peut définir une implémentation par défaut pour tout ou partie de ces membres.</span><span class="sxs-lookup"><span data-stu-id="a8af4-152">An interface may define a default implementation for any or all of these members.</span></span> <span data-ttu-id="a8af4-153">Une classe peut implémenter plusieurs interfaces, même si elle ne peut dériver que d’une seule classe de base directe.</span><span class="sxs-lookup"><span data-stu-id="a8af4-153">A class can implement multiple interfaces even though it can derive from only a single direct base class.</span></span>

<span data-ttu-id="a8af4-154">Les interfaces sont utilisées pour définir des fonctionnalités spécifiques pour les classes qui n’ont pas nécessairement une relation « est une ».</span><span class="sxs-lookup"><span data-stu-id="a8af4-154">Interfaces are used to define specific capabilities for classes that don't necessarily have an "is a" relationship.</span></span> <span data-ttu-id="a8af4-155">Par exemple, l' <xref:System.IEquatable%601?displayProperty=nameWithType> interface peut être implémentée par n’importe quelle classe ou structure pour déterminer si deux objets du type sont équivalents (Toutefois, le type définit l’équivalence).</span><span class="sxs-lookup"><span data-stu-id="a8af4-155">For example, the <xref:System.IEquatable%601?displayProperty=nameWithType> interface can be implemented by any class or struct to determine whether two objects of the type are equivalent (however the type defines equivalence).</span></span> <span data-ttu-id="a8af4-156"><xref:System.IEquatable%601>n’implique pas le même type de relation « est un » qui existe entre une classe de base et une classe dérivée (par exemple, un `Mammal` est un `Animal` ).</span><span class="sxs-lookup"><span data-stu-id="a8af4-156"><xref:System.IEquatable%601> doesn't imply the same kind of "is a" relationship that exists between a base class and a derived class (for example, a `Mammal` is an `Animal`).</span></span> <span data-ttu-id="a8af4-157">Pour plus d'informations, consultez [Interfaces](../interfaces/index.md).</span><span class="sxs-lookup"><span data-stu-id="a8af4-157">For more information, see [Interfaces](../interfaces/index.md).</span></span>

## <a name="preventing-further-derivation"></a><span data-ttu-id="a8af4-158">Prévention des dérivations ultérieures</span><span class="sxs-lookup"><span data-stu-id="a8af4-158">Preventing further derivation</span></span>  

<span data-ttu-id="a8af4-159">Une classe peut empêcher d’autres classes d’hériter d’elle, ou de l’un de ses membres, en déclarant elle-même ou le membre comme [`sealed`](../../language-reference/keywords/sealed.md) .</span><span class="sxs-lookup"><span data-stu-id="a8af4-159">A class can prevent other classes from inheriting from it, or from any of its members, by declaring itself or the member as [`sealed`](../../language-reference/keywords/sealed.md).</span></span> <span data-ttu-id="a8af4-160">Pour plus d’informations, consultez [Classes abstract et sealed, et membres de classe](./abstract-and-sealed-classes-and-class-members.md).</span><span class="sxs-lookup"><span data-stu-id="a8af4-160">For more information, see [Abstract and Sealed Classes and Class Members](./abstract-and-sealed-classes-and-class-members.md).</span></span>

## <a name="derived-class-hiding-of-base-class-members"></a><span data-ttu-id="a8af4-161">Masquage de classe dérivée des membres de classe de base</span><span class="sxs-lookup"><span data-stu-id="a8af4-161">Derived class hiding of base class members</span></span>  

<span data-ttu-id="a8af4-162">Une classe dérivée peut masquer des membres de la classe de base en déclarant les membres à l’aide du même nom et de la même signature.</span><span class="sxs-lookup"><span data-stu-id="a8af4-162">A derived class can hide base class members by declaring members with the same name and signature.</span></span> <span data-ttu-id="a8af4-163">Le [`new`](../../language-reference/keywords/new-modifier.md) modificateur peut être utilisé pour indiquer explicitement que le membre n’est pas destiné à être une substitution du membre de base.</span><span class="sxs-lookup"><span data-stu-id="a8af4-163">The [`new`](../../language-reference/keywords/new-modifier.md) modifier can be used to explicitly indicate that the member isn't intended to be an override of the base member.</span></span> <span data-ttu-id="a8af4-164">L’utilisation de [`new`](../../language-reference/keywords/new-modifier.md) n’est pas obligatoire, mais un avertissement du compilateur est généré si [`new`](../../language-reference/keywords/new-modifier.md) n’est pas utilisé.</span><span class="sxs-lookup"><span data-stu-id="a8af4-164">The use of [`new`](../../language-reference/keywords/new-modifier.md) isn't required, but a compiler warning will be generated if [`new`](../../language-reference/keywords/new-modifier.md) isn't used.</span></span> <span data-ttu-id="a8af4-165">Pour plus d’informations, consultez [Gestion de version avec les mots clés override et new](./versioning-with-the-override-and-new-keywords.md) et [Savoir quand utiliser les mots clés override et new](./knowing-when-to-use-override-and-new-keywords.md).</span><span class="sxs-lookup"><span data-stu-id="a8af4-165">For more information, see [Versioning with the Override and New Keywords](./versioning-with-the-override-and-new-keywords.md) and [Knowing When to Use Override and New Keywords](./knowing-when-to-use-override-and-new-keywords.md).</span></span>

## <a name="see-also"></a><span data-ttu-id="a8af4-166">Voir aussi</span><span class="sxs-lookup"><span data-stu-id="a8af4-166">See also</span></span>

- [<span data-ttu-id="a8af4-167">Guide de programmation C#</span><span class="sxs-lookup"><span data-stu-id="a8af4-167">C# Programming Guide</span></span>](../index.md)
- [<span data-ttu-id="a8af4-168">Classes et structs</span><span class="sxs-lookup"><span data-stu-id="a8af4-168">Classes and Structs</span></span>](./index.md)
- [<span data-ttu-id="a8af4-169">class</span><span class="sxs-lookup"><span data-stu-id="a8af4-169">class</span></span>](../../language-reference/keywords/class.md)
