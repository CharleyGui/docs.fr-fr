---
title: Comment définir l’égalité des valeurs pour un type-guide de programmation C#
description: Découvrez comment définir l’égalité des valeurs pour un type. Consultez des exemples de code et affichez des ressources disponibles supplémentaires.
ms.date: 07/20/2015
helpviewer_keywords:
- overriding Equals method [C#]
- object equivalence [C#]
- Equals method [C#], overriding
- value equality [C#]
- equivalence [C#]
ms.assetid: 4084581e-b931-498b-9534-cf7ef5b68690
ms.openlocfilehash: 9523ba99f877fde7207042ecb8d28548168a68cb
ms.sourcegitcommit: ff5a4eb5cffbcac9521bc44a907a118cd7e8638d
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 10/17/2020
ms.locfileid: "92162724"
---
# <a name="how-to-define-value-equality-for-a-type-c-programming-guide"></a><span data-ttu-id="1ed51-104">Comment définir l’égalité des valeurs pour un type (Guide de programmation C#)</span><span class="sxs-lookup"><span data-stu-id="1ed51-104">How to define value equality for a type (C# Programming Guide)</span></span>

<span data-ttu-id="1ed51-105">Quand vous définissez une classe ou un struct, vous décidez s’il est judicieux de créer une définition personnalisée de l’égalité des valeurs (ou équivalence) pour le type.</span><span class="sxs-lookup"><span data-stu-id="1ed51-105">When you define a class or struct, you decide whether it makes sense to create a custom definition of value equality (or equivalence) for the type.</span></span> <span data-ttu-id="1ed51-106">En général, vous implémentez l’égalité des valeurs quand des objets du type sont supposés être ajoutés à une collection quelconque, ou quand leur objectif principal est de stocker un ensemble de champs ou propriétés.</span><span class="sxs-lookup"><span data-stu-id="1ed51-106">Typically, you implement value equality when objects of the type are expected to be added to a collection of some sort, or when their primary purpose is to store a set of fields or properties.</span></span> <span data-ttu-id="1ed51-107">Vous pouvez baser votre définition de l’égalité des valeurs sur une comparaison de tous les champs et propriétés du type, ou vous pouvez la baser sur un sous-ensemble.</span><span class="sxs-lookup"><span data-stu-id="1ed51-107">You can base your definition of value equality on a comparison of all the fields and properties in the type, or you can base the definition on a subset.</span></span>

<span data-ttu-id="1ed51-108">Dans les deux cas, et dans les classes et les structs, votre implémentation doit suivre les cinq garanties d’équivalence (pour les règles suivantes, supposez que `x` , `y` et ne `z` sont pas null) :</span><span class="sxs-lookup"><span data-stu-id="1ed51-108">In either case, and in both classes and structs, your implementation should follow the five guarantees of equivalence (For the following rules, assume that `x`, `y` and `z` are not null):</span></span>  
  
1. <span data-ttu-id="1ed51-109">`x.Equals(x)` retourne `true`.</span><span class="sxs-lookup"><span data-stu-id="1ed51-109">`x.Equals(x)` returns `true`.</span></span> <span data-ttu-id="1ed51-110">Il s’agit de la propriété réflexive.</span><span class="sxs-lookup"><span data-stu-id="1ed51-110">This is called the reflexive property.</span></span>  
  
2. <span data-ttu-id="1ed51-111">`x.Equals(y)` retourne la même valeur que `y.Equals(x)`.</span><span class="sxs-lookup"><span data-stu-id="1ed51-111">`x.Equals(y)` returns the same value as `y.Equals(x)`.</span></span> <span data-ttu-id="1ed51-112">Il s’agit de la propriété symétrique.</span><span class="sxs-lookup"><span data-stu-id="1ed51-112">This is called the symmetric property.</span></span>  
  
3. <span data-ttu-id="1ed51-113">si `(x.Equals(y) && y.Equals(z))` retourne `true`, `x.Equals(z)` retourne `true`.</span><span class="sxs-lookup"><span data-stu-id="1ed51-113">if `(x.Equals(y) && y.Equals(z))` returns `true`, then `x.Equals(z)` returns `true`.</span></span> <span data-ttu-id="1ed51-114">Il s’agit de la propriété transitive.</span><span class="sxs-lookup"><span data-stu-id="1ed51-114">This is called the transitive property.</span></span>  
  
4. <span data-ttu-id="1ed51-115">Les appels successifs de `x.Equals(y)` retournent la même valeur tant que les objets référencés par x et y ne sont pas modifiés.</span><span class="sxs-lookup"><span data-stu-id="1ed51-115">Successive invocations of `x.Equals(y)` return the same value as long as the objects referenced by x and y are not modified.</span></span>  
  
5. <span data-ttu-id="1ed51-116">Toute valeur non NULL n’est pas égale à null.</span><span class="sxs-lookup"><span data-stu-id="1ed51-116">Any non-null value is not equal to null.</span></span> <span data-ttu-id="1ed51-117">Toutefois, le CLR vérifie la valeur NULL sur tous les appels de méthode et lève une `NullReferenceException` si la `this` référence aurait la valeur null.</span><span class="sxs-lookup"><span data-stu-id="1ed51-117">However, the CLR checks for null on all method calls and throws a `NullReferenceException` if the `this` reference would be null.</span></span> <span data-ttu-id="1ed51-118">Par conséquent, `x.Equals(y)` lève une exception lorsque `x` a la valeur null.</span><span class="sxs-lookup"><span data-stu-id="1ed51-118">Therefore, `x.Equals(y)` throws an exception when `x` is null.</span></span> <span data-ttu-id="1ed51-119">Cela arrête les règles 1 ou 2, en fonction de l’argument de `Equals` .</span><span class="sxs-lookup"><span data-stu-id="1ed51-119">That breaks rules 1 or 2, depending on the argument to `Equals`.</span></span>

<span data-ttu-id="1ed51-120">Tout struct que vous définissez a déjà une implémentation par défaut de l’égalité des valeurs dont il hérite de la substitution <xref:System.ValueType?displayProperty=nameWithType> de la méthode <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="1ed51-120">Any struct that you define already has a default implementation of value equality that it inherits from the <xref:System.ValueType?displayProperty=nameWithType> override of the <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType> method.</span></span> <span data-ttu-id="1ed51-121">Cette implémentation utilise la réflexion pour examiner tous les champs et propriétés du type.</span><span class="sxs-lookup"><span data-stu-id="1ed51-121">This implementation uses reflection to examine all the fields and properties in the type.</span></span> <span data-ttu-id="1ed51-122">Bien que cette implémentation produise des résultats corrects, elle est relativement lente par rapport à une implémentation personnalisée que vous écrivez spécifiquement pour le type.</span><span class="sxs-lookup"><span data-stu-id="1ed51-122">Although this implementation produces correct results, it is relatively slow compared to a custom implementation that you write specifically for the type.</span></span>  
  
<span data-ttu-id="1ed51-123">Les détails d’implémentation pour l’égalité des valeurs sont différents pour les classes et les structs.</span><span class="sxs-lookup"><span data-stu-id="1ed51-123">The implementation details for value equality are different for classes and structs.</span></span> <span data-ttu-id="1ed51-124">Toutefois, les classes et les structs nécessitent tous deux les mêmes étapes de base pour l’implémentation de l’égalité :</span><span class="sxs-lookup"><span data-stu-id="1ed51-124">However, both classes and structs require the same basic steps for implementing equality:</span></span>  
  
1. <span data-ttu-id="1ed51-125">Substituez la méthode [virtual](../../language-reference/keywords/virtual.md) <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="1ed51-125">Override the [virtual](../../language-reference/keywords/virtual.md) <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType> method.</span></span> <span data-ttu-id="1ed51-126">Dans la plupart des cas, votre implémentation de `bool Equals( object obj )` doit simplement appeler la méthode `Equals` propre au type qui est l’implémentation de l’interface <xref:System.IEquatable%601?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="1ed51-126">In most cases, your implementation of `bool Equals( object obj )` should just call into the type-specific `Equals` method that is the implementation of the <xref:System.IEquatable%601?displayProperty=nameWithType> interface.</span></span> <span data-ttu-id="1ed51-127">(Voir l’étape 2.)</span><span class="sxs-lookup"><span data-stu-id="1ed51-127">(See step 2.)</span></span>  
  
2. <span data-ttu-id="1ed51-128">Implémentez l’interface <xref:System.IEquatable%601?displayProperty=nameWithType> en fournissant une méthode `Equals` propre au type.</span><span class="sxs-lookup"><span data-stu-id="1ed51-128">Implement the <xref:System.IEquatable%601?displayProperty=nameWithType> interface by providing a type-specific `Equals` method.</span></span> <span data-ttu-id="1ed51-129">C’est ici qu’est effectuée la comparaison d’équivalence.</span><span class="sxs-lookup"><span data-stu-id="1ed51-129">This is where the actual equivalence comparison is performed.</span></span> <span data-ttu-id="1ed51-130">Par exemple, vous pouvez décider de définir l’égalité en comparant seulement un ou deux champs de votre type.</span><span class="sxs-lookup"><span data-stu-id="1ed51-130">For example, you might decide to define equality by comparing only one or two fields in your type.</span></span> <span data-ttu-id="1ed51-131">Ne levez pas d'exceptions à partir de la méthode `Equals`.</span><span class="sxs-lookup"><span data-stu-id="1ed51-131">Do not throw exceptions from `Equals`.</span></span> <span data-ttu-id="1ed51-132">Pour les classes uniquement : cette méthode doit examiner uniquement les champs qui sont déclarés dans la classe.</span><span class="sxs-lookup"><span data-stu-id="1ed51-132">For classes only: This method should examine only fields that are declared in the class.</span></span> <span data-ttu-id="1ed51-133">Elle doit appeler `base.Equals` pour examiner les champs qui sont dans la classe de base.</span><span class="sxs-lookup"><span data-stu-id="1ed51-133">It should call `base.Equals` to examine fields that are in the base class.</span></span> <span data-ttu-id="1ed51-134">(Ne faites pas cela si le type hérite directement d’<xref:System.Object>, car l’implémentation <xref:System.Object> de <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType> effectue une vérification de l’égalité de référence.)</span><span class="sxs-lookup"><span data-stu-id="1ed51-134">(Do not do this if the type inherits directly from <xref:System.Object>, because the <xref:System.Object> implementation of <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType> performs a reference equality check.)</span></span>  
  
3. <span data-ttu-id="1ed51-135">Facultatif mais recommandé : Surchargez les [==](../../language-reference/operators/equality-operators.md#equality-operator-) opérateurs et [! =](../../language-reference/operators/equality-operators.md#inequality-operator-) .</span><span class="sxs-lookup"><span data-stu-id="1ed51-135">Optional but recommended: Overload the [==](../../language-reference/operators/equality-operators.md#equality-operator-) and [!=](../../language-reference/operators/equality-operators.md#inequality-operator-) operators.</span></span>  
  
4. <span data-ttu-id="1ed51-136">Substituez <xref:System.Object.GetHashCode%2A?displayProperty=nameWithType> pour que deux objets ayant une égalité des valeurs produisent le même code de hachage.</span><span class="sxs-lookup"><span data-stu-id="1ed51-136">Override <xref:System.Object.GetHashCode%2A?displayProperty=nameWithType> so that two objects that have value equality produce the same hash code.</span></span>  
  
5. <span data-ttu-id="1ed51-137">Facultatif : pour prendre en charge les définitions pour « supérieur à » ou « inférieur à », implémentez l' <xref:System.IComparable%601> interface pour votre type, et surchargez également les [<=](../../language-reference/operators/comparison-operators.md#less-than-or-equal-operator-) [>=](../../language-reference/operators/comparison-operators.md#greater-than-or-equal-operator-) opérateurs et.</span><span class="sxs-lookup"><span data-stu-id="1ed51-137">Optional: To support definitions for "greater than" or "less than," implement the <xref:System.IComparable%601> interface for your type, and also overload the [<=](../../language-reference/operators/comparison-operators.md#less-than-or-equal-operator-) and [>=](../../language-reference/operators/comparison-operators.md#greater-than-or-equal-operator-) operators.</span></span>  

> [!NOTE]
> <span data-ttu-id="1ed51-138">À compter de C# 9,0, vous pouvez utiliser des enregistrements pour récupérer la sémantique d’égalité des valeurs sans code réutilisable inutile.</span><span class="sxs-lookup"><span data-stu-id="1ed51-138">Starting in C# 9.0, you can use records to get value equality semantics without any unnecessary boilerplate code.</span></span>

## <a name="class-example"></a><span data-ttu-id="1ed51-139">Exemple de classe</span><span class="sxs-lookup"><span data-stu-id="1ed51-139">Class example</span></span>

<span data-ttu-id="1ed51-140">L’exemple suivant montre comment implémenter l’égalité des valeurs dans une classe (type référence).</span><span class="sxs-lookup"><span data-stu-id="1ed51-140">The following example shows how to implement value equality in a class (reference type).</span></span>

[!code-csharp[csProgGuideStatements#19](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideStatements/CS/Statements.cs#19)]

<span data-ttu-id="1ed51-141">Sur les classes (types référence), l’implémentation par défaut des deux méthodes <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType> effectue une comparaison d’égalité de référence, et non une vérification de l’égalité des valeurs.</span><span class="sxs-lookup"><span data-stu-id="1ed51-141">On classes (reference types), the default implementation of both <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType> methods performs a reference equality comparison, not a value equality check.</span></span> <span data-ttu-id="1ed51-142">Quand un implémenteur substitue la méthode virtuelle, l’objectif est de lui donner une sémantique d’égalité des valeurs.</span><span class="sxs-lookup"><span data-stu-id="1ed51-142">When an implementer overrides the virtual method, the purpose is to give it value equality semantics.</span></span>

<span data-ttu-id="1ed51-143">Les opérateurs `==` et `!=` peuvent être utilisés avec des classes, même si la classe ne les surcharge pas.</span><span class="sxs-lookup"><span data-stu-id="1ed51-143">The `==` and `!=` operators can be used with classes even if the class does not overload them.</span></span> <span data-ttu-id="1ed51-144">Toutefois, le comportement par défaut consiste à effectuer une vérification de l’égalité de référence.</span><span class="sxs-lookup"><span data-stu-id="1ed51-144">However, the default behavior is to perform a reference equality check.</span></span> <span data-ttu-id="1ed51-145">Dans une classe, si vous surchargez la méthode `Equals`, vous devez surcharger les opérateurs `==` et `!=`, mais cela n’est pas obligatoire.</span><span class="sxs-lookup"><span data-stu-id="1ed51-145">In a class, if you overload the `Equals` method, you should overload the `==` and `!=` operators, but it is not required.</span></span>

## <a name="struct-example"></a><span data-ttu-id="1ed51-146">Exemple de struct</span><span class="sxs-lookup"><span data-stu-id="1ed51-146">Struct example</span></span>

<span data-ttu-id="1ed51-147">L’exemple suivant montre comment implémenter l’égalité des valeurs dans un struct (type valeur) :</span><span class="sxs-lookup"><span data-stu-id="1ed51-147">The following example shows how to implement value equality in a struct (value type):</span></span>

[!code-csharp[csProgGuideStatements#20](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideStatements/CS/Statements.cs#20)]
  
<span data-ttu-id="1ed51-148">Pour les structs, l’implémentation par défaut de <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType> (qui est la version substituée dans <xref:System.ValueType?displayProperty=nameWithType>) effectue une vérification de l’égalité des valeurs à l’aide de la réflexion pour comparer les valeurs de chaque champ dans le type.</span><span class="sxs-lookup"><span data-stu-id="1ed51-148">For structs, the default implementation of <xref:System.Object.Equals%28System.Object%29?displayProperty=nameWithType> (which is the overridden version in <xref:System.ValueType?displayProperty=nameWithType>) performs a value equality check by using reflection to compare the values of every field in the type.</span></span> <span data-ttu-id="1ed51-149">Quand un implémenteur substitue la méthode `Equals` virtuelle dans un struct, l’objectif est de fournir un moyen plus efficace d’effectuer la vérification de l’égalité des valeurs, et éventuellement de baser la comparaison sur un sous-ensemble des propriétés ou du champ du struct.</span><span class="sxs-lookup"><span data-stu-id="1ed51-149">When an implementer overrides the virtual `Equals` method in a struct, the purpose is to provide a more efficient means of performing the value equality check and optionally to base the comparison on some subset of the struct's field or properties.</span></span>
  
<span data-ttu-id="1ed51-150">Les [==](../../language-reference/operators/equality-operators.md#equality-operator-) opérateurs et [! =](../../language-reference/operators/equality-operators.md#inequality-operator-) ne peuvent pas fonctionner sur un struct, sauf si le struct les surcharge explicitement.</span><span class="sxs-lookup"><span data-stu-id="1ed51-150">The [==](../../language-reference/operators/equality-operators.md#equality-operator-) and [!=](../../language-reference/operators/equality-operators.md#inequality-operator-) operators cannot operate on a struct unless the struct explicitly overloads them.</span></span>

## <a name="see-also"></a><span data-ttu-id="1ed51-151">Voir aussi</span><span class="sxs-lookup"><span data-stu-id="1ed51-151">See also</span></span>

- [<span data-ttu-id="1ed51-152">Comparaisons d’égalité</span><span class="sxs-lookup"><span data-stu-id="1ed51-152">Equality comparisons</span></span>](equality-comparisons.md)
- [<span data-ttu-id="1ed51-153">Guide de programmation C#</span><span class="sxs-lookup"><span data-stu-id="1ed51-153">C# programming guide</span></span>](../index.md)
