---
title: Erreur du compilateur CS0459
ms.date: 07/20/2015
f1_keywords:
- CS0459
helpviewer_keywords:
- CS0459
ms.assetid: 01b058dd-8d65-4e9d-9de1-d47f9488d22a
ms.openlocfilehash: 4f6ae378f90f19060a5adcd4f2491fe81a299882
ms.sourcegitcommit: 559259da2738a7b33a46c0130e51d336091c2097
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 10/22/2019
ms.locfileid: "72773844"
---
# <a name="compiler-error-cs0459"></a><span data-ttu-id="df1ce-102">Erreur du compilateur CS0459</span><span class="sxs-lookup"><span data-stu-id="df1ce-102">Compiler Error CS0459</span></span>

<span data-ttu-id="df1ce-103">Impossible de prendre l'adresse d'une variable locale en lecture seule</span><span class="sxs-lookup"><span data-stu-id="df1ce-103">Cannot take the address of a read-only local variable</span></span>

 <span data-ttu-id="df1ce-104">En langage C#, trois scénarios communs génèrent des variables locales en lecture seule : `foreach`, `using`et `fixed`.</span><span class="sxs-lookup"><span data-stu-id="df1ce-104">There are three common scenarios in the C# language that generate read-only local variables: `foreach`, `using`, and `fixed`.</span></span> <span data-ttu-id="df1ce-105">Dans chacun de ces cas, vous n’êtes pas autorisé à écrire dans la variable locale en lecture seule ou à prendre son adresse.</span><span class="sxs-lookup"><span data-stu-id="df1ce-105">In each of these cases, you are not allowed to write to the read-only local variable, or to take its address.</span></span> <span data-ttu-id="df1ce-106">Cette erreur est générée lorsque le compilateur se rend compte que vous essayez de prendre l’adresse d’une variable locale en lecture seule.</span><span class="sxs-lookup"><span data-stu-id="df1ce-106">This error is generated when the compiler realizes you are trying to take the address of a read-only local variable.</span></span>

## <a name="example"></a><span data-ttu-id="df1ce-107">Exemple</span><span class="sxs-lookup"><span data-stu-id="df1ce-107">Example</span></span>

 <span data-ttu-id="df1ce-108">L’exemple suivant génère CS0459 lorsqu’une tentative est effectuée pour prendre l’adresse d’une variable locale en lecture seule dans une boucle `foreach` et dans un bloc d’instructions `fixed` :</span><span class="sxs-lookup"><span data-stu-id="df1ce-108">The following example generates CS0459 when an attempt is made to take the address of a read-only local variable in a `foreach` loop and in a `fixed` statement block:</span></span>

```csharp
// CS0459.cs
// compile with: /unsafe

class Program
{
    public unsafe void M1()
    {
        int[] ints = new int[] { 1, 2, 3 };
        foreach (int i in ints)
        {
            int *j = &i;  // CS0459
        }

        fixed (int *i = &_i)
        {
            int **j = &i;  // CS0459
        }
    }

    private int _i = 0;
}
```

> [!NOTE]
> <span data-ttu-id="df1ce-109">Le compilateur Roslyn a été mis à jour et cette erreur du compilateur a été supprimée à partir de Visual Studio 2017 version 15,5, de sorte que le code précédent se compilera correctement avec cette version et les versions ultérieures.</span><span class="sxs-lookup"><span data-stu-id="df1ce-109">Roslyn compiler was updated and this compiler error was removed starting with Visual Studio 2017 version 15.5, so the previous code would compile successfully with this version and later.</span></span>
