---
title: XAML Namespaces pour .NET XAML Services
ms.date: 03/30/2017
ms.assetid: e4f15f13-c420-4c1e-aeab-9b6f50212047
ms.openlocfilehash: 2ae57d08fade85c59fea2a54b653a2f775b57415
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 03/12/2020
ms.locfileid: "82071842"
---
# <a name="xaml-namespaces-for-net-xaml-services"></a><span data-ttu-id="7ff54-102">XAML Namespaces pour .NET XAML Services</span><span class="sxs-lookup"><span data-stu-id="7ff54-102">XAML Namespaces for .NET XAML Services</span></span>
<span data-ttu-id="7ff54-103">Un espace de nom XAML est un concept qui s’étend sur la définition d’un espace de nom XML.</span><span class="sxs-lookup"><span data-stu-id="7ff54-103">A XAML namespace is a concept that expands on the definition of an XML namespace.</span></span> <span data-ttu-id="7ff54-104">Semblable à un espace de nom XML, vous pouvez `xmlns` définir un espace de nom XAML à l’aide d’un attribut dans la balisage.</span><span class="sxs-lookup"><span data-stu-id="7ff54-104">Similar to an XML namespace, you can define a XAML namespace using an `xmlns` attribute in markup.</span></span> <span data-ttu-id="7ff54-105">Les espaces de noms XAML sont également représentés dans le flux de nœuds XAML et d’autres API de XAML Services.</span><span class="sxs-lookup"><span data-stu-id="7ff54-105">XAML namespaces are also represented in the XAML node stream and other XAML Services APIs.</span></span> <span data-ttu-id="7ff54-106">Ce sujet définit le concept XAML namespace, et décrit comment les espaces de nom XAML peuvent être définis et sont utilisés par les contextes schémas XAML et d’autres aspects de .NET XAML Services.</span><span class="sxs-lookup"><span data-stu-id="7ff54-106">This topic defines the XAML namespace concept, and describes how XAML namespaces can be defined and are used by XAML schema contexts and other aspects of .NET XAML Services.</span></span>  
  
## <a name="xml-namespace-and-xaml-namespace"></a><span data-ttu-id="7ff54-107">XML Namespace et XAML Namespace</span><span class="sxs-lookup"><span data-stu-id="7ff54-107">XML Namespace and XAML Namespace</span></span>  
 <span data-ttu-id="7ff54-108">Un espace de nom XAML est un espace de nom XML spécialisé, tout comme XAML est une forme spécialisée de XML et utilise le formulaire XML de base pour sa balisage.</span><span class="sxs-lookup"><span data-stu-id="7ff54-108">A XAML namespace is a specialized XML namespace, just as XAML is a specialized form of XML and uses the basic XML form for its markup.</span></span> <span data-ttu-id="7ff54-109">En balisage, vous déclarez un espace de `xmlns` nom XAML et sa cartographie à travers un attribut appliqué à un élément.</span><span class="sxs-lookup"><span data-stu-id="7ff54-109">In markup, you declare a XAML namespace and its mapping through an `xmlns` attribute applied to an element.</span></span> <span data-ttu-id="7ff54-110">La `xmlns` déclaration peut être faite au même élément que l’espace de nom XAML est déclaré dans.</span><span class="sxs-lookup"><span data-stu-id="7ff54-110">The `xmlns` declaration can be made to the same element that the XAML namespace is declared in.</span></span> <span data-ttu-id="7ff54-111">Une déclaration XAML namespace faite à un élément est valable pour cet élément, tous les attributs de cet élément, et tous les enfants de cet élément.</span><span class="sxs-lookup"><span data-stu-id="7ff54-111">A XAML namespace declaration made to an element is valid for that element, all attributes of that element, and all children of that element.</span></span> <span data-ttu-id="7ff54-112">Les attributs peuvent utiliser un espace de nom XAML qui n’est pas le même que l’élément qui contient l’attribut, tant que le nom d’attribut lui-même fait référence au préfixe dans le cadre de son nom d’attribut dans la balisage.</span><span class="sxs-lookup"><span data-stu-id="7ff54-112">Attributes can use a XAML namespace that is not the same as the element that contains the attribute, so long as the attribute name itself references the prefix as part of its attribute name in markup.</span></span>  
  
 <span data-ttu-id="7ff54-113">La distinction d’un espace de nom XAML par rapport à un espace de nom XML est qu’un espace de nom XML peut être utilisé pour référencer un schéma ou simplement pour différencier les entités.</span><span class="sxs-lookup"><span data-stu-id="7ff54-113">The distinction of a XAML namespace versus an XML namespace is that an XML namespace might be used to reference a schema or simply to differentiate entities.</span></span> <span data-ttu-id="7ff54-114">Pour XAML, les types et les membres utilisés dans XAML doivent finalement être résolus aux types de support, et les concepts de schémas XML ne s’appliquent pas bien à cette capacité.</span><span class="sxs-lookup"><span data-stu-id="7ff54-114">For XAML, the types and members as used in XAML must ultimately be resolved to backing types, and XML schema concepts do not apply well to this capability.</span></span> <span data-ttu-id="7ff54-115">L’espace de nom XAML contient des informations que le contexte du schéma XAML doit avoir disponible afin d’effectuer cette cartographie de type.</span><span class="sxs-lookup"><span data-stu-id="7ff54-115">The XAML namespace contains information that the XAML schema context must have available in order to perform this type mapping.</span></span>  
  
## <a name="xaml-namespace-components"></a><span data-ttu-id="7ff54-116">Composants XAML Namespace</span><span class="sxs-lookup"><span data-stu-id="7ff54-116">XAML Namespace Components</span></span>  
 <span data-ttu-id="7ff54-117">La définition XAML namespace comporte deux composants : un préfixe et un identifiant.</span><span class="sxs-lookup"><span data-stu-id="7ff54-117">The XAML namespace definition has two components: a prefix, and an identifier.</span></span> <span data-ttu-id="7ff54-118">Chacun de ces composants est présent lorsqu’un espace de nom XAML est déclaré en marge, ou défini dans le système de type XAML.</span><span class="sxs-lookup"><span data-stu-id="7ff54-118">Each of these components is present when a XAML namespace is declared in markup, or defined in the XAML type system.</span></span>  
  
 <span data-ttu-id="7ff54-119">Le préfixe peut être n’importe quelle chaîne comme le permet les [namespaces W3C dans XML 1.0 spécification](https://www.w3.org/TR/REC-xml-names/).</span><span class="sxs-lookup"><span data-stu-id="7ff54-119">The prefix can be any string as allowed by the [W3C Namespaces in XML 1.0 specification](https://www.w3.org/TR/REC-xml-names/).</span></span> <span data-ttu-id="7ff54-120">Par convention, les préfixes sont généralement des cordes courtes, parce que le préfixe est répété plusieurs fois dans un fichier de balisage typique.</span><span class="sxs-lookup"><span data-stu-id="7ff54-120">By convention, the prefixes are typically short strings, because the prefix is repeated many times in a typical markup file.</span></span> <span data-ttu-id="7ff54-121">Certains espaces de noms XAML qui sont destinés à être utilisés dans plusieurs implémentations XAML utilisent des préfixes conventionnels particuliers.</span><span class="sxs-lookup"><span data-stu-id="7ff54-121">Certain XAML namespaces that are intended to be used in multiple XAML implementations use particular conventional prefixes.</span></span> <span data-ttu-id="7ff54-122">Par exemple, l’espace de nom XAML en langue `x`XAML est généralement cartographié à l’aide du préfixe .</span><span class="sxs-lookup"><span data-stu-id="7ff54-122">For example, the XAML language XAML namespace is typically mapped using the prefix `x`.</span></span> <span data-ttu-id="7ff54-123">Vous pouvez définir un espace de nom XAML par défaut, où le préfixe n’est pas donné dans la définition, mais est représenté comme une chaîne vide si défini ou interrogé by.NET’API XAML Services.</span><span class="sxs-lookup"><span data-stu-id="7ff54-123">You can define a default XAML namespace, where the prefix is not given in the definition but is represented as an empty string if defined or queried by.NET XAML Services API.</span></span> <span data-ttu-id="7ff54-124">Typiquement, l’espace de nom XAML par défaut est délibérément choisi afin de promouvoir une quantité maximisée de préfixe-omettant balisage par une technologie de mise en œuvre XAML et ses scénarios et vocabulaires.</span><span class="sxs-lookup"><span data-stu-id="7ff54-124">Typically, the default XAML namespace is deliberately chosen in order to promote a maximized amount of prefix-omitting markup by a XAML-implementing technology and its scenarios and vocabularies.</span></span>  
  
 <span data-ttu-id="7ff54-125">L’identifiant peut être n’importe quelle chaîne comme le permet les [namespaces W3C dans la spécification XML 1.0](https://www.w3.org/TR/REC-xml-names/).</span><span class="sxs-lookup"><span data-stu-id="7ff54-125">The identifier can be any string as allowed by the [W3C Namespaces in XML 1.0 specification](https://www.w3.org/TR/REC-xml-names/).</span></span> <span data-ttu-id="7ff54-126">Par convention, les identifiants pour les espaces de noms XML ou les espaces de noms XAML sont souvent donnés sous forme d’URI, généralement comme un URI absolu qualifié par protocole.</span><span class="sxs-lookup"><span data-stu-id="7ff54-126">By convention, identifiers for either XML namespaces or XAML namespaces are often given in URI form, typically as a protocol-qualified absolute URI.</span></span> <span data-ttu-id="7ff54-127">Souvent, l’information de version qui définit un vocabulaire XAML particulier est implicite dans le cadre de la chaîne de chemin.</span><span class="sxs-lookup"><span data-stu-id="7ff54-127">Often, version information that defines a particular XAML vocabulary is implied as part of the path string.</span></span> <span data-ttu-id="7ff54-128">Les espaces de noms XAML ajoutent une convention d’identification supplémentaire au-delà de la convention XML URI.</span><span class="sxs-lookup"><span data-stu-id="7ff54-128">XAML namespaces add an additional identifier convention beyond the XML URI convention.</span></span> <span data-ttu-id="7ff54-129">Pour les espaces de noms XAML, l’identifiant communique les informations qui sont nécessaires par un contexte schéma XAML afin de résoudre les types qui sont spécifiés comme éléments en vertu de cet espace de nom XAML, ou de résoudre les attributs aux membres.</span><span class="sxs-lookup"><span data-stu-id="7ff54-129">For XAML namespaces, the identifier communicates information that is needed by a XAML schema context in order to resolve the types that are specified as elements under that XAML namespace, or to resolve attributes to members.</span></span>  
  
 <span data-ttu-id="7ff54-130">Aux fins de la communication d’informations à un contexte de schéma XAML, l’identifiant d’un espace de nom XAML peut encore être sous forme d’URI.</span><span class="sxs-lookup"><span data-stu-id="7ff54-130">For purposes of communicating information to a XAML schema context, the identifier for a XAML namespace might still be in URI form.</span></span> <span data-ttu-id="7ff54-131">Toutefois, dans ce cas, l’URI est également déclarée identificateur correspondant dans une assemblée ou une liste particulière d’assemblées.</span><span class="sxs-lookup"><span data-stu-id="7ff54-131">However, in this case the URI is also declared as a matching identifier in a particular assembly or list of assemblies.</span></span> <span data-ttu-id="7ff54-132">Cela se fait dans les assemblées <xref:System.Windows.Markup.XmlnsDefinitionAttribute>en attribuant l’assemblée avec .</span><span class="sxs-lookup"><span data-stu-id="7ff54-132">This is done in assemblies by attributing the assembly with <xref:System.Windows.Markup.XmlnsDefinitionAttribute>.</span></span> <span data-ttu-id="7ff54-133">Cette méthode d’identification de l’espace de nom XAML et de soutien d’un comportement de résolution de type CLR dans l’assemblage attribué est soutenue par le contexte par défaut de schéma XAML dans .NET XAML Services.</span><span class="sxs-lookup"><span data-stu-id="7ff54-133">This method of identifying the XAML namespace and supporting a CLR-based type resolution behavior in the attributed assembly is supported by the default XAML schema context in .NET XAML Services.</span></span> <span data-ttu-id="7ff54-134">Plus généralement, cette convention peut être utilisée pour les cas où le contexte du schéma XAML intègre le CLR ou est basé sur le contexte de schéma XAML par défaut, qui est nécessaire pour lire les attributs CLR des assemblages CLR.</span><span class="sxs-lookup"><span data-stu-id="7ff54-134">More generally, this convention can be used for cases where the XAML schema context incorporates the CLR or is based on the default XAML schema context, which is necessary in order to read CLR attributes from CLR assemblies.</span></span>  
  
 <span data-ttu-id="7ff54-135">Les espaces de noms XAML peuvent également être identifiés par une convention qui communique un espace de nom CLR et un assemblage de type définissant.</span><span class="sxs-lookup"><span data-stu-id="7ff54-135">XAML namespaces also can be identified by a convention that communicates a CLR namespace and a type-defining assembly.</span></span> <span data-ttu-id="7ff54-136">Cette convention est utilisée <xref:System.Windows.Markup.XmlnsDefinitionAttribute> dans les cas où il n’existe pas d’attribution dans les assemblées qui contiennent des types.</span><span class="sxs-lookup"><span data-stu-id="7ff54-136">This convention is used in cases where no <xref:System.Windows.Markup.XmlnsDefinitionAttribute> attribution exists in the assemblies that contain types.</span></span> <span data-ttu-id="7ff54-137">Cette convention est potentiellement plus complexe que la convention de l’URI, et a également le potentiel d’ambiguïté et de duplication, parce qu’il existe de multiples façons de se référer à une assemblée.</span><span class="sxs-lookup"><span data-stu-id="7ff54-137">This convention is potentially more complex than the URI convention, and also has the potential for ambiguity and duplication, because there are multiple ways of referring to an assembly.</span></span>  
  
 <span data-ttu-id="7ff54-138">La forme la plus élémentaire d’un identifiant qui utilise l’espace de nom CLR et la convention d’assemblage est la suivante:</span><span class="sxs-lookup"><span data-stu-id="7ff54-138">The most basic form of an identifier that uses the CLR namespace and assembly convention is as follows:</span></span>  
  
 `clr-namespace:clrnsName; assembly=assemblyShortName`
  
 <span data-ttu-id="7ff54-139">`clr-namespace:`et `; assembly=` sont des composants littérals de la syntaxe.</span><span class="sxs-lookup"><span data-stu-id="7ff54-139">`clr-namespace:` and `; assembly=` are literal components of the syntax.</span></span>  
  
 <span data-ttu-id="7ff54-140">*clrnsName* est le nom de chaîne qui identifie un namespace CLR.</span><span class="sxs-lookup"><span data-stu-id="7ff54-140">*clrnsName* is the string name that identifies a CLR namespace.</span></span> <span data-ttu-id="7ff54-141">Ce nom de chaîne inclut tous les caractères de point interne (.) qui fournissent des conseils au sujet de l’espace de nom CLR et sa relation aux autres espaces de nom CLR.</span><span class="sxs-lookup"><span data-stu-id="7ff54-141">This string name includes any internal dot characters (.) that provide hints about the CLR namespace and its relation to other CLR namespaces.</span></span>
  
 <span data-ttu-id="7ff54-142">*assemblageShortName* est le nom de chaîne d’un assemblage qui définit les types qui sont utiles dans XAML.</span><span class="sxs-lookup"><span data-stu-id="7ff54-142">*assemblyShortName* is the string name of an assembly that defines types that are useful in XAML.</span></span> <span data-ttu-id="7ff54-143">Les types à accéder par l’espace de nom XAML déclarés devraient être définis par l’assemblage et être déclarés dans l’espace nom CLR spécifié par *clrnsName*.</span><span class="sxs-lookup"><span data-stu-id="7ff54-143">The types to be accessed through the declared XAML namespace are expected to be defined by the assembly and to be declared within the CLR namespace specified by *clrnsName*.</span></span> <span data-ttu-id="7ff54-144">Ce nom de chaîne est généralement <xref:System.Reflection.AssemblyName.Name%2A?displayProperty=nameWithType>parallèle à l’information telle que rapportée par .</span><span class="sxs-lookup"><span data-stu-id="7ff54-144">This string name typically parallels the information as reported by <xref:System.Reflection.AssemblyName.Name%2A?displayProperty=nameWithType>.</span></span>  
  
 <span data-ttu-id="7ff54-145">Une définition plus complète de l’espace de nom CLR et de la convention d’assemblage est la suivante :</span><span class="sxs-lookup"><span data-stu-id="7ff54-145">A more complete definition of the CLR namespace and assembly convention is as follows:</span></span>  
  
 `clr-namespace:clrnsName; assembly=assemblyName`
  
 <span data-ttu-id="7ff54-146">*assemblyName* représente toute chaîne qui <xref:System.Reflection.Assembly.Load%28System.String%29?displayProperty=nameWithType> est légale comme une entrée.</span><span class="sxs-lookup"><span data-stu-id="7ff54-146">*assemblyName* represents any string that is legal as an <xref:System.Reflection.Assembly.Load%28System.String%29?displayProperty=nameWithType> input.</span></span> <span data-ttu-id="7ff54-147">Cette chaîne peut inclure la culture, la clé publique, ou l’information <xref:System.Reflection.Assembly>de version (définitions de ces concepts sont définies dans le sujet de référence pour ).</span><span class="sxs-lookup"><span data-stu-id="7ff54-147">This string can include culture, public key, or version information (definitions of these concepts are defined in the reference topic for <xref:System.Reflection.Assembly>).</span></span> <span data-ttu-id="7ff54-148">Le format et les éléments de preuve <xref:System.Reflection.Assembly.Load%2A>du COFF (utilisés par d’autres surcharges) ne sont pas pertinents aux fins de chargement d’assemblage XAML; toutes les informations de charge doivent être présentées comme une chaîne.</span><span class="sxs-lookup"><span data-stu-id="7ff54-148">COFF format and evidence (as used by other overloads of <xref:System.Reflection.Assembly.Load%2A>) are not relevant for XAML assembly loading purposes; all load information must be presented as a string.</span></span>  
  
 <span data-ttu-id="7ff54-149">Spécifier une clé publique pour l’assemblage est une technique utile pour la sécurité XAML, ou pour éliminer l’ambiguïté possible qui peut exister si les assemblages sont chargés par un nom simple, ou pré-exister dans un cache ou un domaine d’application.</span><span class="sxs-lookup"><span data-stu-id="7ff54-149">Specifying a public key for the assembly is a useful technique for XAML security, or for removing possible ambiguity that can exist if assemblies are loaded by simple name, or pre-exist in a cache or application domain.</span></span> <span data-ttu-id="7ff54-150">Pour plus d’informations, voir [XAML Security Considerations](security-considerations.md).</span><span class="sxs-lookup"><span data-stu-id="7ff54-150">For more information, see [XAML Security Considerations](security-considerations.md).</span></span>  
  
## <a name="xaml-namespace-declarations-in-the-xaml-services-api"></a><span data-ttu-id="7ff54-151">Déclarations XAML Namespace dans l’API XAML Services</span><span class="sxs-lookup"><span data-stu-id="7ff54-151">XAML Namespace Declarations in the XAML Services API</span></span>  
 <span data-ttu-id="7ff54-152">Dans l’API XAML Services, une déclaration XAML <xref:System.Xaml.NamespaceDeclaration> namespace est représentée par un objet.</span><span class="sxs-lookup"><span data-stu-id="7ff54-152">In the XAML Services API, a XAML namespace declaration is represented by a <xref:System.Xaml.NamespaceDeclaration> object.</span></span> <span data-ttu-id="7ff54-153">Si vous déclarez un espace de nom XAML dans le code, vous appelez le <xref:System.Xaml.NamespaceDeclaration.%23ctor%28System.String%2CSystem.String%29> constructeur.</span><span class="sxs-lookup"><span data-stu-id="7ff54-153">If you are declaring a XAML namespace in code, you call the <xref:System.Xaml.NamespaceDeclaration.%23ctor%28System.String%2CSystem.String%29> constructor.</span></span> <span data-ttu-id="7ff54-154">Les `ns` `prefix` paramètres et les paramètres sont spécifiés comme des chaînes, et l’entrée à fournir pour ces paramètres correspond à la définition de l’identifiant XAML namespace et XAML namespace préfixe comme prévu précédemment dans ce sujet.</span><span class="sxs-lookup"><span data-stu-id="7ff54-154">The `ns` and `prefix` parameters are specified as strings, and the input to provide for these parameters corresponds to the definition of XAML namespace identifier and XAML namespace prefix as provided previously in this topic.</span></span>  
  
 <span data-ttu-id="7ff54-155">Si vous examinez les informations XAML namespace dans le cadre d’un flux de <xref:System.Xaml.NamespaceDeclaration.Namespace%2A?displayProperty=nameWithType> nœuds XAML ou <xref:System.Xaml.NamespaceDeclaration.Prefix%2A?displayProperty=nameWithType> par le biais d’un autre accès au système de type XAML, signale l’identifiant XAML namespace, et signale le préfixe XAML namespace.</span><span class="sxs-lookup"><span data-stu-id="7ff54-155">If you are examining XAML namespace information as part of a XAML node stream or through other access to the XAML type system, <xref:System.Xaml.NamespaceDeclaration.Namespace%2A?displayProperty=nameWithType> reports the XAML namespace identifier, and <xref:System.Xaml.NamespaceDeclaration.Prefix%2A?displayProperty=nameWithType> reports the XAML namespace prefix.</span></span>  
  
 <span data-ttu-id="7ff54-156">Dans un flux de nœuds XAML, les informations XAML namespace peuvent apparaître comme un nœud XAML qui précède l’entité à laquelle il s’applique.</span><span class="sxs-lookup"><span data-stu-id="7ff54-156">In a XAML node stream, the XAML namespace information can appear as a XAML node that precedes the entity to which it applies.</span></span> <span data-ttu-id="7ff54-157">Cela comprend les cas où l’information `StartObject` XAML namespace précède l’élément racine XAML.</span><span class="sxs-lookup"><span data-stu-id="7ff54-157">This includes cases where the XAML namespace information precedes the `StartObject` of the XAML root element.</span></span> <span data-ttu-id="7ff54-158">Pour plus d'informations, consultez [Understanding XAML Node Stream Structures and Concepts](understanding-xaml-node-stream-structures-and-concepts.md).</span><span class="sxs-lookup"><span data-stu-id="7ff54-158">For more information, see [Understanding XAML Node Stream Structures and Concepts](understanding-xaml-node-stream-structures-and-concepts.md).</span></span>  
  
 <span data-ttu-id="7ff54-159">Pour de nombreux scénarios qui utilisent .NET XAML Services API, au moins une déclaration XAML namespace devrait exister, et la déclaration doit soit contenir ou se référer à des informations qui sont nécessaires par un contexte schéma XAML.</span><span class="sxs-lookup"><span data-stu-id="7ff54-159">For many scenarios that use .NET XAML Services API, at least one XAML namespace declaration is expected to exist, and the declaration must either contain or refer to information that is required by a XAML schema context.</span></span> <span data-ttu-id="7ff54-160">Les espaces de noms XAML doivent soit spécifier les assemblages à charger, soit aider à résoudre des types spécifiques dans des espaces nominaux et des assemblages qui sont déjà chargés ou connus par le contexte du schéma XAML.</span><span class="sxs-lookup"><span data-stu-id="7ff54-160">The XAML namespaces must either specify assemblies to be loaded, or assist in resolving specific types within namespaces and assemblies that are already loaded or known by the XAML schema context.</span></span>  
  
 <span data-ttu-id="7ff54-161">Afin de générer un flux de nœuds XAML, des informations de type XAML doivent être disponibles, dans le contexte du schéma XAML.</span><span class="sxs-lookup"><span data-stu-id="7ff54-161">In order to generate a XAML node stream, XAML type information must be available, through the XAML schema context.</span></span> <span data-ttu-id="7ff54-162">Les informations de type XAML ne peuvent pas être déterminées sans d’abord déterminer l’espace de nom XAML pertinent pour chaque nœud à créer.</span><span class="sxs-lookup"><span data-stu-id="7ff54-162">The XAML type information cannot be determined without first determining the relevant XAML namespace for each node to create.</span></span> <span data-ttu-id="7ff54-163">À ce stade, aucun cas de types n’est encore créé, mais le contexte du schéma XAML peut avoir besoin de rechercher des informations de l’assemblage définissant et le type de soutien.</span><span class="sxs-lookup"><span data-stu-id="7ff54-163">At this point, no instances of types are created yet, but the XAML schema context may need to look up information from the defining assembly and backing type.</span></span> <span data-ttu-id="7ff54-164">Par exemple, afin de traiter `<Party><PartyFavor/></Party>`le balisage , le contexte schéma XAML `ContentProperty` doit `Party`être en mesure de déterminer le nom `Party` `PartyFavor`et le type de , et doit donc également connaître les informations XAML namespace pour et .</span><span class="sxs-lookup"><span data-stu-id="7ff54-164">For example, in order to process the markup `<Party><PartyFavor/></Party>`, the XAML schema context must be able to determine the name and type of the `ContentProperty` of `Party`, and thus also must know the XAML namespace information for `Party` and `PartyFavor`.</span></span> <span data-ttu-id="7ff54-165">Dans le cas du contexte par défaut du schéma XAML, la réflexion statique rapporte une grande partie des informations du système de type XAML qui sont nécessaires pour générer des nœuds de type XAML dans le flux de nœuds.</span><span class="sxs-lookup"><span data-stu-id="7ff54-165">In the case of the default XAML schema context, static reflection reports much of the XAML type system information that is needed to generate XAML type nodes in the node stream.</span></span>  
  
 <span data-ttu-id="7ff54-166">Afin de générer un graphique d’objets à partir d’un flux de nœuds XAML, les déclarations de namespace XAML doivent exister pour chaque préfixe XAML utilisé dans le balisage original et enregistré dans le flux de nœuds XAML.</span><span class="sxs-lookup"><span data-stu-id="7ff54-166">In order to generate an object graph from a XAML node stream, XAML namespace declarations must exist for each XAML prefix used in the original markup and recorded in the XAML node stream.</span></span> <span data-ttu-id="7ff54-167">À ce stade, des instances sont créées, et un véritable comportement de cartographie de type se produit.</span><span class="sxs-lookup"><span data-stu-id="7ff54-167">At this point, instances are being created, and true type-mapping behavior occurs.</span></span>  
  
 <span data-ttu-id="7ff54-168">Si vous avez besoin de prépopuler les informations XAML namespace, dans les cas où l’espace de nom XAML vous avez l’intention du <xref:System.Xml.XmlParserContext> contexte <xref:System.Xml.XmlReader>schéma XAML à utiliser n’est pas défini dans le balisage, une technique que vous pouvez utiliser est de déclarer les déclarations XML namespace dans le pour un .</span><span class="sxs-lookup"><span data-stu-id="7ff54-168">If you need to prepopulate XAML namespace information, in cases where the XAML namespace you intend the XAML schema context to use is not defined in the markup, one technique you can use is to declare XML namespace declarations in the <xref:System.Xml.XmlParserContext> for an <xref:System.Xml.XmlReader>.</span></span> <span data-ttu-id="7ff54-169">Ensuite, <xref:System.Xml.XmlReader> utilisez-le comme entrée pour un <xref:System.Xaml.XamlServices.Load%28System.Xml.XmlReader%29?displayProperty=nameWithType>constructeur de lecteurs XAML, ou .</span><span class="sxs-lookup"><span data-stu-id="7ff54-169">Then use that <xref:System.Xml.XmlReader> as input for a XAML reader constructor, or <xref:System.Xaml.XamlServices.Load%28System.Xml.XmlReader%29?displayProperty=nameWithType>.</span></span>  
  
 <span data-ttu-id="7ff54-170">Deux autres API qui sont pertinents pour la manipulation de l’espace <xref:System.Windows.Markup.XmlnsDefinitionAttribute> de <xref:System.Windows.Markup.XmlnsPrefixAttribute>nom XAML dans .NET XAML Services sont les attributs et .</span><span class="sxs-lookup"><span data-stu-id="7ff54-170">Two other APIs that are relevant for XAML namespace handling in .NET XAML Services are the attributes <xref:System.Windows.Markup.XmlnsDefinitionAttribute> and <xref:System.Windows.Markup.XmlnsPrefixAttribute>.</span></span> <span data-ttu-id="7ff54-171">Ces attributs s’appliquent aux assemblages.</span><span class="sxs-lookup"><span data-stu-id="7ff54-171">These attributes apply to assemblies.</span></span> <span data-ttu-id="7ff54-172"><xref:System.Windows.Markup.XmlnsDefinitionAttribute>est utilisé par un contexte de schéma XAML pour interpréter toute déclaration de namespace XAML qui comprend un URI.</span><span class="sxs-lookup"><span data-stu-id="7ff54-172"><xref:System.Windows.Markup.XmlnsDefinitionAttribute> is used by a XAML schema context to interpret any XAML namespace declaration that includes a URI.</span></span> <span data-ttu-id="7ff54-173"><xref:System.Windows.Markup.XmlnsPrefixAttribute>est utilisé par des outils qui émettent XAML de sorte qu’un espace de nom XAML particulier peut être sérialisé avec un préfixe prévisible.</span><span class="sxs-lookup"><span data-stu-id="7ff54-173"><xref:System.Windows.Markup.XmlnsPrefixAttribute> is used by tools that emit XAML so that a particular XAML namespace can be serialized with a predictable prefix.</span></span> <span data-ttu-id="7ff54-174">Pour plus d’informations, consultez [les attributs CLR liés à XAML pour les types et bibliothèques personnalisés](clr-attributes-with-custom-types-and-libraries.md).</span><span class="sxs-lookup"><span data-stu-id="7ff54-174">For more information, see [XAML-Related CLR Attributes for Custom Types and Libraries](clr-attributes-with-custom-types-and-libraries.md).</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="7ff54-175">Voir aussi</span><span class="sxs-lookup"><span data-stu-id="7ff54-175">See also</span></span>

- [<span data-ttu-id="7ff54-176">Fonctionnement des concepts et structures du flux de nœud XAML</span><span class="sxs-lookup"><span data-stu-id="7ff54-176">Understanding XAML Node Stream Structures and Concepts</span></span>](understanding-xaml-node-stream-structures-and-concepts.md)
