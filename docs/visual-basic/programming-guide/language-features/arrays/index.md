---
title: Tableaux
ms.date: 12/06/2017
f1_keywords:
- vb.Array
helpviewer_keywords:
- arrays [Visual Basic]
- Visual Basic, arrays
ms.assetid: dbf29737-b589-4443-bee6-a27588d9c67e
ms.openlocfilehash: 5093f28f05c5b72294dce9a4e69723acafb31a9f
ms.sourcegitcommit: f8c270376ed905f6a8896ce0fe25b4f4b38ff498
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 06/04/2020
ms.locfileid: "84413090"
---
# <a name="arrays-in-visual-basic"></a><span data-ttu-id="74f2c-102">Tableaux dans Visual Basic</span><span class="sxs-lookup"><span data-stu-id="74f2c-102">Arrays in Visual Basic</span></span>

<span data-ttu-id="74f2c-103">Un tableau est un ensemble de valeurs, qui sont des *éléments*de termes, qui sont logiquement liés les uns aux autres.</span><span class="sxs-lookup"><span data-stu-id="74f2c-103">An array is a set of values, which are termed *elements*, that are logically related to each other.</span></span> <span data-ttu-id="74f2c-104">Par exemple, un tableau peut comprendre le nombre d’étudiants de chaque grade dans une école grammaticale. chaque élément du tableau est le nombre d’élèves dans une seule classe.</span><span class="sxs-lookup"><span data-stu-id="74f2c-104">For example, an array may consist of the number of students in each grade in a grammar school; each element of the array is the number of students in a single grade.</span></span> <span data-ttu-id="74f2c-105">De même, un tableau peut se composer des notes d’un étudiant pour une classe ; chaque élément du tableau est un seul niveau.</span><span class="sxs-lookup"><span data-stu-id="74f2c-105">Similarly, an array may consist of a student's grades for a class; each element of the array is a single grade.</span></span>

<span data-ttu-id="74f2c-106">Il peut y avoir des variables individuelles pour stocker chacun de nos éléments de données.</span><span class="sxs-lookup"><span data-stu-id="74f2c-106">It is possible individual variables to store each of our data items.</span></span> <span data-ttu-id="74f2c-107">Par exemple, si notre application analyse les notes des étudiants, nous pouvons utiliser une variable distincte pour chaque grade de l’étudiant, tel que `englishGrade1` , `englishGrade2` , etc. Cette approche présente trois principales limitations :</span><span class="sxs-lookup"><span data-stu-id="74f2c-107">For example, if our application analyzes student grades, we can use a separate variable for each student's grade, such as `englishGrade1`, `englishGrade2`, etc. This approach has three major limitations:</span></span>

- <span data-ttu-id="74f2c-108">Nous devons savoir au moment de la conception exactement le nombre de notes à gérer.</span><span class="sxs-lookup"><span data-stu-id="74f2c-108">We have to know at design time exactly how many grades we have to handle.</span></span>
- <span data-ttu-id="74f2c-109">La gestion d’un grand nombre de grades devient rapidement difficile.</span><span class="sxs-lookup"><span data-stu-id="74f2c-109">Handling large numbers of grades quickly becomes unwieldy.</span></span> <span data-ttu-id="74f2c-110">Cela rend à son tour une application plus susceptible d’avoir des bogues sérieux.</span><span class="sxs-lookup"><span data-stu-id="74f2c-110">This in turn makes an application much more likely to have serious bugs.</span></span>
- <span data-ttu-id="74f2c-111">Il est difficile à gérer.</span><span class="sxs-lookup"><span data-stu-id="74f2c-111">It is difficult to maintain.</span></span> <span data-ttu-id="74f2c-112">Chaque nouvelle classe ajoutée requiert que l’application soit modifiée, recompilée et redéployée.</span><span class="sxs-lookup"><span data-stu-id="74f2c-112">Each new grade that we add requires that the application be modified, recompiled, and redeployed.</span></span>

<span data-ttu-id="74f2c-113">En utilisant un tableau, vous pouvez faire référence à ces valeurs associées par le même nom et utiliser un nombre appelé *index* ou *indice* pour identifier un élément individuel en fonction de sa position dans le tableau.</span><span class="sxs-lookup"><span data-stu-id="74f2c-113">By using an array, you can refer to these related values by the same name, and use a number that’s called an *index* or *subscript* to identify an individual element based on its position in the array.</span></span> <span data-ttu-id="74f2c-114">Les index d’un tableau sont compris entre 0 et un nombre inférieur au nombre total d’éléments dans le tableau.</span><span class="sxs-lookup"><span data-stu-id="74f2c-114">The indexes of an array range from 0 to one less than the total number of elements in the array.</span></span> <span data-ttu-id="74f2c-115">Quand vous utilisez Visual Basic syntaxe pour définir la taille d’un tableau, vous spécifiez son index le plus élevé, et non le nombre total d’éléments dans le tableau.</span><span class="sxs-lookup"><span data-stu-id="74f2c-115">When you use Visual Basic syntax to define the size of an array, you specify its highest index, not the total number of elements in the array.</span></span> <span data-ttu-id="74f2c-116">Vous pouvez utiliser le tableau en tant qu’unité, et la possibilité d’itérer ses éléments vous évite de devoir connaître exactement le nombre d’éléments qu’il contient au moment de la conception.</span><span class="sxs-lookup"><span data-stu-id="74f2c-116">You can work with the array as a unit, and the ability to iterate its elements frees you from needing to know exactly how many elements it contains at design time.</span></span>

<span data-ttu-id="74f2c-117">Voici quelques exemples sommaires avant d’entrer dans les détails :</span><span class="sxs-lookup"><span data-stu-id="74f2c-117">Some quick examples before explanation:</span></span>

```vb
' Declare a single-dimension array of 5 numbers.
Dim numbers(4) As Integer

' Declare a single-dimension array and set its 4 values.
Dim numbers = New Integer() {1, 2, 4, 8}

' Change the size of an existing array to 16 elements and retain the current values.
ReDim Preserve numbers(15)

' Redefine the size of an existing array and reset the values.
ReDim numbers(15)

' Declare a 6 x 6 multidimensional array.
Dim matrix(5, 5) As Double

' Declare a 4 x 3 multidimensional array and set array element values.
Dim matrix = New Integer(3, 2) {{1, 2, 3}, {2, 3, 4}, {3, 4, 5}, {4, 5, 6}}

' Declare a jagged array
Dim sales()() As Double = New Double(11)() {}
```

## <a name="array-elements-in-a-simple-array"></a><span data-ttu-id="74f2c-118">Éléments de tableau dans un tableau simple</span><span class="sxs-lookup"><span data-stu-id="74f2c-118">Array elements in a simple array</span></span>

<span data-ttu-id="74f2c-119">Nous allons créer un tableau nommé `students` pour stocker le nombre d’étudiants de chaque niveau dans une école grammaticale.</span><span class="sxs-lookup"><span data-stu-id="74f2c-119">Let's create an array named `students` to store the number of students in each grade in a grammar school.</span></span> <span data-ttu-id="74f2c-120">Les index des éléments s’échelonnent de 0 à 6.</span><span class="sxs-lookup"><span data-stu-id="74f2c-120">The indexes of the elements range from 0 through 6.</span></span> <span data-ttu-id="74f2c-121">Il est plus simple d’utiliser ce tableau que de déclarer sept variables.</span><span class="sxs-lookup"><span data-stu-id="74f2c-121">Using this array is simpler than declaring seven variables.</span></span>

<span data-ttu-id="74f2c-122">L’illustration suivante montre le `students` tableau.</span><span class="sxs-lookup"><span data-stu-id="74f2c-122">The following illustration shows the `students` array.</span></span> <span data-ttu-id="74f2c-123">Pour chaque élément du tableau :</span><span class="sxs-lookup"><span data-stu-id="74f2c-123">For each element of the array:</span></span>

- <span data-ttu-id="74f2c-124">L’index de l’élément représente le niveau scolaire (l’index 0 représente le niveau maternelle).</span><span class="sxs-lookup"><span data-stu-id="74f2c-124">The index of the element represents the grade (index 0 represents kindergarten).</span></span>

- <span data-ttu-id="74f2c-125">La valeur contenue dans l’élément représente le nombre d’élèves de cette catégorie.</span><span class="sxs-lookup"><span data-stu-id="74f2c-125">The value that’s contained in the element represents the number of students in that grade.</span></span>

![Diagramme montrant un tableau des nombres d’élèves](./media/index/students-array-elements.gif)

<span data-ttu-id="74f2c-127">L’exemple suivant contient le code Visual Basic qui crée et utilise le tableau :</span><span class="sxs-lookup"><span data-stu-id="74f2c-127">The following example contains the Visual Basic code that creates and uses the array:</span></span>

[!code-vb[simple-array](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/simple-array.vb)]

<span data-ttu-id="74f2c-128">L’exemple effectue trois opérations :</span><span class="sxs-lookup"><span data-stu-id="74f2c-128">The example does three things:</span></span>

- <span data-ttu-id="74f2c-129">Elle déclare un `students` tableau avec sept éléments.</span><span class="sxs-lookup"><span data-stu-id="74f2c-129">It declares a `students` array with seven elements.</span></span> <span data-ttu-id="74f2c-130">Le nombre `6` dans la déclaration de tableau indique le dernier index dans le tableau ; il est inférieur ou égal au nombre d’éléments du tableau.</span><span class="sxs-lookup"><span data-stu-id="74f2c-130">The number `6` in the array declaration indicates the last index in the array; it is one less than the number of elements in the array.</span></span>
- <span data-ttu-id="74f2c-131">Elle assigne des valeurs à chaque élément du tableau.</span><span class="sxs-lookup"><span data-stu-id="74f2c-131">It assigns values to each element in the array.</span></span> <span data-ttu-id="74f2c-132">Les éléments de tableau sont accessibles en utilisant le nom du tableau et en incluant l’index de l’élément individuel entre parenthèses.</span><span class="sxs-lookup"><span data-stu-id="74f2c-132">Array elements are accessed by using the array name and including the index of the individual element in parentheses.</span></span>
- <span data-ttu-id="74f2c-133">Elle répertorie chaque valeur du tableau.</span><span class="sxs-lookup"><span data-stu-id="74f2c-133">It lists each value of the array.</span></span> <span data-ttu-id="74f2c-134">L’exemple utilise une [`For`](../../../language-reference/statements/for-next-statement.md) instruction pour accéder à chaque élément du tableau par son numéro d’index.</span><span class="sxs-lookup"><span data-stu-id="74f2c-134">The example uses a [`For`](../../../language-reference/statements/for-next-statement.md) statement to access each element of the array by its index number.</span></span>

<span data-ttu-id="74f2c-135">`students`Dans l’exemple précédent, le tableau est un tableau unidimensionnel, car il utilise un index.</span><span class="sxs-lookup"><span data-stu-id="74f2c-135">The `students` array in the preceding example is a one-dimensional array because it uses one index.</span></span> <span data-ttu-id="74f2c-136">Un tableau qui utilise plusieurs index ou indices est appelé *multidimensionnel*.</span><span class="sxs-lookup"><span data-stu-id="74f2c-136">An array that uses more than one index or subscript is called *multidimensional*.</span></span> <span data-ttu-id="74f2c-137">Pour plus d’informations, consultez le reste de cet article et les [dimensions des tableaux dans Visual Basic](array-dimensions.md).</span><span class="sxs-lookup"><span data-stu-id="74f2c-137">For more information, see the rest of this article and [Array Dimensions in Visual Basic](array-dimensions.md).</span></span>

## <a name="creating-an-array"></a><span data-ttu-id="74f2c-138">Création d’un tableau</span><span class="sxs-lookup"><span data-stu-id="74f2c-138">Creating an array</span></span>

<span data-ttu-id="74f2c-139">Vous pouvez définir la taille d’un tableau de plusieurs façons :</span><span class="sxs-lookup"><span data-stu-id="74f2c-139">You can define the size of an array in several ways:</span></span>

- <span data-ttu-id="74f2c-140">Vous pouvez spécifier la taille lorsque le tableau est déclaré :</span><span class="sxs-lookup"><span data-stu-id="74f2c-140">You can specify the size when the array is declared:</span></span>

  [!code-vb[creating1](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/create-array.vb#1)]

- <span data-ttu-id="74f2c-141">Vous pouvez utiliser une `New` clause pour indiquer la taille d’un tableau lors de sa création :</span><span class="sxs-lookup"><span data-stu-id="74f2c-141">You can use a `New` clause to supply the size of an array when it’s created:</span></span>

  [!code-vb[creating2](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/create-array.vb#2)]

<span data-ttu-id="74f2c-142">Si vous disposez d’un tableau existant, vous pouvez redéfinir sa taille à l’aide de l' [`ReDim`](../../../language-reference/statements/redim-statement.md) instruction.</span><span class="sxs-lookup"><span data-stu-id="74f2c-142">If you have an existing array, you can redefine its size by using the [`ReDim`](../../../language-reference/statements/redim-statement.md) statement.</span></span> <span data-ttu-id="74f2c-143">Vous pouvez spécifier que l' `ReDim` instruction conserve les valeurs qui se trouvent dans le tableau, ou vous pouvez spécifier qu’elle crée un tableau vide.</span><span class="sxs-lookup"><span data-stu-id="74f2c-143">You can specify that the `ReDim` statement keep the values that are in the array, or you can specify that it create an empty array.</span></span> <span data-ttu-id="74f2c-144">L’exemple suivant montre les différentes utilisations possibles de l’instruction `ReDim` pour modifier la taille d’un tableau existant.</span><span class="sxs-lookup"><span data-stu-id="74f2c-144">The following example shows different uses of the `ReDim` statement to modify the size of an existing array.</span></span>

[!code-vb[redimensioning](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/create-array.vb#3)]

<span data-ttu-id="74f2c-145">Pour plus d’informations, consultez l' [instruction ReDim](../../../language-reference/statements/redim-statement.md).</span><span class="sxs-lookup"><span data-stu-id="74f2c-145">For more information, see the [ReDim Statement](../../../language-reference/statements/redim-statement.md).</span></span>

## <a name="storing-values-in-an-array"></a><span data-ttu-id="74f2c-146">Stockage de valeurs dans un tableau</span><span class="sxs-lookup"><span data-stu-id="74f2c-146">Storing values in an array</span></span>

<span data-ttu-id="74f2c-147">Vous pouvez accéder à chaque emplacement d’un tableau en utilisant un index de type `Integer`.</span><span class="sxs-lookup"><span data-stu-id="74f2c-147">You can access each location in an array by using an index of type `Integer`.</span></span> <span data-ttu-id="74f2c-148">Vous pouvez stocker des valeurs dans un tableau et les récupérer par la suite en référençant chaque emplacement du tableau en utilisant son index entre parenthèses.</span><span class="sxs-lookup"><span data-stu-id="74f2c-148">You can store and retrieve values in an array by referencing each array location by using its index enclosed in parentheses.</span></span> <span data-ttu-id="74f2c-149">Les index des tableaux multidimensionnels sont séparés par des virgules (,).</span><span class="sxs-lookup"><span data-stu-id="74f2c-149">Indexes for multidimensional arrays are separated by commas (,).</span></span> <span data-ttu-id="74f2c-150">Vous avez besoin d’un index pour chaque dimension de tableau.</span><span class="sxs-lookup"><span data-stu-id="74f2c-150">You need one index for each array dimension.</span></span>

<span data-ttu-id="74f2c-151">L’exemple suivant montre des instructions qui stockent et récupèrent des valeurs dans des tableaux.</span><span class="sxs-lookup"><span data-stu-id="74f2c-151">The following example shows some statements that store and retrieve values in arrays.</span></span>

[!code-vb[store-and-retrieve](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/store-and-retrieve.vb)]

## <a name="populating-an-array-with-array-literals"></a><span data-ttu-id="74f2c-152">Remplissage d’un tableau avec des littéraux de tableau</span><span class="sxs-lookup"><span data-stu-id="74f2c-152">Populating an array with array literals</span></span>

<span data-ttu-id="74f2c-153">En utilisant un littéral de tableau, vous pouvez remplir un tableau avec un ensemble initial de valeurs en même temps que vous le créez.</span><span class="sxs-lookup"><span data-stu-id="74f2c-153">By using an array literal, you can populate an array with an initial set of values at the same time that you create it.</span></span> <span data-ttu-id="74f2c-154">Un littéral de tableau se compose d’une liste de valeurs séparées par des virgules mise entre accolades (`{}`).</span><span class="sxs-lookup"><span data-stu-id="74f2c-154">An array literal consists of a list of comma-separated values that are enclosed in braces (`{}`).</span></span>

<span data-ttu-id="74f2c-155">Quand vous créez un tableau en utilisant un littéral de tableau, vous pouvez soit indiquer le type du tableau, soit utiliser l’inférence de type pour déterminer le type du tableau.</span><span class="sxs-lookup"><span data-stu-id="74f2c-155">When you create an array by using an array literal, you can either supply the array type or use type inference to determine the array type.</span></span> <span data-ttu-id="74f2c-156">L’exemple suivant illustre les deux options.</span><span class="sxs-lookup"><span data-stu-id="74f2c-156">The following example shows both options.</span></span>

[!code-vb[create-with-literals](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/create-array.vb#4)]

<span data-ttu-id="74f2c-157">Quand vous utilisez l’inférence de type, le type du tableau est déterminé par le *type dominant* dans la liste de valeurs littérales.</span><span class="sxs-lookup"><span data-stu-id="74f2c-157">When you use type inference, the type of the array is determined by the *dominant type* in the list of literal values.</span></span> <span data-ttu-id="74f2c-158">Le type dominant est le type vers lequel tous les autres types du tableau peuvent s’étendre.</span><span class="sxs-lookup"><span data-stu-id="74f2c-158">The dominant type is the type to which all other types in the array can widen.</span></span> <span data-ttu-id="74f2c-159">Si ce type unique ne peut pas être déterminé, le type dominant est le type unique auquel tous les autres types du tableau peuvent se réduire.</span><span class="sxs-lookup"><span data-stu-id="74f2c-159">If this unique type can’t be determined, the dominant type is the unique type to which all other types in the array can narrow.</span></span> <span data-ttu-id="74f2c-160">Si aucun de ces types uniques ne peut être déterminé, le type dominant est `Object`.</span><span class="sxs-lookup"><span data-stu-id="74f2c-160">If neither of these unique types can be determined, the dominant type is `Object`.</span></span> <span data-ttu-id="74f2c-161">Par exemple, si la liste de valeurs fournie au littéral de tableau contient des valeurs de type `Integer`, `Long`et `Double`, le tableau qui en résulte est de type `Double`.</span><span class="sxs-lookup"><span data-stu-id="74f2c-161">For example, if the list of values that’s supplied to the array literal contains values of type `Integer`, `Long`, and `Double`, the resulting array is of type `Double`.</span></span> <span data-ttu-id="74f2c-162">Étant donné que `Integer` et `Long` s’étendent uniquement à `Double` , `Double` est le type dominant.</span><span class="sxs-lookup"><span data-stu-id="74f2c-162">Because `Integer` and `Long` widen only to `Double`, `Double` is the dominant type.</span></span> <span data-ttu-id="74f2c-163">Pour plus d’informations, consultez [Widening and Narrowing Conversions](../data-types/widening-and-narrowing-conversions.md).</span><span class="sxs-lookup"><span data-stu-id="74f2c-163">For more information, see [Widening and Narrowing Conversions](../data-types/widening-and-narrowing-conversions.md).</span></span>

> [!NOTE]
> <span data-ttu-id="74f2c-164">Vous pouvez utiliser l’inférence de type uniquement pour les tableaux qui sont définis en tant que variables locales dans un membre de type.</span><span class="sxs-lookup"><span data-stu-id="74f2c-164">You can use type inference only for arrays that are defined as local variables in a type member.</span></span> <span data-ttu-id="74f2c-165">Si une définition de type explicite est absente, les tableaux définis avec des littéraux de tableau au niveau de la classe sont de type `Object[]` .</span><span class="sxs-lookup"><span data-stu-id="74f2c-165">If an explicit type definition is absent, arrays defined with array literals at the class level are of type `Object[]`.</span></span> <span data-ttu-id="74f2c-166">Pour plus d’informations, consultez [inférence de type local](../variables/local-type-inference.md).</span><span class="sxs-lookup"><span data-stu-id="74f2c-166">For more information, see [Local type inference](../variables/local-type-inference.md).</span></span>

<span data-ttu-id="74f2c-167">Notez que l’exemple précédent définit `values` sous la forme d’un tableau de type, `Double` même si tous les littéraux de tableau sont de type `Integer` .</span><span class="sxs-lookup"><span data-stu-id="74f2c-167">Note that the previous example defines `values` as an array of type `Double` even though all the array literals are of type `Integer`.</span></span> <span data-ttu-id="74f2c-168">Vous pouvez créer ce tableau, car les valeurs du littéral de tableau peuvent s’étendre aux `Double` valeurs.</span><span class="sxs-lookup"><span data-stu-id="74f2c-168">You can create this array because the values in the array literal can widen to `Double` values.</span></span>

<span data-ttu-id="74f2c-169">Vous pouvez également créer et remplir un tableau multidimensionnel en utilisant des *littéraux de tableau imbriqués*.</span><span class="sxs-lookup"><span data-stu-id="74f2c-169">You can also create and populate a multidimensional array by using *nested array literals*.</span></span> <span data-ttu-id="74f2c-170">Les littéraux de tableau imbriqués doivent avoir un certain nombre de dimensions qui sont cohérentes avec le tableau résultant.</span><span class="sxs-lookup"><span data-stu-id="74f2c-170">Nested array literals must have a number of dimensions that’s consistent with the resulting array.</span></span> <span data-ttu-id="74f2c-171">L’exemple suivant crée un tableau à deux dimensions d’entiers à l’aide de littéraux de tableaux imbriqués.</span><span class="sxs-lookup"><span data-stu-id="74f2c-171">The following example creates a two-dimensional array of integers by using nested array literals.</span></span>

[!code-vb[nested-array-literals](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/create-array.vb#5)]

<span data-ttu-id="74f2c-172">Lorsque vous utilisez des littéraux de tableaux imbriqués pour créer et remplir un tableau, une erreur se produit si le nombre d’éléments contenus dans les littéraux de tableau imbriqués ne correspond pas.</span><span class="sxs-lookup"><span data-stu-id="74f2c-172">When using nested array literals to create and populate an array, an error occurs if the number of elements in the nested array literals don't match.</span></span> <span data-ttu-id="74f2c-173">Une erreur se produit également si vous déclarez explicitement la variable tableau pour avoir un nombre de dimensions différent de celui des littéraux de tableau.</span><span class="sxs-lookup"><span data-stu-id="74f2c-173">An error also occurs if you explicitly declare the array variable to have a different number of dimensions than the array literals.</span></span>

<span data-ttu-id="74f2c-174">De la même façon que pour les tableaux unidimensionnels, vous pouvez vous appuyer sur l’inférence de type lors de la création d’un tableau multidimensionnel avec des littéraux de tableau imbriqués.</span><span class="sxs-lookup"><span data-stu-id="74f2c-174">Just as you can for one-dimensional arrays, you can rely on type inference when creating a multidimensional array with nested array literals.</span></span> <span data-ttu-id="74f2c-175">Le type déduit est le type dominant pour toutes les valeurs de tous les littéraux de tableau pour tous les niveaux d’imbrication.</span><span class="sxs-lookup"><span data-stu-id="74f2c-175">The inferred type is the dominant type for all the values in all the array literals for all nesting level.</span></span> <span data-ttu-id="74f2c-176">L’exemple suivant crée un tableau à deux dimensions de type `Double[,]` à partir de valeurs de type `Integer` et `Double` .</span><span class="sxs-lookup"><span data-stu-id="74f2c-176">The following example creates a two-dimensional array of type `Double[,]` from values that are of type `Integer` and `Double`.</span></span>

[!code-vb[nested-type-inference](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/create-array.vb#6)]

<span data-ttu-id="74f2c-177">Pour obtenir des exemples supplémentaires, consultez [Comment : initialiser une variable tableau en Visual Basic](how-to-initialize-an-array-variable.md).</span><span class="sxs-lookup"><span data-stu-id="74f2c-177">For additional examples, see [How to: Initialize an Array Variable in Visual Basic](how-to-initialize-an-array-variable.md).</span></span>

## <a name="iterating-through-an-array"></a><span data-ttu-id="74f2c-178">Itération au sein d’un tableau</span><span class="sxs-lookup"><span data-stu-id="74f2c-178">Iterating through an array</span></span>

<span data-ttu-id="74f2c-179">Lorsque vous itérez au sein d’un tableau, vous accédez à chaque élément du tableau de l’index le plus bas au plus élevé ou du plus élevé au plus bas.</span><span class="sxs-lookup"><span data-stu-id="74f2c-179">When you iterate through an array, you access each element in the array from the lowest index to the highest or from the highest to the lowest.</span></span> <span data-ttu-id="74f2c-180">En général, utilisez l’une [des... Next Statement](../../../language-reference/statements/for-next-statement.md) ou les instructions [for each... Instruction Next](../../../language-reference/statements/for-each-next-statement.md) pour itérer au sein des éléments d’un tableau.</span><span class="sxs-lookup"><span data-stu-id="74f2c-180">Typically, use either the [For...Next Statement](../../../language-reference/statements/for-next-statement.md) or the [For Each...Next Statement](../../../language-reference/statements/for-each-next-statement.md) to iterate through the elements of an array.</span></span> <span data-ttu-id="74f2c-181">Lorsque vous ne connaissez pas les limites supérieures du tableau, vous pouvez appeler la <xref:System.Array.GetUpperBound%2A?displayProperty=nameWithType> méthode pour obtenir la valeur la plus élevée de l’index.</span><span class="sxs-lookup"><span data-stu-id="74f2c-181">When you don't know the upper bounds of the array, you can call the <xref:System.Array.GetUpperBound%2A?displayProperty=nameWithType> method to get the highest value of the index.</span></span> <span data-ttu-id="74f2c-182">Bien que la valeur d’index la plus basse soit presque toujours 0, vous pouvez appeler la <xref:System.Array.GetLowerBound%2A?displayProperty=nameWithType> méthode pour récupérer la valeur la plus faible de l’index.</span><span class="sxs-lookup"><span data-stu-id="74f2c-182">Although lowest index value is almost always 0, you can call the <xref:System.Array.GetLowerBound%2A?displayProperty=nameWithType> method to get the lowest value of the index.</span></span>

<span data-ttu-id="74f2c-183">L’exemple suivant itère au sein d’un tableau unidimensionnel à l’aide de l' [`For...Next`](../../../language-reference/statements/for-next-statement.md) instruction.</span><span class="sxs-lookup"><span data-stu-id="74f2c-183">The following example iterates through a one-dimensional array by using the [`For...Next`](../../../language-reference/statements/for-next-statement.md) statement.</span></span>

[!code-vb[iterate-one-dimensional-array](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/iterate1d.vb)]

<span data-ttu-id="74f2c-184">L’exemple suivant itère au sein d’un tableau multidimensionnel à l’aide d’une [`For...Next`](../../../language-reference/statements/for-next-statement.md) instruction.</span><span class="sxs-lookup"><span data-stu-id="74f2c-184">The following example iterates through a multidimensional array by using a [`For...Next`](../../../language-reference/statements/for-next-statement.md) statement.</span></span> <span data-ttu-id="74f2c-185">La méthode <xref:System.Array.GetUpperBound%2A> dispose d’un paramètre qui spécifie la dimension.</span><span class="sxs-lookup"><span data-stu-id="74f2c-185">The <xref:System.Array.GetUpperBound%2A> method has a parameter that specifies the dimension.</span></span> <span data-ttu-id="74f2c-186">`GetUpperBound(0)`retourne l’index le plus élevé de la première dimension et `GetUpperBound(1)` retourne l’index le plus élevé de la deuxième dimension.</span><span class="sxs-lookup"><span data-stu-id="74f2c-186">`GetUpperBound(0)` returns the highest index of the first dimension, and `GetUpperBound(1)` returns the highest index of the second dimension.</span></span>

[!code-vb[iterate-two-dimensional-array](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/iterate2d.vb)]

<span data-ttu-id="74f2c-187">L’exemple suivant utilise une commande [for each... Next Statement](../../../language-reference/statements/for-each-next-statement.md)pour itérer au sein d’un tableau unidimensionnel et d’un tableau à deux dimensions.</span><span class="sxs-lookup"><span data-stu-id="74f2c-187">The following example uses a [For Each...Next Statement](../../../language-reference/statements/for-each-next-statement.md)to iterate through a one-dimensional array and a two-dimensional array.</span></span>

[!code-vb[iterate-for-each-next](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/iterate-for-each-next.vb)]

## <a name="array-size"></a><span data-ttu-id="74f2c-188">Taille du tableau</span><span class="sxs-lookup"><span data-stu-id="74f2c-188">Array size</span></span>

<span data-ttu-id="74f2c-189">La taille d’un tableau est le produit des longueurs de toutes ses dimensions.</span><span class="sxs-lookup"><span data-stu-id="74f2c-189">The size of an array is the product of the lengths of all its dimensions.</span></span> <span data-ttu-id="74f2c-190">Elle représente le nombre total d’éléments actuellement contenus dans le tableau.</span><span class="sxs-lookup"><span data-stu-id="74f2c-190">It represents the total number of elements currently contained in the array.</span></span>  <span data-ttu-id="74f2c-191">Par exemple, l’exemple suivant déclare un tableau à deux dimensions avec quatre éléments dans chaque dimension.</span><span class="sxs-lookup"><span data-stu-id="74f2c-191">For example, the following example declares a 2-dimensional array with four elements in each dimension.</span></span> <span data-ttu-id="74f2c-192">Comme le montre la sortie de l’exemple, la taille du tableau est 16 (ou (3 + 1) \* (3 + 1).</span><span class="sxs-lookup"><span data-stu-id="74f2c-192">As the output from the example shows, the array's size is 16 (or (3 + 1) \* (3 + 1).</span></span>

[!code-vb[array-size](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/array-size.vb)]

> [!NOTE]
> <span data-ttu-id="74f2c-193">Cette présentation de la taille d’un tableau ne s’applique pas aux tableaux en escalier.</span><span class="sxs-lookup"><span data-stu-id="74f2c-193">This discussion of array size does not apply to jagged arrays.</span></span> <span data-ttu-id="74f2c-194">Pour plus d’informations sur les tableaux en escalier et la détermination de la taille d’un tableau en escalier, consultez la section [tableaux en escalier](#jagged-arrays) .</span><span class="sxs-lookup"><span data-stu-id="74f2c-194">For information on jagged arrays and determining the size of a jagged array, see the [Jagged arrays](#jagged-arrays) section.</span></span>

<span data-ttu-id="74f2c-195">Vous pouvez trouver la taille d’un tableau en utilisant la propriété <xref:System.Array.Length%2A?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="74f2c-195">You can find the size of an array by using the <xref:System.Array.Length%2A?displayProperty=nameWithType> property.</span></span> <span data-ttu-id="74f2c-196">Vous pouvez trouver la longueur de chaque dimension d’un tableau multidimensionnel à l’aide de la <xref:System.Array.GetLength%2A?displayProperty=nameWithType> méthode.</span><span class="sxs-lookup"><span data-stu-id="74f2c-196">You can find the length of each dimension of a multidimensional array by using the <xref:System.Array.GetLength%2A?displayProperty=nameWithType> method.</span></span>

<span data-ttu-id="74f2c-197">Vous pouvez redimensionner une variable tableau en lui assignant un nouvel objet tableau ou en utilisant l’instruction [ `ReDim` Statement](../../../language-reference/statements/redim-statement.md) .</span><span class="sxs-lookup"><span data-stu-id="74f2c-197">You can resize an array variable by assigning a new array object to it or by using the [`ReDim` Statement](../../../language-reference/statements/redim-statement.md) statement.</span></span> <span data-ttu-id="74f2c-198">L’exemple suivant utilise l' `ReDim` instruction pour remplacer un tableau de 100 éléments par un tableau de 51 éléments.</span><span class="sxs-lookup"><span data-stu-id="74f2c-198">The following example uses the `ReDim` statement to change a 100-element array to a 51-element array.</span></span>

[!code-vb[resize-an-array](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/array-size2.vb)]

<span data-ttu-id="74f2c-199">Il y a plusieurs points à prendre en compte en ce qui concerne la taille d’un tableau.</span><span class="sxs-lookup"><span data-stu-id="74f2c-199">There are several things to keep in mind when dealing with the size of an array.</span></span>

|||
|---|---|
|<span data-ttu-id="74f2c-200">Longueur de dimension</span><span class="sxs-lookup"><span data-stu-id="74f2c-200">Dimension Length</span></span>|<span data-ttu-id="74f2c-201">L’index de chaque dimension est basé sur 0, ce qui signifie qu’elle est comprise entre 0 et sa limite supérieure.</span><span class="sxs-lookup"><span data-stu-id="74f2c-201">The index of each dimension is 0-based, which means it ranges from 0 to its upper bound.</span></span> <span data-ttu-id="74f2c-202">Par conséquent, la longueur d’une dimension donnée est supérieure à la limite supérieure déclarée de cette dimension.</span><span class="sxs-lookup"><span data-stu-id="74f2c-202">Therefore, the length of a given dimension is one greater than the declared upper bound of that dimension.</span></span>|
|<span data-ttu-id="74f2c-203">Limites de longueur</span><span class="sxs-lookup"><span data-stu-id="74f2c-203">Length Limits</span></span>|<span data-ttu-id="74f2c-204">La longueur de chaque dimension d’un tableau est limitée à la valeur maximale du `Integer` type de données, qui est <xref:System.Int32.MaxValue?displayProperty=nameWithType> ou (2 ^ 31)-1.</span><span class="sxs-lookup"><span data-stu-id="74f2c-204">The length of every dimension of an array is limited to the maximum value of the `Integer` data type, which is <xref:System.Int32.MaxValue?displayProperty=nameWithType> or (2 ^ 31) - 1.</span></span> <span data-ttu-id="74f2c-205">Cependant, la taille totale d’un tableau est aussi limitée par la mémoire disponible sur votre système.</span><span class="sxs-lookup"><span data-stu-id="74f2c-205">However, the total size of an array is also limited by the memory available on your system.</span></span> <span data-ttu-id="74f2c-206">Si vous tentez d’initialiser un tableau qui dépasse la quantité de mémoire disponible, le runtime lève une exception <xref:System.OutOfMemoryException> .</span><span class="sxs-lookup"><span data-stu-id="74f2c-206">If you attempt to initialize an array that exceeds the amount of available memory, the runtime throws an <xref:System.OutOfMemoryException>.</span></span>|
|<span data-ttu-id="74f2c-207">Taille et taille d’élément</span><span class="sxs-lookup"><span data-stu-id="74f2c-207">Size and Element Size</span></span>|<span data-ttu-id="74f2c-208">La taille d’un tableau est indépendante du type de données de ses éléments.</span><span class="sxs-lookup"><span data-stu-id="74f2c-208">An array's size is independent of the data type of its elements.</span></span> <span data-ttu-id="74f2c-209">La taille représente toujours le nombre total d’éléments, et non le nombre d’octets qu’ils consomment en mémoire.</span><span class="sxs-lookup"><span data-stu-id="74f2c-209">The size always represents the total number of elements, not the number of bytes that they consume in memory.</span></span>|
|<span data-ttu-id="74f2c-210">Consommation de mémoire</span><span class="sxs-lookup"><span data-stu-id="74f2c-210">Memory Consumption</span></span>|<span data-ttu-id="74f2c-211">Il est déconseillé de faire des hypothèses sur la façon dont un tableau est stocké en mémoire.</span><span class="sxs-lookup"><span data-stu-id="74f2c-211">It is not safe to make any assumptions regarding how an array is stored in memory.</span></span> <span data-ttu-id="74f2c-212">Le stockage varie selon la largeur de données de la plateforme. Par exemple, un même tableau utilise plus de mémoire sur un système 64 bits que sur un système 32 bits.</span><span class="sxs-lookup"><span data-stu-id="74f2c-212">Storage varies on platforms of different data widths, so the same array can consume more memory on a 64-bit system than on a 32-bit system.</span></span> <span data-ttu-id="74f2c-213">Selon la configuration du système au moment de l’initialisation d’un tableau, le Common Language Runtime (CLR) peut assigner du stockage de façon à regrouper les éléments aussi près que possible les uns des autres ou pour tous les adapter aux limites matérielles naturelles.</span><span class="sxs-lookup"><span data-stu-id="74f2c-213">Depending on system configuration when you initialize an array, the common language runtime (CLR) can assign storage either to pack elements as close together as possible, or to align them all on natural hardware boundaries.</span></span> <span data-ttu-id="74f2c-214">De même, un tableau nécessite un supplément de stockage pour ses informations de contrôle, un supplément qui augmente d’autant à chaque dimension ajoutée.</span><span class="sxs-lookup"><span data-stu-id="74f2c-214">Also, an array requires a storage overhead for its control information, and this overhead increases with each added dimension.</span></span>|

## <a name="the-array-type"></a><span data-ttu-id="74f2c-215">Le type de tableau</span><span class="sxs-lookup"><span data-stu-id="74f2c-215">The array type</span></span>

<span data-ttu-id="74f2c-216">Chaque tableau a un type de données, qui diffère du type de données de ses éléments.</span><span class="sxs-lookup"><span data-stu-id="74f2c-216">Every array has a data type, which differs from the data type of its elements.</span></span> <span data-ttu-id="74f2c-217">Le type de données varie d’un tableau à un autre.</span><span class="sxs-lookup"><span data-stu-id="74f2c-217">There is no single data type for all arrays.</span></span> <span data-ttu-id="74f2c-218">En effet, le type de données d’un tableau est déterminé par le nombre de dimensions (ou *rang*) qu’il possède et par le type de données de ses éléments.</span><span class="sxs-lookup"><span data-stu-id="74f2c-218">Instead, the data type of an array is determined by the number of dimensions, or *rank*, of the array, and the data type of the elements in the array.</span></span> <span data-ttu-id="74f2c-219">Deux variables de tableau sont du même type de données uniquement lorsqu’elles ont le même rang et que leurs éléments ont le même type de données.</span><span class="sxs-lookup"><span data-stu-id="74f2c-219">Two array variables are of the same data type only when they have the same rank and their elements have the same data type.</span></span> <span data-ttu-id="74f2c-220">Les longueurs des dimensions d’un tableau n’influencent pas le type de données du tableau.</span><span class="sxs-lookup"><span data-stu-id="74f2c-220">The lengths of the dimensions of an array do not influence the array data type.</span></span>

<span data-ttu-id="74f2c-221">Chaque tableau hérite de la classe <xref:System.Array?displayProperty=nameWithType> et vous pouvez déclarer une variable de type `Array`, mais vous ne pouvez pas créer un tableau de type `Array`.</span><span class="sxs-lookup"><span data-stu-id="74f2c-221">Every array inherits from the <xref:System.Array?displayProperty=nameWithType> class, and you can declare a variable to be of type `Array`, but you cannot create an array of type `Array`.</span></span> <span data-ttu-id="74f2c-222">Par exemple, bien que le code suivant déclare que la `arr` variable soit de type `Array` et appelle la <xref:System.Array.CreateInstance%2A?displayProperty=nameWithType> méthode pour instancier le tableau, le type du tableau s’avère être Object [].</span><span class="sxs-lookup"><span data-stu-id="74f2c-222">For example, although the following code declares the `arr` variable to be of type `Array` and calls the <xref:System.Array.CreateInstance%2A?displayProperty=nameWithType> method to instantiate the array, the array's type proves to be Object[].</span></span>

[!code-vb[array-class](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/array-class.vb)]

<span data-ttu-id="74f2c-223">De même, [l’instruction ReDim](../../../language-reference/statements/redim-statement.md) ne peut pas opérer sur une variable déclarée comme étant du type `Array`.</span><span class="sxs-lookup"><span data-stu-id="74f2c-223">Also, the [ReDim Statement](../../../language-reference/statements/redim-statement.md) cannot operate on a variable declared as type `Array`.</span></span> <span data-ttu-id="74f2c-224">Pour ces raisons, et pour la sécurité de type, il est recommandé de déclarer chaque tableau en tant que type spécifique.</span><span class="sxs-lookup"><span data-stu-id="74f2c-224">For these reasons, and for type safety, it is advisable to declare every array as a specific type.</span></span>

<span data-ttu-id="74f2c-225">Vous pouvez déterminer le type de données d’un tableau ou de ses éléments de plusieurs façons.</span><span class="sxs-lookup"><span data-stu-id="74f2c-225">You can find out the data type of either an array or its elements in several ways.</span></span>

- <span data-ttu-id="74f2c-226">Vous pouvez appeler la <xref:System.Object.GetType%2A> méthode sur la variable pour obtenir un <xref:System.Type> objet qui représente le type au moment de l’exécution de la variable.</span><span class="sxs-lookup"><span data-stu-id="74f2c-226">You can call the <xref:System.Object.GetType%2A> method on the variable to get a <xref:System.Type> object that represents the run-time type of the variable.</span></span> <span data-ttu-id="74f2c-227">L’objet <xref:System.Type> contient des informations complètes dans ses propriétés et méthodes.</span><span class="sxs-lookup"><span data-stu-id="74f2c-227">The <xref:System.Type> object holds extensive information in its properties and methods.</span></span>
- <span data-ttu-id="74f2c-228">Vous pouvez passer la variable à la <xref:Microsoft.VisualBasic.Information.TypeName%2A> fonction pour obtenir un `String` avec le nom du type au moment de l’exécution.</span><span class="sxs-lookup"><span data-stu-id="74f2c-228">You can pass the variable to the <xref:Microsoft.VisualBasic.Information.TypeName%2A> function to get a `String` with the name of run-time type.</span></span>

<span data-ttu-id="74f2c-229">L’exemple suivant appelle la `GetType` méthode et la `TypeName` fonction pour déterminer le type d’un tableau.</span><span class="sxs-lookup"><span data-stu-id="74f2c-229">The following example calls the both the `GetType` method and the `TypeName` function to determine the type of an array.</span></span> <span data-ttu-id="74f2c-230">Le type de tableau est `Byte(,)` .</span><span class="sxs-lookup"><span data-stu-id="74f2c-230">The array type is `Byte(,)`.</span></span> <span data-ttu-id="74f2c-231">Notez que la <xref:System.Type.BaseType%2A?displayProperty=nameWithType> propriété indique également que le type de base du tableau d’octets est la <xref:System.Array> classe.</span><span class="sxs-lookup"><span data-stu-id="74f2c-231">Note that the <xref:System.Type.BaseType%2A?displayProperty=nameWithType> property also indicates that the base type of the byte array is the <xref:System.Array> class.</span></span>

[!code-vb[array-type](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/array-type.vb)]

## <a name="arrays-as-return-values-and-parameters"></a><span data-ttu-id="74f2c-232">Tableaux en tant que valeurs de retour et paramètres</span><span class="sxs-lookup"><span data-stu-id="74f2c-232">Arrays as return values and parameters</span></span>

<span data-ttu-id="74f2c-233">Pour retourner un tableau à partir d’une procédure `Function`, spécifiez le type de données du tableau et le nombre de dimensions en tant que type de retour de [l’instruction Function](../../../language-reference/statements/function-statement.md).</span><span class="sxs-lookup"><span data-stu-id="74f2c-233">To return an array from a `Function` procedure, specify the array data type and the number of dimensions as the return type of the [Function Statement](../../../language-reference/statements/function-statement.md).</span></span> <span data-ttu-id="74f2c-234">Dans la fonction, déclarez une variable tableau locale avec le même type de données et le même nombre de dimensions.</span><span class="sxs-lookup"><span data-stu-id="74f2c-234">Within the function, declare a local array variable with same data type and number of dimensions.</span></span> <span data-ttu-id="74f2c-235">Dans [l’instruction Return](../../../language-reference/statements/return-statement.md), incluez la variable tableau locale sans parenthèses.</span><span class="sxs-lookup"><span data-stu-id="74f2c-235">In the [Return Statement](../../../language-reference/statements/return-statement.md), include the local array variable without parentheses.</span></span>

<span data-ttu-id="74f2c-236">Pour spécifier un tableau en tant que paramètre d’une procédure `Sub` ou `Function` , définissez le paramètre en tant que tableau avec un type de données et un nombre de dimensions spécifiés.</span><span class="sxs-lookup"><span data-stu-id="74f2c-236">To specify an array as a parameter to a `Sub` or `Function` procedure, define the parameter as an array with a specified data type and number of dimensions.</span></span> <span data-ttu-id="74f2c-237">Dans l’appel à la procédure, transmettez une variable tableau avec le même type de données et le même nombre de dimensions.</span><span class="sxs-lookup"><span data-stu-id="74f2c-237">In the call to the procedure, pass an array variable with the same data type and number of dimensions.</span></span>

<span data-ttu-id="74f2c-238">Dans l’exemple suivant, la `GetNumbers` fonction retourne un `Integer()` , un tableau unidimensionnel de type `Integer` .</span><span class="sxs-lookup"><span data-stu-id="74f2c-238">In the following example, the `GetNumbers` function returns an `Integer()`, a one-dimensional array of type `Integer`.</span></span> <span data-ttu-id="74f2c-239">La procédure `ShowNumbers` accepte un argument `Integer()` .</span><span class="sxs-lookup"><span data-stu-id="74f2c-239">The `ShowNumbers` procedure accepts an `Integer()` argument.</span></span>

[!code-vb[return-value-and-params](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/return-values-and-params.vb)]

<span data-ttu-id="74f2c-240">Dans l’exemple suivant, la `GetNumbersMultiDim` fonction retourne un `Integer(,)` , un tableau à deux dimensions de type `Integer` .</span><span class="sxs-lookup"><span data-stu-id="74f2c-240">In the following example, the `GetNumbersMultiDim` function returns an `Integer(,)`, a two-dimensional array of type `Integer`.</span></span>  <span data-ttu-id="74f2c-241">La procédure `ShowNumbersMultiDim` accepte un argument `Integer(,)` .</span><span class="sxs-lookup"><span data-stu-id="74f2c-241">The `ShowNumbersMultiDim` procedure accepts an `Integer(,)` argument.</span></span>

[!code-vb[multidimensional-return-value](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/return-values-and-params-2d.vb)]

## <a name="jagged-arrays"></a><span data-ttu-id="74f2c-242">Tableaux en escalier</span><span class="sxs-lookup"><span data-stu-id="74f2c-242">Jagged arrays</span></span>

<span data-ttu-id="74f2c-243">Parfois, la structure de données de votre application est à deux dimensions, mais pas rectangulaire.</span><span class="sxs-lookup"><span data-stu-id="74f2c-243">Sometimes the data structure in your application is two-dimensional but not rectangular.</span></span> <span data-ttu-id="74f2c-244">Par exemple, vous pouvez utiliser un tableau pour stocker des données sur la température élevée de chaque jour du mois.</span><span class="sxs-lookup"><span data-stu-id="74f2c-244">For example, you might use an array to store data about the high temperature of each day of the month.</span></span> <span data-ttu-id="74f2c-245">La première dimension du tableau représente le mois, mais la deuxième dimension représente le nombre de jours et le nombre de jours dans un mois n’est pas uniforme.</span><span class="sxs-lookup"><span data-stu-id="74f2c-245">The first dimension of the array represents the month, but the second dimension represents the number of days, and the number of days in a month is not uniform.</span></span> <span data-ttu-id="74f2c-246">Un *tableau en escalier*, qui est également appelé un *tableau de tableaux*, est conçu pour de tels scénarios.</span><span class="sxs-lookup"><span data-stu-id="74f2c-246">A *jagged array*, which is also called an *array of arrays*, is designed for such scenarios.</span></span> <span data-ttu-id="74f2c-247">Un tableau en escalier est un tableau dont les éléments sont également des tableaux.</span><span class="sxs-lookup"><span data-stu-id="74f2c-247">A jagged array is an array whose elements are also arrays.</span></span> <span data-ttu-id="74f2c-248">Un tableau en escalier et chaque élément qu’il contient peut avoir une ou plusieurs dimensions.</span><span class="sxs-lookup"><span data-stu-id="74f2c-248">A jagged array and each element in a jagged array can have one or more dimensions.</span></span>

<span data-ttu-id="74f2c-249">L’exemple suivant utilise un tableau de mois, dont chaque élément est un tableau de jours.</span><span class="sxs-lookup"><span data-stu-id="74f2c-249">The following example uses an array of months, each element of which is an array of days.</span></span> <span data-ttu-id="74f2c-250">L’exemple utilise un tableau en escalier, car les différents mois ont un nombre de jours différent.</span><span class="sxs-lookup"><span data-stu-id="74f2c-250">The example uses a jagged array because different months have different numbers of days.</span></span>  <span data-ttu-id="74f2c-251">L’exemple montre comment créer un tableau en escalier, lui assigner des valeurs et récupérer et afficher ses valeurs.</span><span class="sxs-lookup"><span data-stu-id="74f2c-251">The example shows how to create a jagged array, assign values to it, and retrieve and display its values.</span></span>

[!code-vb[jagged-arrays](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/jagged.vb)]

<span data-ttu-id="74f2c-252">L’exemple précédent assigne des valeurs au tableau en escalier sur une base élément par élément à l’aide d’une `For...Next` boucle.</span><span class="sxs-lookup"><span data-stu-id="74f2c-252">The previous example assigns values to the jagged array on an element-by-element basis by using a `For...Next` loop.</span></span> <span data-ttu-id="74f2c-253">Vous pouvez également assigner des valeurs aux éléments d’un tableau en escalier en utilisant des littéraux de tableau imbriqués.</span><span class="sxs-lookup"><span data-stu-id="74f2c-253">You can also assign values to the elements of a jagged array by using nested array literals.</span></span> <span data-ttu-id="74f2c-254">Toutefois, la tentative d’utilisation de littéraux de tableaux imbriqués (par exemple, `Dim valuesjagged = {{1, 2}, {2, 3, 4}}` ) génère une erreur de compilateur [BC30568](../../../misc/bc30568.md).</span><span class="sxs-lookup"><span data-stu-id="74f2c-254">However, the attempt to use nested array literals (for example, `Dim valuesjagged = {{1, 2}, {2, 3, 4}}`) generates compiler error [BC30568](../../../misc/bc30568.md).</span></span> <span data-ttu-id="74f2c-255">Pour corriger l’erreur, placez les littéraux de tableau internes entre parenthèses.</span><span class="sxs-lookup"><span data-stu-id="74f2c-255">To correct the error, enclose the inner array literals in parentheses.</span></span> <span data-ttu-id="74f2c-256">Les parenthèses forcent l’évaluation de l’expression de littéral de tableau, et les valeurs résultantes sont utilisées avec le littéral de tableau externe, comme le montre l’exemple suivant.</span><span class="sxs-lookup"><span data-stu-id="74f2c-256">The parentheses force the array literal expression to be evaluated, and the resulting values are used with the outer array literal, as the following example shows.</span></span>

[!code-vb[jagged-array-initialization](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/jagged-assign.vb)]

<span data-ttu-id="74f2c-257">Un tableau en escalier est un tableau unidimensionnel dont les éléments contiennent des tableaux.</span><span class="sxs-lookup"><span data-stu-id="74f2c-257">A jagged array is a one-dimensional array whose elements contain arrays.</span></span> <span data-ttu-id="74f2c-258">Par conséquent, la <xref:System.Array.Length%2A?displayProperty=nameWithType> propriété et la `Array.GetLength(0)` méthode retournent le nombre d’éléments dans le tableau unidimensionnel et `Array.GetLength(1)` lèvent un <xref:System.IndexOutOfRangeException> , car un tableau en escalier n’est pas multidimensionnel.</span><span class="sxs-lookup"><span data-stu-id="74f2c-258">Therefore, the <xref:System.Array.Length%2A?displayProperty=nameWithType> property and the `Array.GetLength(0)` method return the number of elements in the one-dimensional array, and `Array.GetLength(1)` throws an <xref:System.IndexOutOfRangeException> because a jagged array is not multidimensional.</span></span> <span data-ttu-id="74f2c-259">Vous déterminez le nombre d’éléments dans chaque sous-tableau en extrayant la valeur de la propriété de chaque sous-tableau <xref:System.Array.Length%2A?displayProperty=nameWithType> .</span><span class="sxs-lookup"><span data-stu-id="74f2c-259">You determine the number of elements in each subarray by retrieving the value of each subarray's <xref:System.Array.Length%2A?displayProperty=nameWithType> property.</span></span> <span data-ttu-id="74f2c-260">L’exemple suivant montre comment déterminer le nombre d’éléments dans un tableau en escalier.</span><span class="sxs-lookup"><span data-stu-id="74f2c-260">The following example illustrates how to determine the number of elements in a jagged array.</span></span>

[!code-vb[jagged-array-size](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/jagged-length.vb)]

## <a name="zero-length-arrays"></a><span data-ttu-id="74f2c-261">Tableaux de longueur zéro</span><span class="sxs-lookup"><span data-stu-id="74f2c-261">Zero-length arrays</span></span>

<span data-ttu-id="74f2c-262">Visual Basic fait la distinction entre un tableau non initialisé (un tableau dont la valeur est `Nothing` ) et un tableau de *longueur zéro* ou un tableau vide (tableau sans éléments). Un tableau non initialisé est un tableau qui n’a pas été dimensionné ou auquel des valeurs ont été assignées.</span><span class="sxs-lookup"><span data-stu-id="74f2c-262">Visual Basic differentiates between a uninitialized array (an array whose value is `Nothing`) and a *zero-length array* or empty array (an array that has no elements.) An uninitialized array is one that has not been dimensioned or had any values assigned to it.</span></span> <span data-ttu-id="74f2c-263">Par exemple :</span><span class="sxs-lookup"><span data-stu-id="74f2c-263">For example:</span></span>

```vb
Dim arr() As String
```

<span data-ttu-id="74f2c-264">Un tableau de longueur zéro est déclaré avec une dimension de-1.</span><span class="sxs-lookup"><span data-stu-id="74f2c-264">A zero-length array is declared with a dimension of -1.</span></span> <span data-ttu-id="74f2c-265">Par exemple :</span><span class="sxs-lookup"><span data-stu-id="74f2c-265">For example:</span></span>

```vb
Dim arrZ(-1) As String
```

<span data-ttu-id="74f2c-266">Vous pouvez être amené à créer un tableau de longueur zéro dans les cas suivants :</span><span class="sxs-lookup"><span data-stu-id="74f2c-266">You might need to create a zero-length array under the following circumstances:</span></span>

- <span data-ttu-id="74f2c-267">Sans risquer une <xref:System.NullReferenceException> exception, votre code doit accéder aux membres de la <xref:System.Array> classe, tels que <xref:System.Array.Length%2A> ou <xref:System.Array.Rank%2A> , ou appeler une fonction Visual Basic telle que <xref:Microsoft.VisualBasic.Information.UBound%2A> .</span><span class="sxs-lookup"><span data-stu-id="74f2c-267">Without risking a <xref:System.NullReferenceException> exception, your code must access members of the <xref:System.Array> class, such as <xref:System.Array.Length%2A> or <xref:System.Array.Rank%2A>, or call a Visual Basic function such as <xref:Microsoft.VisualBasic.Information.UBound%2A>.</span></span>

- <span data-ttu-id="74f2c-268">Vous souhaitez simplifier votre code en n’ayant pas besoin de vérifier la présence de `Nothing` comme un cas particulier.</span><span class="sxs-lookup"><span data-stu-id="74f2c-268">You want to keep your code simple by not having to check for `Nothing` as a special case.</span></span>

- <span data-ttu-id="74f2c-269">Votre code interagit avec une interface de programmation d’applications (API) qui vous oblige à passer un tableau de longueur zéro à une ou plusieurs procédures ou qui retourne un tableau de longueur zéro à partir d’une ou plusieurs procédures.</span><span class="sxs-lookup"><span data-stu-id="74f2c-269">Your code interacts with an application programming interface (API) that either requires you to pass a zero-length array to one or more procedures or returns a zero-length array from one or more procedures.</span></span>

## <a name="splitting-an-array"></a><span data-ttu-id="74f2c-270">Fractionnement d’un tableau</span><span class="sxs-lookup"><span data-stu-id="74f2c-270">Splitting an array</span></span>

<span data-ttu-id="74f2c-271">Dans certains cas, il se peut que vous deviez fractionner un tableau unique en plusieurs tableaux.</span><span class="sxs-lookup"><span data-stu-id="74f2c-271">In some cases, you may need to split a single array into multiple arrays.</span></span> <span data-ttu-id="74f2c-272">Cela implique d’identifier le ou les points à partir desquels le tableau doit être fractionné, puis de Spitting le tableau en deux ou plusieurs tableaux distincts.</span><span class="sxs-lookup"><span data-stu-id="74f2c-272">This involves identifying the point or points at which the array is to be split, and then spitting the array into two or more separate arrays.</span></span>

> [!NOTE]
> <span data-ttu-id="74f2c-273">Cette section n’aborde pas le fractionnement d’une chaîne unique en un tableau de chaînes en fonction d’un délimiteur.</span><span class="sxs-lookup"><span data-stu-id="74f2c-273">This section does not discuss splitting a single string into a string array based on some delimiter.</span></span> <span data-ttu-id="74f2c-274">Pour plus d’informations sur le fractionnement d’une chaîne, consultez la <xref:System.String.Split%2A?displayProperty=nameWithType> méthode.</span><span class="sxs-lookup"><span data-stu-id="74f2c-274">For information on splitting a string, see the <xref:System.String.Split%2A?displayProperty=nameWithType> method.</span></span>

<span data-ttu-id="74f2c-275">Les critères les plus courants pour le fractionnement d’un tableau sont les suivants :</span><span class="sxs-lookup"><span data-stu-id="74f2c-275">The most common criteria for splitting an array are:</span></span>

- <span data-ttu-id="74f2c-276">Nombre d’éléments dans le tableau.</span><span class="sxs-lookup"><span data-stu-id="74f2c-276">The number of elements in the array.</span></span> <span data-ttu-id="74f2c-277">Par exemple, vous souhaiterez peut-être fractionner un tableau de plus d’un nombre spécifié d’éléments en plusieurs parties égales.</span><span class="sxs-lookup"><span data-stu-id="74f2c-277">For example, you might want to split an array of more than a specified number of elements into a number of approximately equal parts.</span></span> <span data-ttu-id="74f2c-278">À cet effet, vous pouvez utiliser la valeur retournée par la <xref:System.Array.Length%2A?displayProperty=nameWithType> <xref:System.Array.GetLength%2A?displayProperty=nameWithType> méthode ou.</span><span class="sxs-lookup"><span data-stu-id="74f2c-278">For this purpose, you can use the value returned by either the <xref:System.Array.Length%2A?displayProperty=nameWithType> or <xref:System.Array.GetLength%2A?displayProperty=nameWithType> method.</span></span>

- <span data-ttu-id="74f2c-279">Valeur d’un élément, qui sert de délimiteur qui indique où le tableau doit être fractionné.</span><span class="sxs-lookup"><span data-stu-id="74f2c-279">The value of an element, which serves as a delimiter that indicates where the array should be split.</span></span> <span data-ttu-id="74f2c-280">Vous pouvez rechercher une valeur spécifique en appelant les <xref:System.Array.FindIndex%2A?displayProperty=nameWithType> méthodes et <xref:System.Array.FindLastIndex%2A?displayProperty=nameWithType> .</span><span class="sxs-lookup"><span data-stu-id="74f2c-280">You can search for a specific value by calling the <xref:System.Array.FindIndex%2A?displayProperty=nameWithType> and <xref:System.Array.FindLastIndex%2A?displayProperty=nameWithType> methods.</span></span>

<span data-ttu-id="74f2c-281">Une fois que vous avez déterminé l’index ou les index auxquels le tableau doit être fractionné, vous pouvez créer les tableaux individuels en appelant la <xref:System.Array.Copy%2A?displayProperty=nameWithType> méthode.</span><span class="sxs-lookup"><span data-stu-id="74f2c-281">Once you've determined the index or indexes at which the array should be split, you can then create the individual arrays by calling the <xref:System.Array.Copy%2A?displayProperty=nameWithType> method.</span></span>

<span data-ttu-id="74f2c-282">L’exemple suivant fractionne un tableau en deux tableaux de taille approximativement égale.</span><span class="sxs-lookup"><span data-stu-id="74f2c-282">The following example splits an array into two arrays of approximately equal size.</span></span> <span data-ttu-id="74f2c-283">(Si le nombre total d’éléments de tableau est impair, le premier tableau a un élément supérieur à la seconde.)</span><span class="sxs-lookup"><span data-stu-id="74f2c-283">(If the total number of array elements is odd, the first array has one more element than the second.)</span></span>

[!code-vb[splitting-an-array-by-length](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/split1.vb)]

<span data-ttu-id="74f2c-284">L’exemple suivant fractionne un tableau de chaînes en deux tableaux en fonction de la présence d’un élément dont la valeur est « zzz », qui sert de délimiteur de tableau.</span><span class="sxs-lookup"><span data-stu-id="74f2c-284">The following example splits a string array into two arrays based on the presence of an element whose value is "zzz", which serves as the array delimiter.</span></span> <span data-ttu-id="74f2c-285">Les nouveaux tableaux n’incluent pas l’élément qui contient le délimiteur.</span><span class="sxs-lookup"><span data-stu-id="74f2c-285">The new arrays do not include the element that contains the delimiter.</span></span>

[!code-vb[splitting-an-array-by-delimiter](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/split2.vb)]

## <a name="joining-arrays"></a><span data-ttu-id="74f2c-286">Jointure de tableaux</span><span class="sxs-lookup"><span data-stu-id="74f2c-286">Joining arrays</span></span>

<span data-ttu-id="74f2c-287">Vous pouvez également combiner un certain nombre de tableaux dans un même tableau de plus grande taille.</span><span class="sxs-lookup"><span data-stu-id="74f2c-287">You can also combine a number of arrays into a single larger array.</span></span> <span data-ttu-id="74f2c-288">Pour ce faire, vous utilisez également la <xref:System.Array.Copy%2A?displayProperty=nameWithType> méthode.</span><span class="sxs-lookup"><span data-stu-id="74f2c-288">To do this, you also use the <xref:System.Array.Copy%2A?displayProperty=nameWithType> method.</span></span>

> [!NOTE]
> <span data-ttu-id="74f2c-289">Cette section n’aborde pas la jointure d’un tableau de chaînes en une seule chaîne.</span><span class="sxs-lookup"><span data-stu-id="74f2c-289">This section does not discuss joining a string array into a single string.</span></span> <span data-ttu-id="74f2c-290">Pour plus d’informations sur la jointure d’un tableau de chaînes, consultez la <xref:System.String.Join%2A?displayProperty=nameWithType> méthode.</span><span class="sxs-lookup"><span data-stu-id="74f2c-290">For information on joining a string array, see the <xref:System.String.Join%2A?displayProperty=nameWithType> method.</span></span>

<span data-ttu-id="74f2c-291">Avant de copier les éléments de chaque tableau dans le nouveau tableau, vous devez d’abord vous assurer que vous avez initialisé le tableau afin qu’il soit suffisamment grand pour accueillir le nouveau tableau.</span><span class="sxs-lookup"><span data-stu-id="74f2c-291">Before copying the elements of each array into the new array, you must first ensure that you have initialized the array so that it is large enough to accommodate the new array.</span></span> <span data-ttu-id="74f2c-292">Vous pouvez le faire de deux façons :</span><span class="sxs-lookup"><span data-stu-id="74f2c-292">You can do this in one of two ways:</span></span>

- <span data-ttu-id="74f2c-293">Utilisez l' [`ReDim Preserve`](../../../language-reference/statements/redim-statement.md) instruction pour développer dynamiquement le tableau avant d’y ajouter de nouveaux éléments.</span><span class="sxs-lookup"><span data-stu-id="74f2c-293">Use the [`ReDim Preserve`](../../../language-reference/statements/redim-statement.md) statement to dynamically expand the array before adding new elements to it.</span></span> <span data-ttu-id="74f2c-294">Il s’agit de la technique la plus simple, mais elle peut entraîner une dégradation des performances et une consommation excessive de la mémoire lorsque vous copiez de grands tableaux.</span><span class="sxs-lookup"><span data-stu-id="74f2c-294">This is the easiest technique, but it can result in performance degradation and excessive memory consumption when you are copying large arrays.</span></span>
- <span data-ttu-id="74f2c-295">Calculez le nombre total d’éléments nécessaires pour le nouveau tableau volumineux, puis ajoutez-y les éléments de chaque tableau source.</span><span class="sxs-lookup"><span data-stu-id="74f2c-295">Calculate the total number of elements needed for the new large array, then add the elements of each source array to it.</span></span>

<span data-ttu-id="74f2c-296">L’exemple suivant utilise la deuxième approche pour ajouter quatre tableaux avec dix éléments à un seul tableau.</span><span class="sxs-lookup"><span data-stu-id="74f2c-296">The following example uses the second approach to add four arrays with ten elements each to a single array.</span></span>

[!code-vb[joining-an-array](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/join.vb)]

<span data-ttu-id="74f2c-297">Étant donné que, dans ce cas, les tableaux sources sont tous petits, nous pouvons également développer le tableau de manière dynamique à mesure que nous y ajoutons les éléments de chaque nouveau tableau.</span><span class="sxs-lookup"><span data-stu-id="74f2c-297">Since in this case the source arrays are all small, we can also dynamically expand the array as we add the elements of each new array to it.</span></span> <span data-ttu-id="74f2c-298">L’exemple suivant effectue cette opération.</span><span class="sxs-lookup"><span data-stu-id="74f2c-298">The following example does that.</span></span>

[!code-vb[joining-an-array-dynamically](~/samples/snippets/visualbasic/programming-guide/language-features/arrays/join2.vb)]

## <a name="collections-as-an-alternative-to-arrays"></a><span data-ttu-id="74f2c-299">Collections comme alternative aux tableaux</span><span class="sxs-lookup"><span data-stu-id="74f2c-299">Collections as an alternative to arrays</span></span>

<span data-ttu-id="74f2c-300">Les tableaux s’avèrent particulièrement utiles pour créer et utiliser un nombre fixe d’objets fortement typés.</span><span class="sxs-lookup"><span data-stu-id="74f2c-300">Arrays are most useful for creating and working with a fixed number of strongly typed objects.</span></span> <span data-ttu-id="74f2c-301">Les collections offrent plus de souplesse quand il s’agit d’utiliser des groupes d’objets.</span><span class="sxs-lookup"><span data-stu-id="74f2c-301">Collections provide a more flexible way to work with groups of objects.</span></span> <span data-ttu-id="74f2c-302">Contrairement aux tableaux, qui requièrent que vous modifiiez explicitement la taille d’un tableau avec l' [ `ReDim` instruction](../../../language-reference/statements/redim-statement.md), les collections augmentent et diminuent de manière dynamique à mesure que les besoins d’une application changent.</span><span class="sxs-lookup"><span data-stu-id="74f2c-302">Unlike arrays, which require that you explicitly change the size of an array with the [`ReDim` Statement](../../../language-reference/statements/redim-statement.md), collections grow and shrink dynamically as the needs of an application change.</span></span>

<span data-ttu-id="74f2c-303">Lorsque vous utilisez `ReDim` pour redimensionner un tableau, Visual Basic crée un nouveau tableau et libère le précédent.</span><span class="sxs-lookup"><span data-stu-id="74f2c-303">When you use `ReDim` to redimension an array, Visual Basic creates a new array and releases the previous one.</span></span> <span data-ttu-id="74f2c-304">Cela prend du temps d’exécution.</span><span class="sxs-lookup"><span data-stu-id="74f2c-304">This takes execution time.</span></span> <span data-ttu-id="74f2c-305">Par conséquent, si le nombre d’éléments que vous utilisez change fréquemment ou si vous ne pouvez pas prédire le nombre maximal d’éléments dont vous avez besoin, vous obtiendrez généralement de meilleures performances à l’aide d’une collection.</span><span class="sxs-lookup"><span data-stu-id="74f2c-305">Therefore, if the number of items you are working with changes frequently, or you cannot predict the maximum number of items you need, you'll usually obtain better performance by using a collection.</span></span>

<span data-ttu-id="74f2c-306">Pour certaines collections, vous pouvez assigner une clé à un objet que vous placez dans la collection pour vous permettre de récupérer rapidement l’objet à l’aide de la clé.</span><span class="sxs-lookup"><span data-stu-id="74f2c-306">For some collections, you can assign a key to any object that you put into the collection so that you can quickly retrieve the object by using the key.</span></span>

<span data-ttu-id="74f2c-307">Si votre collection contient des éléments d’un seul type de données, vous pouvez utiliser une des classes dans l’espace de noms <xref:System.Collections.Generic?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="74f2c-307">If your collection contains elements of only one data type, you can use one of the classes in the <xref:System.Collections.Generic?displayProperty=nameWithType> namespace.</span></span> <span data-ttu-id="74f2c-308">Une collection générique applique la cohérence des types pour éviter qu’un autre type puisse y être ajouté.</span><span class="sxs-lookup"><span data-stu-id="74f2c-308">A generic collection enforces type safety so that no other data type can be added to it.</span></span>

<span data-ttu-id="74f2c-309">Pour plus d’informations sur les collections, consultez [Collections](../../concepts/collections.md).</span><span class="sxs-lookup"><span data-stu-id="74f2c-309">For more information about collections, see [Collections](../../concepts/collections.md).</span></span>

## <a name="related-topics"></a><span data-ttu-id="74f2c-310">Rubriques connexes</span><span class="sxs-lookup"><span data-stu-id="74f2c-310">Related topics</span></span>

|<span data-ttu-id="74f2c-311">Terme</span><span class="sxs-lookup"><span data-stu-id="74f2c-311">Term</span></span>|<span data-ttu-id="74f2c-312">Définition</span><span class="sxs-lookup"><span data-stu-id="74f2c-312">Definition</span></span>|
|----------|----------------|
|[<span data-ttu-id="74f2c-313">Array Dimensions in Visual Basic</span><span class="sxs-lookup"><span data-stu-id="74f2c-313">Array Dimensions in Visual Basic</span></span>](array-dimensions.md)|<span data-ttu-id="74f2c-314">Explique le rang et les dimensions des tableaux.</span><span class="sxs-lookup"><span data-stu-id="74f2c-314">Explains rank and dimensions in arrays.</span></span>|
|[<span data-ttu-id="74f2c-315">Comment : initialiser une variable tableau en Visual Basic</span><span class="sxs-lookup"><span data-stu-id="74f2c-315">How to: Initialize an Array Variable in Visual Basic</span></span>](how-to-initialize-an-array-variable.md)|<span data-ttu-id="74f2c-316">Explique comment remplir les tableaux de valeurs initiales.</span><span class="sxs-lookup"><span data-stu-id="74f2c-316">Describes how to populate arrays with initial values.</span></span>|
|[<span data-ttu-id="74f2c-317">Comment : trier un tableau dans Visual Basic</span><span class="sxs-lookup"><span data-stu-id="74f2c-317">How to: Sort An Array in Visual Basic</span></span>](how-to-sort-an-array.md)|<span data-ttu-id="74f2c-318">Montre comment trier les éléments d’un tableau par ordre alphabétique.</span><span class="sxs-lookup"><span data-stu-id="74f2c-318">Shows how to sort the elements of an array alphabetically.</span></span>|
|[<span data-ttu-id="74f2c-319">Comment : assigner un tableau à un autre tableau</span><span class="sxs-lookup"><span data-stu-id="74f2c-319">How to: Assign One Array to Another Array</span></span>](how-to-assign-one-array-to-another-array.md)|<span data-ttu-id="74f2c-320">Décrit les règles et les étapes à suivre pour assigner un tableau à une autre variable tableau.</span><span class="sxs-lookup"><span data-stu-id="74f2c-320">Describes the rules and steps for assigning an array to another array variable.</span></span>|
|[<span data-ttu-id="74f2c-321">Résolution des problèmes de tableaux</span><span class="sxs-lookup"><span data-stu-id="74f2c-321">Troubleshooting Arrays</span></span>](troubleshooting-arrays.md)|<span data-ttu-id="74f2c-322">Décrit des problèmes courants qui surviennent dans le cadre de l’utilisation de tableaux.</span><span class="sxs-lookup"><span data-stu-id="74f2c-322">Discusses some common problems that arise when working with arrays.</span></span>|

## <a name="see-also"></a><span data-ttu-id="74f2c-323">Voir aussi</span><span class="sxs-lookup"><span data-stu-id="74f2c-323">See also</span></span>

- <xref:System.Array?displayProperty=nameWithType>
- [<span data-ttu-id="74f2c-324">Dim (instruction)</span><span class="sxs-lookup"><span data-stu-id="74f2c-324">Dim Statement</span></span>](../../../language-reference/statements/dim-statement.md)
- [<span data-ttu-id="74f2c-325">ReDim (instruction)</span><span class="sxs-lookup"><span data-stu-id="74f2c-325">ReDim Statement</span></span>](../../../language-reference/statements/redim-statement.md)
