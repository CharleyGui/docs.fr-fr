---
title: Gestion des événements
ms.date: 07/20/2015
helpviewer_keywords:
- event handling [Visual Basic], walkthroughs
- walkthroughs [Visual Basic], event handling
- variables [Visual Basic], WithEvents
- events [Visual Basic], walkthroughs
- WithEvents keyword [Visual Basic], walkthroughs
- event handlers [Visual Basic], walkthroughs
ms.assetid: f145b3fc-5ae0-4509-a2aa-1ff6934706bd
ms.openlocfilehash: 4489f75e50a783a9b1acfb9c30568fdec6614488
ms.sourcegitcommit: bf5c5850654187705bc94cc40ebfb62fe346ab02
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 09/23/2020
ms.locfileid: "91057908"
---
# <a name="walkthrough-handling-events-visual-basic"></a><span data-ttu-id="0d340-102">Procédure pas à pas : gestion des événements (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="0d340-102">Walkthrough: Handling Events (Visual Basic)</span></span>

<span data-ttu-id="0d340-103">Il s’agit de la deuxième des deux rubriques qui montrent comment utiliser les événements.</span><span class="sxs-lookup"><span data-stu-id="0d340-103">This is the second of two topics that demonstrate how to work with events.</span></span> <span data-ttu-id="0d340-104">La première rubrique, [procédure pas à pas : déclaration et déclenchement d’événements](walkthrough-declaring-and-raising-events.md), montre comment déclarer et déclencher des événements.</span><span class="sxs-lookup"><span data-stu-id="0d340-104">The first topic, [Walkthrough: Declaring and Raising Events](walkthrough-declaring-and-raising-events.md), shows how to declare and raise events.</span></span> <span data-ttu-id="0d340-105">Cette section utilise le formulaire et la classe de cette procédure pas à pas pour montrer comment gérer les événements lorsqu’ils se produisent.</span><span class="sxs-lookup"><span data-stu-id="0d340-105">This section uses the form and class from that walkthrough to show how to handle events when they take place.</span></span>  
  
 <span data-ttu-id="0d340-106">L' `Widget` exemple de classe utilise des instructions de gestion des événements traditionnelles.</span><span class="sxs-lookup"><span data-stu-id="0d340-106">The `Widget` class example uses traditional event-handling statements.</span></span> <span data-ttu-id="0d340-107">Visual Basic fournit d’autres techniques pour l’utilisation des événements.</span><span class="sxs-lookup"><span data-stu-id="0d340-107">Visual Basic provides other techniques for working with events.</span></span> <span data-ttu-id="0d340-108">En guise d’exercice, vous pouvez modifier cet exemple pour utiliser les `AddHandler` `Handles` instructions et.</span><span class="sxs-lookup"><span data-stu-id="0d340-108">As an exercise, you can modify this example to use the `AddHandler` and `Handles` statements.</span></span>  
  
### <a name="to-handle-the-percentdone-event-of-the-widget-class"></a><span data-ttu-id="0d340-109">Pour gérer l’événement PercentDone de la classe widget</span><span class="sxs-lookup"><span data-stu-id="0d340-109">To handle the PercentDone event of the Widget class</span></span>  
  
1. <span data-ttu-id="0d340-110">Placez le code suivant dans `Form1` :</span><span class="sxs-lookup"><span data-stu-id="0d340-110">Place the following code in `Form1`:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#4](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#4)]  
  
     <span data-ttu-id="0d340-111">Le `WithEvents` mot clé spécifie que la variable `mWidget` est utilisée pour gérer les événements d’un objet.</span><span class="sxs-lookup"><span data-stu-id="0d340-111">The `WithEvents` keyword specifies that the variable `mWidget` is used to handle an object's events.</span></span> <span data-ttu-id="0d340-112">Vous spécifiez le type d’objet en fournissant le nom de la classe à partir de laquelle l’objet sera créé.</span><span class="sxs-lookup"><span data-stu-id="0d340-112">You specify the kind of object by supplying the name of the class from which the object will be created.</span></span>  
  
     <span data-ttu-id="0d340-113">La variable `mWidget` est déclarée dans `Form1` , car les `WithEvents` variables doivent être au niveau de la classe.</span><span class="sxs-lookup"><span data-stu-id="0d340-113">The variable `mWidget` is declared in `Form1` because `WithEvents` variables must be class-level.</span></span> <span data-ttu-id="0d340-114">Cela est vrai quel que soit le type de classe dans lequel vous les placez.</span><span class="sxs-lookup"><span data-stu-id="0d340-114">This is true regardless of the type of class you place them in.</span></span>  
  
     <span data-ttu-id="0d340-115">La variable `mblnCancel` est utilisée pour annuler la `LongTask` méthode.</span><span class="sxs-lookup"><span data-stu-id="0d340-115">The variable `mblnCancel` is used to cancel the `LongTask` method.</span></span>  
  
## <a name="writing-code-to-handle-an-event"></a><span data-ttu-id="0d340-116">Écriture de code pour gérer un événement</span><span class="sxs-lookup"><span data-stu-id="0d340-116">Writing Code to Handle an Event</span></span>  

 <span data-ttu-id="0d340-117">Dès que vous déclarez une variable à l’aide `WithEvents` de, le nom de la variable apparaît dans la liste déroulante de gauche de l' **éditeur de code**de la classe.</span><span class="sxs-lookup"><span data-stu-id="0d340-117">As soon as you declare a variable using `WithEvents`, the variable name appears in the left drop-down list of the class's **Code Editor**.</span></span> <span data-ttu-id="0d340-118">Lorsque vous sélectionnez `mWidget` , les `Widget` événements de la classe s’affichent dans la liste déroulante de droite.</span><span class="sxs-lookup"><span data-stu-id="0d340-118">When you select `mWidget`, the `Widget` class's events appear in the right drop-down list.</span></span> <span data-ttu-id="0d340-119">La sélection d’un événement affiche la procédure d’événement correspondante, avec le préfixe `mWidget` et un trait de soulignement.</span><span class="sxs-lookup"><span data-stu-id="0d340-119">Selecting an event displays the corresponding event procedure, with the prefix `mWidget` and an underscore.</span></span> <span data-ttu-id="0d340-120">Toutes les procédures d’événement associées à une `WithEvents` variable reçoivent le nom de la variable en tant que préfixe.</span><span class="sxs-lookup"><span data-stu-id="0d340-120">All the event procedures associated with a `WithEvents` variable are given the variable name as a prefix.</span></span>  
  
#### <a name="to-handle-an-event"></a><span data-ttu-id="0d340-121">Pour gérer un événement</span><span class="sxs-lookup"><span data-stu-id="0d340-121">To handle an event</span></span>  
  
1. <span data-ttu-id="0d340-122">Sélectionnez dans `mWidget` la liste déroulante de gauche dans l' **éditeur de code**.</span><span class="sxs-lookup"><span data-stu-id="0d340-122">Select `mWidget` from the left drop-down list in the **Code Editor**.</span></span>  
  
2. <span data-ttu-id="0d340-123">Sélectionnez l' `PercentDone` événement dans la liste déroulante de droite.</span><span class="sxs-lookup"><span data-stu-id="0d340-123">Select the `PercentDone` event from the right drop-down list.</span></span> <span data-ttu-id="0d340-124">L' **éditeur de code** ouvre la `mWidget_PercentDone` procédure événementielle.</span><span class="sxs-lookup"><span data-stu-id="0d340-124">The **Code Editor** opens the `mWidget_PercentDone` event procedure.</span></span>  
  
    > [!NOTE]
    > <span data-ttu-id="0d340-125">L' **éditeur de code** est utile, mais pas obligatoire, pour l’insertion de nouveaux gestionnaires d’événements.</span><span class="sxs-lookup"><span data-stu-id="0d340-125">The **Code Editor** is useful, but not required, for inserting new event handlers.</span></span> <span data-ttu-id="0d340-126">Dans cette procédure pas à pas, il est plus direct de copier uniquement les gestionnaires d’événements directement dans votre code.</span><span class="sxs-lookup"><span data-stu-id="0d340-126">In this walkthrough, it is more direct to just copy the event handlers directly into your code.</span></span>  
  
3. <span data-ttu-id="0d340-127">Ajoutez le code suivant au gestionnaire d'événements `mWidget_PercentDone` :</span><span class="sxs-lookup"><span data-stu-id="0d340-127">Add the following code to the `mWidget_PercentDone` event handler:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#5](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#5)]  
  
     <span data-ttu-id="0d340-128">Chaque fois que l' `PercentDone` événement est déclenché, la procédure événementielle affiche le pourcentage effectué dans un `Label` contrôle.</span><span class="sxs-lookup"><span data-stu-id="0d340-128">Whenever the `PercentDone` event is raised, the event procedure displays the percent complete in a `Label` control.</span></span> <span data-ttu-id="0d340-129">La `DoEvents` méthode permet à l’étiquette de repeindre et donne également à l’utilisateur la possibilité de cliquer sur le bouton **Annuler** .</span><span class="sxs-lookup"><span data-stu-id="0d340-129">The `DoEvents` method allows the label to repaint, and also gives the user the opportunity to click the **Cancel** button.</span></span>  
  
4. <span data-ttu-id="0d340-130">Ajoutez le code suivant pour le `Button2_Click` Gestionnaire d’événements :</span><span class="sxs-lookup"><span data-stu-id="0d340-130">Add the following code for the `Button2_Click` event handler:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#6](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#6)]  
  
 <span data-ttu-id="0d340-131">Si l’utilisateur clique sur le bouton **Annuler** pendant que `LongTask` est en cours d’exécution, l' `Button2_Click` événement est exécuté dès que l' `DoEvents` instruction autorise le traitement de l’événement.</span><span class="sxs-lookup"><span data-stu-id="0d340-131">If the user clicks the **Cancel** button while `LongTask` is running, the `Button2_Click` event is executed as soon as the `DoEvents` statement allows event processing to occur.</span></span> <span data-ttu-id="0d340-132">La variable au niveau de la classe `mblnCancel` a la valeur `True` , et l' `mWidget_PercentDone` événement la teste, puis définit l' `ByRef Cancel` argument sur `True` .</span><span class="sxs-lookup"><span data-stu-id="0d340-132">The class-level variable `mblnCancel` is set to `True`, and the `mWidget_PercentDone` event then tests it and sets the `ByRef Cancel` argument to `True`.</span></span>  
  
## <a name="connecting-a-withevents-variable-to-an-object"></a><span data-ttu-id="0d340-133">Connexion d’une variable WithEvents à un objet</span><span class="sxs-lookup"><span data-stu-id="0d340-133">Connecting a WithEvents Variable to an Object</span></span>  

 <span data-ttu-id="0d340-134">`Form1` est maintenant configuré pour gérer les `Widget` événements d’un objet.</span><span class="sxs-lookup"><span data-stu-id="0d340-134">`Form1` is now set up to handle a `Widget` object's events.</span></span> <span data-ttu-id="0d340-135">Il ne reste plus qu’à trouver `Widget` quelque part.</span><span class="sxs-lookup"><span data-stu-id="0d340-135">All that remains is to find a `Widget` somewhere.</span></span>  
  
 <span data-ttu-id="0d340-136">Quand vous déclarez une variable `WithEvents` au moment de la conception, aucun objet n’est associé à celle-ci.</span><span class="sxs-lookup"><span data-stu-id="0d340-136">When you declare a variable `WithEvents` at design time, no object is associated with it.</span></span> <span data-ttu-id="0d340-137">Une `WithEvents` variable est semblable à toute autre variable objet.</span><span class="sxs-lookup"><span data-stu-id="0d340-137">A `WithEvents` variable is just like any other object variable.</span></span> <span data-ttu-id="0d340-138">Vous devez créer un objet et lui assigner une référence avec la `WithEvents` variable.</span><span class="sxs-lookup"><span data-stu-id="0d340-138">You have to create an object and assign a reference to it with the `WithEvents` variable.</span></span>  
  
#### <a name="to-create-an-object-and-assign-a-reference-to-it"></a><span data-ttu-id="0d340-139">Pour créer un objet et lui assigner une référence</span><span class="sxs-lookup"><span data-stu-id="0d340-139">To create an object and assign a reference to it</span></span>  
  
1. <span data-ttu-id="0d340-140">Sélectionnez **(événements Form1)** dans la liste déroulante de gauche de l' **éditeur de code**.</span><span class="sxs-lookup"><span data-stu-id="0d340-140">Select **(Form1 Events)** from the left drop-down list in the **Code Editor**.</span></span>  
  
2. <span data-ttu-id="0d340-141">Sélectionnez l' `Load` événement dans la liste déroulante de droite.</span><span class="sxs-lookup"><span data-stu-id="0d340-141">Select the `Load` event from the right drop-down list.</span></span> <span data-ttu-id="0d340-142">L' **éditeur de code** ouvre la `Form1_Load` procédure événementielle.</span><span class="sxs-lookup"><span data-stu-id="0d340-142">The **Code Editor** opens the `Form1_Load` event procedure.</span></span>  
  
3. <span data-ttu-id="0d340-143">Ajoutez le code suivant pour la `Form1_Load` procédure événementielle afin de créer le `Widget` :</span><span class="sxs-lookup"><span data-stu-id="0d340-143">Add the following code for the `Form1_Load` event procedure to create the `Widget`:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#7](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#7)]  
  
 <span data-ttu-id="0d340-144">Lorsque ce code s’exécute, Visual Basic crée un `Widget` objet et connecte ses événements aux procédures d’événement associées à `mWidget` .</span><span class="sxs-lookup"><span data-stu-id="0d340-144">When this code executes, Visual Basic creates a `Widget` object and connects its events to the event procedures associated with `mWidget`.</span></span> <span data-ttu-id="0d340-145">À partir de là, chaque fois que le `Widget` déclenche son `PercentDone` événement, la `mWidget_PercentDone` procédure événementielle est exécutée.</span><span class="sxs-lookup"><span data-stu-id="0d340-145">From that point on, whenever the `Widget` raises its `PercentDone` event, the `mWidget_PercentDone` event procedure is executed.</span></span>  
  
#### <a name="to-call-the-longtask-method"></a><span data-ttu-id="0d340-146">Pour appeler la méthode LongTask</span><span class="sxs-lookup"><span data-stu-id="0d340-146">To call the LongTask method</span></span>  
  
- <span data-ttu-id="0d340-147">Ajoutez le code suivant au gestionnaire d'événements `Button1_Click` :</span><span class="sxs-lookup"><span data-stu-id="0d340-147">Add the following code to the `Button1_Click` event handler:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#8](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#8)]  
  
 <span data-ttu-id="0d340-148">Avant que la `LongTask` méthode ne soit appelée, l’étiquette qui affiche le pourcentage d’achèvement doit être initialisée et l' `Boolean` indicateur de niveau classe pour annuler la méthode doit avoir la valeur `False` .</span><span class="sxs-lookup"><span data-stu-id="0d340-148">Before the `LongTask` method is called, the label that displays the percent complete must be initialized, and the class-level `Boolean` flag for canceling the method must be set to `False`.</span></span>  
  
 <span data-ttu-id="0d340-149">`LongTask` est appelé avec une durée de tâche de 12,2 secondes.</span><span class="sxs-lookup"><span data-stu-id="0d340-149">`LongTask` is called with a task duration of 12.2 seconds.</span></span> <span data-ttu-id="0d340-150">L' `PercentDone` événement est déclenché une fois tous les tiers un seconde.</span><span class="sxs-lookup"><span data-stu-id="0d340-150">The `PercentDone` event is raised once every one-third of a second.</span></span> <span data-ttu-id="0d340-151">Chaque fois que l’événement est déclenché, la `mWidget_PercentDone` procédure événementielle est exécutée.</span><span class="sxs-lookup"><span data-stu-id="0d340-151">Each time the event is raised, the `mWidget_PercentDone` event procedure is executed.</span></span>  
  
 <span data-ttu-id="0d340-152">Lorsque `LongTask` est terminé, `mblnCancel` est testé pour voir si l’opération s’est `LongTask` terminée normalement ou si elle s’est arrêtée car `mblnCancel` a la valeur `True` .</span><span class="sxs-lookup"><span data-stu-id="0d340-152">When `LongTask` is done, `mblnCancel` is tested to see if `LongTask` ended normally, or if it stopped because `mblnCancel` was set to `True`.</span></span> <span data-ttu-id="0d340-153">Le pourcentage d’achèvement est mis à jour uniquement dans le cas précédent.</span><span class="sxs-lookup"><span data-stu-id="0d340-153">The percent complete is updated only in the former case.</span></span>  
  
#### <a name="to-run-the-program"></a><span data-ttu-id="0d340-154">Pour exécuter le programme</span><span class="sxs-lookup"><span data-stu-id="0d340-154">To run the program</span></span>  
  
1. <span data-ttu-id="0d340-155">Appuyez sur F5 pour mettre le projet en mode exécution.</span><span class="sxs-lookup"><span data-stu-id="0d340-155">Press F5 to put the project in run mode.</span></span>  
  
2. <span data-ttu-id="0d340-156">Cliquez sur le bouton **Démarrer la tâche** .</span><span class="sxs-lookup"><span data-stu-id="0d340-156">Click the **Start Task** button.</span></span> <span data-ttu-id="0d340-157">Chaque fois `PercentDone` que l’événement est déclenché, l’étiquette est mise à jour avec le pourcentage de la tâche qui est terminé.</span><span class="sxs-lookup"><span data-stu-id="0d340-157">Each time the `PercentDone` event is raised, the label is updated with the percentage of the task that is complete.</span></span>  
  
3. <span data-ttu-id="0d340-158">Cliquez sur le bouton **Annuler** pour arrêter la tâche.</span><span class="sxs-lookup"><span data-stu-id="0d340-158">Click the **Cancel** button to stop the task.</span></span> <span data-ttu-id="0d340-159">Notez que l’apparence du bouton **Annuler** ne change pas immédiatement lorsque vous cliquez dessus.</span><span class="sxs-lookup"><span data-stu-id="0d340-159">Notice that the appearance of the **Cancel** button does not change immediately when you click it.</span></span> <span data-ttu-id="0d340-160">L' `Click` événement ne peut pas se produire tant que l’instruction n’autorise pas le `My.Application.DoEvents` traitement des événements.</span><span class="sxs-lookup"><span data-stu-id="0d340-160">The `Click` event cannot happen until the `My.Application.DoEvents` statement allows event processing.</span></span>  
  
    > [!NOTE]
    > <span data-ttu-id="0d340-161">La `My.Application.DoEvents` méthode ne traite pas les événements exactement de la même façon que le formulaire.</span><span class="sxs-lookup"><span data-stu-id="0d340-161">The `My.Application.DoEvents` method does not process events in exactly the same way as the form does.</span></span> <span data-ttu-id="0d340-162">Par exemple, dans cette procédure pas à pas, vous devez cliquer deux fois sur le bouton **Annuler** .</span><span class="sxs-lookup"><span data-stu-id="0d340-162">For example, in this walkthrough, you must click the **Cancel** button twice.</span></span> <span data-ttu-id="0d340-163">Pour permettre au formulaire de gérer directement les événements, vous pouvez utiliser le multithreading.</span><span class="sxs-lookup"><span data-stu-id="0d340-163">To allow the form to handle the events directly, you can use multithreading.</span></span> <span data-ttu-id="0d340-164">Pour plus d’informations, consultez [threads managés](../../../../standard/threading/index.md).</span><span class="sxs-lookup"><span data-stu-id="0d340-164">For more information, see [Managed Threading](../../../../standard/threading/index.md).</span></span>
  
 <span data-ttu-id="0d340-165">Il peut être intéressant d’exécuter le programme avec F11 et de parcourir le code une ligne à la fois.</span><span class="sxs-lookup"><span data-stu-id="0d340-165">You may find it instructive to run the program with F11 and step through the code a line at a time.</span></span> <span data-ttu-id="0d340-166">Vous pouvez voir clairement la façon dont l’exécution entre `LongTask` en vigueur, puis entrer brièvement `Form1` chaque fois que l' `PercentDone` événement est déclenché.</span><span class="sxs-lookup"><span data-stu-id="0d340-166">You can clearly see how execution enters `LongTask`, and then briefly re-enters `Form1` each time the `PercentDone` event is raised.</span></span>  
  
 <span data-ttu-id="0d340-167">Que se passerait-il si, alors que l’exécution était de retour dans le code de `Form1` , la `LongTask` méthode était appelée à nouveau ?</span><span class="sxs-lookup"><span data-stu-id="0d340-167">What would happen if, while execution was back in the code of `Form1`, the `LongTask` method were called again?</span></span> <span data-ttu-id="0d340-168">Au pire, un dépassement de capacité de la pile peut se produire si `LongTask` a été appelé chaque fois que l’événement a été déclenché.</span><span class="sxs-lookup"><span data-stu-id="0d340-168">At worst, a stack overflow might occur if `LongTask` were called every time the event was raised.</span></span>  
  
 <span data-ttu-id="0d340-169">Vous pouvez faire en sorte que la variable `mWidget` gère les événements d’un autre `Widget` objet en affectant une référence au nouveau `Widget` à `mWidget` .</span><span class="sxs-lookup"><span data-stu-id="0d340-169">You can cause the variable `mWidget` to handle events for a different `Widget` object by assigning a reference to the new `Widget` to `mWidget`.</span></span> <span data-ttu-id="0d340-170">En fait, vous pouvez faire en sorte que le code effectue `Button1_Click` cette opération chaque fois que vous cliquez sur le bouton.</span><span class="sxs-lookup"><span data-stu-id="0d340-170">In fact, you can make the code in `Button1_Click` do this every time you click the button.</span></span>  
  
#### <a name="to-handle-events-for-a-different-widget"></a><span data-ttu-id="0d340-171">Pour gérer les événements d’un autre widget</span><span class="sxs-lookup"><span data-stu-id="0d340-171">To handle events for a different widget</span></span>  
  
- <span data-ttu-id="0d340-172">Ajoutez la ligne de code suivante à la `Button1_Click` procédure, juste avant la ligne qui indique `mWidget.LongTask(12.2, 0.33)` :</span><span class="sxs-lookup"><span data-stu-id="0d340-172">Add the following line of code to the `Button1_Click` procedure, immediately preceding the line that reads `mWidget.LongTask(12.2, 0.33)`:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#9](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#9)]  
  
 <span data-ttu-id="0d340-173">Le code ci-dessus crée un nouveau `Widget` chaque fois que l’utilisateur clique sur le bouton.</span><span class="sxs-lookup"><span data-stu-id="0d340-173">The code above creates a new `Widget` each time the button is clicked.</span></span> <span data-ttu-id="0d340-174">Dès que la `LongTask` méthode se termine, la référence à `Widget` est libérée et `Widget` est détruite.</span><span class="sxs-lookup"><span data-stu-id="0d340-174">As soon as the `LongTask` method completes, the reference to the `Widget` is released, and the `Widget` is destroyed.</span></span>  
  
 <span data-ttu-id="0d340-175">Une `WithEvents` variable ne peut contenir qu’une seule référence d’objet à la fois. par conséquent, si vous assignez un autre `Widget` objet à `mWidget` , les `Widget` événements de l’objet précédent ne seront plus gérés.</span><span class="sxs-lookup"><span data-stu-id="0d340-175">A `WithEvents` variable can contain only one object reference at a time, so if you assign a different `Widget` object to `mWidget`, the previous `Widget` object's events will no longer be handled.</span></span> <span data-ttu-id="0d340-176">Si `mWidget` est la seule variable objet contenant une référence à l’ancien `Widget` , l’objet est détruit.</span><span class="sxs-lookup"><span data-stu-id="0d340-176">If `mWidget` is the only object variable containing a reference to the old `Widget`, the object is destroyed.</span></span> <span data-ttu-id="0d340-177">Si vous souhaitez gérer les événements de plusieurs `Widget` objets, utilisez l' `AddHandler` instruction pour traiter les événements de chaque objet séparément.</span><span class="sxs-lookup"><span data-stu-id="0d340-177">If you want to handle events from several `Widget` objects, use the `AddHandler` statement to process events from each object separately.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="0d340-178">Vous pouvez déclarer autant `WithEvents` de variables que vous le souhaitez, mais les tableaux de `WithEvents` variables ne sont pas pris en charge.</span><span class="sxs-lookup"><span data-stu-id="0d340-178">You can declare as many `WithEvents` variables as you need, but arrays of `WithEvents` variables are not supported.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="0d340-179">Voir aussi</span><span class="sxs-lookup"><span data-stu-id="0d340-179">See also</span></span>

- [<span data-ttu-id="0d340-180">Procédure pas à pas : déclaration et déclenchement des événements</span><span class="sxs-lookup"><span data-stu-id="0d340-180">Walkthrough: Declaring and Raising Events</span></span>](walkthrough-declaring-and-raising-events.md)
- [<span data-ttu-id="0d340-181">Événements</span><span class="sxs-lookup"><span data-stu-id="0d340-181">Events</span></span>](index.md)
