---
title: Gestion des événements
ms.date: 07/20/2015
helpviewer_keywords:
- event handling [Visual Basic], walkthroughs
- walkthroughs [Visual Basic], event handling
- variables [Visual Basic], WithEvents
- events [Visual Basic], walkthroughs
- WithEvents keyword [Visual Basic], walkthroughs
- event handlers [Visual Basic], walkthroughs
ms.assetid: f145b3fc-5ae0-4509-a2aa-1ff6934706bd
ms.openlocfilehash: cb42f2e41e366cf8765cb9395d1a5641434bab40
ms.sourcegitcommit: 17ee6605e01ef32506f8fdc686954244ba6911de
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 11/22/2019
ms.locfileid: "74345082"
---
# <a name="walkthrough-handling-events-visual-basic"></a><span data-ttu-id="9f9ff-102">Procédure pas à pas : gestion des événements (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="9f9ff-102">Walkthrough: Handling Events (Visual Basic)</span></span>
<span data-ttu-id="9f9ff-103">Il s’agit de la deuxième des deux rubriques qui montrent comment utiliser les événements.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-103">This is the second of two topics that demonstrate how to work with events.</span></span> <span data-ttu-id="9f9ff-104">La première rubrique, [procédure pas à pas : déclaration et déclenchement d’événements](../../../../visual-basic/programming-guide/language-features/events/walkthrough-declaring-and-raising-events.md), montre comment déclarer et déclencher des événements.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-104">The first topic, [Walkthrough: Declaring and Raising Events](../../../../visual-basic/programming-guide/language-features/events/walkthrough-declaring-and-raising-events.md), shows how to declare and raise events.</span></span> <span data-ttu-id="9f9ff-105">Cette section utilise le formulaire et la classe de cette procédure pas à pas pour montrer comment gérer les événements lorsqu’ils se produisent.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-105">This section uses the form and class from that walkthrough to show how to handle events when they take place.</span></span>  
  
 <span data-ttu-id="9f9ff-106">L’exemple de classe `Widget` utilise des instructions de gestion des événements traditionnelles.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-106">The `Widget` class example uses traditional event-handling statements.</span></span> <span data-ttu-id="9f9ff-107">Visual Basic fournit d’autres techniques pour l’utilisation des événements.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-107">Visual Basic provides other techniques for working with events.</span></span> <span data-ttu-id="9f9ff-108">En guise d’exercice, vous pouvez modifier cet exemple pour utiliser les instructions `AddHandler` et `Handles`.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-108">As an exercise, you can modify this example to use the `AddHandler` and `Handles` statements.</span></span>  
  
### <a name="to-handle-the-percentdone-event-of-the-widget-class"></a><span data-ttu-id="9f9ff-109">Pour gérer l’événement PercentDone de la classe widget</span><span class="sxs-lookup"><span data-stu-id="9f9ff-109">To handle the PercentDone event of the Widget class</span></span>  
  
1. <span data-ttu-id="9f9ff-110">Placez le code suivant dans `Form1`:</span><span class="sxs-lookup"><span data-stu-id="9f9ff-110">Place the following code in `Form1`:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#4](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#4)]  
  
     <span data-ttu-id="9f9ff-111">Le mot clé `WithEvents` spécifie que la variable `mWidget` est utilisée pour gérer les événements d’un objet.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-111">The `WithEvents` keyword specifies that the variable `mWidget` is used to handle an object's events.</span></span> <span data-ttu-id="9f9ff-112">Vous spécifiez le type d’objet en fournissant le nom de la classe à partir de laquelle l’objet sera créé.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-112">You specify the kind of object by supplying the name of the class from which the object will be created.</span></span>  
  
     <span data-ttu-id="9f9ff-113">La variable `mWidget` est déclarée dans `Form1`, car `WithEvents` variables doivent être au niveau de la classe.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-113">The variable `mWidget` is declared in `Form1` because `WithEvents` variables must be class-level.</span></span> <span data-ttu-id="9f9ff-114">Cela est vrai quel que soit le type de classe dans lequel vous les placez.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-114">This is true regardless of the type of class you place them in.</span></span>  
  
     <span data-ttu-id="9f9ff-115">La variable `mblnCancel` est utilisée pour annuler la méthode `LongTask`.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-115">The variable `mblnCancel` is used to cancel the `LongTask` method.</span></span>  
  
## <a name="writing-code-to-handle-an-event"></a><span data-ttu-id="9f9ff-116">Écriture de code pour gérer un événement</span><span class="sxs-lookup"><span data-stu-id="9f9ff-116">Writing Code to Handle an Event</span></span>  
 <span data-ttu-id="9f9ff-117">Dès que vous déclarez une variable à l’aide de `WithEvents`, le nom de la variable apparaît dans la liste déroulante de gauche de l' **éditeur de code**de la classe.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-117">As soon as you declare a variable using `WithEvents`, the variable name appears in the left drop-down list of the class's **Code Editor**.</span></span> <span data-ttu-id="9f9ff-118">Lorsque vous sélectionnez `mWidget`, les événements de la classe `Widget` s’affichent dans la liste déroulante de droite.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-118">When you select `mWidget`, the `Widget` class's events appear in the right drop-down list.</span></span> <span data-ttu-id="9f9ff-119">La sélection d’un événement affiche la procédure d’événement correspondante, avec le préfixe `mWidget` et un trait de soulignement.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-119">Selecting an event displays the corresponding event procedure, with the prefix `mWidget` and an underscore.</span></span> <span data-ttu-id="9f9ff-120">Toutes les procédures événementielles associées à une variable `WithEvents` reçoivent le nom de la variable en tant que préfixe.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-120">All the event procedures associated with a `WithEvents` variable are given the variable name as a prefix.</span></span>  
  
#### <a name="to-handle-an-event"></a><span data-ttu-id="9f9ff-121">Pour gérer un événement</span><span class="sxs-lookup"><span data-stu-id="9f9ff-121">To handle an event</span></span>  
  
1. <span data-ttu-id="9f9ff-122">Sélectionnez `mWidget` dans la liste déroulante de gauche dans l' **éditeur de code**.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-122">Select `mWidget` from the left drop-down list in the **Code Editor**.</span></span>  
  
2. <span data-ttu-id="9f9ff-123">Sélectionnez l’événement `PercentDone` dans la liste déroulante de droite.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-123">Select the `PercentDone` event from the right drop-down list.</span></span> <span data-ttu-id="9f9ff-124">L' **éditeur de code** ouvre la procédure d’événement `mWidget_PercentDone`.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-124">The **Code Editor** opens the `mWidget_PercentDone` event procedure.</span></span>  
  
    > [!NOTE]
    > <span data-ttu-id="9f9ff-125">L' **éditeur de code** est utile, mais pas obligatoire, pour l’insertion de nouveaux gestionnaires d’événements.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-125">The **Code Editor** is useful, but not required, for inserting new event handlers.</span></span> <span data-ttu-id="9f9ff-126">Dans cette procédure pas à pas, il est plus direct de copier uniquement les gestionnaires d’événements directement dans votre code.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-126">In this walkthrough, it is more direct to just copy the event handlers directly into your code.</span></span>  
  
3. <span data-ttu-id="9f9ff-127">Ajoutez le code suivant au gestionnaire d'événements `mWidget_PercentDone` :</span><span class="sxs-lookup"><span data-stu-id="9f9ff-127">Add the following code to the `mWidget_PercentDone` event handler:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#5](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#5)]  
  
     <span data-ttu-id="9f9ff-128">Chaque fois que l’événement `PercentDone` est déclenché, la procédure événementielle affiche le pourcentage effectué dans un contrôle `Label`.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-128">Whenever the `PercentDone` event is raised, the event procedure displays the percent complete in a `Label` control.</span></span> <span data-ttu-id="9f9ff-129">La méthode `DoEvents` permet à l’étiquette de repeindre, et donne également à l’utilisateur la possibilité de cliquer sur le bouton **Annuler** .</span><span class="sxs-lookup"><span data-stu-id="9f9ff-129">The `DoEvents` method allows the label to repaint, and also gives the user the opportunity to click the **Cancel** button.</span></span>  
  
4. <span data-ttu-id="9f9ff-130">Ajoutez le code suivant pour le gestionnaire d’événements `Button2_Click` :</span><span class="sxs-lookup"><span data-stu-id="9f9ff-130">Add the following code for the `Button2_Click` event handler:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#6](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#6)]  
  
 <span data-ttu-id="9f9ff-131">Si l’utilisateur clique sur le bouton **Annuler** pendant que `LongTask` est en cours d’exécution, l’événement `Button2_Click` est exécuté dès que l’instruction `DoEvents` permet au traitement des événements de se produire.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-131">If the user clicks the **Cancel** button while `LongTask` is running, the `Button2_Click` event is executed as soon as the `DoEvents` statement allows event processing to occur.</span></span> <span data-ttu-id="9f9ff-132">La variable de niveau classe `mblnCancel` est définie sur `True`, et l’événement `mWidget_PercentDone` le teste et définit l’argument `ByRef Cancel` sur `True`.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-132">The class-level variable `mblnCancel` is set to `True`, and the `mWidget_PercentDone` event then tests it and sets the `ByRef Cancel` argument to `True`.</span></span>  
  
## <a name="connecting-a-withevents-variable-to-an-object"></a><span data-ttu-id="9f9ff-133">Connexion d’une variable WithEvents à un objet</span><span class="sxs-lookup"><span data-stu-id="9f9ff-133">Connecting a WithEvents Variable to an Object</span></span>  
 <span data-ttu-id="9f9ff-134">`Form1` est maintenant configuré pour gérer les événements d’un objet `Widget`.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-134">`Form1` is now set up to handle a `Widget` object's events.</span></span> <span data-ttu-id="9f9ff-135">Il ne reste plus qu’à trouver un `Widget` quelque part.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-135">All that remains is to find a `Widget` somewhere.</span></span>  
  
 <span data-ttu-id="9f9ff-136">Quand vous déclarez une variable `WithEvents` au moment de la conception, aucun objet n’est associé à celle-ci.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-136">When you declare a variable `WithEvents` at design time, no object is associated with it.</span></span> <span data-ttu-id="9f9ff-137">Une variable `WithEvents` se présente comme n’importe quelle autre variable objet.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-137">A `WithEvents` variable is just like any other object variable.</span></span> <span data-ttu-id="9f9ff-138">Vous devez créer un objet et lui assigner une référence avec la variable `WithEvents`.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-138">You have to create an object and assign a reference to it with the `WithEvents` variable.</span></span>  
  
#### <a name="to-create-an-object-and-assign-a-reference-to-it"></a><span data-ttu-id="9f9ff-139">Pour créer un objet et lui assigner une référence</span><span class="sxs-lookup"><span data-stu-id="9f9ff-139">To create an object and assign a reference to it</span></span>  
  
1. <span data-ttu-id="9f9ff-140">Sélectionnez **(événements Form1)** dans la liste déroulante de gauche de l' **éditeur de code**.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-140">Select **(Form1 Events)** from the left drop-down list in the **Code Editor**.</span></span>  
  
2. <span data-ttu-id="9f9ff-141">Sélectionnez l’événement `Load` dans la liste déroulante de droite.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-141">Select the `Load` event from the right drop-down list.</span></span> <span data-ttu-id="9f9ff-142">L' **éditeur de code** ouvre la procédure d’événement `Form1_Load`.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-142">The **Code Editor** opens the `Form1_Load` event procedure.</span></span>  
  
3. <span data-ttu-id="9f9ff-143">Ajoutez le code suivant pour la procédure d’événement `Form1_Load` pour créer l' `Widget`:</span><span class="sxs-lookup"><span data-stu-id="9f9ff-143">Add the following code for the `Form1_Load` event procedure to create the `Widget`:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#7](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#7)]  
  
 <span data-ttu-id="9f9ff-144">Lorsque ce code s’exécute, Visual Basic crée un objet `Widget` et connecte ses événements aux procédures d’événements associées à `mWidget`.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-144">When this code executes, Visual Basic creates a `Widget` object and connects its events to the event procedures associated with `mWidget`.</span></span> <span data-ttu-id="9f9ff-145">À partir de là, chaque fois que le `Widget` déclenche son événement `PercentDone`, la procédure d’événement `mWidget_PercentDone` est exécutée.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-145">From that point on, whenever the `Widget` raises its `PercentDone` event, the `mWidget_PercentDone` event procedure is executed.</span></span>  
  
#### <a name="to-call-the-longtask-method"></a><span data-ttu-id="9f9ff-146">Pour appeler la méthode LongTask</span><span class="sxs-lookup"><span data-stu-id="9f9ff-146">To call the LongTask method</span></span>  
  
- <span data-ttu-id="9f9ff-147">Ajoutez le code suivant au gestionnaire d'événements `Button1_Click` :</span><span class="sxs-lookup"><span data-stu-id="9f9ff-147">Add the following code to the `Button1_Click` event handler:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#8](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#8)]  
  
 <span data-ttu-id="9f9ff-148">Avant que la méthode `LongTask` soit appelée, l’étiquette qui affiche le pourcentage d’achèvement doit être initialisée et l’indicateur de `Boolean` au niveau de la classe pour annuler la méthode doit avoir la valeur `False`.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-148">Before the `LongTask` method is called, the label that displays the percent complete must be initialized, and the class-level `Boolean` flag for canceling the method must be set to `False`.</span></span>  
  
 <span data-ttu-id="9f9ff-149">`LongTask` est appelée avec une durée de tâche de 12,2 secondes.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-149">`LongTask` is called with a task duration of 12.2 seconds.</span></span> <span data-ttu-id="9f9ff-150">L’événement `PercentDone` est déclenché une fois tous les tiers d’une seconde.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-150">The `PercentDone` event is raised once every one-third of a second.</span></span> <span data-ttu-id="9f9ff-151">Chaque fois que l’événement est déclenché, la procédure d’événement `mWidget_PercentDone` est exécutée.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-151">Each time the event is raised, the `mWidget_PercentDone` event procedure is executed.</span></span>  
  
 <span data-ttu-id="9f9ff-152">Une fois `LongTask` terminé, `mblnCancel` est testé pour vérifier si `LongTask` s’est terminé normalement, ou s’il s’est arrêté en raison de l' `mblnCancel` a été défini sur `True`.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-152">When `LongTask` is done, `mblnCancel` is tested to see if `LongTask` ended normally, or if it stopped because `mblnCancel` was set to `True`.</span></span> <span data-ttu-id="9f9ff-153">Le pourcentage d’achèvement est mis à jour uniquement dans le cas précédent.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-153">The percent complete is updated only in the former case.</span></span>  
  
#### <a name="to-run-the-program"></a><span data-ttu-id="9f9ff-154">Pour exécuter le programme</span><span class="sxs-lookup"><span data-stu-id="9f9ff-154">To run the program</span></span>  
  
1. <span data-ttu-id="9f9ff-155">Appuyez sur F5 pour mettre le projet en mode exécution.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-155">Press F5 to put the project in run mode.</span></span>  
  
2. <span data-ttu-id="9f9ff-156">Cliquez sur le bouton **Démarrer la tâche** .</span><span class="sxs-lookup"><span data-stu-id="9f9ff-156">Click the **Start Task** button.</span></span> <span data-ttu-id="9f9ff-157">Chaque fois que l’événement `PercentDone` est déclenché, l’étiquette est mise à jour avec le pourcentage de la tâche qui est terminé.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-157">Each time the `PercentDone` event is raised, the label is updated with the percentage of the task that is complete.</span></span>  
  
3. <span data-ttu-id="9f9ff-158">Cliquez sur le bouton **Annuler** pour arrêter la tâche.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-158">Click the **Cancel** button to stop the task.</span></span> <span data-ttu-id="9f9ff-159">Notez que l’apparence du bouton **Annuler** ne change pas immédiatement lorsque vous cliquez dessus.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-159">Notice that the appearance of the **Cancel** button does not change immediately when you click it.</span></span> <span data-ttu-id="9f9ff-160">L’événement `Click` ne peut pas se produire tant que l’instruction `My.Application.DoEvents` autorise le traitement des événements.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-160">The `Click` event cannot happen until the `My.Application.DoEvents` statement allows event processing.</span></span>  
  
    > [!NOTE]
    > <span data-ttu-id="9f9ff-161">La méthode `My.Application.DoEvents` ne traite pas les événements exactement de la même façon que le formulaire.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-161">The `My.Application.DoEvents` method does not process events in exactly the same way as the form does.</span></span> <span data-ttu-id="9f9ff-162">Par exemple, dans cette procédure pas à pas, vous devez cliquer deux fois sur le bouton **Annuler** .</span><span class="sxs-lookup"><span data-stu-id="9f9ff-162">For example, in this walkthrough, you must click the **Cancel** button twice.</span></span> <span data-ttu-id="9f9ff-163">Pour permettre au formulaire de gérer directement les événements, vous pouvez utiliser le multithreading.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-163">To allow the form to handle the events directly, you can use multithreading.</span></span> <span data-ttu-id="9f9ff-164">Pour plus d’informations, consultez [threads managés](../../../../standard/threading/index.md).</span><span class="sxs-lookup"><span data-stu-id="9f9ff-164">For more information, see [Managed Threading](../../../../standard/threading/index.md).</span></span>
  
 <span data-ttu-id="9f9ff-165">Il peut être intéressant d’exécuter le programme avec F11 et de parcourir le code une ligne à la fois.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-165">You may find it instructive to run the program with F11 and step through the code a line at a time.</span></span> <span data-ttu-id="9f9ff-166">Vous pouvez voir clairement la façon dont l’exécution entre `LongTask`, puis entre rapidement `Form1` chaque fois que l’événement `PercentDone` est déclenché.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-166">You can clearly see how execution enters `LongTask`, and then briefly re-enters `Form1` each time the `PercentDone` event is raised.</span></span>  
  
 <span data-ttu-id="9f9ff-167">Que se passerait-il si, alors que l’exécution était de retour dans le code de `Form1`, la méthode `LongTask` était appelée à nouveau ?</span><span class="sxs-lookup"><span data-stu-id="9f9ff-167">What would happen if, while execution was back in the code of `Form1`, the `LongTask` method were called again?</span></span> <span data-ttu-id="9f9ff-168">Au pire, un dépassement de capacité de la pile peut se produire si `LongTask` ont été appelées chaque fois que l’événement a été déclenché.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-168">At worst, a stack overflow might occur if `LongTask` were called every time the event was raised.</span></span>  
  
 <span data-ttu-id="9f9ff-169">Vous pouvez faire en sorte que la variable `mWidget` gérer les événements d’un autre objet `Widget` en affectant une référence au nouveau `Widget` à `mWidget`.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-169">You can cause the variable `mWidget` to handle events for a different `Widget` object by assigning a reference to the new `Widget` to `mWidget`.</span></span> <span data-ttu-id="9f9ff-170">En fait, vous pouvez créer le code dans `Button1_Click` effectuer cette opération chaque fois que vous cliquez sur le bouton.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-170">In fact, you can make the code in `Button1_Click` do this every time you click the button.</span></span>  
  
#### <a name="to-handle-events-for-a-different-widget"></a><span data-ttu-id="9f9ff-171">Pour gérer les événements d’un autre widget</span><span class="sxs-lookup"><span data-stu-id="9f9ff-171">To handle events for a different widget</span></span>  
  
- <span data-ttu-id="9f9ff-172">Ajoutez la ligne de code suivante à la procédure `Button1_Click`, juste avant la ligne qui lit `mWidget.LongTask(12.2, 0.33)`:</span><span class="sxs-lookup"><span data-stu-id="9f9ff-172">Add the following line of code to the `Button1_Click` procedure, immediately preceding the line that reads `mWidget.LongTask(12.2, 0.33)`:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#9](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#9)]  
  
 <span data-ttu-id="9f9ff-173">Le code ci-dessus crée un `Widget` chaque fois que l’utilisateur clique sur le bouton.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-173">The code above creates a new `Widget` each time the button is clicked.</span></span> <span data-ttu-id="9f9ff-174">Dès que la méthode `LongTask` se termine, la référence à la `Widget` est libérée et la `Widget` est détruite.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-174">As soon as the `LongTask` method completes, the reference to the `Widget` is released, and the `Widget` is destroyed.</span></span>  
  
 <span data-ttu-id="9f9ff-175">Une variable `WithEvents` ne peut contenir qu’une seule référence d’objet à la fois. par conséquent, si vous assignez un autre objet `Widget` à `mWidget`, les événements de l’objet `Widget` précédent ne seront plus gérés.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-175">A `WithEvents` variable can contain only one object reference at a time, so if you assign a different `Widget` object to `mWidget`, the previous `Widget` object's events will no longer be handled.</span></span> <span data-ttu-id="9f9ff-176">Si `mWidget` est la seule variable objet contenant une référence à l’ancienne `Widget`, l’objet est détruit.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-176">If `mWidget` is the only object variable containing a reference to the old `Widget`, the object is destroyed.</span></span> <span data-ttu-id="9f9ff-177">Si vous souhaitez gérer les événements de plusieurs objets `Widget`, utilisez l’instruction `AddHandler` pour traiter les événements de chaque objet séparément.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-177">If you want to handle events from several `Widget` objects, use the `AddHandler` statement to process events from each object separately.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="9f9ff-178">Vous pouvez déclarer autant de variables `WithEvents` que vous le souhaitez, mais les tableaux de variables `WithEvents` ne sont pas pris en charge.</span><span class="sxs-lookup"><span data-stu-id="9f9ff-178">You can declare as many `WithEvents` variables as you need, but arrays of `WithEvents` variables are not supported.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="9f9ff-179">Voir aussi</span><span class="sxs-lookup"><span data-stu-id="9f9ff-179">See also</span></span>

- [<span data-ttu-id="9f9ff-180">Procédure pas à pas : déclaration et déclenchement des événements</span><span class="sxs-lookup"><span data-stu-id="9f9ff-180">Walkthrough: Declaring and Raising Events</span></span>](../../../../visual-basic/programming-guide/language-features/events/walkthrough-declaring-and-raising-events.md)
- [<span data-ttu-id="9f9ff-181">Événements</span><span class="sxs-lookup"><span data-stu-id="9f9ff-181">Events</span></span>](../../../../visual-basic/programming-guide/language-features/events/index.md)
