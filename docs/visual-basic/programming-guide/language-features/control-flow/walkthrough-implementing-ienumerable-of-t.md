---
title: Mise en œuvre de IEnumerable
ms.date: 07/31/2018
helpviewer_keywords:
- control flow [Visual Basic]
- enumerable interfaces
- loop structures [Visual Basic], optimizing performance
- control flow [Visual Basic]
ms.assetid: c60d7589-51f2-4463-a2d5-22506bbc1554
ms.openlocfilehash: 4151a680050f234d450d8de5e67a767c54e8df68
ms.sourcegitcommit: 43d10ef65f0f1fd6c3b515e363bde11a3fcd8d6d
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 03/04/2020
ms.locfileid: "78266909"
---
# <a name="walkthrough-implementing-ienumerableof-t-in-visual-basic"></a><span data-ttu-id="3fe3a-102">Procédure pas à pas : implémentation d'IEnumerable(Of T) en Visual Basic</span><span class="sxs-lookup"><span data-stu-id="3fe3a-102">Walkthrough: Implementing IEnumerable(Of T) in Visual Basic</span></span>
<span data-ttu-id="3fe3a-103">L’interface <xref:System.Collections.Generic.IEnumerable%601> est implémentée par des classes qui peuvent retourner une séquence de valeurs un élément à la fois.</span><span class="sxs-lookup"><span data-stu-id="3fe3a-103">The <xref:System.Collections.Generic.IEnumerable%601> interface is implemented by classes that can return a sequence of values one item at a time.</span></span> <span data-ttu-id="3fe3a-104">L’avantage de retourner des données un élément à la fois est que vous n’avez pas à charger l’ensemble complet de données en mémoire pour travailler avec elle.</span><span class="sxs-lookup"><span data-stu-id="3fe3a-104">The advantage of returning data one item at a time is that you do not have to load the complete set of data into memory to work with it.</span></span> <span data-ttu-id="3fe3a-105">Vous n’avez qu’à utiliser suffisamment de mémoire pour charger un seul élément à partir des données.</span><span class="sxs-lookup"><span data-stu-id="3fe3a-105">You only have to use sufficient memory to load a single item from the data.</span></span> <span data-ttu-id="3fe3a-106">Les classes `IEnumerable(T)` qui implémenter l’interface peuvent être utilisées avec `For Each` des boucles ou des requêtes LINQ.</span><span class="sxs-lookup"><span data-stu-id="3fe3a-106">Classes that implement the `IEnumerable(T)` interface can be used with `For Each` loops or LINQ queries.</span></span>  
  
 <span data-ttu-id="3fe3a-107">Par exemple, considérez une application qui doit lire un fichier texte volumineux et retourner chaque ligne du fichier qui correspond à des critères de recherche particuliers.</span><span class="sxs-lookup"><span data-stu-id="3fe3a-107">For example, consider an application that must read a large text file and return each line from the file that matches particular search criteria.</span></span> <span data-ttu-id="3fe3a-108">L’application utilise une requête LINQ pour retourner les lignes du fichier qui correspondent aux critères spécifiés.</span><span class="sxs-lookup"><span data-stu-id="3fe3a-108">The application uses a LINQ query to return lines from the file that match the specified criteria.</span></span> <span data-ttu-id="3fe3a-109">Pour interroger le contenu du fichier à l’aide d’une requête LINQ, l’application peut charger le contenu du fichier dans un tableau ou une collection.</span><span class="sxs-lookup"><span data-stu-id="3fe3a-109">To query the contents of the file by using a LINQ query, the application could load the contents of the file into an array or a collection.</span></span> <span data-ttu-id="3fe3a-110">Cependant, le chargement de l’ensemble du fichier dans un tableau ou une collection consommerait beaucoup plus de mémoire que nécessaire.</span><span class="sxs-lookup"><span data-stu-id="3fe3a-110">However, loading the whole file into an array or collection would consume far more memory than is required.</span></span> <span data-ttu-id="3fe3a-111">La requête linQ pourrait plutôt interroger le contenu du fichier en utilisant une classe enumérable, ne retournant que les valeurs qui correspondent aux critères de recherche.</span><span class="sxs-lookup"><span data-stu-id="3fe3a-111">The LINQ query could instead query the file contents by using an enumerable class, returning only values that match the search criteria.</span></span> <span data-ttu-id="3fe3a-112">Les requêtes qui ne renvoient que quelques valeurs correspondantes consommeraient beaucoup moins de mémoire.</span><span class="sxs-lookup"><span data-stu-id="3fe3a-112">Queries that return only a few matching values would consume far less memory.</span></span>  
  
 <span data-ttu-id="3fe3a-113">Vous pouvez créer une classe <xref:System.Collections.Generic.IEnumerable%601> qui implémente l’interface pour exposer les données sources comme des données enumérables.</span><span class="sxs-lookup"><span data-stu-id="3fe3a-113">You can create a class that implements the <xref:System.Collections.Generic.IEnumerable%601> interface to expose source data as enumerable data.</span></span> <span data-ttu-id="3fe3a-114">Votre classe qui `IEnumerable(T)` implémente l’interface <xref:System.Collections.Generic.IEnumerator%601> nécessitera une autre classe qui implémente l’interface pour itérer à travers les données source.</span><span class="sxs-lookup"><span data-stu-id="3fe3a-114">Your class that implements the `IEnumerable(T)` interface will require another class that implements the <xref:System.Collections.Generic.IEnumerator%601> interface to iterate through the source data.</span></span> <span data-ttu-id="3fe3a-115">Ces deux classes vous permettent de retourner des éléments de données séquentiellement en tant que type spécifique.</span><span class="sxs-lookup"><span data-stu-id="3fe3a-115">These two classes enable you to return items of data sequentially as a specific type.</span></span>  
  
 <span data-ttu-id="3fe3a-116">Dans cette procédure pas à pas, vous allez `IEnumerable(Of String)` créer une classe qui `IEnumerator(Of String)` implémente l’interface et une classe qui implémente l’interface pour lire un fichier texte une ligne à la fois.</span><span class="sxs-lookup"><span data-stu-id="3fe3a-116">In this walkthrough, you will create a class that implements the `IEnumerable(Of String)` interface and a class that implements the `IEnumerator(Of String)` interface to read a text file one line at a time.</span></span>  
  
[!INCLUDE[note_settings_general](~/includes/note-settings-general-md.md)]  
  
## <a name="creating-the-enumerable-class"></a><span data-ttu-id="3fe3a-117">Création de la classe Enumerable</span><span class="sxs-lookup"><span data-stu-id="3fe3a-117">Creating the Enumerable Class</span></span>  
  
<span data-ttu-id="3fe3a-118">**Créer le projet de classe enumerable**</span><span class="sxs-lookup"><span data-stu-id="3fe3a-118">**Create the enumerable class project**</span></span>

1. <span data-ttu-id="3fe3a-119">Dans Visual Basic, sur le menu **du fichier,** pointez vers **New** puis cliquez sur **Project**.</span><span class="sxs-lookup"><span data-stu-id="3fe3a-119">In Visual Basic, on the **File** menu, point to **New** and then click **Project**.</span></span>

1. <span data-ttu-id="3fe3a-120">Dans la boîte de dialogue **Nouveau projet**, dans le volet **Types de projets**, vérifiez que **Windows** est sélectionné.</span><span class="sxs-lookup"><span data-stu-id="3fe3a-120">In the **New Project** dialog box, in the **Project Types** pane, make sure that **Windows** is selected.</span></span> <span data-ttu-id="3fe3a-121">Sélectionnez **Bibliothèque de classes** dans le volet **Modèles**.</span><span class="sxs-lookup"><span data-stu-id="3fe3a-121">Select **Class Library** in the **Templates** pane.</span></span> <span data-ttu-id="3fe3a-122">Dans la zone **Nom**, tapez `StreamReaderEnumerable`, puis cliquez sur **OK**.</span><span class="sxs-lookup"><span data-stu-id="3fe3a-122">In the **Name** box, type `StreamReaderEnumerable`, and then click **OK**.</span></span> <span data-ttu-id="3fe3a-123">Le nouveau projet est affiché.</span><span class="sxs-lookup"><span data-stu-id="3fe3a-123">The new project is displayed.</span></span>

1. <span data-ttu-id="3fe3a-124">Dans **Solution Explorer**, cliquez à droite sur le fichier Class1.vb et cliquez sur **Rename**.</span><span class="sxs-lookup"><span data-stu-id="3fe3a-124">In **Solution Explorer**, right-click the Class1.vb file and click **Rename**.</span></span> <span data-ttu-id="3fe3a-125">Renommez le fichier `StreamReaderEnumerable.vb` et appuyez sur Entrée.</span><span class="sxs-lookup"><span data-stu-id="3fe3a-125">Rename the file to `StreamReaderEnumerable.vb` and press ENTER.</span></span> <span data-ttu-id="3fe3a-126">Quand vous renommez le fichier, la classe est également renommée `StreamReaderEnumerable`.</span><span class="sxs-lookup"><span data-stu-id="3fe3a-126">Renaming the file will also rename the class to `StreamReaderEnumerable`.</span></span> <span data-ttu-id="3fe3a-127">Cette classe va implémenter l’interface `IEnumerable(Of String)`.</span><span class="sxs-lookup"><span data-stu-id="3fe3a-127">This class will implement the `IEnumerable(Of String)` interface.</span></span>

1. <span data-ttu-id="3fe3a-128">Cliquez à droite sur le projet StreamReaderEnumerable, pointez **d’ajouter,** puis cliquez sur **Nouvel article**.</span><span class="sxs-lookup"><span data-stu-id="3fe3a-128">Right-click the StreamReaderEnumerable project, point to **Add**, and then click **New Item**.</span></span> <span data-ttu-id="3fe3a-129">Sélectionnez le modèle **de classe.**</span><span class="sxs-lookup"><span data-stu-id="3fe3a-129">Select the **Class** template.</span></span> <span data-ttu-id="3fe3a-130">Dans la zone **Name** (Nom), entrez `StreamReaderEnumerator.vb` et cliquez sur **OK**.</span><span class="sxs-lookup"><span data-stu-id="3fe3a-130">In the **Name** box, type `StreamReaderEnumerator.vb` and click **OK**.</span></span>

 <span data-ttu-id="3fe3a-131">La première classe de ce projet est la classe `IEnumerable(Of String)` enumerable et mettra en œuvre l’interface.</span><span class="sxs-lookup"><span data-stu-id="3fe3a-131">The first class in this project is the enumerable class and will implement the `IEnumerable(Of String)` interface.</span></span> <span data-ttu-id="3fe3a-132">Cette interface générique <xref:System.Collections.IEnumerable> implémente l’interface et garantit que `String`les consommateurs de cette classe peuvent accéder à des valeurs tapées comme .</span><span class="sxs-lookup"><span data-stu-id="3fe3a-132">This generic interface implements the <xref:System.Collections.IEnumerable> interface and guarantees that consumers of this class can access values typed as `String`.</span></span>  
  
<span data-ttu-id="3fe3a-133">**Ajouter le code pour implémenter IEnumerable**</span><span class="sxs-lookup"><span data-stu-id="3fe3a-133">**Add the code to implement IEnumerable**</span></span>

1. <span data-ttu-id="3fe3a-134">Ouvrez le fichier StreamReaderEnumerable.vb.</span><span class="sxs-lookup"><span data-stu-id="3fe3a-134">Open the StreamReaderEnumerable.vb file.</span></span>

2. <span data-ttu-id="3fe3a-135">Sur la `Public Class StreamReaderEnumerable`ligne après , tapez ce qui suit et appuyez sur ENTER.</span><span class="sxs-lookup"><span data-stu-id="3fe3a-135">On the line after `Public Class StreamReaderEnumerable`, type the following and press ENTER.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#1](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#1)]

   <span data-ttu-id="3fe3a-136">Visual Basic remplit automatiquement la classe avec les `IEnumerable(Of String)` membres qui sont requis par l’interface.</span><span class="sxs-lookup"><span data-stu-id="3fe3a-136">Visual Basic automatically populates the class with the members that are required by the `IEnumerable(Of String)` interface.</span></span>
  
3. <span data-ttu-id="3fe3a-137">Cette classe enumerable lira les lignes d’un fichier texte une ligne à la fois.</span><span class="sxs-lookup"><span data-stu-id="3fe3a-137">This enumerable class will read lines from a text file one line at a time.</span></span> <span data-ttu-id="3fe3a-138">Ajoutez le code suivant à la classe pour exposer un constructeur public qui prend un chemin de fichier comme paramètre d’entrée.</span><span class="sxs-lookup"><span data-stu-id="3fe3a-138">Add the following code to the class to expose a public constructor that takes a file path as an input parameter.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#2](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#2)]

4. <span data-ttu-id="3fe3a-139">Votre mise <xref:System.Collections.Generic.IEnumerable%601.GetEnumerator%2A> en œuvre de la méthode de l’interface `IEnumerable(Of String)` retournera une nouvelle instance de la `StreamReaderEnumerator` classe.</span><span class="sxs-lookup"><span data-stu-id="3fe3a-139">Your implementation of the <xref:System.Collections.Generic.IEnumerable%601.GetEnumerator%2A> method of the `IEnumerable(Of String)` interface will return a new instance of the `StreamReaderEnumerator` class.</span></span> <span data-ttu-id="3fe3a-140">La mise `GetEnumerator` en œuvre de la méthode de l’interface `IEnumerable` peut être faite, `Private`car vous devez exposer uniquement les membres de l’interface. `IEnumerable(Of String)`</span><span class="sxs-lookup"><span data-stu-id="3fe3a-140">The implementation of the `GetEnumerator` method of the `IEnumerable` interface can be made `Private`, because you have to expose only members of the `IEnumerable(Of String)` interface.</span></span> <span data-ttu-id="3fe3a-141">Remplacez le code généré `GetEnumerator` par Visual Basic pour les méthodes par le code suivant.</span><span class="sxs-lookup"><span data-stu-id="3fe3a-141">Replace the code that Visual Basic generated for the `GetEnumerator` methods with the following code.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#3](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#3)]  
  
<span data-ttu-id="3fe3a-142">**Ajouter le code pour implémenter IEnumerator**</span><span class="sxs-lookup"><span data-stu-id="3fe3a-142">**Add the code to implement IEnumerator**</span></span>

1. <span data-ttu-id="3fe3a-143">Ouvrez le fichier StreamReaderEnumerator.vb.</span><span class="sxs-lookup"><span data-stu-id="3fe3a-143">Open the StreamReaderEnumerator.vb file.</span></span>

2. <span data-ttu-id="3fe3a-144">Sur la `Public Class StreamReaderEnumerator`ligne après , tapez ce qui suit et appuyez sur ENTER.</span><span class="sxs-lookup"><span data-stu-id="3fe3a-144">On the line after `Public Class StreamReaderEnumerator`, type the following and press ENTER.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#4](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#4)]

   <span data-ttu-id="3fe3a-145">Visual Basic remplit automatiquement la classe avec les `IEnumerator(Of String)` membres qui sont requis par l’interface.</span><span class="sxs-lookup"><span data-stu-id="3fe3a-145">Visual Basic automatically populates the class with the members that are required by the `IEnumerator(Of String)` interface.</span></span>

3. <span data-ttu-id="3fe3a-146">La classe d’enumérateur ouvre le fichier texte et exécute le fichier I/O pour lire les lignes du fichier.</span><span class="sxs-lookup"><span data-stu-id="3fe3a-146">The enumerator class opens the text file and performs the file I/O to read the lines from the file.</span></span> <span data-ttu-id="3fe3a-147">Ajoutez le code suivant à la classe pour exposer un constructeur public qui prend un chemin de fichier comme paramètre d’entrée et ouvrir le fichier texte pour la lecture.</span><span class="sxs-lookup"><span data-stu-id="3fe3a-147">Add the following code to the class to expose a public constructor that takes a file path as an input parameter and open the text file for reading.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#5](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#5)]

4. <span data-ttu-id="3fe3a-148">Les `Current` propriétés `IEnumerator(Of String)` pour `IEnumerator` les deux et les interfaces `String`retournent l’élément actuel à partir du fichier texte comme un .</span><span class="sxs-lookup"><span data-stu-id="3fe3a-148">The `Current` properties for both the `IEnumerator(Of String)` and `IEnumerator` interfaces return the current item from the text file as a `String`.</span></span> <span data-ttu-id="3fe3a-149">La mise `Current` en œuvre de la propriété de l’interface `IEnumerator` peut être faite, `Private`car vous devez exposer uniquement les membres de l’interface. `IEnumerator(Of String)`</span><span class="sxs-lookup"><span data-stu-id="3fe3a-149">The implementation of the `Current` property of the `IEnumerator` interface can be made `Private`, because you have to expose only members of the `IEnumerator(Of String)` interface.</span></span> <span data-ttu-id="3fe3a-150">Remplacez le code généré `Current` par Visual Basic pour les propriétés par le code suivant.</span><span class="sxs-lookup"><span data-stu-id="3fe3a-150">Replace the code that Visual Basic generated for the `Current` properties with the following code.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#6](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#6)]

5. <span data-ttu-id="3fe3a-151">La `MoveNext` méthode `IEnumerator` de l’interface navigue vers l’élément suivant dans le `Current` fichier texte et met à jour la valeur qui est retournée par la propriété.</span><span class="sxs-lookup"><span data-stu-id="3fe3a-151">The `MoveNext` method of the `IEnumerator` interface navigates to the next item in the text file and updates the value that is returned by the `Current` property.</span></span> <span data-ttu-id="3fe3a-152">S’il n’y a `MoveNext` plus `False`d’éléments à lire, la méthode revient; sinon `MoveNext` la `True`méthode revient .</span><span class="sxs-lookup"><span data-stu-id="3fe3a-152">If there are no more items to read, the `MoveNext` method returns `False`; otherwise the `MoveNext` method returns `True`.</span></span> <span data-ttu-id="3fe3a-153">Ajoutez le code suivant à la méthode `MoveNext` .</span><span class="sxs-lookup"><span data-stu-id="3fe3a-153">Add the following code to the `MoveNext` method.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#7](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#7)]

6. <span data-ttu-id="3fe3a-154">La `Reset` méthode `IEnumerator` de l’interface oriente l’itérateur vers le début du fichier texte et efface la valeur actuelle de l’élément.</span><span class="sxs-lookup"><span data-stu-id="3fe3a-154">The `Reset` method of the `IEnumerator` interface directs the iterator to point to the start of the text file and clears the current item value.</span></span> <span data-ttu-id="3fe3a-155">Ajoutez le code suivant à la méthode `Reset` .</span><span class="sxs-lookup"><span data-stu-id="3fe3a-155">Add the following code to the `Reset` method.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#8](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#8)]

7. <span data-ttu-id="3fe3a-156">La `Dispose` méthode `IEnumerator` de l’interface garantit que toutes les ressources non mentées sont libérées avant que l’itérateur ne soit détruit.</span><span class="sxs-lookup"><span data-stu-id="3fe3a-156">The `Dispose` method of the `IEnumerator` interface guarantees that all unmanaged resources are released before the iterator is destroyed.</span></span> <span data-ttu-id="3fe3a-157">La poignée de fichier `StreamReader` qui est utilisée par l’objet est une ressource non gérée et doit être fermée avant que l’instance d’itérateur soit détruite.</span><span class="sxs-lookup"><span data-stu-id="3fe3a-157">The file handle that is used by the `StreamReader` object is an unmanaged resource and must be closed before the iterator instance is destroyed.</span></span> <span data-ttu-id="3fe3a-158">Remplacez le code généré `Dispose` par Visual Basic pour la méthode par le code suivant.</span><span class="sxs-lookup"><span data-stu-id="3fe3a-158">Replace the code that Visual Basic generated for the `Dispose` method with the following code.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#9](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#9)]
  
## <a name="using-the-sample-iterator"></a><span data-ttu-id="3fe3a-159">À l’aide de l’itérateur d’échantillon</span><span class="sxs-lookup"><span data-stu-id="3fe3a-159">Using the Sample Iterator</span></span>

 <span data-ttu-id="3fe3a-160">Vous pouvez utiliser une classe enumérable dans votre code avec `IEnumerable`des structures `For Next` de contrôle qui nécessitent un objet qui implémente, comme une boucle ou une requête LINQ.</span><span class="sxs-lookup"><span data-stu-id="3fe3a-160">You can use an enumerable class in your code together with control structures that require an object that implements `IEnumerable`, such as a `For Next` loop or a LINQ query.</span></span> <span data-ttu-id="3fe3a-161">L’exemple suivant `StreamReaderEnumerable` montre le dans une requête LINQ.</span><span class="sxs-lookup"><span data-stu-id="3fe3a-161">The following example shows the `StreamReaderEnumerable` in a LINQ query.</span></span>  
  
 [!code-vb[VbVbalrIteratorWalkthrough#10](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/Module1.vb#10)]  
  
## <a name="see-also"></a><span data-ttu-id="3fe3a-162">Voir aussi</span><span class="sxs-lookup"><span data-stu-id="3fe3a-162">See also</span></span>

- [<span data-ttu-id="3fe3a-163">Introduction à LINQ en Visual Basic</span><span class="sxs-lookup"><span data-stu-id="3fe3a-163">Introduction to LINQ in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/linq/introduction-to-linq.md)
- [<span data-ttu-id="3fe3a-164">Flux de contrôle</span><span class="sxs-lookup"><span data-stu-id="3fe3a-164">Control Flow</span></span>](../../../../visual-basic/programming-guide/language-features/control-flow/index.md)
- [<span data-ttu-id="3fe3a-165">Structures de boucle</span><span class="sxs-lookup"><span data-stu-id="3fe3a-165">Loop Structures</span></span>](../../../../visual-basic/programming-guide/language-features/control-flow/loop-structures.md)
- [<span data-ttu-id="3fe3a-166">For Each...Next (instruction)</span><span class="sxs-lookup"><span data-stu-id="3fe3a-166">For Each...Next Statement</span></span>](../../../../visual-basic/language-reference/statements/for-each-next-statement.md)
