---
title: Implémentation d’IEnumerable
ms.date: 07/31/2018
helpviewer_keywords:
- control flow [Visual Basic]
- enumerable interfaces
- loop structures [Visual Basic], optimizing performance
- control flow [Visual Basic]
ms.assetid: c60d7589-51f2-4463-a2d5-22506bbc1554
ms.openlocfilehash: f1f0036c38299f2392f8c8705e67b7bb6b7db068
ms.sourcegitcommit: bf5c5850654187705bc94cc40ebfb62fe346ab02
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 09/23/2020
ms.locfileid: "91058636"
---
# <a name="walkthrough-implementing-ienumerableof-t-in-visual-basic"></a><span data-ttu-id="1904e-102">Procédure pas à pas : implémentation d'IEnumerable(Of T) en Visual Basic</span><span class="sxs-lookup"><span data-stu-id="1904e-102">Walkthrough: Implementing IEnumerable(Of T) in Visual Basic</span></span>

<span data-ttu-id="1904e-103">L' <xref:System.Collections.Generic.IEnumerable%601> interface est implémentée par les classes qui peuvent retourner une séquence de valeurs un élément à la fois.</span><span class="sxs-lookup"><span data-stu-id="1904e-103">The <xref:System.Collections.Generic.IEnumerable%601> interface is implemented by classes that can return a sequence of values one item at a time.</span></span> <span data-ttu-id="1904e-104">L’avantage de retourner des données un élément à la fois est que vous n’avez pas besoin de charger l’ensemble complet de données en mémoire pour l’utiliser.</span><span class="sxs-lookup"><span data-stu-id="1904e-104">The advantage of returning data one item at a time is that you do not have to load the complete set of data into memory to work with it.</span></span> <span data-ttu-id="1904e-105">Vous devez uniquement utiliser suffisamment de mémoire pour charger un seul élément à partir des données.</span><span class="sxs-lookup"><span data-stu-id="1904e-105">You only have to use sufficient memory to load a single item from the data.</span></span> <span data-ttu-id="1904e-106">Les classes qui implémentent l' `IEnumerable(T)` interface peuvent être utilisées avec des `For Each` boucles ou des requêtes LINQ.</span><span class="sxs-lookup"><span data-stu-id="1904e-106">Classes that implement the `IEnumerable(T)` interface can be used with `For Each` loops or LINQ queries.</span></span>  
  
 <span data-ttu-id="1904e-107">Par exemple, considérez une application qui doit lire un grand fichier texte et retourner chaque ligne du fichier qui correspond aux critères de recherche particuliers.</span><span class="sxs-lookup"><span data-stu-id="1904e-107">For example, consider an application that must read a large text file and return each line from the file that matches particular search criteria.</span></span> <span data-ttu-id="1904e-108">L’application utilise une requête LINQ pour retourner les lignes du fichier qui correspondent aux critères spécifiés.</span><span class="sxs-lookup"><span data-stu-id="1904e-108">The application uses a LINQ query to return lines from the file that match the specified criteria.</span></span> <span data-ttu-id="1904e-109">Pour interroger le contenu du fichier à l’aide d’une requête LINQ, l’application peut charger le contenu du fichier dans un tableau ou une collection.</span><span class="sxs-lookup"><span data-stu-id="1904e-109">To query the contents of the file by using a LINQ query, the application could load the contents of the file into an array or a collection.</span></span> <span data-ttu-id="1904e-110">Toutefois, le chargement de l’ensemble du fichier dans un tableau ou une collection consomme beaucoup plus de mémoire que nécessaire.</span><span class="sxs-lookup"><span data-stu-id="1904e-110">However, loading the whole file into an array or collection would consume far more memory than is required.</span></span> <span data-ttu-id="1904e-111">La requête LINQ peut à la place interroger le contenu du fichier à l’aide d’une classe énumérable, en retournant uniquement les valeurs qui correspondent aux critères de recherche.</span><span class="sxs-lookup"><span data-stu-id="1904e-111">The LINQ query could instead query the file contents by using an enumerable class, returning only values that match the search criteria.</span></span> <span data-ttu-id="1904e-112">Les requêtes qui retournent uniquement quelques valeurs correspondantes consomment beaucoup moins de mémoire.</span><span class="sxs-lookup"><span data-stu-id="1904e-112">Queries that return only a few matching values would consume far less memory.</span></span>  
  
 <span data-ttu-id="1904e-113">Vous pouvez créer une classe qui implémente l' <xref:System.Collections.Generic.IEnumerable%601> interface pour exposer des données sources sous forme de données énumérables.</span><span class="sxs-lookup"><span data-stu-id="1904e-113">You can create a class that implements the <xref:System.Collections.Generic.IEnumerable%601> interface to expose source data as enumerable data.</span></span> <span data-ttu-id="1904e-114">Votre classe qui implémente l' `IEnumerable(T)` interface nécessitera une autre classe qui implémente l' <xref:System.Collections.Generic.IEnumerator%601> interface pour itérer au sein des données sources.</span><span class="sxs-lookup"><span data-stu-id="1904e-114">Your class that implements the `IEnumerable(T)` interface will require another class that implements the <xref:System.Collections.Generic.IEnumerator%601> interface to iterate through the source data.</span></span> <span data-ttu-id="1904e-115">Ces deux classes vous permettent de retourner des éléments de données de façon séquentielle sous la forme d’un type spécifique.</span><span class="sxs-lookup"><span data-stu-id="1904e-115">These two classes enable you to return items of data sequentially as a specific type.</span></span>  
  
 <span data-ttu-id="1904e-116">Dans cette procédure pas à pas, vous allez créer une classe qui implémente l' `IEnumerable(Of String)` interface et une classe qui implémente l' `IEnumerator(Of String)` interface pour lire un fichier texte ligne par ligne.</span><span class="sxs-lookup"><span data-stu-id="1904e-116">In this walkthrough, you will create a class that implements the `IEnumerable(Of String)` interface and a class that implements the `IEnumerator(Of String)` interface to read a text file one line at a time.</span></span>  
  
[!INCLUDE[note_settings_general](~/includes/note-settings-general-md.md)]  
  
## <a name="creating-the-enumerable-class"></a><span data-ttu-id="1904e-117">Création de la classe Enumerable</span><span class="sxs-lookup"><span data-stu-id="1904e-117">Creating the Enumerable Class</span></span>  
  
<span data-ttu-id="1904e-118">**Créer le projet de classe Enumerable**</span><span class="sxs-lookup"><span data-stu-id="1904e-118">**Create the enumerable class project**</span></span>

1. <span data-ttu-id="1904e-119">Dans Visual Basic, dans le menu **fichier** , pointez sur **nouveau** , puis cliquez sur **projet**.</span><span class="sxs-lookup"><span data-stu-id="1904e-119">In Visual Basic, on the **File** menu, point to **New** and then click **Project**.</span></span>

1. <span data-ttu-id="1904e-120">Dans la boîte de dialogue **Nouveau projet**, dans le volet **Types de projets**, vérifiez que **Windows** est sélectionné.</span><span class="sxs-lookup"><span data-stu-id="1904e-120">In the **New Project** dialog box, in the **Project Types** pane, make sure that **Windows** is selected.</span></span> <span data-ttu-id="1904e-121">Sélectionnez **Bibliothèque de classes** dans le volet **Modèles**.</span><span class="sxs-lookup"><span data-stu-id="1904e-121">Select **Class Library** in the **Templates** pane.</span></span> <span data-ttu-id="1904e-122">Dans la zone **Nom**, tapez `StreamReaderEnumerable`, puis cliquez sur **OK**.</span><span class="sxs-lookup"><span data-stu-id="1904e-122">In the **Name** box, type `StreamReaderEnumerable`, and then click **OK**.</span></span> <span data-ttu-id="1904e-123">Le nouveau projet s’affiche.</span><span class="sxs-lookup"><span data-stu-id="1904e-123">The new project is displayed.</span></span>

1. <span data-ttu-id="1904e-124">Dans **Explorateur de solutions**, cliquez avec le bouton droit sur le fichier Class1. vb, puis cliquez sur **Renommer**.</span><span class="sxs-lookup"><span data-stu-id="1904e-124">In **Solution Explorer**, right-click the Class1.vb file and click **Rename**.</span></span> <span data-ttu-id="1904e-125">Renommez le fichier `StreamReaderEnumerable.vb` et appuyez sur Entrée.</span><span class="sxs-lookup"><span data-stu-id="1904e-125">Rename the file to `StreamReaderEnumerable.vb` and press ENTER.</span></span> <span data-ttu-id="1904e-126">Quand vous renommez le fichier, la classe est également renommée `StreamReaderEnumerable`.</span><span class="sxs-lookup"><span data-stu-id="1904e-126">Renaming the file will also rename the class to `StreamReaderEnumerable`.</span></span> <span data-ttu-id="1904e-127">Cette classe va implémenter l’interface `IEnumerable(Of String)`.</span><span class="sxs-lookup"><span data-stu-id="1904e-127">This class will implement the `IEnumerable(Of String)` interface.</span></span>

1. <span data-ttu-id="1904e-128">Cliquez avec le bouton droit sur le projet StreamReaderEnumerable, pointez sur **Ajouter**, puis cliquez sur **nouvel élément**.</span><span class="sxs-lookup"><span data-stu-id="1904e-128">Right-click the StreamReaderEnumerable project, point to **Add**, and then click **New Item**.</span></span> <span data-ttu-id="1904e-129">Sélectionnez le modèle de **classe** .</span><span class="sxs-lookup"><span data-stu-id="1904e-129">Select the **Class** template.</span></span> <span data-ttu-id="1904e-130">Dans la zone **Name** (Nom), entrez `StreamReaderEnumerator.vb` et cliquez sur **OK**.</span><span class="sxs-lookup"><span data-stu-id="1904e-130">In the **Name** box, type `StreamReaderEnumerator.vb` and click **OK**.</span></span>

 <span data-ttu-id="1904e-131">La première classe de ce projet est la classe Enumerable et implémente l' `IEnumerable(Of String)` interface.</span><span class="sxs-lookup"><span data-stu-id="1904e-131">The first class in this project is the enumerable class and will implement the `IEnumerable(Of String)` interface.</span></span> <span data-ttu-id="1904e-132">Cette interface générique implémente l' <xref:System.Collections.IEnumerable> interface et garantit que les consommateurs de cette classe peuvent accéder aux valeurs de type `String` .</span><span class="sxs-lookup"><span data-stu-id="1904e-132">This generic interface implements the <xref:System.Collections.IEnumerable> interface and guarantees that consumers of this class can access values typed as `String`.</span></span>  
  
<span data-ttu-id="1904e-133">**Ajouter le code pour implémenter IEnumerable**</span><span class="sxs-lookup"><span data-stu-id="1904e-133">**Add the code to implement IEnumerable**</span></span>

1. <span data-ttu-id="1904e-134">Ouvrez le fichier StreamReaderEnumerable. vb.</span><span class="sxs-lookup"><span data-stu-id="1904e-134">Open the StreamReaderEnumerable.vb file.</span></span>

2. <span data-ttu-id="1904e-135">Sur la ligne `Public Class StreamReaderEnumerable` qui suit, tapez la commande suivante et appuyez sur entrée.</span><span class="sxs-lookup"><span data-stu-id="1904e-135">On the line after `Public Class StreamReaderEnumerable`, type the following and press ENTER.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#1](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#1)]

   <span data-ttu-id="1904e-136">Visual Basic remplit automatiquement la classe avec les membres qui sont requis par l' `IEnumerable(Of String)` interface.</span><span class="sxs-lookup"><span data-stu-id="1904e-136">Visual Basic automatically populates the class with the members that are required by the `IEnumerable(Of String)` interface.</span></span>
  
3. <span data-ttu-id="1904e-137">Cette classe énumérable lit les lignes d’un fichier texte une ligne à la fois.</span><span class="sxs-lookup"><span data-stu-id="1904e-137">This enumerable class will read lines from a text file one line at a time.</span></span> <span data-ttu-id="1904e-138">Ajoutez le code suivant à la classe pour exposer un constructeur public qui prend un chemin d’accès de fichier en tant que paramètre d’entrée.</span><span class="sxs-lookup"><span data-stu-id="1904e-138">Add the following code to the class to expose a public constructor that takes a file path as an input parameter.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#2](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#2)]

4. <span data-ttu-id="1904e-139">Votre implémentation de la <xref:System.Collections.Generic.IEnumerable%601.GetEnumerator%2A> méthode de l' `IEnumerable(Of String)` interface retourne une nouvelle instance de la `StreamReaderEnumerator` classe.</span><span class="sxs-lookup"><span data-stu-id="1904e-139">Your implementation of the <xref:System.Collections.Generic.IEnumerable%601.GetEnumerator%2A> method of the `IEnumerable(Of String)` interface will return a new instance of the `StreamReaderEnumerator` class.</span></span> <span data-ttu-id="1904e-140">L’implémentation de la `GetEnumerator` méthode de l' `IEnumerable` interface peut être effectuée `Private` , car vous devez exposer uniquement les membres de l' `IEnumerable(Of String)` interface.</span><span class="sxs-lookup"><span data-stu-id="1904e-140">The implementation of the `GetEnumerator` method of the `IEnumerable` interface can be made `Private`, because you have to expose only members of the `IEnumerable(Of String)` interface.</span></span> <span data-ttu-id="1904e-141">Remplacez le code qui Visual Basic généré pour les `GetEnumerator` méthodes par le code suivant.</span><span class="sxs-lookup"><span data-stu-id="1904e-141">Replace the code that Visual Basic generated for the `GetEnumerator` methods with the following code.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#3](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#3)]  
  
<span data-ttu-id="1904e-142">**Ajouter le code pour implémenter IEnumerator**</span><span class="sxs-lookup"><span data-stu-id="1904e-142">**Add the code to implement IEnumerator**</span></span>

1. <span data-ttu-id="1904e-143">Ouvrez le fichier StreamReaderEnumerator. vb.</span><span class="sxs-lookup"><span data-stu-id="1904e-143">Open the StreamReaderEnumerator.vb file.</span></span>

2. <span data-ttu-id="1904e-144">Sur la ligne `Public Class StreamReaderEnumerator` qui suit, tapez la commande suivante et appuyez sur entrée.</span><span class="sxs-lookup"><span data-stu-id="1904e-144">On the line after `Public Class StreamReaderEnumerator`, type the following and press ENTER.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#4](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#4)]

   <span data-ttu-id="1904e-145">Visual Basic remplit automatiquement la classe avec les membres qui sont requis par l' `IEnumerator(Of String)` interface.</span><span class="sxs-lookup"><span data-stu-id="1904e-145">Visual Basic automatically populates the class with the members that are required by the `IEnumerator(Of String)` interface.</span></span>

3. <span data-ttu-id="1904e-146">La classe Enumerator ouvre le fichier texte et effectue l’e/s de fichier pour lire les lignes du fichier.</span><span class="sxs-lookup"><span data-stu-id="1904e-146">The enumerator class opens the text file and performs the file I/O to read the lines from the file.</span></span> <span data-ttu-id="1904e-147">Ajoutez le code suivant à la classe pour exposer un constructeur public qui utilise un chemin d’accès de fichier comme paramètre d’entrée et ouvrir le fichier texte pour la lecture.</span><span class="sxs-lookup"><span data-stu-id="1904e-147">Add the following code to the class to expose a public constructor that takes a file path as an input parameter and open the text file for reading.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#5](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#5)]

4. <span data-ttu-id="1904e-148">Les `Current` Propriétés des `IEnumerator(Of String)` `IEnumerator` interfaces et retournent l’élément actuel du fichier texte sous forme de `String` .</span><span class="sxs-lookup"><span data-stu-id="1904e-148">The `Current` properties for both the `IEnumerator(Of String)` and `IEnumerator` interfaces return the current item from the text file as a `String`.</span></span> <span data-ttu-id="1904e-149">L’implémentation de la `Current` propriété de l' `IEnumerator` interface peut être effectuée `Private` , car vous devez exposer uniquement les membres de l' `IEnumerator(Of String)` interface.</span><span class="sxs-lookup"><span data-stu-id="1904e-149">The implementation of the `Current` property of the `IEnumerator` interface can be made `Private`, because you have to expose only members of the `IEnumerator(Of String)` interface.</span></span> <span data-ttu-id="1904e-150">Remplacez le code qui Visual Basic généré pour les `Current` propriétés par le code suivant.</span><span class="sxs-lookup"><span data-stu-id="1904e-150">Replace the code that Visual Basic generated for the `Current` properties with the following code.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#6](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#6)]

5. <span data-ttu-id="1904e-151">La `MoveNext` méthode de l' `IEnumerator` interface accède à l’élément suivant dans le fichier texte et met à jour la valeur retournée par la `Current` propriété.</span><span class="sxs-lookup"><span data-stu-id="1904e-151">The `MoveNext` method of the `IEnumerator` interface navigates to the next item in the text file and updates the value that is returned by the `Current` property.</span></span> <span data-ttu-id="1904e-152">S’il n’y a plus d’éléments à lire, la `MoveNext` méthode retourne `False` ; sinon, la `MoveNext` méthode retourne `True` .</span><span class="sxs-lookup"><span data-stu-id="1904e-152">If there are no more items to read, the `MoveNext` method returns `False`; otherwise the `MoveNext` method returns `True`.</span></span> <span data-ttu-id="1904e-153">Ajoutez le code suivant à la méthode `MoveNext` .</span><span class="sxs-lookup"><span data-stu-id="1904e-153">Add the following code to the `MoveNext` method.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#7](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#7)]

6. <span data-ttu-id="1904e-154">La `Reset` méthode de l' `IEnumerator` interface indique à l’itérateur de pointer vers le début du fichier texte et efface la valeur de l’élément actuel.</span><span class="sxs-lookup"><span data-stu-id="1904e-154">The `Reset` method of the `IEnumerator` interface directs the iterator to point to the start of the text file and clears the current item value.</span></span> <span data-ttu-id="1904e-155">Ajoutez le code suivant à la méthode `Reset` .</span><span class="sxs-lookup"><span data-stu-id="1904e-155">Add the following code to the `Reset` method.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#8](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#8)]

7. <span data-ttu-id="1904e-156">La `Dispose` méthode de l' `IEnumerator` interface garantit que toutes les ressources non managées sont libérées avant la destruction de l’itérateur.</span><span class="sxs-lookup"><span data-stu-id="1904e-156">The `Dispose` method of the `IEnumerator` interface guarantees that all unmanaged resources are released before the iterator is destroyed.</span></span> <span data-ttu-id="1904e-157">Le handle de fichier utilisé par l' `StreamReader` objet est une ressource non managée et doit être fermé avant la destruction de l’instance d’itérateur.</span><span class="sxs-lookup"><span data-stu-id="1904e-157">The file handle that is used by the `StreamReader` object is an unmanaged resource and must be closed before the iterator instance is destroyed.</span></span> <span data-ttu-id="1904e-158">Remplacez le code qui Visual Basic généré pour la `Dispose` méthode par le code suivant.</span><span class="sxs-lookup"><span data-stu-id="1904e-158">Replace the code that Visual Basic generated for the `Dispose` method with the following code.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#9](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#9)]
  
## <a name="using-the-sample-iterator"></a><span data-ttu-id="1904e-159">Utilisation de l’exemple d’itérateur</span><span class="sxs-lookup"><span data-stu-id="1904e-159">Using the Sample Iterator</span></span>

 <span data-ttu-id="1904e-160">Vous pouvez utiliser une classe Enumerable dans votre code avec des structures de contrôle qui requièrent un objet qui implémente `IEnumerable` , tel qu’une `For Next` boucle ou une requête LINQ.</span><span class="sxs-lookup"><span data-stu-id="1904e-160">You can use an enumerable class in your code together with control structures that require an object that implements `IEnumerable`, such as a `For Next` loop or a LINQ query.</span></span> <span data-ttu-id="1904e-161">L’exemple suivant montre le `StreamReaderEnumerable` dans une requête LINQ.</span><span class="sxs-lookup"><span data-stu-id="1904e-161">The following example shows the `StreamReaderEnumerable` in a LINQ query.</span></span>  
  
 [!code-vb[VbVbalrIteratorWalkthrough#10](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/Module1.vb#10)]  
  
## <a name="see-also"></a><span data-ttu-id="1904e-162">Voir aussi</span><span class="sxs-lookup"><span data-stu-id="1904e-162">See also</span></span>

- [<span data-ttu-id="1904e-163">Introduction à LINQ en Visual Basic</span><span class="sxs-lookup"><span data-stu-id="1904e-163">Introduction to LINQ in Visual Basic</span></span>](../linq/introduction-to-linq.md)
- [<span data-ttu-id="1904e-164">Flux de contrôle</span><span class="sxs-lookup"><span data-stu-id="1904e-164">Control Flow</span></span>](index.md)
- [<span data-ttu-id="1904e-165">Structures de boucle</span><span class="sxs-lookup"><span data-stu-id="1904e-165">Loop Structures</span></span>](loop-structures.md)
- [<span data-ttu-id="1904e-166">For Each...Next (instruction)</span><span class="sxs-lookup"><span data-stu-id="1904e-166">For Each...Next Statement</span></span>](../../../language-reference/statements/for-each-next-statement.md)
