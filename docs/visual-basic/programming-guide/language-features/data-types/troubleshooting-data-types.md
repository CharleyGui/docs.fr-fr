---
title: Dépannage des types de données
ms.date: 07/20/2015
helpviewer_keywords:
- Char data type [Visual Basic], converting
- Decimal data type [Visual Basic], conversions
- data types [Visual Basic], troubleshooting
- literals [Visual Basic], default types
- type characters [Visual Basic], literal
- Mod operator [Visual Basic], in floating-point operations
- troubleshooting Visual Basic, data types
- troubleshooting data types [Visual Basic]
- floating-point numbers [Visual Basic], precision
- Boolean data type [Visual Basic], converting
- literal types [Visual Basic]
- literal type characters [Visual Basic]
- floating-point numbers [Visual Basic], imprecision
- String data type [Visual Basic], converting
- floating-point numbers [Visual Basic], comparison
- floating-point numbers
ms.assetid: 90040d67-b630-4125-a6ae-37195b079042
ms.openlocfilehash: 239e1c2f908a9023aeca6e92aff4633b60f27b69
ms.sourcegitcommit: f8c270376ed905f6a8896ce0fe25b4f4b38ff498
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 06/04/2020
ms.locfileid: "84393401"
---
# <a name="troubleshooting-data-types-visual-basic"></a><span data-ttu-id="71ff5-102">Dépannage des types de données (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="71ff5-102">Troubleshooting Data Types (Visual Basic)</span></span>

<span data-ttu-id="71ff5-103">Cette page répertorie certains problèmes courants qui peuvent se produire lorsque vous effectuez des opérations sur des types de données intrinsèques.</span><span class="sxs-lookup"><span data-stu-id="71ff5-103">This page lists some common problems that can occur when you perform operations on intrinsic data types.</span></span>

## <a name="floating-point-expressions-do-not-compare-as-equal"></a><span data-ttu-id="71ff5-104">Les expressions à virgule flottante ne sont pas considérées comme égales</span><span class="sxs-lookup"><span data-stu-id="71ff5-104">Floating-Point Expressions Do Not Compare as Equal</span></span>

<span data-ttu-id="71ff5-105">Lorsque vous travaillez avec des nombres à virgule flottante ([type de données unique](../../../language-reference/data-types/single-data-type.md) et [type de données double](../../../language-reference/data-types/double-data-type.md)), n’oubliez pas qu’ils sont stockés sous forme de fractions binaires.</span><span class="sxs-lookup"><span data-stu-id="71ff5-105">When you work with floating-point numbers ([Single Data Type](../../../language-reference/data-types/single-data-type.md) and [Double Data Type](../../../language-reference/data-types/double-data-type.md)), remember that they are stored as binary fractions.</span></span> <span data-ttu-id="71ff5-106">Cela signifie qu’elles ne peuvent pas contenir une représentation exacte d’une quantité qui n’est pas une fraction binaire (au format k/(2 ^ n) où k et n sont des entiers).</span><span class="sxs-lookup"><span data-stu-id="71ff5-106">This means they cannot hold an exact representation of any quantity that is not a binary fraction (of the form k / (2 ^ n) where k and n are integers).</span></span> <span data-ttu-id="71ff5-107">Par exemple, 0,5 (= 1/2) et 0,3125 (= 5/16) peuvent être conservés comme valeurs précises, alors que 0,2 (= 1/5) et 0,3 (= 3/10) peuvent uniquement être des approximations.</span><span class="sxs-lookup"><span data-stu-id="71ff5-107">For example, 0.5 (= 1/2) and 0.3125 (= 5/16) can be held as precise values, whereas 0.2 (= 1/5) and 0.3 (= 3/10) can be only approximations.</span></span>

<span data-ttu-id="71ff5-108">En raison de ce imprécision, vous ne pouvez pas vous reposer sur des résultats exacts lorsque vous utilisez des valeurs à virgule flottante.</span><span class="sxs-lookup"><span data-stu-id="71ff5-108">Because of this imprecision, you cannot rely on exact results when you operate on floating-point values.</span></span> <span data-ttu-id="71ff5-109">En particulier, deux valeurs théoriquement égales peuvent avoir des représentations légèrement différentes.</span><span class="sxs-lookup"><span data-stu-id="71ff5-109">In particular, two values that are theoretically equal might have slightly different representations.</span></span>

| <span data-ttu-id="71ff5-110">Pour comparer les quantités à virgule flottante</span><span class="sxs-lookup"><span data-stu-id="71ff5-110">To compare floating-point quantities</span></span> |
|---|
|<span data-ttu-id="71ff5-111">1. Calculez la valeur absolue de la différence en utilisant la <xref:System.Math.Abs%2A> méthode de la <xref:System.Math> classe dans l' <xref:System> espace de noms.</span><span class="sxs-lookup"><span data-stu-id="71ff5-111">1.  Calculate the absolute value of their difference by using the <xref:System.Math.Abs%2A> method of the <xref:System.Math> class in the <xref:System> namespace.</span></span><br /><span data-ttu-id="71ff5-112">2. Déterminez une différence maximale acceptable, de sorte que vous pouvez considérer les deux quantités comme étant égales pour des raisons pratiques si leur différence n’est pas supérieure.</span><span class="sxs-lookup"><span data-stu-id="71ff5-112">2.  Determine an acceptable maximum difference, such that you can consider the two quantities to be equal for practical purposes if their difference is no larger.</span></span><br /><span data-ttu-id="71ff5-113">3. Comparez la valeur absolue de la différence à la différence acceptable.</span><span class="sxs-lookup"><span data-stu-id="71ff5-113">3.  Compare the absolute value of the difference to the acceptable difference.</span></span>|

<span data-ttu-id="71ff5-114">L’exemple suivant montre à la fois une comparaison incorrecte et correcte de deux `Double` valeurs.</span><span class="sxs-lookup"><span data-stu-id="71ff5-114">The following example demonstrates both incorrect and correct comparison of two `Double` values.</span></span>

[!code-vb[VbVbalrDataTypes#10](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrDataTypes/VB/Class1.vb#10)]

<span data-ttu-id="71ff5-115">L’exemple précédent utilise la <xref:System.Double.ToString%2A> méthode de la <xref:System.Double> structure afin qu’il puisse spécifier une meilleure précision que le `CStr` mot clé utilise.</span><span class="sxs-lookup"><span data-stu-id="71ff5-115">The previous example uses the <xref:System.Double.ToString%2A> method of the <xref:System.Double> structure so that it can specify better  precision than the `CStr` keyword uses.</span></span> <span data-ttu-id="71ff5-116">La valeur par défaut est 15 chiffres, mais le format « G17 » l’étend jusqu’à 17 chiffres.</span><span class="sxs-lookup"><span data-stu-id="71ff5-116">The default is 15 digits, but the "G17" format extends it to 17 digits.</span></span>

## <a name="mod-operator-does-not-return-accurate-result"></a><span data-ttu-id="71ff5-117">L’opérateur mod ne retourne pas de résultat exact</span><span class="sxs-lookup"><span data-stu-id="71ff5-117">Mod Operator Does Not Return Accurate Result</span></span>

<span data-ttu-id="71ff5-118">En raison du imprécision de stockage à virgule flottante, l' [opérateur mod](../../../language-reference/operators/mod-operator.md) peut retourner un résultat inattendu lorsqu’au moins l’un des opérandes est à virgule flottante.</span><span class="sxs-lookup"><span data-stu-id="71ff5-118">Because of the imprecision of floating-point storage, the [Mod Operator](../../../language-reference/operators/mod-operator.md) can return an unexpected result when at least one of the operands is floating-point.</span></span>

<span data-ttu-id="71ff5-119">Le [type de données decimal](../../../language-reference/data-types/decimal-data-type.md) n’utilise pas la représentation à virgule flottante.</span><span class="sxs-lookup"><span data-stu-id="71ff5-119">The [Decimal Data Type](../../../language-reference/data-types/decimal-data-type.md) does not use floating-point representation.</span></span> <span data-ttu-id="71ff5-120">De nombreux nombres qui sont inexacts dans `Single` et `Double` sont exacts dans `Decimal` (par exemple 0,2 et 0,3).</span><span class="sxs-lookup"><span data-stu-id="71ff5-120">Many numbers that are inexact in `Single` and `Double` are exact in `Decimal` (for example 0.2 and 0.3).</span></span> <span data-ttu-id="71ff5-121">Bien que l’arithmétique soit plus lente dans `Decimal` que dans le calcul à virgule flottante, il peut être utile de réduire les performances pour obtenir une meilleure précision.</span><span class="sxs-lookup"><span data-stu-id="71ff5-121">Although arithmetic is slower in `Decimal` than in floating-point, it might be worth the performance decrease to achieve better precision.</span></span>

|<span data-ttu-id="71ff5-122">Pour rechercher le reste entier des quantités à virgule flottante</span><span class="sxs-lookup"><span data-stu-id="71ff5-122">To find the integer remainder of floating-point quantities</span></span>|
|---|
|<span data-ttu-id="71ff5-123">1. déclarez les variables en tant que `Decimal` .</span><span class="sxs-lookup"><span data-stu-id="71ff5-123">1.  Declare variables as `Decimal`.</span></span><br /><span data-ttu-id="71ff5-124">2. Utilisez le caractère de type littéral `D` pour forcer des littéraux `Decimal` , si leurs valeurs sont trop élevées pour le `Long` type de données.</span><span class="sxs-lookup"><span data-stu-id="71ff5-124">2.  Use the literal type character `D` to force literals to `Decimal`, in case their values are too large for the `Long` data type.</span></span>|

<span data-ttu-id="71ff5-125">L’exemple suivant illustre le imprécision potentiel des opérandes à virgule flottante.</span><span class="sxs-lookup"><span data-stu-id="71ff5-125">The following example demonstrates the potential imprecision of floating-point operands.</span></span>

[!code-vb[VbVbalrDataTypes#11](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrDataTypes/VB/Class1.vb#11)]

<span data-ttu-id="71ff5-126">L’exemple précédent utilise la <xref:System.Double.ToString%2A> méthode de la <xref:System.Double> structure afin qu’il puisse spécifier une meilleure précision que le `CStr` mot clé utilise.</span><span class="sxs-lookup"><span data-stu-id="71ff5-126">The previous example uses the <xref:System.Double.ToString%2A> method of the <xref:System.Double> structure so that it can specify better precision than the `CStr` keyword uses.</span></span> <span data-ttu-id="71ff5-127">La valeur par défaut est 15 chiffres, mais le format « G17 » l’étend jusqu’à 17 chiffres.</span><span class="sxs-lookup"><span data-stu-id="71ff5-127">The default is 15 digits, but the "G17" format extends it to 17 digits.</span></span>

<span data-ttu-id="71ff5-128">Comme `zeroPointTwo` est `Double` , sa valeur pour 0,2 est une fraction binaire à répétition infinie avec une valeur stockée de 0.20000000000000001.</span><span class="sxs-lookup"><span data-stu-id="71ff5-128">Because `zeroPointTwo` is `Double`, its value for 0.2 is an infinitely repeating binary fraction with a stored value of 0.20000000000000001.</span></span> <span data-ttu-id="71ff5-129">La Division de 2,0 par cette quantité produit 9.9999999999999995 avec un reste de 0.19999999999999991.</span><span class="sxs-lookup"><span data-stu-id="71ff5-129">Dividing 2.0 by this quantity yields 9.9999999999999995 with a remainder of 0.19999999999999991.</span></span>

<span data-ttu-id="71ff5-130">Dans l’expression pour `decimalRemainder` , le caractère de type de littéral `D` force les deux opérandes à `Decimal` , et 0,2 a une représentation précise.</span><span class="sxs-lookup"><span data-stu-id="71ff5-130">In the expression for `decimalRemainder`, the literal type character `D` forces both operands to `Decimal`, and 0.2 has a precise representation.</span></span> <span data-ttu-id="71ff5-131">Par conséquent, l' `Mod` opérateur produit le reste attendu de 0,0.</span><span class="sxs-lookup"><span data-stu-id="71ff5-131">Therefore the `Mod` operator yields the expected remainder of 0.0.</span></span>

<span data-ttu-id="71ff5-132">Notez qu’il n’est pas suffisant de déclarer `decimalRemainder` en tant que `Decimal` .</span><span class="sxs-lookup"><span data-stu-id="71ff5-132">Note that it is not sufficient to declare `decimalRemainder` as `Decimal`.</span></span> <span data-ttu-id="71ff5-133">Vous devez également forcer les littéraux sur `Decimal` , ou ils utilisent `Double` par défaut et `decimalRemainder` reçoivent la même valeur inexacte que `doubleRemainder` .</span><span class="sxs-lookup"><span data-stu-id="71ff5-133">You must also force the literals to `Decimal`, or they use `Double` by default and `decimalRemainder` receives the same inaccurate value as `doubleRemainder`.</span></span>

## <a name="boolean-type-does-not-convert-to-numeric-type-accurately"></a><span data-ttu-id="71ff5-134">Le type booléen ne convertit pas correctement le type numérique</span><span class="sxs-lookup"><span data-stu-id="71ff5-134">Boolean Type Does Not Convert to Numeric Type Accurately</span></span>

<span data-ttu-id="71ff5-135">Les valeurs de [type de données Boolean](../../../language-reference/data-types/boolean-data-type.md) ne sont pas stockées sous forme de nombres, et les valeurs stockées ne sont pas destinées à être équivalentes aux nombres.</span><span class="sxs-lookup"><span data-stu-id="71ff5-135">[Boolean Data Type](../../../language-reference/data-types/boolean-data-type.md) values are not stored as numbers, and the stored values are not intended to be equivalent to numbers.</span></span> <span data-ttu-id="71ff5-136">Pour la compatibilité avec les versions antérieures, Visual Basic fournit des mots clés de conversion ([fonction CType](../../../language-reference/functions/ctype-function.md),, `CBool` , etc `CInt` .) pour effectuer une conversion entre `Boolean` des types numériques et.</span><span class="sxs-lookup"><span data-stu-id="71ff5-136">For compatibility with earlier versions, Visual Basic provides conversion keywords ([CType Function](../../../language-reference/functions/ctype-function.md), `CBool`, `CInt`, and so on) to convert between `Boolean` and numeric types.</span></span> <span data-ttu-id="71ff5-137">Toutefois, d’autres langages effectuent parfois ces conversions différemment, comme les méthodes .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="71ff5-137">However, other languages sometimes perform these conversions differently, as do the .NET Framework methods.</span></span>

<span data-ttu-id="71ff5-138">Vous ne devez jamais écrire du code qui s’appuie sur des valeurs numériques équivalentes pour `True` et `False` .</span><span class="sxs-lookup"><span data-stu-id="71ff5-138">You should never write code that relies on equivalent numeric values for `True` and `False`.</span></span> <span data-ttu-id="71ff5-139">Dans la mesure du possible, limitez l’utilisation des `Boolean` variables aux valeurs logiques pour lesquelles elles sont conçues.</span><span class="sxs-lookup"><span data-stu-id="71ff5-139">Whenever possible, you should restrict usage of `Boolean` variables to the logical values for which they are designed.</span></span> <span data-ttu-id="71ff5-140">Si vous devez mélanger `Boolean` des valeurs et des valeurs numériques, assurez-vous que vous comprenez la méthode de conversion que vous sélectionnez.</span><span class="sxs-lookup"><span data-stu-id="71ff5-140">If you must mix `Boolean` and numeric values, make sure that you understand the conversion method that you select.</span></span>

### <a name="conversion-in-visual-basic"></a><span data-ttu-id="71ff5-141">Conversion en Visual Basic</span><span class="sxs-lookup"><span data-stu-id="71ff5-141">Conversion in Visual Basic</span></span>

<span data-ttu-id="71ff5-142">Lorsque vous utilisez les `CType` `CBool` Mots clés de conversion ou pour convertir des types de données numériques en `Boolean` , 0 devient `False` et toutes les autres valeurs deviennent `True` .</span><span class="sxs-lookup"><span data-stu-id="71ff5-142">When you use the `CType` or `CBool` conversion keywords to convert numeric data types to `Boolean`, 0 becomes `False` and all other values become `True`.</span></span> <span data-ttu-id="71ff5-143">Lorsque vous convertissez des `Boolean` valeurs en types numériques à l’aide des mots clés de conversion, `False` devient 0 et `True` devient-1.</span><span class="sxs-lookup"><span data-stu-id="71ff5-143">When you convert `Boolean` values to numeric types by using the conversion keywords, `False` becomes 0 and `True` becomes -1.</span></span>

### <a name="conversion-in-the-framework"></a><span data-ttu-id="71ff5-144">Conversion dans le Framework</span><span class="sxs-lookup"><span data-stu-id="71ff5-144">Conversion in the Framework</span></span>

<span data-ttu-id="71ff5-145">La <xref:System.Convert.ToInt32%2A> méthode de la <xref:System.Convert> classe de l' <xref:System> espace de noms est convertie `True` en + 1.</span><span class="sxs-lookup"><span data-stu-id="71ff5-145">The <xref:System.Convert.ToInt32%2A> method of the <xref:System.Convert> class in the <xref:System> namespace converts `True` to +1.</span></span>

<span data-ttu-id="71ff5-146">Si vous devez convertir une `Boolean` valeur en un type de données numérique, soyez prudent quant à la méthode de conversion que vous utilisez.</span><span class="sxs-lookup"><span data-stu-id="71ff5-146">If you must convert a `Boolean` value to a numeric data type, be careful about which conversion method you use.</span></span>

## <a name="character-literal-generates-compiler-error"></a><span data-ttu-id="71ff5-147">Le littéral de caractère génère une erreur du compilateur</span><span class="sxs-lookup"><span data-stu-id="71ff5-147">Character Literal Generates Compiler Error</span></span>

<span data-ttu-id="71ff5-148">En l’absence de caractères de type, Visual Basic utilise des types de données par défaut pour les littéraux.</span><span class="sxs-lookup"><span data-stu-id="71ff5-148">In the absence of any type characters, Visual Basic assumes default data types for literals.</span></span> <span data-ttu-id="71ff5-149">Le type par défaut d’un littéral de caractère, placé entre guillemets ( `" "` ), est `String` .</span><span class="sxs-lookup"><span data-stu-id="71ff5-149">The default type for a character literal — enclosed in quotation marks (`" "`) — is `String`.</span></span>

<span data-ttu-id="71ff5-150">Le `String` type de données ne s’étend pas au [type de données char](../../../language-reference/data-types/char-data-type.md).</span><span class="sxs-lookup"><span data-stu-id="71ff5-150">The `String` data type does not widen to the [Char Data Type](../../../language-reference/data-types/char-data-type.md).</span></span> <span data-ttu-id="71ff5-151">Cela signifie que si vous souhaitez assigner un littéral à une `Char` variable, vous devez effectuer une conversion restrictive ou forcer le littéral au `Char` type.</span><span class="sxs-lookup"><span data-stu-id="71ff5-151">This means that if you want to assign a literal to a `Char` variable, you must either make a narrowing conversion or force the literal to the `Char` type.</span></span>

|<span data-ttu-id="71ff5-152">Pour créer un littéral de caractère à assigner à une variable ou une constante</span><span class="sxs-lookup"><span data-stu-id="71ff5-152">To create a Char literal to assign to a variable or constant</span></span>|
|---|
|<span data-ttu-id="71ff5-153">1. déclarez la variable ou la constante comme `Char` .</span><span class="sxs-lookup"><span data-stu-id="71ff5-153">1.  Declare the variable or constant as `Char`.</span></span><br /><span data-ttu-id="71ff5-154">2. Placez la valeur de caractère entre guillemets ( `" "` ).</span><span class="sxs-lookup"><span data-stu-id="71ff5-154">2.  Enclose the character value in quotation marks (`" "`).</span></span><br /><span data-ttu-id="71ff5-155">3. Suivez le guillemet double fermant avec le caractère de type `C` de littéral pour forcer le littéral `Char` .</span><span class="sxs-lookup"><span data-stu-id="71ff5-155">3.  Follow the closing double quotation mark with the literal type character `C` to force the literal to `Char`.</span></span> <span data-ttu-id="71ff5-156">Cela est nécessaire si le commutateur de vérification de type ([Option Strict Statement](../../../language-reference/statements/option-strict-statement.md)) est `On` , et il est souhaitable dans tous les cas.</span><span class="sxs-lookup"><span data-stu-id="71ff5-156">This is necessary if the type checking switch ([Option Strict Statement](../../../language-reference/statements/option-strict-statement.md)) is `On`, and it is desirable in any case.</span></span>|

<span data-ttu-id="71ff5-157">L’exemple suivant montre les assignations infructueuses et réussies d’un littéral à une `Char` variable.</span><span class="sxs-lookup"><span data-stu-id="71ff5-157">The following example demonstrates both unsuccessful and successful assignments of a literal to a `Char` variable.</span></span>

[!code-vb[VbVbalrDataTypes#12](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrDataTypes/VB/Class1.vb#12)]

<span data-ttu-id="71ff5-158">Il y a toujours un risque d’utiliser des conversions restrictives, car elles peuvent échouer au moment de l’exécution.</span><span class="sxs-lookup"><span data-stu-id="71ff5-158">There is always a risk in using narrowing conversions, because they can fail at run time.</span></span> <span data-ttu-id="71ff5-159">Par exemple, une conversion de `String` en `Char` peut échouer si la `String` valeur contient plusieurs caractères.</span><span class="sxs-lookup"><span data-stu-id="71ff5-159">For example, a conversion from `String` to `Char` can fail if the `String` value contains more than one character.</span></span> <span data-ttu-id="71ff5-160">Par conséquent, il est préférable d’utiliser le `C` caractère de type.</span><span class="sxs-lookup"><span data-stu-id="71ff5-160">Therefore, it is better programming to use the `C` type character.</span></span>

## <a name="string-conversion-fails-at-run-time"></a><span data-ttu-id="71ff5-161">La conversion de chaîne échoue au moment de l’exécution</span><span class="sxs-lookup"><span data-stu-id="71ff5-161">String Conversion Fails at Run Time</span></span>

<span data-ttu-id="71ff5-162">Le [type de données String](../../../language-reference/data-types/string-data-type.md) participe à très peu de conversions étendues.</span><span class="sxs-lookup"><span data-stu-id="71ff5-162">The [String Data Type](../../../language-reference/data-types/string-data-type.md) participates in very few widening conversions.</span></span> <span data-ttu-id="71ff5-163">`String`s’étend uniquement à lui-même et `Object` , et seuls `Char` et `Char()` (un `Char` tableau) s’étendent à `String` .</span><span class="sxs-lookup"><span data-stu-id="71ff5-163">`String` widens only to itself and `Object`, and only `Char` and `Char()` (a `Char` array) widen to `String`.</span></span> <span data-ttu-id="71ff5-164">Cela est dû au fait que `String` les variables et les constantes peuvent contenir des valeurs que d’autres types de données ne peuvent pas contenir.</span><span class="sxs-lookup"><span data-stu-id="71ff5-164">This is because `String` variables and constants can contain values that other data types cannot contain.</span></span>

<span data-ttu-id="71ff5-165">Quand le commutateur de vérification de type ([Option Strict Statement](../../../language-reference/statements/option-strict-statement.md)) est `On` , le compilateur n’autorise pas toutes les conversions restrictives implicites.</span><span class="sxs-lookup"><span data-stu-id="71ff5-165">When the type checking switch ([Option Strict Statement](../../../language-reference/statements/option-strict-statement.md)) is `On`, the compiler disallows all implicit narrowing conversions.</span></span> <span data-ttu-id="71ff5-166">Cela comprend ceux qui impliquent `String` .</span><span class="sxs-lookup"><span data-stu-id="71ff5-166">This includes those involving `String`.</span></span> <span data-ttu-id="71ff5-167">Votre code peut toujours utiliser des mots clés de conversion, tels que `CStr` et la [fonction CType](../../../language-reference/functions/ctype-function.md), qui dirigent le .NET Framework à tenter la conversion.</span><span class="sxs-lookup"><span data-stu-id="71ff5-167">Your code can still use conversion keywords such as `CStr` and [CType Function](../../../language-reference/functions/ctype-function.md), which direct the .NET Framework to attempt the conversion.</span></span>

> [!NOTE]
> <span data-ttu-id="71ff5-168">L’erreur de conversion restrictive est supprimée pour les conversions des éléments d’une `For Each…Next` collection vers la variable de contrôle de boucle.</span><span class="sxs-lookup"><span data-stu-id="71ff5-168">The narrowing-conversion error is suppressed for conversions from the elements in a `For Each…Next` collection to the loop control variable.</span></span> <span data-ttu-id="71ff5-169">Pour plus d’informations et d’exemples, consultez la section « conversions restrictives » dans [for each... Instruction suivante](../../../language-reference/statements/for-each-next-statement.md).</span><span class="sxs-lookup"><span data-stu-id="71ff5-169">For more information and examples, see the "Narrowing Conversions" section in [For Each...Next Statement](../../../language-reference/statements/for-each-next-statement.md).</span></span>

### <a name="narrowing-conversion-protection"></a><span data-ttu-id="71ff5-170">Protection de la conversion restrictive</span><span class="sxs-lookup"><span data-stu-id="71ff5-170">Narrowing Conversion Protection</span></span>

<span data-ttu-id="71ff5-171">L’inconvénient des conversions restrictives réside dans le fait qu’elles peuvent échouer au moment de l’exécution.</span><span class="sxs-lookup"><span data-stu-id="71ff5-171">The disadvantage of narrowing conversions is that they can fail at run time.</span></span> <span data-ttu-id="71ff5-172">Par exemple, si une `String` variable contient une valeur autre que « true » ou « false », elle ne peut pas être convertie en `Boolean` .</span><span class="sxs-lookup"><span data-stu-id="71ff5-172">For example, if a `String` variable contains anything other than "True" or "False," it cannot be converted to `Boolean`.</span></span> <span data-ttu-id="71ff5-173">S’il contient des caractères de ponctuation, la conversion en un type numérique échoue.</span><span class="sxs-lookup"><span data-stu-id="71ff5-173">If it contains punctuation characters, conversion to any numeric type fails.</span></span> <span data-ttu-id="71ff5-174">À moins que vous sachiez que votre `String` variable contient toujours des valeurs que le type de destination peut accepter, vous ne devez pas essayer de convertir.</span><span class="sxs-lookup"><span data-stu-id="71ff5-174">Unless you know that your `String` variable always holds values that the destination type can accept, you should not try a conversion.</span></span>

<span data-ttu-id="71ff5-175">Si vous devez `String` effectuer une conversion de vers un autre type de données, la procédure la plus sûre consiste à encadrer la tentative de conversion dans le [bloc try... Catch... Finally, instruction](../../../language-reference/statements/try-catch-finally-statement.md).</span><span class="sxs-lookup"><span data-stu-id="71ff5-175">If you must convert from `String` to another data type, the safest procedure is to enclose the attempted conversion in the [Try...Catch...Finally Statement](../../../language-reference/statements/try-catch-finally-statement.md).</span></span> <span data-ttu-id="71ff5-176">Cela vous permet de gérer un échec au moment de l’exécution.</span><span class="sxs-lookup"><span data-stu-id="71ff5-176">This lets you deal with a run-time failure.</span></span>

### <a name="character-arrays"></a><span data-ttu-id="71ff5-177">Tableaux de caractères</span><span class="sxs-lookup"><span data-stu-id="71ff5-177">Character Arrays</span></span>

<span data-ttu-id="71ff5-178">Un unique `Char` et un tableau d' `Char` éléments s’étendent tous deux à `String` .</span><span class="sxs-lookup"><span data-stu-id="71ff5-178">A single `Char` and an array of `Char` elements both widen to `String`.</span></span> <span data-ttu-id="71ff5-179">Toutefois, `String` n’est pas étendu à `Char()` .</span><span class="sxs-lookup"><span data-stu-id="71ff5-179">However, `String` does not widen to `Char()`.</span></span> <span data-ttu-id="71ff5-180">Pour convertir une `String` valeur en `Char` tableau, vous pouvez utiliser la <xref:System.String.ToCharArray%2A> méthode de la <xref:System.String?displayProperty=nameWithType> classe.</span><span class="sxs-lookup"><span data-stu-id="71ff5-180">To convert a `String` value to a `Char` array, you can use the <xref:System.String.ToCharArray%2A> method of the <xref:System.String?displayProperty=nameWithType> class.</span></span>

### <a name="meaningless-values"></a><span data-ttu-id="71ff5-181">Valeurs sans signification</span><span class="sxs-lookup"><span data-stu-id="71ff5-181">Meaningless Values</span></span>

<span data-ttu-id="71ff5-182">En général, les `String` valeurs ne sont pas significatives dans les autres types de données, et la conversion est extrêmement artificielle et dangereuse.</span><span class="sxs-lookup"><span data-stu-id="71ff5-182">In general, `String` values are not meaningful in other data types, and conversion is highly artificial and dangerous.</span></span> <span data-ttu-id="71ff5-183">Dans la mesure du possible, limitez l’utilisation des `String` variables aux séquences de caractères pour lesquelles elles sont conçues.</span><span class="sxs-lookup"><span data-stu-id="71ff5-183">Whenever possible, you should restrict usage of `String` variables to the character sequences for which they are designed.</span></span> <span data-ttu-id="71ff5-184">Vous ne devez jamais écrire du code qui s’appuie sur des valeurs équivalentes dans d’autres types.</span><span class="sxs-lookup"><span data-stu-id="71ff5-184">You should never write code that relies on equivalent values in other types.</span></span>

## <a name="see-also"></a><span data-ttu-id="71ff5-185">Voir aussi</span><span class="sxs-lookup"><span data-stu-id="71ff5-185">See also</span></span>

- [<span data-ttu-id="71ff5-186">Types de données</span><span class="sxs-lookup"><span data-stu-id="71ff5-186">Data Types</span></span>](index.md)
- [<span data-ttu-id="71ff5-187">Caractères de type</span><span class="sxs-lookup"><span data-stu-id="71ff5-187">Type Characters</span></span>](type-characters.md)
- [<span data-ttu-id="71ff5-188">Types valeur et types référence</span><span class="sxs-lookup"><span data-stu-id="71ff5-188">Value Types and Reference Types</span></span>](value-types-and-reference-types.md)
- [<span data-ttu-id="71ff5-189">Conversions de type en Visual Basic</span><span class="sxs-lookup"><span data-stu-id="71ff5-189">Type Conversions in Visual Basic</span></span>](type-conversions.md)
- [<span data-ttu-id="71ff5-190">Types de données</span><span class="sxs-lookup"><span data-stu-id="71ff5-190">Data Types</span></span>](../../../language-reference/data-types/index.md)
- [<span data-ttu-id="71ff5-191">Type Conversion Functions</span><span class="sxs-lookup"><span data-stu-id="71ff5-191">Type Conversion Functions</span></span>](../../../language-reference/functions/type-conversion-functions.md)
- [<span data-ttu-id="71ff5-192">Utilisation efficace des types de données</span><span class="sxs-lookup"><span data-stu-id="71ff5-192">Efficient Use of Data Types</span></span>](efficient-use-of-data-types.md)
