---
title: Dépannage des types de données (Visual Basic)
ms.date: 07/20/2015
helpviewer_keywords:
- Char data type [Visual Basic], converting
- Decimal data type [Visual Basic], conversions
- data types [Visual Basic], troubleshooting
- literals [Visual Basic], default types
- type characters [Visual Basic], literal
- Mod operator [Visual Basic], in floating-point operations
- troubleshooting Visual Basic, data types
- troubleshooting data types [Visual Basic]
- floating-point numbers [Visual Basic], precision
- Boolean data type [Visual Basic], converting
- literal types [Visual Basic]
- literal type characters [Visual Basic]
- floating-point numbers [Visual Basic], imprecision
- String data type [Visual Basic], converting
- floating-point numbers [Visual Basic], comparison
- floating-point numbers
ms.assetid: 90040d67-b630-4125-a6ae-37195b079042
ms.openlocfilehash: 5b2cb0d5270b7e14c3462aeaf54942f939511fd7
ms.sourcegitcommit: 68653db98c5ea7744fd438710248935f70020dfb
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 08/22/2019
ms.locfileid: "69933229"
---
# <a name="troubleshooting-data-types-visual-basic"></a><span data-ttu-id="09434-102">Dépannage des types de données (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="09434-102">Troubleshooting Data Types (Visual Basic)</span></span>
<span data-ttu-id="09434-103">Cette page répertorie certains problèmes courants qui peuvent se produire lorsque vous effectuez des opérations sur des types de données intrinsèques.</span><span class="sxs-lookup"><span data-stu-id="09434-103">This page lists some common problems that can occur when you perform operations on intrinsic data types.</span></span>  
  
## <a name="floating-point-expressions-do-not-compare-as-equal"></a><span data-ttu-id="09434-104">Les expressions à virgule flottante ne sont pas considérées comme égales</span><span class="sxs-lookup"><span data-stu-id="09434-104">Floating-Point Expressions Do Not Compare as Equal</span></span>  
 <span data-ttu-id="09434-105">Lorsque vous travaillez avec des nombres à virgule flottante ([type de données unique](../../../../visual-basic/language-reference/data-types/single-data-type.md) et [type de données double](../../../../visual-basic/language-reference/data-types/double-data-type.md)), n’oubliez pas qu’ils sont stockés sous forme de fractions binaires.</span><span class="sxs-lookup"><span data-stu-id="09434-105">When you work with floating-point numbers ([Single Data Type](../../../../visual-basic/language-reference/data-types/single-data-type.md) and [Double Data Type](../../../../visual-basic/language-reference/data-types/double-data-type.md)), remember that they are stored as binary fractions.</span></span> <span data-ttu-id="09434-106">Cela signifie qu’elles ne peuvent pas contenir une représentation exacte d’une quantité qui n’est pas une fraction binaire (au format k/(2 ^ n) où k et n sont des entiers).</span><span class="sxs-lookup"><span data-stu-id="09434-106">This means they cannot hold an exact representation of any quantity that is not a binary fraction (of the form k / (2 ^ n) where k and n are integers).</span></span> <span data-ttu-id="09434-107">Par exemple, 0,5 (= 1/2) et 0,3125 (= 5/16) peuvent être conservés comme valeurs précises, alors que 0,2 (= 1/5) et 0,3 (= 3/10) peuvent uniquement être des approximations.</span><span class="sxs-lookup"><span data-stu-id="09434-107">For example, 0.5 (= 1/2) and 0.3125 (= 5/16) can be held as precise values, whereas 0.2 (= 1/5) and 0.3 (= 3/10) can be only approximations.</span></span>  
  
 <span data-ttu-id="09434-108">En raison de ce imprécision, vous ne pouvez pas vous reposer sur des résultats exacts lorsque vous utilisez des valeurs à virgule flottante.</span><span class="sxs-lookup"><span data-stu-id="09434-108">Because of this imprecision, you cannot rely on exact results when you operate on floating-point values.</span></span> <span data-ttu-id="09434-109">En particulier, deux valeurs théoriquement égales peuvent avoir des représentations légèrement différentes.</span><span class="sxs-lookup"><span data-stu-id="09434-109">In particular, two values that are theoretically equal might have slightly different representations.</span></span>  
  
| <span data-ttu-id="09434-110">Pour comparer les quantités à virgule flottante</span><span class="sxs-lookup"><span data-stu-id="09434-110">To compare floating-point quantities</span></span> | 
|---| 
|<span data-ttu-id="09434-111">1.  Calculez la valeur absolue de la différence à l' <xref:System.Math.Abs%2A> aide de la <xref:System.Math> méthode de la <xref:System> classe dans l’espace de noms.</span><span class="sxs-lookup"><span data-stu-id="09434-111">1.  Calculate the absolute value of their difference by using the <xref:System.Math.Abs%2A> method of the <xref:System.Math> class in the <xref:System> namespace.</span></span><br /><span data-ttu-id="09434-112">2.  Déterminez une différence maximale acceptable, de sorte que vous pouvez considérer les deux quantités comme étant égales pour des raisons pratiques si leur différence n’est pas plus importante.</span><span class="sxs-lookup"><span data-stu-id="09434-112">2.  Determine an acceptable maximum difference, such that you can consider the two quantities to be equal for practical purposes if their difference is no larger.</span></span><br /><span data-ttu-id="09434-113">3.  Comparez la valeur absolue de la différence à la différence acceptable.</span><span class="sxs-lookup"><span data-stu-id="09434-113">3.  Compare the absolute value of the difference to the acceptable difference.</span></span>|  
  
 <span data-ttu-id="09434-114">L’exemple suivant montre à la fois une comparaison incorrecte et correcte de deux `Double` valeurs.</span><span class="sxs-lookup"><span data-stu-id="09434-114">The following example demonstrates both incorrect and correct comparison of two `Double` values.</span></span>  
  
 [!code-vb[VbVbalrDataTypes#10](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrDataTypes/VB/Class1.vb#10)]  
  
 <span data-ttu-id="09434-115">L’exemple précédent utilise la <xref:System.Double.ToString%2A> méthode de la <xref:System.Double> structure afin qu’il puisse spécifier une meilleure précision que `CStr` le mot clé utilise.</span><span class="sxs-lookup"><span data-stu-id="09434-115">The previous example uses the <xref:System.Double.ToString%2A> method of the <xref:System.Double> structure so that it can specify better  precision than the `CStr` keyword uses.</span></span> <span data-ttu-id="09434-116">La valeur par défaut est 15 chiffres, mais le format «G17» l’étend jusqu’à 17 chiffres.</span><span class="sxs-lookup"><span data-stu-id="09434-116">The default is 15 digits, but the "G17" format extends it to 17 digits.</span></span>  
  
## <a name="mod-operator-does-not-return-accurate-result"></a><span data-ttu-id="09434-117">L’opérateur mod ne retourne pas de résultat exact</span><span class="sxs-lookup"><span data-stu-id="09434-117">Mod Operator Does Not Return Accurate Result</span></span>  
 <span data-ttu-id="09434-118">En raison du imprécision de stockage à virgule flottante, l' [opérateur mod](../../../../visual-basic/language-reference/operators/mod-operator.md) peut retourner un résultat inattendu lorsqu’au moins l’un des opérandes est à virgule flottante.</span><span class="sxs-lookup"><span data-stu-id="09434-118">Because of the imprecision of floating-point storage, the [Mod Operator](../../../../visual-basic/language-reference/operators/mod-operator.md) can return an unexpected result when at least one of the operands is floating-point.</span></span>  
  
 <span data-ttu-id="09434-119">Le [type de données decimal](../../../../visual-basic/language-reference/data-types/decimal-data-type.md) n’utilise pas la représentation à virgule flottante.</span><span class="sxs-lookup"><span data-stu-id="09434-119">The [Decimal Data Type](../../../../visual-basic/language-reference/data-types/decimal-data-type.md) does not use floating-point representation.</span></span> <span data-ttu-id="09434-120">De nombreux nombres qui sont inexacts `Double` dans `Single` et sont `Decimal` exacts dans (par exemple 0,2 et 0,3).</span><span class="sxs-lookup"><span data-stu-id="09434-120">Many numbers that are inexact in `Single` and `Double` are exact in `Decimal` (for example 0.2 and 0.3).</span></span> <span data-ttu-id="09434-121">Bien que l’arithmétique soit `Decimal` plus lente dans que dans le calcul à virgule flottante, il peut être utile de réduire les performances pour obtenir une meilleure précision.</span><span class="sxs-lookup"><span data-stu-id="09434-121">Although arithmetic is slower in `Decimal` than in floating-point, it might be worth the performance decrease to achieve better precision.</span></span>  
  
|<span data-ttu-id="09434-122">Pour rechercher le reste entier des quantités à virgule flottante</span><span class="sxs-lookup"><span data-stu-id="09434-122">To find the integer remainder of floating-point quantities</span></span>|  
|---|  
|<span data-ttu-id="09434-123">1.  Déclarez les `Decimal`variables en tant que.</span><span class="sxs-lookup"><span data-stu-id="09434-123">1.  Declare variables as `Decimal`.</span></span><br /><span data-ttu-id="09434-124">2.  Utilisez le caractère `D` de type de littéral pour forcer des `Decimal`littéraux, si leurs valeurs sont trop élevées pour `Long` le type de données.</span><span class="sxs-lookup"><span data-stu-id="09434-124">2.  Use the literal type character `D` to force literals to `Decimal`, in case their values are too large for the `Long` data type.</span></span>|  
  
 <span data-ttu-id="09434-125">L’exemple suivant illustre le imprécision potentiel des opérandes à virgule flottante.</span><span class="sxs-lookup"><span data-stu-id="09434-125">The following example demonstrates the potential imprecision of floating-point operands.</span></span>  
  
 [!code-vb[VbVbalrDataTypes#11](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrDataTypes/VB/Class1.vb#11)]  
  
 <span data-ttu-id="09434-126">L’exemple précédent utilise la <xref:System.Double.ToString%2A> méthode de la <xref:System.Double> structure afin qu’il puisse spécifier une meilleure précision que `CStr` le mot clé utilise.</span><span class="sxs-lookup"><span data-stu-id="09434-126">The previous example uses the <xref:System.Double.ToString%2A> method of the <xref:System.Double> structure so that it can specify better precision than the `CStr` keyword uses.</span></span> <span data-ttu-id="09434-127">La valeur par défaut est 15 chiffres, mais le format «G17» l’étend jusqu’à 17 chiffres.</span><span class="sxs-lookup"><span data-stu-id="09434-127">The default is 15 digits, but the "G17" format extends it to 17 digits.</span></span>  
  
 <span data-ttu-id="09434-128">Comme `zeroPointTwo` est`Double`, sa valeur pour 0,2 est une fraction binaire à répétition infinie avec une valeur stockée de 0.20000000000000001.</span><span class="sxs-lookup"><span data-stu-id="09434-128">Because `zeroPointTwo` is `Double`, its value for 0.2 is an infinitely repeating binary fraction with a stored value of 0.20000000000000001.</span></span> <span data-ttu-id="09434-129">La Division de 2,0 par cette quantité produit 9.9999999999999995 avec un reste de 0.19999999999999991.</span><span class="sxs-lookup"><span data-stu-id="09434-129">Dividing 2.0 by this quantity yields 9.9999999999999995 with a remainder of 0.19999999999999991.</span></span>  
  
 <span data-ttu-id="09434-130">Dans l’expression pour `decimalRemainder`, le caractère `D` de type de littéral force les deux `Decimal`opérandes à, et 0,2 a une représentation précise.</span><span class="sxs-lookup"><span data-stu-id="09434-130">In the expression for `decimalRemainder`, the literal type character `D` forces both operands to `Decimal`, and 0.2 has a precise representation.</span></span> <span data-ttu-id="09434-131">Par conséquent `Mod` , l’opérateur produit le reste attendu de 0,0.</span><span class="sxs-lookup"><span data-stu-id="09434-131">Therefore the `Mod` operator yields the expected remainder of 0.0.</span></span>  
  
 <span data-ttu-id="09434-132">Notez qu’il n’est pas suffisant de `decimalRemainder` déclarer `Decimal`en tant que.</span><span class="sxs-lookup"><span data-stu-id="09434-132">Note that it is not sufficient to declare `decimalRemainder` as `Decimal`.</span></span> <span data-ttu-id="09434-133">Vous devez également forcer les littéraux sur `Decimal`, ou ils utilisent `Double` par défaut et `decimalRemainder` reçoivent la même valeur inexacte `doubleRemainder`que.</span><span class="sxs-lookup"><span data-stu-id="09434-133">You must also force the literals to `Decimal`, or they use `Double` by default and `decimalRemainder` receives the same inaccurate value as `doubleRemainder`.</span></span>  
  
## <a name="boolean-type-does-not-convert-to-numeric-type-accurately"></a><span data-ttu-id="09434-134">Le type booléen ne convertit pas correctement le type numérique</span><span class="sxs-lookup"><span data-stu-id="09434-134">Boolean Type Does Not Convert to Numeric Type Accurately</span></span>  
 <span data-ttu-id="09434-135">Les valeurs de [type de données Boolean](../../../../visual-basic/language-reference/data-types/boolean-data-type.md) ne sont pas stockées sous forme de nombres, et les valeurs stockées ne sont pas destinées à être équivalentes aux nombres.</span><span class="sxs-lookup"><span data-stu-id="09434-135">[Boolean Data Type](../../../../visual-basic/language-reference/data-types/boolean-data-type.md) values are not stored as numbers, and the stored values are not intended to be equivalent to numbers.</span></span> <span data-ttu-id="09434-136">Pour la compatibilité avec les versions antérieures, Visual Basic fournit des mots clés de `CBool`conversion `CInt`([fonction CType](../../../../visual-basic/language-reference/functions/ctype-function.md),,, etc. `Boolean` ) pour effectuer une conversion entre des types numériques et.</span><span class="sxs-lookup"><span data-stu-id="09434-136">For compatibility with earlier versions, Visual Basic provides conversion keywords ([CType Function](../../../../visual-basic/language-reference/functions/ctype-function.md), `CBool`, `CInt`, and so on) to convert between `Boolean` and numeric types.</span></span> <span data-ttu-id="09434-137">Toutefois, d’autres langages effectuent parfois ces conversions différemment, comme les méthodes .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="09434-137">However, other languages sometimes perform these conversions differently, as do the .NET Framework methods.</span></span>  
  
 <span data-ttu-id="09434-138">Vous ne devez jamais écrire du code qui s’appuie sur des valeurs `True` numériques `False`équivalentes pour et.</span><span class="sxs-lookup"><span data-stu-id="09434-138">You should never write code that relies on equivalent numeric values for `True` and `False`.</span></span> <span data-ttu-id="09434-139">Dans la mesure du possible, limitez `Boolean` l’utilisation des variables aux valeurs logiques pour lesquelles elles sont conçues.</span><span class="sxs-lookup"><span data-stu-id="09434-139">Whenever possible, you should restrict usage of `Boolean` variables to the logical values for which they are designed.</span></span> <span data-ttu-id="09434-140">Si vous devez mélanger `Boolean` des valeurs et des valeurs numériques, assurez-vous que vous comprenez la méthode de conversion que vous sélectionnez.</span><span class="sxs-lookup"><span data-stu-id="09434-140">If you must mix `Boolean` and numeric values, make sure that you understand the conversion method that you select.</span></span>  
  
### <a name="conversion-in-visual-basic"></a><span data-ttu-id="09434-141">Conversion en Visual Basic</span><span class="sxs-lookup"><span data-stu-id="09434-141">Conversion in Visual Basic</span></span>  
 <span data-ttu-id="09434-142">Lorsque vous utilisez les `CType` mots `CBool` clés de conversion ou pour convertir des types `Boolean`de données numériques `False` en, 0 devient et `True`toutes les autres valeurs deviennent.</span><span class="sxs-lookup"><span data-stu-id="09434-142">When you use the `CType` or `CBool` conversion keywords to convert numeric data types to `Boolean`, 0 becomes `False` and all other values become `True`.</span></span> <span data-ttu-id="09434-143">Lorsque vous convertissez `Boolean` des valeurs en types numériques à l’aide des mots clés de `True` conversion, `False` devient 0 et devient-1.</span><span class="sxs-lookup"><span data-stu-id="09434-143">When you convert `Boolean` values to numeric types by using the conversion keywords, `False` becomes 0 and `True` becomes -1.</span></span>  
  
### <a name="conversion-in-the-framework"></a><span data-ttu-id="09434-144">Conversion dans le Framework</span><span class="sxs-lookup"><span data-stu-id="09434-144">Conversion in the Framework</span></span>  
 <span data-ttu-id="09434-145">La <xref:System.Convert.ToInt32%2A> méthode de la <xref:System.Convert> <xref:System> classe`True` de l’espace de noms est convertie en + 1.</span><span class="sxs-lookup"><span data-stu-id="09434-145">The <xref:System.Convert.ToInt32%2A> method of the <xref:System.Convert> class in the <xref:System> namespace converts `True` to +1.</span></span>  
  
 <span data-ttu-id="09434-146">Si vous devez convertir une `Boolean` valeur en un type de données numérique, soyez prudent quant à la méthode de conversion que vous utilisez.</span><span class="sxs-lookup"><span data-stu-id="09434-146">If you must convert a `Boolean` value to a numeric data type, be careful about which conversion method you use.</span></span>  
  
## <a name="character-literal-generates-compiler-error"></a><span data-ttu-id="09434-147">Le littéral de caractère génère une erreur du compilateur</span><span class="sxs-lookup"><span data-stu-id="09434-147">Character Literal Generates Compiler Error</span></span>  
 <span data-ttu-id="09434-148">En l’absence de caractères de type, Visual Basic utilise des types de données par défaut pour les littéraux.</span><span class="sxs-lookup"><span data-stu-id="09434-148">In the absence of any type characters, Visual Basic assumes default data types for literals.</span></span> <span data-ttu-id="09434-149">Le type par défaut d’un littéral de caractère, placé entre guillemets (`" "`), est. `String`</span><span class="sxs-lookup"><span data-stu-id="09434-149">The default type for a character literal — enclosed in quotation marks (`" "`) — is `String`.</span></span>  
  
 <span data-ttu-id="09434-150">Le `String` type de données ne s’étend pas au [type de données char](../../../../visual-basic/language-reference/data-types/char-data-type.md).</span><span class="sxs-lookup"><span data-stu-id="09434-150">The `String` data type does not widen to the [Char Data Type](../../../../visual-basic/language-reference/data-types/char-data-type.md).</span></span> <span data-ttu-id="09434-151">Cela signifie que si vous souhaitez assigner un littéral à `Char` une variable, vous devez effectuer une conversion restrictive ou forcer le littéral `Char` au type.</span><span class="sxs-lookup"><span data-stu-id="09434-151">This means that if you want to assign a literal to a `Char` variable, you must either make a narrowing conversion or force the literal to the `Char` type.</span></span>  

|<span data-ttu-id="09434-152">Pour créer un littéral de caractère à assigner à une variable ou une constante</span><span class="sxs-lookup"><span data-stu-id="09434-152">To create a Char literal to assign to a variable or constant</span></span>|
|---|  
|<span data-ttu-id="09434-153">1.  Déclarez la variable ou la `Char`constante en tant que.</span><span class="sxs-lookup"><span data-stu-id="09434-153">1.  Declare the variable or constant as `Char`.</span></span><br /><span data-ttu-id="09434-154">2.  Placez la valeur de caractère entre guillemets`" "`().</span><span class="sxs-lookup"><span data-stu-id="09434-154">2.  Enclose the character value in quotation marks (`" "`).</span></span><br /><span data-ttu-id="09434-155">3.  Suivez le guillemet double fermant avec le caractère `C` de type de littéral pour forcer le `Char`littéral.</span><span class="sxs-lookup"><span data-stu-id="09434-155">3.  Follow the closing double quotation mark with the literal type character `C` to force the literal to `Char`.</span></span> <span data-ttu-id="09434-156">Cela est nécessaire si le commutateur de vérification de type ([Option Strict Statement](../../../../visual-basic/language-reference/statements/option-strict-statement.md)) est `On`, et il est souhaitable dans tous les cas.</span><span class="sxs-lookup"><span data-stu-id="09434-156">This is necessary if the type checking switch ([Option Strict Statement](../../../../visual-basic/language-reference/statements/option-strict-statement.md)) is `On`, and it is desirable in any case.</span></span>|  
  
 <span data-ttu-id="09434-157">L’exemple suivant montre les assignations infructueuses et réussies d’un `Char` littéral à une variable.</span><span class="sxs-lookup"><span data-stu-id="09434-157">The following example demonstrates both unsuccessful and successful assignments of a literal to a `Char` variable.</span></span>  
  
 [!code-vb[VbVbalrDataTypes#12](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrDataTypes/VB/Class1.vb#12)]  
  
 <span data-ttu-id="09434-158">Il y a toujours un risque d’utiliser des conversions restrictives, car elles peuvent échouer au moment de l’exécution.</span><span class="sxs-lookup"><span data-stu-id="09434-158">There is always a risk in using narrowing conversions, because they can fail at run time.</span></span> <span data-ttu-id="09434-159">Par exemple, une conversion de `String` en `Char` peut échouer si `String` la valeur contient plusieurs caractères.</span><span class="sxs-lookup"><span data-stu-id="09434-159">For example, a conversion from `String` to `Char` can fail if the `String` value contains more than one character.</span></span> <span data-ttu-id="09434-160">Par conséquent, il est préférable d’utiliser le `C` caractère de type.</span><span class="sxs-lookup"><span data-stu-id="09434-160">Therefore, it is better programming to use the `C` type character.</span></span>  
  
## <a name="string-conversion-fails-at-run-time"></a><span data-ttu-id="09434-161">La conversion de chaîne échoue au moment de l’exécution</span><span class="sxs-lookup"><span data-stu-id="09434-161">String Conversion Fails at Run Time</span></span>  
 <span data-ttu-id="09434-162">Le [type de données String](../../../../visual-basic/language-reference/data-types/string-data-type.md) participe à très peu de conversions étendues.</span><span class="sxs-lookup"><span data-stu-id="09434-162">The [String Data Type](../../../../visual-basic/language-reference/data-types/string-data-type.md) participates in very few widening conversions.</span></span> <span data-ttu-id="09434-163">`String`s’étend uniquement à lui- `Object`même et, `Char` et `Char()` seuls et `Char` (un tableau) `String`s’étendent à.</span><span class="sxs-lookup"><span data-stu-id="09434-163">`String` widens only to itself and `Object`, and only `Char` and `Char()` (a `Char` array) widen to `String`.</span></span> <span data-ttu-id="09434-164">Cela est dû `String` au fait que les variables et les constantes peuvent contenir des valeurs que d’autres types de données ne peuvent pas contenir.</span><span class="sxs-lookup"><span data-stu-id="09434-164">This is because `String` variables and constants can contain values that other data types cannot contain.</span></span>  
  
 <span data-ttu-id="09434-165">Quand le commutateur de vérification de type ([Option Strict Statement](../../../../visual-basic/language-reference/statements/option-strict-statement.md)) est, le compilateur n' `On`autorise pas toutes les conversions restrictives implicites.</span><span class="sxs-lookup"><span data-stu-id="09434-165">When the type checking switch ([Option Strict Statement](../../../../visual-basic/language-reference/statements/option-strict-statement.md)) is `On`, the compiler disallows all implicit narrowing conversions.</span></span> <span data-ttu-id="09434-166">Cela comprend ceux qui `String`impliquent.</span><span class="sxs-lookup"><span data-stu-id="09434-166">This includes those involving `String`.</span></span> <span data-ttu-id="09434-167">Votre code peut toujours utiliser des mots clés de `CStr` conversion, tels que et la [fonction CType](../../../../visual-basic/language-reference/functions/ctype-function.md), qui dirigent le .NET Framework à tenter la conversion.</span><span class="sxs-lookup"><span data-stu-id="09434-167">Your code can still use conversion keywords such as `CStr` and [CType Function](../../../../visual-basic/language-reference/functions/ctype-function.md), which direct the .NET Framework to attempt the conversion.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="09434-168">L’erreur de conversion restrictive est supprimée pour les conversions des éléments d’une `For Each…Next` collection vers la variable de contrôle de boucle.</span><span class="sxs-lookup"><span data-stu-id="09434-168">The narrowing-conversion error is suppressed for conversions from the elements in a `For Each…Next` collection to the loop control variable.</span></span> <span data-ttu-id="09434-169">Pour plus d’informations et d’exemples, consultez la section «conversions restrictives» dans [for each... Instruction suivante](../../../../visual-basic/language-reference/statements/for-each-next-statement.md).</span><span class="sxs-lookup"><span data-stu-id="09434-169">For more information and examples, see the "Narrowing Conversions" section in [For Each...Next Statement](../../../../visual-basic/language-reference/statements/for-each-next-statement.md).</span></span>  
  
### <a name="narrowing-conversion-protection"></a><span data-ttu-id="09434-170">Protection de la conversion restrictive</span><span class="sxs-lookup"><span data-stu-id="09434-170">Narrowing Conversion Protection</span></span>  
 <span data-ttu-id="09434-171">L’inconvénient des conversions restrictives réside dans le fait qu’elles peuvent échouer au moment de l’exécution.</span><span class="sxs-lookup"><span data-stu-id="09434-171">The disadvantage of narrowing conversions is that they can fail at run time.</span></span> <span data-ttu-id="09434-172">Par exemple, si une `String` variable contient une valeur autre que «true» ou «false», elle ne peut `Boolean`pas être convertie en.</span><span class="sxs-lookup"><span data-stu-id="09434-172">For example, if a `String` variable contains anything other than "True" or "False," it cannot be converted to `Boolean`.</span></span> <span data-ttu-id="09434-173">S’il contient des caractères de ponctuation, la conversion en un type numérique échoue.</span><span class="sxs-lookup"><span data-stu-id="09434-173">If it contains punctuation characters, conversion to any numeric type fails.</span></span> <span data-ttu-id="09434-174">À moins que vous sachiez que votre variable contient toujours des `String` valeurs que le type de destination peut accepter, vous ne devez pas essayer de convertir.</span><span class="sxs-lookup"><span data-stu-id="09434-174">Unless you know that your `String` variable always holds values that the destination type can accept, you should not try a conversion.</span></span>  
  
 <span data-ttu-id="09434-175">Si vous devez effectuer une `String` conversion de vers un autre type de données, la procédure la plus sûre consiste à encadrer la tentative de conversion dans le [bloc try... Catch... Finally, instruction](../../../../visual-basic/language-reference/statements/try-catch-finally-statement.md).</span><span class="sxs-lookup"><span data-stu-id="09434-175">If you must convert from `String` to another data type, the safest procedure is to enclose the attempted conversion in the [Try...Catch...Finally Statement](../../../../visual-basic/language-reference/statements/try-catch-finally-statement.md).</span></span> <span data-ttu-id="09434-176">Cela vous permet de gérer un échec au moment de l’exécution.</span><span class="sxs-lookup"><span data-stu-id="09434-176">This lets you deal with a run-time failure.</span></span>  
  
### <a name="character-arrays"></a><span data-ttu-id="09434-177">Tableaux de caractères</span><span class="sxs-lookup"><span data-stu-id="09434-177">Character Arrays</span></span>  
 <span data-ttu-id="09434-178">Un unique `Char` et un tableau d' `Char` éléments s’étendent `String`tous deux à.</span><span class="sxs-lookup"><span data-stu-id="09434-178">A single `Char` and an array of `Char` elements both widen to `String`.</span></span> <span data-ttu-id="09434-179">Toutefois, `String` n’est pas étendu `Char()`à.</span><span class="sxs-lookup"><span data-stu-id="09434-179">However, `String` does not widen to `Char()`.</span></span> <span data-ttu-id="09434-180">Pour convertir une `String` valeur `Char` en tableau, vous pouvez utiliser la <xref:System.String.ToCharArray%2A> méthode de la <xref:System.String?displayProperty=nameWithType> classe.</span><span class="sxs-lookup"><span data-stu-id="09434-180">To convert a `String` value to a `Char` array, you can use the <xref:System.String.ToCharArray%2A> method of the <xref:System.String?displayProperty=nameWithType> class.</span></span>  
  
### <a name="meaningless-values"></a><span data-ttu-id="09434-181">Valeurs sans signification</span><span class="sxs-lookup"><span data-stu-id="09434-181">Meaningless Values</span></span>  
 <span data-ttu-id="09434-182">En général, `String` les valeurs ne sont pas significatives dans les autres types de données, et la conversion est extrêmement artificielle et dangereuse.</span><span class="sxs-lookup"><span data-stu-id="09434-182">In general, `String` values are not meaningful in other data types, and conversion is highly artificial and dangerous.</span></span> <span data-ttu-id="09434-183">Dans la mesure du possible, limitez `String` l’utilisation des variables aux séquences de caractères pour lesquelles elles sont conçues.</span><span class="sxs-lookup"><span data-stu-id="09434-183">Whenever possible, you should restrict usage of `String` variables to the character sequences for which they are designed.</span></span> <span data-ttu-id="09434-184">Vous ne devez jamais écrire du code qui s’appuie sur des valeurs équivalentes dans d’autres types.</span><span class="sxs-lookup"><span data-stu-id="09434-184">You should never write code that relies on equivalent values in other types.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="09434-185">Voir aussi</span><span class="sxs-lookup"><span data-stu-id="09434-185">See also</span></span>

- [<span data-ttu-id="09434-186">Types de données</span><span class="sxs-lookup"><span data-stu-id="09434-186">Data Types</span></span>](../../../../visual-basic/programming-guide/language-features/data-types/index.md)
- [<span data-ttu-id="09434-187">Caractères de type</span><span class="sxs-lookup"><span data-stu-id="09434-187">Type Characters</span></span>](../../../../visual-basic/programming-guide/language-features/data-types/type-characters.md)
- [<span data-ttu-id="09434-188">Value Types and Reference Types</span><span class="sxs-lookup"><span data-stu-id="09434-188">Value Types and Reference Types</span></span>](../../../../visual-basic/programming-guide/language-features/data-types/value-types-and-reference-types.md)
- [<span data-ttu-id="09434-189">Conversions de type dans Visual Basic</span><span class="sxs-lookup"><span data-stu-id="09434-189">Type Conversions in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/data-types/type-conversions.md)
- [<span data-ttu-id="09434-190">Types de données</span><span class="sxs-lookup"><span data-stu-id="09434-190">Data Types</span></span>](../../../../visual-basic/language-reference/data-types/index.md)
- [<span data-ttu-id="09434-191">Fonctions de conversion de types</span><span class="sxs-lookup"><span data-stu-id="09434-191">Type Conversion Functions</span></span>](../../../../visual-basic/language-reference/functions/type-conversion-functions.md)
- [<span data-ttu-id="09434-192">Utilisation efficace des types de données</span><span class="sxs-lookup"><span data-stu-id="09434-192">Efficient Use of Data Types</span></span>](../../../../visual-basic/programming-guide/language-features/data-types/efficient-use-of-data-types.md)
