---
title: Quotations de code
description: 'En savoir plus sur les Quotations de code F #, une fonctionnalité de langage qui vous permet de générer et d’utiliser des expressions de code F # par programmation.'
ms.date: 08/13/2020
ms.openlocfilehash: dc37fdbd6cd29e5ee94e5c0186dfe2bfeb666f32
ms.sourcegitcommit: f99115e12a5eb75638abe45072e023a3ce3351ac
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 11/12/2020
ms.locfileid: "94557192"
---
# <a name="code-quotations"></a><span data-ttu-id="62c52-103">Quotations de code</span><span class="sxs-lookup"><span data-stu-id="62c52-103">Code quotations</span></span>

<span data-ttu-id="62c52-104">Cet article décrit les *Quotations de code* , une fonctionnalité de langage qui vous permet de générer et d’utiliser des expressions de code F # par programmation.</span><span class="sxs-lookup"><span data-stu-id="62c52-104">This article describes *code quotations* , a language feature that enables you to generate and work with F# code expressions programmatically.</span></span> <span data-ttu-id="62c52-105">Cette fonctionnalité vous permet de générer une arborescence de syntaxe abstraite qui représente le code F #.</span><span class="sxs-lookup"><span data-stu-id="62c52-105">This feature lets you generate an abstract syntax tree that represents F# code.</span></span> <span data-ttu-id="62c52-106">L’arborescence de syntaxe abstraite peut ensuite être parcourue et traitée en fonction des besoins de votre application.</span><span class="sxs-lookup"><span data-stu-id="62c52-106">The abstract syntax tree can then be traversed and processed according to the needs of your application.</span></span> <span data-ttu-id="62c52-107">Par exemple, vous pouvez utiliser l’arborescence pour générer du code F # ou générer du code dans un autre langage.</span><span class="sxs-lookup"><span data-stu-id="62c52-107">For example, you can use the tree to generate F# code or generate code in some other language.</span></span>

## <a name="quoted-expressions"></a><span data-ttu-id="62c52-108">Expressions entre guillemets</span><span class="sxs-lookup"><span data-stu-id="62c52-108">Quoted Expressions</span></span>

<span data-ttu-id="62c52-109">Une *expression entre guillemets* est une expression f # dans votre code qui est délimitée de telle façon qu’elle n’est pas compilée dans le cadre de votre programme, mais à la place, elle est compilée dans un objet qui représente une expression F #.</span><span class="sxs-lookup"><span data-stu-id="62c52-109">A *quoted expression* is an F# expression in your code that is delimited in such a way that it is not compiled as part of your program, but instead is compiled into an object that represents an F# expression.</span></span> <span data-ttu-id="62c52-110">Vous pouvez marquer une expression entre guillemets de l’une des deux manières suivantes : avec les informations de type ou sans informations de type.</span><span class="sxs-lookup"><span data-stu-id="62c52-110">You can mark a quoted expression in one of two ways: either with type information or without type information.</span></span> <span data-ttu-id="62c52-111">Si vous souhaitez inclure des informations de type, vous utilisez les symboles `<@` et `@>` pour délimiter l’expression entre guillemets.</span><span class="sxs-lookup"><span data-stu-id="62c52-111">If you want to include type information, you use the symbols `<@` and `@>` to delimit the quoted expression.</span></span> <span data-ttu-id="62c52-112">Si vous n’avez pas besoin d’informations de type, vous utilisez les symboles `<@@` et `@@>` .</span><span class="sxs-lookup"><span data-stu-id="62c52-112">If you do not need type information, you use the symbols `<@@` and `@@>`.</span></span> <span data-ttu-id="62c52-113">Le code suivant illustre des Quotations typées et non typées.</span><span class="sxs-lookup"><span data-stu-id="62c52-113">The following code shows typed and untyped quotations.</span></span>

[!code-fsharp[Main](~/samples/snippets/fsharp/lang-ref-3/snippet501.fs)]

<span data-ttu-id="62c52-114">Parcourir une grande arborescence d’expressions est plus rapide si vous n’incluez pas d’informations de type.</span><span class="sxs-lookup"><span data-stu-id="62c52-114">Traversing a large expression tree is faster if you do not include type information.</span></span> <span data-ttu-id="62c52-115">Le type résultant d’une expression entre guillemets avec les symboles typés est `Expr<'T>` , où le paramètre de type a le type de l’expression tel que déterminé par l’algorithme d’inférence de type du compilateur F #.</span><span class="sxs-lookup"><span data-stu-id="62c52-115">The resulting type of an expression quoted with the typed symbols is `Expr<'T>`, where the type parameter has the type of the expression as determined by the F# compiler's type inference algorithm.</span></span> <span data-ttu-id="62c52-116">Quand vous utilisez des Quotations de code sans informations de type, le type de l’expression entre guillemets est le type non générique [expr](https://fsharp.github.io/fsharp-core-docs/reference/fsharp-quotations-fsharpexpr.html).</span><span class="sxs-lookup"><span data-stu-id="62c52-116">When you use code quotations without type information, the type of the quoted expression is the non-generic type [Expr](https://fsharp.github.io/fsharp-core-docs/reference/fsharp-quotations-fsharpexpr.html).</span></span> <span data-ttu-id="62c52-117">Vous pouvez appeler la propriété [RAW](https://fsharp.github.io/fsharp-core-docs/reference/fsharp-quotations-fsharpexpr-1.html#Raw) sur la classe typée `Expr` pour obtenir l’objet non typé `Expr` .</span><span class="sxs-lookup"><span data-stu-id="62c52-117">You can call the [Raw](https://fsharp.github.io/fsharp-core-docs/reference/fsharp-quotations-fsharpexpr-1.html#Raw) property on the typed `Expr` class to obtain the untyped `Expr` object.</span></span>

<span data-ttu-id="62c52-118">Il existe une variété de méthodes statiques qui vous permettent de générer des objets d’expression F # par programmation dans la `Expr` classe sans utiliser d’expressions entre guillemets.</span><span class="sxs-lookup"><span data-stu-id="62c52-118">There are a variety of static methods that allow you to generate F# expression objects programmatically in the `Expr` class without using quoted expressions.</span></span>

<span data-ttu-id="62c52-119">Notez qu’un devis de code doit inclure une expression complète.</span><span class="sxs-lookup"><span data-stu-id="62c52-119">Note that a code quotation must include a complete expression.</span></span> <span data-ttu-id="62c52-120">Pour une `let` liaison, par exemple, vous avez besoin à la fois de la définition du nom lié et d’une expression supplémentaire qui utilise la liaison.</span><span class="sxs-lookup"><span data-stu-id="62c52-120">For a `let` binding, for example, you need both the definition of the bound name and an additional expression that uses the binding.</span></span> <span data-ttu-id="62c52-121">En syntaxe détaillée, il s’agit d’une expression qui suit le `in` mot clé.</span><span class="sxs-lookup"><span data-stu-id="62c52-121">In verbose syntax, this is an expression that follows the `in` keyword.</span></span> <span data-ttu-id="62c52-122">Au niveau supérieur d’un module, il s’agit simplement de l’expression suivante dans le module, mais dans une quotation, il est explicitement requis.</span><span class="sxs-lookup"><span data-stu-id="62c52-122">At the top-level in a module, this is just the next expression in the module, but in a quotation, it is explicitly required.</span></span>

<span data-ttu-id="62c52-123">Par conséquent, l’expression suivante n’est pas valide.</span><span class="sxs-lookup"><span data-stu-id="62c52-123">Therefore, the following expression is not valid.</span></span>

```fsharp
// Not valid:
// <@ let f x = x + 1 @>
```

<span data-ttu-id="62c52-124">Toutefois, les expressions suivantes sont valides.</span><span class="sxs-lookup"><span data-stu-id="62c52-124">But the following expressions are valid.</span></span>

[!code-fsharp[Main](~/samples/snippets/fsharp/lang-ref-3/snippet502.fs)]

<span data-ttu-id="62c52-125">Pour évaluer les guillemets F #, vous devez utiliser l' [évaluateur de guillemets f #](https://github.com/fsprojects/FSharp.Quotations.Evaluator).</span><span class="sxs-lookup"><span data-stu-id="62c52-125">To evaluate F# quotations, you must use the [F# Quotation Evaluator](https://github.com/fsprojects/FSharp.Quotations.Evaluator).</span></span> <span data-ttu-id="62c52-126">Il prend en charge l’évaluation et l’exécution des objets d’expression F #.</span><span class="sxs-lookup"><span data-stu-id="62c52-126">It provides support for evaluating and executing F# expression objects.</span></span>

<span data-ttu-id="62c52-127">Les guillemets F # conservent également les informations de contrainte de type.</span><span class="sxs-lookup"><span data-stu-id="62c52-127">F# quotations also retain type constraint information.</span></span> <span data-ttu-id="62c52-128">Prenons l’exemple suivant :</span><span class="sxs-lookup"><span data-stu-id="62c52-128">Consider the following example:</span></span>

```fsharp
open FSharp.Linq.RuntimeHelpers

let eval q = LeafExpressionConverter.EvaluateQuotation q

let inline negate x = -x
// val inline negate: x: ^a ->  ^a when  ^a : (static member ( ~- ) :  ^a ->  ^a)

<@ negate 1.0 @>  |> eval
```

<span data-ttu-id="62c52-129">La contrainte générée par la `inline` fonction est conservée dans le code qutoation.</span><span class="sxs-lookup"><span data-stu-id="62c52-129">The constraint generated by the `inline` function is retained in the code qutoation.</span></span> <span data-ttu-id="62c52-130">Le `negate` formulaire quotated de la fonction peut maintenant être évalué.</span><span class="sxs-lookup"><span data-stu-id="62c52-130">The `negate` function's quotated form can now be evaluated.</span></span>

## <a name="expr-type"></a><span data-ttu-id="62c52-131">Expr (type)</span><span class="sxs-lookup"><span data-stu-id="62c52-131">Expr Type</span></span>

<span data-ttu-id="62c52-132">Une instance du `Expr` type représente une expression F #.</span><span class="sxs-lookup"><span data-stu-id="62c52-132">An instance of the `Expr` type represents an F# expression.</span></span> <span data-ttu-id="62c52-133">Les types génériques et non génériques `Expr` sont décrits dans la documentation de la bibliothèque F #.</span><span class="sxs-lookup"><span data-stu-id="62c52-133">Both the generic and the non-generic `Expr` types are documented in the F# library documentation.</span></span> <span data-ttu-id="62c52-134">Pour plus d’informations, consultez l' [espace de noms FSharp. Quotations](https://fsharp.github.io/fsharp-core-docs/reference/fsharp-quotations.html) et [Quotations. expr Class](https://fsharp.github.io/fsharp-core-docs/reference/fsharp-quotations-fsharpexpr.html).</span><span class="sxs-lookup"><span data-stu-id="62c52-134">For more information, see [FSharp.Quotations Namespace](https://fsharp.github.io/fsharp-core-docs/reference/fsharp-quotations.html) and [Quotations.Expr Class](https://fsharp.github.io/fsharp-core-docs/reference/fsharp-quotations-fsharpexpr.html).</span></span>

## <a name="splicing-operators"></a><span data-ttu-id="62c52-135">Opérateurs d’épissure</span><span class="sxs-lookup"><span data-stu-id="62c52-135">Splicing Operators</span></span>

<span data-ttu-id="62c52-136">L’ajout de jointures vous permet de combiner des Quotations de code littéral avec des expressions que vous avez créées par programmation ou à partir d’un autre devis de code.</span><span class="sxs-lookup"><span data-stu-id="62c52-136">Splicing enables you to combine literal code quotations with expressions that you have created programmatically or from another code quotation.</span></span> <span data-ttu-id="62c52-137">Les `%` `%%` opérateurs et vous permettent d’ajouter un objet d’expression F # dans une quotation de code.</span><span class="sxs-lookup"><span data-stu-id="62c52-137">The `%` and `%%` operators enable you to add an F# expression object into a code quotation.</span></span> <span data-ttu-id="62c52-138">Vous utilisez l' `%` opérateur pour insérer un objet expression typé dans un devis typé ; vous utilisez l' `%%` opérateur pour insérer un objet expression non typé dans une quotation non typée.</span><span class="sxs-lookup"><span data-stu-id="62c52-138">You use the `%` operator to insert a typed expression object into a typed quotation; you use the `%%` operator to insert an untyped expression object into an untyped quotation.</span></span> <span data-ttu-id="62c52-139">Les deux opérateurs sont des opérateurs de préfixe unaires.</span><span class="sxs-lookup"><span data-stu-id="62c52-139">Both operators are unary prefix operators.</span></span> <span data-ttu-id="62c52-140">Ainsi `expr` , si est une expression non typée de type `Expr` , le code suivant est valide.</span><span class="sxs-lookup"><span data-stu-id="62c52-140">Thus if `expr` is an untyped expression of type `Expr`, the following code is valid.</span></span>

```fsharp
<@@ 1 + %%expr @@>
```

<span data-ttu-id="62c52-141">Et si `expr` est une quotation typée de type `Expr<int>` , le code suivant est valide.</span><span class="sxs-lookup"><span data-stu-id="62c52-141">And if `expr` is a typed quotation of type `Expr<int>`, the following code is valid.</span></span>

```fsharp
<@ 1 + %expr @>
```

## <a name="example"></a><span data-ttu-id="62c52-142">Exemple</span><span class="sxs-lookup"><span data-stu-id="62c52-142">Example</span></span>

### <a name="description"></a><span data-ttu-id="62c52-143">Description</span><span class="sxs-lookup"><span data-stu-id="62c52-143">Description</span></span>

<span data-ttu-id="62c52-144">L’exemple suivant illustre l’utilisation de quotations de code pour placer du code F # dans un objet expression, puis imprimer le code F # qui représente l’expression.</span><span class="sxs-lookup"><span data-stu-id="62c52-144">The following example illustrates the use of code quotations to put F# code into an expression object and then print the F# code that represents the expression.</span></span> <span data-ttu-id="62c52-145">Une fonction `println` est définie et contient une fonction récursive `print` qui affiche un objet d’expression F # (de type `Expr` ) dans un format convivial.</span><span class="sxs-lookup"><span data-stu-id="62c52-145">A function `println` is defined that contains a recursive function `print` that displays an F# expression object (of type `Expr`) in a friendly format.</span></span> <span data-ttu-id="62c52-146">Il existe plusieurs modèles actifs dans les modules [FSharp. Quotations. Patterns](https://fsharp.github.io/fsharp-core-docs/reference/fsharp-quotations-patternsmodule.html) et [FSharp. Quotations. DerivedPatterns](https://fsharp.github.io/fsharp-core-docs/reference/fsharp-quotations-derivedpatternsmodule.html) qui peuvent être utilisés pour analyser des objets expression.</span><span class="sxs-lookup"><span data-stu-id="62c52-146">There are several active patterns in the [FSharp.Quotations.Patterns](https://fsharp.github.io/fsharp-core-docs/reference/fsharp-quotations-patternsmodule.html) and [FSharp.Quotations.DerivedPatterns](https://fsharp.github.io/fsharp-core-docs/reference/fsharp-quotations-derivedpatternsmodule.html) modules that can be used to analyze expression objects.</span></span> <span data-ttu-id="62c52-147">Cet exemple n’inclut pas tous les modèles possibles qui peuvent apparaître dans une expression F #.</span><span class="sxs-lookup"><span data-stu-id="62c52-147">This example does not include all the possible patterns that might appear in an F# expression.</span></span> <span data-ttu-id="62c52-148">Tout modèle non reconnu déclenche une correspondance avec le modèle de caractère générique ( `_` ) et est rendu à l’aide de la `ToString` méthode, qui, sur le `Expr` type, vous permet de connaître le modèle actif à ajouter à votre expression de correspondance.</span><span class="sxs-lookup"><span data-stu-id="62c52-148">Any unrecognized pattern triggers a match to the wildcard pattern (`_`) and is rendered by using the `ToString` method, which, on the `Expr` type, lets you know the active pattern to add to your match expression.</span></span>

### <a name="code"></a><span data-ttu-id="62c52-149">Code</span><span class="sxs-lookup"><span data-stu-id="62c52-149">Code</span></span>

[!code-fsharp[Main](~/samples/snippets/fsharp/lang-ref-3/snippet601.fs)]

### <a name="output"></a><span data-ttu-id="62c52-150">Sortie</span><span class="sxs-lookup"><span data-stu-id="62c52-150">Output</span></span>

```fsharp
fun (x:System.Int32) -> x + 1
a + 1
let f = fun (x:System.Int32) -> x + 10 in f 10
```

## <a name="example"></a><span data-ttu-id="62c52-151">Exemple</span><span class="sxs-lookup"><span data-stu-id="62c52-151">Example</span></span>

### <a name="description"></a><span data-ttu-id="62c52-152">Description</span><span class="sxs-lookup"><span data-stu-id="62c52-152">Description</span></span>

<span data-ttu-id="62c52-153">Vous pouvez également utiliser les trois modèles actifs dans le [module ExprShape](https://fsharp.github.io/fsharp-core-docs/reference/fsharp-quotations-exprshapemodule.html) pour parcourir les arborescences d’expressions avec moins de modèles actifs.</span><span class="sxs-lookup"><span data-stu-id="62c52-153">You can also use the three active patterns in the [ExprShape module](https://fsharp.github.io/fsharp-core-docs/reference/fsharp-quotations-exprshapemodule.html) to traverse expression trees with fewer active patterns.</span></span> <span data-ttu-id="62c52-154">Ces modèles actifs peuvent être utiles lorsque vous souhaitez parcourir une arborescence, mais que vous n’avez pas besoin de toutes les informations de la plupart des nœuds.</span><span class="sxs-lookup"><span data-stu-id="62c52-154">These active patterns can be useful when you want to traverse a tree but you do not need all the information in most of the nodes.</span></span> <span data-ttu-id="62c52-155">Lorsque vous utilisez ces modèles, toute expression F # correspond à l’un des trois modèles suivants : `ShapeVar` si l’expression est une variable, `ShapeLambda` si l’expression est une expression lambda, ou `ShapeCombination` si l’expression est autre chose.</span><span class="sxs-lookup"><span data-stu-id="62c52-155">When you use these patterns, any F# expression matches one of the following three patterns: `ShapeVar` if the expression is a variable, `ShapeLambda` if the expression is a lambda expression, or `ShapeCombination` if the expression is anything else.</span></span> <span data-ttu-id="62c52-156">Si vous parcourez une arborescence d’expressions en utilisant les modèles actifs comme dans l’exemple de code précédent, vous devez utiliser beaucoup plus de modèles pour gérer tous les types d’expression F # possibles, et votre code sera plus complexe.</span><span class="sxs-lookup"><span data-stu-id="62c52-156">If you traverse an expression tree by using the active patterns as in the previous code example, you have to use many more patterns to handle all possible F# expression types, and your code will be more complex.</span></span> <span data-ttu-id="62c52-157">Pour plus d’informations, consultez [ExprShape. ShapeVar&#124;ShapeLambda&#124;ShapeCombination modèle actif](https://fsharp.github.io/fsharp-core-docs/reference/fsharp-quotations-exprshapemodule.html#(%20|ShapeVar|ShapeLambda|ShapeCombination|%20)).</span><span class="sxs-lookup"><span data-stu-id="62c52-157">For more information, see [ExprShape.ShapeVar&#124;ShapeLambda&#124;ShapeCombination Active Pattern](https://fsharp.github.io/fsharp-core-docs/reference/fsharp-quotations-exprshapemodule.html#(%20|ShapeVar|ShapeLambda|ShapeCombination|%20)).</span></span>

<span data-ttu-id="62c52-158">L’exemple de code suivant peut être utilisé comme base pour des parcours plus complexes.</span><span class="sxs-lookup"><span data-stu-id="62c52-158">The following code example can be used as a basis for more complex traversals.</span></span> <span data-ttu-id="62c52-159">Dans ce code, une arborescence de l’expression est créée pour une expression qui implique un appel de fonction, `add` .</span><span class="sxs-lookup"><span data-stu-id="62c52-159">In this code, an expression tree is created for an expression that involves a function call, `add`.</span></span> <span data-ttu-id="62c52-160">Le modèle actif [SpecificCall](https://fsharp.github.io/fsharp-core-docs/reference/fsharp-quotations-derivedpatternsmodule.html#(%20|SpecificCall|_|%20)) est utilisé pour détecter tout appel à `add` dans l’arborescence de l’expression.</span><span class="sxs-lookup"><span data-stu-id="62c52-160">The [SpecificCall](https://fsharp.github.io/fsharp-core-docs/reference/fsharp-quotations-derivedpatternsmodule.html#(%20|SpecificCall|_|%20)) active pattern is used to detect any call to `add` in the expression tree.</span></span> <span data-ttu-id="62c52-161">Ce modèle actif assigne les arguments de l’appel à la `exprList` valeur.</span><span class="sxs-lookup"><span data-stu-id="62c52-161">This active pattern assigns the arguments of the call to the `exprList` value.</span></span> <span data-ttu-id="62c52-162">Dans ce cas, il n’y en a que deux, donc elles sont extraites et la fonction est appelée de manière récursive sur les arguments.</span><span class="sxs-lookup"><span data-stu-id="62c52-162">In this case, there are only two, so these are pulled out and the function is called recursively on the arguments.</span></span> <span data-ttu-id="62c52-163">Les résultats sont insérés dans un quotation de code qui représente un appel à à `mul` l’aide de l’opérateur Splice ( `%%` ).</span><span class="sxs-lookup"><span data-stu-id="62c52-163">The results are inserted into a code quotation that represents a call to `mul` by using the splice operator (`%%`).</span></span> <span data-ttu-id="62c52-164">La `println` fonction de l’exemple précédent est utilisée pour afficher les résultats.</span><span class="sxs-lookup"><span data-stu-id="62c52-164">The `println` function from the previous example is used to display the results.</span></span>

<span data-ttu-id="62c52-165">Le code dans les autres branches de modèles actifs régénère simplement la même arborescence d’expressions, donc la seule modification dans l’expression résultante est la modification de `add` à `mul` .</span><span class="sxs-lookup"><span data-stu-id="62c52-165">The code in the other active pattern branches just regenerates the same expression tree, so the only change in the resulting expression is the change from `add` to `mul`.</span></span>

### <a name="code"></a><span data-ttu-id="62c52-166">Code</span><span class="sxs-lookup"><span data-stu-id="62c52-166">Code</span></span>

[!code-fsharp[Main](~/samples/snippets/fsharp/lang-ref-3/snippet701.fs)]

### <a name="output"></a><span data-ttu-id="62c52-167">Sortie</span><span class="sxs-lookup"><span data-stu-id="62c52-167">Output</span></span>

```fsharp
1 + Module1.add(2,Module1.add(3,4))
1 + Module1.mul(2,Module1.mul(3,4))
```

## <a name="see-also"></a><span data-ttu-id="62c52-168">Voir aussi</span><span class="sxs-lookup"><span data-stu-id="62c52-168">See also</span></span>

- [<span data-ttu-id="62c52-169">Informations de référence sur le langage F #</span><span class="sxs-lookup"><span data-stu-id="62c52-169">F# Language Reference</span></span>](index.md)
