---
title: Quotations de code
description: 'En savoir plus sur les Quotations de code F #, une fonctionnalité de langage qui vous permet de générer et d’utiliser des expressions de code F # par programmation.'
ms.date: 08/13/2020
ms.openlocfilehash: 070e127397a5da7d70281d08ef7cafdb9b4f4fe5
ms.sourcegitcommit: 8bfeb5930ca48b2ee6053f16082dcaf24d46d221
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 08/18/2020
ms.locfileid: "88558333"
---
# <a name="code-quotations"></a><span data-ttu-id="2473f-103">Quotations de code</span><span class="sxs-lookup"><span data-stu-id="2473f-103">Code quotations</span></span>

<span data-ttu-id="2473f-104">Cet article décrit les *Quotations de code*, une fonctionnalité de langage qui vous permet de générer et d’utiliser des expressions de code F # par programmation.</span><span class="sxs-lookup"><span data-stu-id="2473f-104">This article describes *code quotations*, a language feature that enables you to generate and work with F# code expressions programmatically.</span></span> <span data-ttu-id="2473f-105">Cette fonctionnalité vous permet de générer une arborescence de syntaxe abstraite qui représente le code F #.</span><span class="sxs-lookup"><span data-stu-id="2473f-105">This feature lets you generate an abstract syntax tree that represents F# code.</span></span> <span data-ttu-id="2473f-106">L’arborescence de syntaxe abstraite peut ensuite être parcourue et traitée en fonction des besoins de votre application.</span><span class="sxs-lookup"><span data-stu-id="2473f-106">The abstract syntax tree can then be traversed and processed according to the needs of your application.</span></span> <span data-ttu-id="2473f-107">Par exemple, vous pouvez utiliser l’arborescence pour générer du code F # ou générer du code dans un autre langage.</span><span class="sxs-lookup"><span data-stu-id="2473f-107">For example, you can use the tree to generate F# code or generate code in some other language.</span></span>

## <a name="quoted-expressions"></a><span data-ttu-id="2473f-108">Expressions entre guillemets</span><span class="sxs-lookup"><span data-stu-id="2473f-108">Quoted Expressions</span></span>

<span data-ttu-id="2473f-109">Une *expression entre guillemets* est une expression f # dans votre code qui est délimitée de telle façon qu’elle n’est pas compilée dans le cadre de votre programme, mais à la place, elle est compilée dans un objet qui représente une expression F #.</span><span class="sxs-lookup"><span data-stu-id="2473f-109">A *quoted expression* is an F# expression in your code that is delimited in such a way that it is not compiled as part of your program, but instead is compiled into an object that represents an F# expression.</span></span> <span data-ttu-id="2473f-110">Vous pouvez marquer une expression entre guillemets de l’une des deux manières suivantes : avec les informations de type ou sans informations de type.</span><span class="sxs-lookup"><span data-stu-id="2473f-110">You can mark a quoted expression in one of two ways: either with type information or without type information.</span></span> <span data-ttu-id="2473f-111">Si vous souhaitez inclure des informations de type, vous utilisez les symboles `<@` et `@>` pour délimiter l’expression entre guillemets.</span><span class="sxs-lookup"><span data-stu-id="2473f-111">If you want to include type information, you use the symbols `<@` and `@>` to delimit the quoted expression.</span></span> <span data-ttu-id="2473f-112">Si vous n’avez pas besoin d’informations de type, vous utilisez les symboles `<@@` et `@@>` .</span><span class="sxs-lookup"><span data-stu-id="2473f-112">If you do not need type information, you use the symbols `<@@` and `@@>`.</span></span> <span data-ttu-id="2473f-113">Le code suivant illustre des Quotations typées et non typées.</span><span class="sxs-lookup"><span data-stu-id="2473f-113">The following code shows typed and untyped quotations.</span></span>

[!code-fsharp[Main](~/samples/snippets/fsharp/lang-ref-3/snippet501.fs)]

<span data-ttu-id="2473f-114">Parcourir une grande arborescence d’expressions est plus rapide si vous n’incluez pas d’informations de type.</span><span class="sxs-lookup"><span data-stu-id="2473f-114">Traversing a large expression tree is faster if you do not include type information.</span></span> <span data-ttu-id="2473f-115">Le type résultant d’une expression entre guillemets avec les symboles typés est `Expr<'T>` , où le paramètre de type a le type de l’expression tel que déterminé par l’algorithme d’inférence de type du compilateur F #.</span><span class="sxs-lookup"><span data-stu-id="2473f-115">The resulting type of an expression quoted with the typed symbols is `Expr<'T>`, where the type parameter has the type of the expression as determined by the F# compiler's type inference algorithm.</span></span> <span data-ttu-id="2473f-116">Quand vous utilisez des Quotations de code sans informations de type, le type de l’expression entre guillemets est le type non générique [expr](https://fsharp.github.io/fsharp-core-docs/reference/fsharp-quotations-fsharpexpr.html).</span><span class="sxs-lookup"><span data-stu-id="2473f-116">When you use code quotations without type information, the type of the quoted expression is the non-generic type [Expr](https://fsharp.github.io/fsharp-core-docs/reference/fsharp-quotations-fsharpexpr.html).</span></span> <span data-ttu-id="2473f-117">Vous pouvez appeler la propriété [RAW](https://fsharp.github.io/fsharp-core-docs/reference/fsharp-quotations-fsharpexpr-1.html#Raw) sur la classe typée `Expr` pour obtenir l’objet non typé `Expr` .</span><span class="sxs-lookup"><span data-stu-id="2473f-117">You can call the [Raw](https://fsharp.github.io/fsharp-core-docs/reference/fsharp-quotations-fsharpexpr-1.html#Raw) property on the typed `Expr` class to obtain the untyped `Expr` object.</span></span>

<span data-ttu-id="2473f-118">Il existe une variété de méthodes statiques qui vous permettent de générer des objets d’expression F # par programmation dans la `Expr` classe sans utiliser d’expressions entre guillemets.</span><span class="sxs-lookup"><span data-stu-id="2473f-118">There are a variety of static methods that allow you to generate F# expression objects programmatically in the `Expr` class without using quoted expressions.</span></span>

<span data-ttu-id="2473f-119">Notez qu’un devis de code doit inclure une expression complète.</span><span class="sxs-lookup"><span data-stu-id="2473f-119">Note that a code quotation must include a complete expression.</span></span> <span data-ttu-id="2473f-120">Pour une `let` liaison, par exemple, vous avez besoin à la fois de la définition du nom lié et d’une expression supplémentaire qui utilise la liaison.</span><span class="sxs-lookup"><span data-stu-id="2473f-120">For a `let` binding, for example, you need both the definition of the bound name and an additional expression that uses the binding.</span></span> <span data-ttu-id="2473f-121">En syntaxe détaillée, il s’agit d’une expression qui suit le `in` mot clé.</span><span class="sxs-lookup"><span data-stu-id="2473f-121">In verbose syntax, this is an expression that follows the `in` keyword.</span></span> <span data-ttu-id="2473f-122">Au niveau supérieur d’un module, il s’agit simplement de l’expression suivante dans le module, mais dans une quotation, il est explicitement requis.</span><span class="sxs-lookup"><span data-stu-id="2473f-122">At the top-level in a module, this is just the next expression in the module, but in a quotation, it is explicitly required.</span></span>

<span data-ttu-id="2473f-123">Par conséquent, l’expression suivante n’est pas valide.</span><span class="sxs-lookup"><span data-stu-id="2473f-123">Therefore, the following expression is not valid.</span></span>

```fsharp
// Not valid:
// <@ let f x = x + 1 @>
```

<span data-ttu-id="2473f-124">Toutefois, les expressions suivantes sont valides.</span><span class="sxs-lookup"><span data-stu-id="2473f-124">But the following expressions are valid.</span></span>

[!code-fsharp[Main](~/samples/snippets/fsharp/lang-ref-3/snippet502.fs)]

<span data-ttu-id="2473f-125">Pour évaluer les guillemets F #, vous devez utiliser l' [évaluateur de guillemets f #](https://github.com/fsprojects/FSharp.Quotations.Evaluator).</span><span class="sxs-lookup"><span data-stu-id="2473f-125">To evaluate F# quotations, you must use the [F# Quotation Evaluator](https://github.com/fsprojects/FSharp.Quotations.Evaluator).</span></span> <span data-ttu-id="2473f-126">Il prend en charge l’évaluation et l’exécution des objets d’expression F #.</span><span class="sxs-lookup"><span data-stu-id="2473f-126">It provides support for evaluating and executing F# expression objects.</span></span>

## <a name="expr-type"></a><span data-ttu-id="2473f-127">Expr (type)</span><span class="sxs-lookup"><span data-stu-id="2473f-127">Expr Type</span></span>

<span data-ttu-id="2473f-128">Une instance du `Expr` type représente une expression F #.</span><span class="sxs-lookup"><span data-stu-id="2473f-128">An instance of the `Expr` type represents an F# expression.</span></span> <span data-ttu-id="2473f-129">Les types génériques et non génériques `Expr` sont décrits dans la documentation de la bibliothèque F #.</span><span class="sxs-lookup"><span data-stu-id="2473f-129">Both the generic and the non-generic `Expr` types are documented in the F# library documentation.</span></span> <span data-ttu-id="2473f-130">Pour plus d’informations, consultez l' [espace de noms FSharp. Quotations](https://fsharp.github.io/fsharp-core-docs/reference/fsharp-quotations.html) et [Quotations. expr Class](https://fsharp.github.io/fsharp-core-docs/reference/fsharp-quotations-fsharpexpr.html).</span><span class="sxs-lookup"><span data-stu-id="2473f-130">For more information, see [FSharp.Quotations Namespace](https://fsharp.github.io/fsharp-core-docs/reference/fsharp-quotations.html) and [Quotations.Expr Class](https://fsharp.github.io/fsharp-core-docs/reference/fsharp-quotations-fsharpexpr.html).</span></span>

## <a name="splicing-operators"></a><span data-ttu-id="2473f-131">Opérateurs d’épissure</span><span class="sxs-lookup"><span data-stu-id="2473f-131">Splicing Operators</span></span>

<span data-ttu-id="2473f-132">L’ajout de jointures vous permet de combiner des Quotations de code littéral avec des expressions que vous avez créées par programmation ou à partir d’un autre devis de code.</span><span class="sxs-lookup"><span data-stu-id="2473f-132">Splicing enables you to combine literal code quotations with expressions that you have created programmatically or from another code quotation.</span></span> <span data-ttu-id="2473f-133">Les `%` `%%` opérateurs et vous permettent d’ajouter un objet d’expression F # dans une quotation de code.</span><span class="sxs-lookup"><span data-stu-id="2473f-133">The `%` and `%%` operators enable you to add an F# expression object into a code quotation.</span></span> <span data-ttu-id="2473f-134">Vous utilisez l' `%` opérateur pour insérer un objet expression typé dans un devis typé ; vous utilisez l' `%%` opérateur pour insérer un objet expression non typé dans une quotation non typée.</span><span class="sxs-lookup"><span data-stu-id="2473f-134">You use the `%` operator to insert a typed expression object into a typed quotation; you use the `%%` operator to insert an untyped expression object into an untyped quotation.</span></span> <span data-ttu-id="2473f-135">Les deux opérateurs sont des opérateurs de préfixe unaires.</span><span class="sxs-lookup"><span data-stu-id="2473f-135">Both operators are unary prefix operators.</span></span> <span data-ttu-id="2473f-136">Ainsi `expr` , si est une expression non typée de type `Expr` , le code suivant est valide.</span><span class="sxs-lookup"><span data-stu-id="2473f-136">Thus if `expr` is an untyped expression of type `Expr`, the following code is valid.</span></span>

```fsharp
<@@ 1 + %%expr @@>
```

<span data-ttu-id="2473f-137">Et si `expr` est une quotation typée de type `Expr<int>` , le code suivant est valide.</span><span class="sxs-lookup"><span data-stu-id="2473f-137">And if `expr` is a typed quotation of type `Expr<int>`, the following code is valid.</span></span>

```fsharp
<@ 1 + %expr @>
```

## <a name="example"></a><span data-ttu-id="2473f-138">Exemple</span><span class="sxs-lookup"><span data-stu-id="2473f-138">Example</span></span>

### <a name="description"></a><span data-ttu-id="2473f-139">Description</span><span class="sxs-lookup"><span data-stu-id="2473f-139">Description</span></span>

<span data-ttu-id="2473f-140">L’exemple suivant illustre l’utilisation de quotations de code pour placer du code F # dans un objet expression, puis imprimer le code F # qui représente l’expression.</span><span class="sxs-lookup"><span data-stu-id="2473f-140">The following example illustrates the use of code quotations to put F# code into an expression object and then print the F# code that represents the expression.</span></span> <span data-ttu-id="2473f-141">Une fonction `println` est définie et contient une fonction récursive `print` qui affiche un objet d’expression F # (de type `Expr` ) dans un format convivial.</span><span class="sxs-lookup"><span data-stu-id="2473f-141">A function `println` is defined that contains a recursive function `print` that displays an F# expression object (of type `Expr`) in a friendly format.</span></span> <span data-ttu-id="2473f-142">Il existe plusieurs modèles actifs dans les modules [FSharp. Quotations. Patterns](https://fsharp.github.io/fsharp-core-docs/reference/fsharp-quotations-patternsmodule.html) et [FSharp. Quotations. DerivedPatterns](https://fsharp.github.io/fsharp-core-docs/reference/fsharp-quotations-derivedpatternsmodule.html) qui peuvent être utilisés pour analyser des objets expression.</span><span class="sxs-lookup"><span data-stu-id="2473f-142">There are several active patterns in the [FSharp.Quotations.Patterns](https://fsharp.github.io/fsharp-core-docs/reference/fsharp-quotations-patternsmodule.html) and [FSharp.Quotations.DerivedPatterns](https://fsharp.github.io/fsharp-core-docs/reference/fsharp-quotations-derivedpatternsmodule.html) modules that can be used to analyze expression objects.</span></span> <span data-ttu-id="2473f-143">Cet exemple n’inclut pas tous les modèles possibles qui peuvent apparaître dans une expression F #.</span><span class="sxs-lookup"><span data-stu-id="2473f-143">This example does not include all the possible patterns that might appear in an F# expression.</span></span> <span data-ttu-id="2473f-144">Tout modèle non reconnu déclenche une correspondance avec le modèle de caractère générique ( `_` ) et est rendu à l’aide de la `ToString` méthode, qui, sur le `Expr` type, vous permet de connaître le modèle actif à ajouter à votre expression de correspondance.</span><span class="sxs-lookup"><span data-stu-id="2473f-144">Any unrecognized pattern triggers a match to the wildcard pattern (`_`) and is rendered by using the `ToString` method, which, on the `Expr` type, lets you know the active pattern to add to your match expression.</span></span>

### <a name="code"></a><span data-ttu-id="2473f-145">Code</span><span class="sxs-lookup"><span data-stu-id="2473f-145">Code</span></span>

[!code-fsharp[Main](~/samples/snippets/fsharp/lang-ref-3/snippet601.fs)]

### <a name="output"></a><span data-ttu-id="2473f-146">Output</span><span class="sxs-lookup"><span data-stu-id="2473f-146">Output</span></span>

```fsharp
fun (x:System.Int32) -> x + 1
a + 1
let f = fun (x:System.Int32) -> x + 10 in f 10
```

## <a name="example"></a><span data-ttu-id="2473f-147">Exemple</span><span class="sxs-lookup"><span data-stu-id="2473f-147">Example</span></span>

### <a name="description"></a><span data-ttu-id="2473f-148">Description</span><span class="sxs-lookup"><span data-stu-id="2473f-148">Description</span></span>

<span data-ttu-id="2473f-149">Vous pouvez également utiliser les trois modèles actifs dans le [module ExprShape](https://fsharp.github.io/fsharp-core-docs/reference/fsharp-quotations-exprshapemodule.html) pour parcourir les arborescences d’expressions avec moins de modèles actifs.</span><span class="sxs-lookup"><span data-stu-id="2473f-149">You can also use the three active patterns in the [ExprShape module](https://fsharp.github.io/fsharp-core-docs/reference/fsharp-quotations-exprshapemodule.html) to traverse expression trees with fewer active patterns.</span></span> <span data-ttu-id="2473f-150">Ces modèles actifs peuvent être utiles lorsque vous souhaitez parcourir une arborescence, mais que vous n’avez pas besoin de toutes les informations de la plupart des nœuds.</span><span class="sxs-lookup"><span data-stu-id="2473f-150">These active patterns can be useful when you want to traverse a tree but you do not need all the information in most of the nodes.</span></span> <span data-ttu-id="2473f-151">Lorsque vous utilisez ces modèles, toute expression F # correspond à l’un des trois modèles suivants : `ShapeVar` si l’expression est une variable, `ShapeLambda` si l’expression est une expression lambda, ou `ShapeCombination` si l’expression est autre chose.</span><span class="sxs-lookup"><span data-stu-id="2473f-151">When you use these patterns, any F# expression matches one of the following three patterns: `ShapeVar` if the expression is a variable, `ShapeLambda` if the expression is a lambda expression, or `ShapeCombination` if the expression is anything else.</span></span> <span data-ttu-id="2473f-152">Si vous parcourez une arborescence d’expressions en utilisant les modèles actifs comme dans l’exemple de code précédent, vous devez utiliser beaucoup plus de modèles pour gérer tous les types d’expression F # possibles, et votre code sera plus complexe.</span><span class="sxs-lookup"><span data-stu-id="2473f-152">If you traverse an expression tree by using the active patterns as in the previous code example, you have to use many more patterns to handle all possible F# expression types, and your code will be more complex.</span></span> <span data-ttu-id="2473f-153">Pour plus d’informations, consultez [ExprShape. ShapeVar&#124;ShapeLambda&#124;ShapeCombination modèle actif](https://fsharp.github.io/fsharp-core-docs/reference/fsharp-quotations-exprshapemodule.html#(%20|ShapeVar|ShapeLambda|ShapeCombination|%20)).</span><span class="sxs-lookup"><span data-stu-id="2473f-153">For more information, see [ExprShape.ShapeVar&#124;ShapeLambda&#124;ShapeCombination Active Pattern](https://fsharp.github.io/fsharp-core-docs/reference/fsharp-quotations-exprshapemodule.html#(%20|ShapeVar|ShapeLambda|ShapeCombination|%20)).</span></span>

<span data-ttu-id="2473f-154">L’exemple de code suivant peut être utilisé comme base pour des parcours plus complexes.</span><span class="sxs-lookup"><span data-stu-id="2473f-154">The following code example can be used as a basis for more complex traversals.</span></span> <span data-ttu-id="2473f-155">Dans ce code, une arborescence de l’expression est créée pour une expression qui implique un appel de fonction, `add` .</span><span class="sxs-lookup"><span data-stu-id="2473f-155">In this code, an expression tree is created for an expression that involves a function call, `add`.</span></span> <span data-ttu-id="2473f-156">Le modèle actif [SpecificCall](https://fsharp.github.io/fsharp-core-docs/reference/fsharp-quotations-derivedpatternsmodule.html#(%20|SpecificCall|_|%20)) est utilisé pour détecter tout appel à `add` dans l’arborescence de l’expression.</span><span class="sxs-lookup"><span data-stu-id="2473f-156">The [SpecificCall](https://fsharp.github.io/fsharp-core-docs/reference/fsharp-quotations-derivedpatternsmodule.html#(%20|SpecificCall|_|%20)) active pattern is used to detect any call to `add` in the expression tree.</span></span> <span data-ttu-id="2473f-157">Ce modèle actif assigne les arguments de l’appel à la `exprList` valeur.</span><span class="sxs-lookup"><span data-stu-id="2473f-157">This active pattern assigns the arguments of the call to the `exprList` value.</span></span> <span data-ttu-id="2473f-158">Dans ce cas, il n’y en a que deux, donc elles sont extraites et la fonction est appelée de manière récursive sur les arguments.</span><span class="sxs-lookup"><span data-stu-id="2473f-158">In this case, there are only two, so these are pulled out and the function is called recursively on the arguments.</span></span> <span data-ttu-id="2473f-159">Les résultats sont insérés dans un quotation de code qui représente un appel à à `mul` l’aide de l’opérateur Splice ( `%%` ).</span><span class="sxs-lookup"><span data-stu-id="2473f-159">The results are inserted into a code quotation that represents a call to `mul` by using the splice operator (`%%`).</span></span> <span data-ttu-id="2473f-160">La `println` fonction de l’exemple précédent est utilisée pour afficher les résultats.</span><span class="sxs-lookup"><span data-stu-id="2473f-160">The `println` function from the previous example is used to display the results.</span></span>

<span data-ttu-id="2473f-161">Le code dans les autres branches de modèles actifs régénère simplement la même arborescence d’expressions, donc la seule modification dans l’expression résultante est la modification de `add` à `mul` .</span><span class="sxs-lookup"><span data-stu-id="2473f-161">The code in the other active pattern branches just regenerates the same expression tree, so the only change in the resulting expression is the change from `add` to `mul`.</span></span>

### <a name="code"></a><span data-ttu-id="2473f-162">Code</span><span class="sxs-lookup"><span data-stu-id="2473f-162">Code</span></span>

[!code-fsharp[Main](~/samples/snippets/fsharp/lang-ref-3/snippet701.fs)]

### <a name="output"></a><span data-ttu-id="2473f-163">Output</span><span class="sxs-lookup"><span data-stu-id="2473f-163">Output</span></span>

```fsharp
1 + Module1.add(2,Module1.add(3,4))
1 + Module1.mul(2,Module1.mul(3,4))
```

## <a name="see-also"></a><span data-ttu-id="2473f-164">Voir aussi</span><span class="sxs-lookup"><span data-stu-id="2473f-164">See also</span></span>

- [<span data-ttu-id="2473f-165">Informations de référence sur le langage F #</span><span class="sxs-lookup"><span data-stu-id="2473f-165">F# Language Reference</span></span>](index.md)
